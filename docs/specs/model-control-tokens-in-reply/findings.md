# bug 單：模型把對話格式的標記吐成文字時，回覆留下的東西不對

**這個目錄沒有版號目錄＝還沒被拿起來。** 拿起來時走 spec-flow 路線 B；階段 0 的產出就是這一份，但**「實際的失敗資料」還缺一塊**（見「我還沒做的事」），進階段 1 之前要先補。

記錄日期：2026-09-18。記錄時的程式碼：`main`，commit `3cf22ae`。

兩個現象放同一張單，因為要問 PM 的是同一個問題：**模型把 ChatML 的控制標記（`<|im_start|>`、`<|im_end|>`、`<|endoftext|>`）當成文字吐出來時，那則回覆該留下什麼。** 拆開只會讓它各決定一半。

# 現象

來源：QA 在 `docs/specs/message-store-normalization/v1/test-report.md` 的旁記（第 28 行、第 116 行），操作途中看到、不屬於當輪受驗條文。RD 沒有在裝置上重現過。

**現象 A：兩種標記疊在一起。** 一則回覆的全文是

> （這次沒有產生回覆，再說一次試試）…（模型卡在重複迴圈，已中斷）

QA 看到它的那段對話是人格 Lover、模型 SmolLM 135M；**舊版建置（`e7de9fd`）與 `40e20af` 都出現過**，所以不是 `message-store-normalization` 那一輪造成的。前半句說「模型什麼都沒產生」，後半句說「模型一直在重複」，兩句互相矛盾。

**現象 B：回覆裡露出 `<|im_start|>user`。** SmolLM 的回覆偶爾帶著這串字。QA 沒有記下完整的回覆文字、也沒有記是哪一句話觸發的。

# 重現步驟

**現象 A 用純函式就能重現（不需要裝置、每次都重現）**：

```kotlin
val raw = "<|im_end|>".repeat(6)          // 60 個字元
raw.isLoopingOnItself()                    // true：長度到 LOOP_WINDOW(60)，週期 10 ≤ LOOP_UNIT_MAX(20)
raw.repairModelText()                      // ""：停止標記被整個拿掉
finalReply("", ReplyEnding.RepetitionLoop) // Keep("（這次沒有產生回覆，再說一次試試）…（模型卡在重複迴圈，已中斷）")
```

得到的字串與 QA 看到的逐字相同。任何「修復之後會變成空白」的重複都一樣：一串停止標記、一串 `\n`（兩個字元的那種，修復後變成換行再被 trim 掉）、一串空白。

**裝置上**：模型選 SmolLM 135M，送出訊息，等它自己結束。不是每次都出現；QA 沒有記下觸發的那一句。**SmolLM 當時實際吐出的原始文字沒有被抓到** —— 上面的純函式重現證明這條路走得到那個字串，但沒有證明 QA 那一次走的就是這條路。

**現象 B**：同樣是 SmolLM，沒有可靠的重現步驟。

# 診斷

**現象 A：判斷「是不是在重複」看的是原始文字，決定「留下什麼字」看的是修復後的文字，兩邊對不上。**

- `chat/ChatViewModel.kt:316` —— `answer.isLoopingOnItself()`，`answer` 是還沒修復的原始累積文字。
- `chat/ChatViewModel.kt:349` —— 收尾時交給 `settle` 的是 `answer.toString().repairModelText()`。
- `chat/TextRepair.kt:19-21` —— 修復會拿掉 `<|im_end|>`、`<|endoftext|>`，最後 `trim()`。
- `chat/ReplyEnding.kt:68-69`、`:78` —— 重複迴圈那一種結束先對文字做 `orSaySoNicely()`（空白就換成「（這次沒有產生回覆，再說一次試試）」），再接上迴圈標記。

所以只要重複的內容是「修復後會消失的東西」，原始文字判成迴圈、修復後的文字是空的，兩句就疊在一起。長度上限那一種（`ReplyEnding.kt:64`）有同樣的形狀：原始文字到 800 字元而修復後是空白時，會得到「（這次沒有產生回覆，再說一次試試）…（已達長度上限）」。

`ChatViewModel.kt:29` 的註解寫著 SmolLM 不會正常發出停止 token；它把停止標記當成普通文字一直吐，是最可能的來源 —— **這一句是推論，沒有原始輸出佐證。**

**現象 B：修復只認得兩個標記，而且做法是「拿掉標記、留下後面的字」。**

- `chat/TextRepair.kt:19-20` 只處理 `<|im_end|>` 與 `<|endoftext|>`，沒有 `<|im_start|>`。
- ChatML 的格式（`llm/ModelSpec.kt:57-62`）裡，一輪結束是 `<|im_end|>`，下一輪開始是 `<|im_start|>user`。模型沒有在 `<|im_end|>` 停下來時，接著吐的就是 `<|im_start|>user` 加上**它替使用者編的下一句話**。
- 現行修復把 `<|im_end|>` 拿掉、`<|im_start|>` 留著，後面那段編出來的「使用者的話」也照樣留在回覆裡。只補一個 `.replace("<|im_start|>", "")` 會讓標記消失，但那段編出來的話還在，而且更難看出它不是模型的回答。

# 要不要 PM

**要。** 正確行為不是自明的，至少有三個要有人決定的地方：

1. **控制標記出現在輸出裡時，是「拿掉標記、其餘照留」還是「在第一個標記那裡把回覆截斷」。** 後者會改變回覆的內容（標記後面的字全部不要），而且等於把它當成模型正常結束；前者是現況，會留下模型替使用者編的話。
2. **修復後是空白、但原始文字觸發了迴圈或長度上限時，留下哪一句。** 只留「（這次沒有產生回覆，再說一次試試）」？只留中斷標記？還是別的。
3. **重複偵測該看原始文字還是修復後的文字。** 這是實作細節，但它決定第 2 點的情形還會不會發生。

**會碰到的既有規格**：`docs/specs/stop-generation/` 的 R8「兩種自動中斷維持現狀」要求兩個中斷標記一字不改、仍會觸發，並且重複的尾巴收短的程度不變 —— 修正不得動到正常的迴圈與長度上限。`docs/product/features.md` 的聊天-11（迴圈標記）、聊天-13（「這次沒有產生回覆」，**尚無 spec**）。文字修復（停止標記怎麼處理）沒有任何一份規格在管，只有 `plan/architecture.html`「模型的輸出到畫面之間隔著兩層損壞」那一節提到。

**不得被修過頭的相鄰行為**：

- 正常的重複迴圈（有看得見的文字在重複）照舊收短、照舊加標記（`stop-generation` R8）。
- 模型真的什麼都沒產生就正常結束時，照舊是「（這次沒有產生回覆，再說一次試試）」。
- 使用者按停止時不經過 `orSaySoNicely()`（`ReplyEnding.kt:71-77` 的註解），這一點不動。
- 使用者自己打的字裡出現 `<|im_end|>` 之類的字串時，不得被改寫 —— 修復只在生成時經過模型的輸出，讀回來的與使用者打的都不碰。
- Gemma 的格式用的是另一組標記（`<end_of_turn>`、`<start_of_turn>`）；有沒有同樣的洩漏沒有人看過。

**修的時候，現象 A 的純函式重現必須進測試套件**（`ReplyEndingTest` 或 `TextRepairTest` 的層級就放得下）。

# 我還沒做的事

- **沒有抓到 SmolLM 當時的原始輸出。** 要補：在裝置上用 SmolLM 反覆送幾句，從資料庫讀出帶著 `<|im_start|>` 的那一則（存下來的是修復後的文字，看得到現象 B 的完整樣子）；現象 A 的原始文字存不下來，要靠 logcat 或暫時的記錄才看得到 —— 產品程式碼不加測試專用入口的裁決仍然有效，所以這要在專案以外的 clone 裡做。
- 沒有動任何程式碼、任何規格。
- 沒有查 Gemma 的控制標記有沒有同樣的洩漏。
- QA 同一段旁記裡還有第三件事（輸入框裡沒送出的字在切換對話後仍留著），與這張單無關，沒有寫進來。
