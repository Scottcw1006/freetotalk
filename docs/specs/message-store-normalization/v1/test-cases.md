# test cases — message-store-normalization v1

- 觀察點數：**41**
- 涵蓋的驗收條數：**88**（本規格 34 條由 QA 判的條文 ＋ 任務 8 指到的別份規格條文 54 條；過程條文 3 條不計）

受測版本 `40e20af`（分支 `message-store-normalization`，工作區乾淨）。裝置：AVD Pixel_9（emulator-5554）。使用者已同意清空 App 資料（模型檔保留）。
縮寫：**MSN**＝本規格；**MCD**＝`message-copy-delete` v2；**SDB**＝`conversation-store-db` v6；**CS**＝`conversation-search` v3.25。「讀儲存」一律照 MSN `spec-tasks.md`「測資與讀取的約定」（App 停止、三個檔一起拉、唯讀、比內容）。

判定依據永遠是條文原文；下面每個觀察點引用的是條文的預期輸出。

---

## 情境 0：不經過裝置（原始碼、測試、文件）

- **觀察 0-1**（跑 `./gradlew clean testDebugUnitTest`，讀結果 XML；`git diff e7de9fd..HEAD -- app/src/test` 逐支對照）
  - MSN 任務 1 驗收 6／任務 8 驗收 1／SDB 任務 7 驗收 11：「全數通過。本輪之前就存在的測試一支都不得變紅、停用、刪除或改弱斷言，只有兩組例外…要驗的性質一項都不得少」「不得…在產品程式碼裡留一個沒有人用的合併值」
- **觀察 0-2**（讀 `LegacyThreadRowsTest`、`ConversationRecordsTest`、`MessageDeletionTest` 的斷言；確認搬遷呼叫的是被測的那一份轉換）
  - MSN 任務 1 驗收 1：「三列…每一列斷言完全相等：對話的 id、人格、模型、建立時間、最後更新時間；每一則的 id、作者、文字、建立時間、刪除狀態；順序…人格與模型用非退回值；訊息 id 含缺號」「測資直接寫成舊結構的形式…不得用本輪的程式產生」
  - MSN 任務 1 驗收 2：「結果恰為 E、F 兩段（內容完全相等），四列各自被判成解不開；整批轉換不丟出例外、在測試時限內結束」
  - MSN 任務 1 驗收 3：「只含助理訊息的一列、訊息陣列是空的一列 → 都判成解得開，內容完全相等」
  - MSN 任務 1 驗收 4：「每一個欄位、每一則（含刪除狀態與順序）完全相等；轉回來沒有任何一則是生成中」「『取全部』恰為那兩段；『依 id 取』對壞的那一段找不到、對好的取得到且內容相等、對不存在的 id 找不到；對每一個 id，兩條路的答案相同」
  - MSN 任務 1 驗收 5：「沒有任何『你說的話』→ 判成『移除那個 id』；『你說的話』全部已刪除 → 判成『寫入』」
  - 可驗證性要求：「搬遷實際執行時用的轉換，必須就是被測試的這一份」
- **觀察 0-3**（讀 `HistoryRowsTest`）
  - MSN 任務 5 驗收 1：「對下列每一段對話，它給的標題、預覽與既有的標題／預覽規則對同一段對話給的完全相同 —— 第一句已刪除…；全部已刪除（『（訊息已刪除）』）；只有助理訊息（『還沒說話』）；每一則都已刪除（預覽是空字串）；第一句超過截短上限且截點落在一個組合 emoji 中間；最後一則含換行」
- **觀察 0-4**（讀 `SearchAssemblyTest`）
  - MSN 任務 6 驗收 1 (i)–(iv) 與「既有的比對測試照舊全部通過、斷言不改」：逐項原文見 spec-tasks.md；重點預期：「片段與標示區間與比對純邏輯直接對同一則訊息算出來的完全相同；查 kiwi 恰好一則、摘要兩個數字是 1 與 1；查 台 不命中」「五項性質一項都不得少」「最後的結果是 he 的；查詢清空之後來源才回應 → 最後沒有任何結果」「第 449 毫秒為止，假來源一次都沒有被要過資料、結果仍是輸入之前的樣子；第 450 毫秒起來源被要了恰好一次，要的是 hel…含開著的那一段的命中」「時間一毫秒都不前進，結果已經是『沒在搜尋』…再前進 1 秒，來源沒有…被要過」「輸入 h 之後第 100 毫秒清空 → …一次都沒有被要過」「正向對照：…結果不必等任何輸入就更新」
- **觀察 0-5**（冷讀 `plan/architecture.html` 全檔，對照程式碼）
  - MSN 任務 7 驗收 1–9（逐條原文見 spec-tasks.md）。驗收 3 另由情境 B、E、F、G 照著實際做一次判「照著做不做得出來」。
- **觀察 0-6**（讀 `docs/product/features.md`、grep 節名）
  - MSN 任務 9 驗收 1–4：保存-12、保存-13 存在並說出條文列的內容、節名 grep 得到；保存-08／09／11 仍在、引用不變；搜尋-02 說出「打字停下來約四分之一秒之後才更新」，兩個節名都 grep 得到。
- 不判：MSN 任務 5 驗收 3、任務 6 驗收 5、任務 7 驗收 10（過程條文，報告寫「過程條文，不判」）。

## 情境 A：升級（清空 → 舊版建置 e7de9fd 造資料 → 基準 → 外加 X1–X3 → 覆蓋安裝本輪建置）　[乾淨起點；順序不得調換]

- **觀察 A-1**（升級後第一次打開，畫面出現後 3 秒內一次 dump）→ MSN 任務 2 驗收 2 (a)「沒有『…已經清空…』，也沒有『目前讀不到之前的對話紀錄…』」、(b)「開著的是 B；每一則文字與記下的相同，那則回覆以『…（已停止）』結尾」
- **觀察 A-2**（開抽屜）→ (c)「抽屜上恰好是 C、A 兩列…順序、標題、預覽、時間文字都與記下的相同；沒有 X1、X2、X3」
- **觀察 A-3**（打開 A）→ (d)「兩個佔位在原位、左右正確，整個畫面上讀不到 zqa-one；其餘每一則與記下的相同」
- **觀察 A-4**（停止 App 讀新結構；冷啟動不做事再讀一次）→ (e)「A、B、C 每一段的 id、人格、模型、建立時間、最後更新時間，每一則訊息的 id、作者、文字、建立時間、刪除狀態、順序，都與基準相同；全部內容裡找不到 zqx-one／two／three，也沒有那三個 id 的對話列或訊息列」、(f)「與 (e) 讀到的相同」；同一次讀取另判 MSN §3.2（沒有舊欄位、沒有備份表）
- **觀察 A-5**（復原、送出 `after upgrade`、新對話搜尋、刪 C、讀儲存）→ (g)「文字逐碼位相同（兩個空白都在）…每一則恰好出現一次、順序正確…查 zqa-two → 命中 A；查 hello world zqa-one → 命中 A 剛復原的那一則…刪掉 C → 那一列消失；…沒有 C 的對話列與訊息列」
  - 同一情境也承接 MCD 任務 4 驗收 4（已由 MSN 任務 2 驗收 2 取代，不另判）。

## 情境 B：對話 T 的逐步存檔（本輪建置、清空後的乾淨起點；人格與模型非退回值）

每一步之後：force-stop → 讀儲存裡的 T → 再打開（續接到 T）→ 一次 dump。

- **觀察 B-1**（步驟 i：送出 `first  zqt-one`（兩個空白），等回覆結束）
  - MSN 任務 3 驗收 1 (i)「T 恰有三則…文字與畫面逐碼位相同，都未刪除」
  - SDB 任務 7 驗收 8 前半「續接到那段，使用者那句與完整回覆都在，頂端的人格與模型與結束前相同」
- **觀察 B-2**（步驟 ii：零文字停止；送出前加長填充把第一個字往後推）→ MSN 任務 3 驗收 1 (ii)「那則空回覆不在畫面上，儲存裡也沒有它那一列；你剛送出的那一句在」（攔不到 → 本環境無法觀察）
- **觀察 B-3**（步驟 iii：生成中按停止，不做別的，立刻 force-stop）
  - MSN 任務 3 驗收 1 (iii)「儲存裡那則回覆以『…（已停止）』結尾，與畫面相同」
  - SDB 任務 7 驗收 1「續接到那段，那則回覆的文字與停止時相同、以中斷標記結尾」
- **觀察 B-4**（步驟 iv：刪除 `first  zqt-one`，立刻 force-stop；步驟 v：復原，立刻 force-stop）
  - MSN 任務 3 驗收 1 (iv)(v)「刪除狀態是已刪除、文字仍是原文；其餘各則不變」「未刪除、文字逐碼位相同；則數與 (iv) 相同、沒有重複」
  - MCD 任務 4 驗收 1「續接到那段對話，那一則是佔位…立刻強制停止再打開 → 那一則是復原後的原文」
- **觀察 B-5**（步驟 vi：開另一段再開回 T）→ MSN 任務 3 驗收 1 (vi)「內容（含最後更新時間）與 (v) 相同」；每一步另判「順序與畫面相同；最後更新時間 (i)–(v) 每一步都晚於前一步」
- **觀察 B-6**（在 T 再刪一則對方回覆，停止 App，只用 SQL 讀）
  - MSN 任務 2 驗收 1「只靠 SQL 查詢…列得出…順序與畫面相同；已刪除的那一則…原文完整；復原過的那一則是『未刪除』」「列出每一張表的每一個欄位：沒有…打包；舊欄位不存在；沒有備份表」「每一個欄位都說得出歸屬」
  - MSN 任務 7 驗收 3 第一件（列出每一段／每一則）照著做得出來

## 情境 C：長按、複製、剪貼簿（沿用情境 B 之後的 T 與另一段新對話）

- **觀察 C-1**（對你說的話、正常結束的回覆、開場白、「…（已停止）」回覆各長按一次）→ MCD 任務 2 驗收 1「都出現恰好含『複製』『刪除』兩項的選單」（兩種造不出來的記本環境無法執行）
- **觀察 C-2**（基準 → 長按＋點外面、長按＋返回 → 讀儲存；再複製 X、對 Y 開關選單兩次、貼上）→ MCD 任務 2 驗收 3「仍在聊天畫面、那一則仍是原樣；儲存的內容與基準相同（含最後更新時間）」「讀回的內容與 X 的文字逐碼位相同」
- **觀察 C-3**（送出 `Copy  me: a*b*c  **x** - y`，複製，貼上讀回）→ MCD 任務 2 驗收 4「與送出的逐碼位相同」
- **觀察 C-4**（複製開場白；請模型用條列粗體回答，複製 → C；讀儲存 → S）→ MCD 任務 2 驗收 5 (i)「與畫面上那則開場白逐碼位相同」、(ii)「C 與 S 逐碼位相同」（S 無記號 → 換提示；仍無 → 本環境無法觀察）
- **觀察 C-5**（複製 T 的「…（已停止）」回覆，貼上；讀儲存）→ MCD 任務 2 驗收 6「以『…（已停止）』結尾，標記之後沒有任何字元；…與讀回的內容逐碼位相同」
- **觀察 C-6**（3,000+ 字元 `BEGIN-…-END`，開頭捲出螢幕時長按看得到的部分 → 複製）→ MCD 任務 2 驗收 7「以 BEGIN- 開頭、以 -END 結尾、長度與送出的相同」
- **觀察 C-7**（基準 → 複製任一則 → force-stop 讀）→ MCD 任務 2 驗收 8「與基準相同（含最後更新時間）；狀態列沒有變成『輸入中…』」
- **觀察 C-8**（一般的點與快速捲動、輸入框長按）→ MCD 任務 5 驗收 3「都不會跳出選單；輸入框的長按（系統的貼上選單）照常可用」

## 情境 D：刪除、佔位、復原、標題／預覽、搜尋（新對話 K：開場白、`first kiwi`、回覆、`second`、回覆）

- **觀察 D-1**（刪 `first kiwi`、再刪一則對方回覆；截圖；深色模式截圖）→ MCD 任務 3 驗收 1「沒有任何確認；靠右的佔位…讀得到『已刪除的對話』與『復原』，整個畫面上讀不到『first kiwi』；其餘四則原樣…佔位靠左」＋視覺分得出來＋深色模式
- **觀察 D-2**（長按佔位）→ MCD 任務 3 驗收 2「沒有選單，佔位還在」
- **觀察 D-3**（再刪最後一則對方回覆；開新對話；開抽屜；回去復原 `first kiwi` 再切走）→ MCD 任務 3 驗收 5「標題是『second』、預覽是『second』…標題是『first kiwi』」
- **觀察 D-4**（`first kiwi` 已刪除時搜尋 `first`／`已刪除`／`second`；復原後 `first`）→ MCD 任務 3 驗收 6「不因為這一則而命中；『找不到符合…』；命中；復原之後…命中」
- **觀察 D-5**（刪三則含開場白 → 復原中間 → 再刪再復原 → 全刪 → 送出一句）→ MCD 任務 3 驗收 4
- **觀察 D-6**（K 裡你說的每一句都刪、切走看抽屜；再把對方每一則含開場白也刪、切走；冷啟動）
  - MSN 任務 5 驗收 2「標題是『（訊息已刪除）』，預覽是最後一則沒被刪的訊息」「預覽那一行沒有字；那一列仍在」「冷啟動之後，上面兩列的標題與預覽不變」
  - MCD 任務 4 驗收 3 前半「抽屜上那一段仍在；冷啟動之後仍在，點開 → 佔位都在，逐一復原得到原文」
- **觀察 D-7**（只有開場白的新對話刪開場白 → 開另一段新的 → 抽屜、讀儲存）→ MCD 任務 4 驗收 3 反向對照「抽屜上沒有多出那一段；…對話段數沒有因為它而增加」
- **觀察 D-8**（新對話 L：有來有往，刪最新一則回覆 → 送 `after delete` → 復原 → 冷啟動）→ MCD 任務 3 驗收 9「不閃退；…復原的那一則在『after delete』之前，…各恰好出現一次。冷啟動 → 同樣」
- **觀察 D-9**（長對話往回捲，刪一行字的訊息、復原）→ MCD 任務 3 驗收 10「佔位在螢幕上，開場白仍在螢幕上，最新一則仍不在螢幕上…同樣三件事成立」
- **觀察 D-10**（復原 `…（已停止）` 回覆：情境 B 的 T）→ MCD 任務 3 驗收 3「回到原位、靠左、文字與記下的逐碼位相同（含標記）」

## 情境 E：儲存層的消失、外來資料、讀不起來（多段已存對話；直接改資料庫檔的都在 App 停止時）

- **觀察 E-1**（P 含已刪除訊息 `zqx-unique-7731`：先讀儲存找得到 → 切走、從抽屜刪 P → 讀儲存 → 冷啟動搜尋；Q 沒被動過）
  - MSN 任務 3 驗收 2 (a)「沒有 P 的對話列，也沒有任何一列訊息屬於 P」＋正向對照「Q 的對話列與每一則訊息列都在、內容不變」
  - MCD 任務 4 驗收 5「找得到…（正向對照）…那一段不在，讀出來的全部內容裡找不到那個字串；冷啟動後搜那個字串 → 無命中」
  - SDB 任務 7 驗收 7「儲存裡少一段、沒有 B、A 的內容不變；冷啟動後抽屜與搜尋都找不到 B」
- **觀察 E-2**（外面寫入只含兩則助理訊息的 X → 冷啟動 → 抽屜看 X → 點 X、再開 Y → 讀儲存 → 冷啟動抽屜與搜尋 → 再開新對話讀 Y）
  - MSN 任務 3 驗收 2 (b)「沒有 X 的對話列與訊息列」
  - SDB 任務 7 驗收 5「儲存裡沒有 X、冷啟動後抽屜與搜尋都找不到 X；…Y 仍在、內容不變」
  - MSN 任務 7 驗收 3 第二件（寫入只含助理訊息的對話）照著做得出來
- **觀察 E-3**（E、F、G，G 開著；把 E 某則作者改成不存在的值；冷啟動；第二次讓「上次停在哪」指著 E）
  - MSN 任務 3 驗收 3「不閃退；抽屜上有 F、沒有 E；搜 zqe-bad → 找不到；搜 F 裡的字 → 命中；打開 F 內容正確…冷啟動不閃退、畫面是一段新的對話」
  - SDB 任務 7 驗收 6（同一件事，做一次）
  - MSN 任務 7 驗收 3 第三件（讓一段讀不起來）照著做得出來
- **觀察 E-4**（刪一則並刪一則回覆 → 切走切回 → 冷啟動 → 搜 → 讀儲存原文仍在 → 復原）→ MCD 任務 4 驗收 2
- **觀察 E-5**（A 開著、早於現在 ≥1 分鐘，不做事，開另一段 → 讀儲存）→ SDB 任務 7 驗收 3「A 的內容（含最後更新時間）與切換前相同」
- **觀察 E-6**（基準 → 進搜尋、打字、點另一段命中列、再進搜尋點回 → 讀儲存）→ SDB 任務 7 驗收 9「對話數與每一段的內容都與基準相同」（MSN §6.4 由此守）
- **觀察 E-7**（A、B、C 彼此 ≥1 分鐘；打開 A 刪一則 t1 → 新對話 → 抽屜順序 → 讀儲存；≥1 分鐘後復原 t2 → 讀儲存）→ MCD 任務 3 驗收 8「A 排在 B、C 前面；A 的最後更新時間落在 t1 前後數秒內，B、C 的不變…落在 t2 前後數秒內（晚於刪除那一次）」
- **觀察 E-8**（換人格產生只有開場白的對話，四種方式離開，各讀儲存；正向對照）→ SDB 任務 7 驗收 4；同一狀態再判 SDB 任務 7 驗收 8 後半「以記住的人格與模型開新的一段」

## 情境 F：搜尋（S：`Hello  World zqs-one`、`plain!`；另外至少三段含 `hello`；一段含 `zqw-gone`）

- **觀察 F-1**（S 不是開著的、冷啟動一次、進搜尋：`hello world`／`HELLO`／`hello,world`／全形 `plain！`／換行詞；再打開 S 重查三個 ASCII）→ MSN 任務 6 驗收 2「命中 S 的那一則，片段裡被標示的字是 Hello World；命中；『找不到…』；…正向對照…結果與上面相同」（標示由截圖判）
- **觀察 F-2**（開著已存對話，刪 `zqw-gone` 立刻進搜尋查；回聊天復原立刻再查）→ MSN 任務 6 驗收 3 (b)「『找不到…』；…命中，而且那段對話只有一個分組」
- **觀察 F-3**（連續輸入 h,e,l,l,o 停 1 秒 vs 一次輸入 hello；全刪停 1 秒、再 2 秒；全程不按鈕不收鍵盤；連續截圖看有沒有載入中）→ MSN 任務 6 驗收 4「兩次的分組、命中列、摘要數字相同…引導文案；再等 2 秒 → 仍是引導文案…沒有『載入中』的樣子」
- **觀察 F-4**（換人格產生 N，進搜尋查開場白裡的詞）→ SDB 任務 3 驗收 1「N 的分組出現」
- **觀察 F-5**（新對話送出含 `harborlight` 的長提示，生成中進搜尋查）→ SDB 任務 3 驗收 2（盡力；算不算數照條文）

## 情境 G：串流中切換與聊天流程（歷史清單長到一個畫面放不下；每次切換前把清單捲過第一列）

- **觀察 G-1**（四個入口各一次＋零文字變體）→ SDB 任務 2 驗收 3 預期 1–5（原文見 SDB `spec.md` §2.2）；同一次「開啟另一段對話 B」另判 SDB 任務 7 驗收 2「A 那則回覆以中斷標記結尾；B 在儲存裡的內容與切換前相同」、MCD 任務 5 驗收 5
- **觀察 G-2**（不在生成時切換／刪一列／送出等結束切走）→ SDB 任務 2 驗收 4 三項
- **觀察 G-3**（串流中切換模型 5 次，至少 3 次從 Qwen 切走）→ SDB 任務 7 驗收 13「App 沒有閃退；頂端換成選的模型；之後送出一則能收到回覆」
- **觀察 G-4**（不停止一次量秒數；停止後立刻再送 ×2）→ SDB 任務 7 驗收 14「任一次比不停止的那一次多出 5 秒以上，即 fail」
- **觀察 G-5**（一輪基本操作）→ SDB 任務 7 驗收 10、MCD 任務 5 驗收 2（連續截圖看逐字出現、自動捲到最新）
- **觀察 G-6**（生成中長按那則回覆；結束後再長按；生成中刪更早一則）→ MCD 任務 2 驗收 2、任務 3 驗收 7（攔不到 → 本環境無法觀察）
- **觀察 G-7**（無已刪除訊息的一段：抽屜標題預覽、搜尋命中與兩個數字）→ MCD 任務 5 驗收 4

## 情境 H：結構版本對不上與內容壞了（P1、P2（P1 含一則已刪除）＋一段新對話；基準）　[H-1→H-2→H-3 依序，H-3 會清掉資料]

- **觀察 H-1**（`PRAGMA user_version=9` 推回、冷啟動；(a)–(d)；還原 (e)；(f)）→ MSN 任務 4 驗收 1 共同判法 (a)–(f)；MSN 任務 7 驗收 3 第四件（改版本號與還原）
- **觀察 H-2**（暫存 clone 把 `SCHEMA_VERSION` 加一、不補搬遷、建置、覆蓋安裝；(a)–(d)；讀儲存 (e)；裝回正常建置 (f)）→ MSN 任務 4 驗收 2；MSN 任務 7 驗收 3 第五件（宣告位置）
- **觀察 H-3**（資料庫檔覆寫成亂碼、刪 -wal/-shm、冷啟動）→ MSN 任務 4 驗收 3＝SDB 任務 5 驗收 1 各項；MSN 任務 7 驗收 3（讓整份讀不起來）

## 情境 I：大量資料（清空 → 本輪建置啟動一次送一句 → 停止 → 灌 300 段×40 則×300–500 字元，恰好 3 段含 `zqbulk-needle`）

- **觀察 I-1** → MSN 任務 6 驗收 6 (a)–(d)「5 秒內看得到歷史清單，順序是最後更新時間新到舊（看最上面三列）；…捲得動…」「輸入停止後 3 秒內出現恰好 3 個分組，摘要是『3 段對話・3 則訊息』」「那一段打得開，40 則都在」「全程沒有閃退、沒有『沒有回應』」。同一狀態順帶看 CS 任務 6 驗收 1 (i)（上半部不動）、(iii) 的「月/日」寫法。判完清空。

## 情境 J：CS 任務 9 的裝置端條文（MSN 任務 8 驗收 5；測資以資料庫檔寫入，查詢經 Gboard 注音版面組字選字）

- **觀察 J-1**（含 U+3000 的訊息：抽屜預覽逐碼位 vs 搜尋片段）→ CS 任務 9 驗收 7
- **觀察 J-2**（長中文訊息中段片語＋20 字內 emoji；鍵盤保持彈出）→ CS 任務 9 驗收 9（兩端「…」）、CS 任務 7 驗收 6、驗收 13
- **觀察 J-3**（「臺」「台」各打一次）→ CS 任務 9 驗收 10
- **觀察 J-4**（U+3000／U+00A0 訊息，查詞組中間半形空白）→ CS 任務 9 驗收 11
- **觀察 J-5**（5／2／2 三段對話；多段第 0 則同時命中）→ CS 任務 9 驗收 8 指到的 CS 任務 11 驗收 2「3 段對話・9 則訊息…3 + 2 + 2 筆」、CS 任務 7 驗收 2、3、5
- **觀察 J-6**（抽屜逐項）→ CS 任務 9 驗收 5 指到的 CS 任務 6 驗收 1 (i)–(vi)；CS 任務 9 驗收 4 指到的 CS 任務 1 驗收 6（冷啟動續接、空白不進歷史、排序、開啟／刪除／送出後更新、一段損毀歷史仍完整）——後者各項與情境 B、E、G 的觀察共用，不另造狀態
  - 綁「對話檔」字面的部分（「檔案存在／不存在」「放一個內容損毀的對話檔」）改以「儲存的內容」判同一件事。

## 情境 K：隨機抽測三項（本輪範圍以外；`docs/product/features.md`）

- 保存-05、模型-06、比較-08（不計入上面兩個數字；結果只寫報告）。

---

重置點：A 前清空；B 前清空（A 的資料不留給 B，避免升級資料影響逐步判讀）；H-3 自帶清空；I 前後清空；J 前清空。C–G 共用 B 之後累積的資料，彼此以不同的對話隔開。
