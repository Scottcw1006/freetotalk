# 驗證結果總覽

**pass。** 本規格由 QA 判的 34 條驗收、任務 8 指到的別份規格條文，逐條在 JVM 測試／原始碼／模擬器上判過，沒有任何一條 fail；3 條過程條文不判；少數子項記「本環境無法執行」（見各條）。另有 4 件條文本身的問題打回 PM（見「問題歸屬」），都不影響本輪判定。

- 受測版本：`40e20af`（分支 `message-store-normalization`；開始時 `git status --porcelain` 無輸出；沒有讀 commit 訊息）。開始時 v1 目錄沒有既有的 `test-cases.md`／`test-report.md`，沒有刪除任何檔案。
- 裝置：AVD Pixel_9（emulator-5554），Android 17，1080x2424／420dpi。受測 APK 由工作區現況建置後覆蓋安裝（`adb install -r -t`，`lastUpdateTime=2026-09-18 20:34:15`，晚於 HEAD 的提交時間 20:14:32；突變建置之後再覆蓋裝回一次，以 APK 位元組數 1858337824 與工作區那份相同確認）。全程沒有解除安裝。
- 舊版建置（`e7de9fd`）與突變建置都在 scratchpad 的 clone 裡建，工作區除了本輪兩份產出與 QA 自己的文件之外沒有任何改動。

# 逐項驗收結果

## 任務 1：舊資料轉換與新儲存形式的純邏輯
- **驗收 1**：pass。`LegacyThreadRowsTest` 三支（`a row from before messages could be deleted…`、`deleted messages come over deleted…`、`text comes over code point for code point`）。測資是手寫的舊結構 JSON 字串；每支對整個 `ThreadRecord` 做 `assertEquals`（對話五欄含 `updatedAt=7_777`、每則六欄＋position）；人格 Teacher、模型 Qwen05B（退回值是 Friend／Gemma1B）；id 含缺號 0、1、2、4；(iii) 列出的八種文字都在。
- **驗收 2**：pass。`a row that cannot be made sense of is left behind alone`（timeout 5 秒）：四種解不開各 `assertNull`，E、F 完全相等，最後斷言非 null 的恰為 `[e, f]`。
- **驗收 3**：pass。`rows the app could not have written, but that make sense, come over as they are`。
- **驗收 4**：pass（附註）。`ConversationRecordsTest`：`what is stored reads back exactly…`（含兩則已刪除、兩種作者、(iii) 的文字；`find` 與 `all` 都 `assertEquals` 原物件）、`a message saved while still streaming…`、`a thread that cannot be read is dropped alone…`（後半每一項都有，含逐 id 兩條路同答案）、另有一支順序測試。**附註**：條文寫「一段…含…一則被標成生成中的訊息的對話」，實作拆成兩支測試（生成中那一則在另一段對話裡）；要驗的兩個性質都各自被斷言到，判 pass，拆法寫在這裡供 PM 知道。MCD 任務 1 驗收 8 後半那支（`MessageDeletionTest`）改成先經 `toThreadRecordOrNull` 再經讀取規則，斷言沒有變弱。
- **驗收 5**：pass。`a thread nobody wrote in is deleted, not written`（未改）＋`MessageDeletionTest` 既有的「全部已刪除 → 寫入」（未改、通過）。
- **驗收 6**：pass。`./gradlew clean testDebugUnitTest`：16 個類別 185 支，0 failure／0 error／0 skipped（log 有 `:app:clean` 與 `:app:testDebugUnitTest`、無 UP-TO-DATE）。對 `e7de9fd` 的 diff：被刪掉的 7 支（`ConversationTest` 4 支、`ConversationSearchTest` 3 支，全是對 `allThreads` 的斷言）在 `SearchAssemblyTest` 各有對應：開著的那段一定在／新到舊且開著的排在中間／同 id 只一次且取記憶體／儲存較新仍取記憶體且先決定再排序（`["other","same"]`、命中數 2）／空白的開著那段也在。`allThreads` 在 `app/src` 已無任何出現。
- **可驗證性要求**：pass。搬遷（`ConversationMigrations.kt:45`）呼叫的就是被測的 `toThreadRecordOrNull`。
- 鑑別力那幾句沒有做突變（本輪只做指定的一個突變建置），判到的是「斷言存在且是完全相等」。

## 任務 2：新結構與搬遷
- **驗收 1**：pass。對話 T（Teacher／Qwen05B）以正常操作建立。`select id, author, text, createdAt, deleted from messages where conversationId=… order by position` 直接列出 6 則，順序與畫面相同；id 2（對方回覆）`deleted=1` 且原文 29 字元完整；刪除後又復原的 id 1 `deleted=0`。表只有 `conversations`(id, persona, model, createdAt, updatedAt)、`messages`(conversationId, id, position, author, text, createdAt, deleted) 加 Room／Android 自己的兩張；沒有 `messages` 打包欄位、沒有備份表、每個欄位都屬於對話或訊息自己的資料（沒有派生欄位）。
- **驗收 2**：pass，(a)–(g) 全部成立。
  - 前置：舊版建置造 A（Teacher／Qwen，刪 `Hello  World zqa-one` 與一則回覆，20:24）、B（Sibling／Qwen，「…（已停止）」，20:30）、C（Lover／SmolLM，刪你說的那句，20:32），最後停在 B；基準讀舊結構；外加 X1（非 JSON）、X2（作者 Robot）、X3（重複 id）。覆蓋安裝本輪建置 `Success`，沒清資料。
  - (a) 焦點出現後 2.1 秒完成的 dump：兩句提示都沒有。(b) 開著 B，三則文字與記下的相同、結尾「…（已停止）」。(c) 抽屜恰為 C、A 兩列，順序、標題（「（訊息已刪除）」「second zqa-two」）、預覽、時間（20:32、20:24）與記下的相同，沒有 X。(d) A 兩個佔位在原位（右、左），畫面讀不到 `zqa-one`。
  - (e) 新舊兩邊正規化成同一種 JSON 後 `diff` 為空（三段每一欄、每一則含 deleted 與順序）；`zqx-`、`x1-id`…都是 0；`user_version=2`；`shared_prefs/conversation_store.xml` 沒有被當成重建。(f) 再冷啟動一次後內容 dump 與 (e) 相同。
  - (g) 復原後文字逐碼位 `48 65 6c 6c 6f 20 20 57…`（兩個空白在）；送出 `after upgrade` 後 7 則各一次、position 0–6；新對話裡查 `zqa-two` → 命中 A；查 `hello world zqa-one` → 命中剛復原的那則；刪 C → 列消失，儲存裡沒有 C 的對話列與訊息列。
  - 旁記（不影響判定）：舊版建置上 C 的那則回覆文字是「（這次沒有產生回覆，再說一次試試）…（模型卡在重複迴圈，已中斷）」，兩種標記疊在一起；本輪建置上也出現過同樣的字串。不屬於本規格，寫在這裡給呼叫者。

## 任務 3：存檔、刪除與讀取在新結構上照樣成立
- **驗收 1**：pass。(i) 三則、`first  zqt-one` 長度 14（兩個空白）、都未刪除；(ii) 以 2,100 字元填充把第一個字往後推、送出後 0.2 秒按停止：畫面與儲存都沒有那則空回覆，你那句在（position 3）；(iii) 儲存裡的回覆（135 字元、含空行）結尾「…（已停止）」，與畫面相同；(iv) 只有 id 1 的 deleted 0→1 與 `updatedAt` 變，其餘逐列相同；(v) 只有 deleted 1→0 與 `updatedAt` 變，則數不變、無重複；(vi) 切走再切回後 T 的內容與 (v) `diff` 為空。`updatedAt`：…046606 < …342074 < …373400 < …419935 < …450706。每一步順序與畫面相同。
- **驗收 2**：pass。(a) P（含已刪除的 `zqx-unique-7731`）從抽屜刪掉後，內容 dump 只少了 P 的 1 列對話＋3 列訊息；(b) 外面寫入的 X（兩則助理訊息）在抽屜上是「還沒說話」，打開再開 Y 之後儲存與寫入前的基準 `diff` 為空（X 的對話列與訊息列都沒了）。正向對照 Q（另一段沒動過的）逐列相同。
- **驗收 3**：pass（造得出來，結構不擋）。E 的一則作者改成 `Robot`：冷啟動不閃退；抽屜（捲完整份）沒有 E、有 F；搜 `zqe-bad` →「找不到…」；搜 `zqu-one` → 命中 F；打開 F 內容正確。第二次讓 `last_thread_id` 指著 E → 冷啟動不閃退，畫面是只有開場白的新對話。

## 任務 4：結構版本對不上時開不起來、資料不動
- **驗收 1**：pass。`PRAGMA user_version=9` 推回。(a) 不閃退、送出有回覆；(b) 啟動後 2.0–5.9 秒的三次 dump 都看得到「目前讀不到之前的對話紀錄（紀錄沒有被刪除），這段期間的對話也可能存不下來。」，整輪沒有「已經清空」；(c) 抽屜「目前讀不到之前的對話紀錄。」；(d)「有一段對話沒能存下來。」（約 10 秒）；(e) 停止後內容與基準 382 行 `diff` 為空（`user_version` 仍是 9）；改回 2 推回；(f) 冷啟動沒有任何提示、清單回來、新送出的那段有存下。
- **驗收 2**：pass。clone 裡只把 `SCHEMA_VERSION` 2→3（建置多匯出一份 `3.json`，與文件說的一致），覆蓋安裝 `Success`。(a)–(d) 與驗收 1 相同的表現（提示 1.9–5.9 秒可見；抽屜文字；存檔失敗提示約 8 秒；不閃退）；(e) 停止後內容＋`user_version` 與基準 `diff` 為空；(f) 裝回正常建置，冷啟動沒有提示、對話都在、內容與基準相同。**升版方向有判到。**
- **驗收 3**：pass。主檔覆寫成 40,000 bytes 亂數並刪 `-wal`／`-shm`：不閃退、沒有解壓橫幅；「之前的對話紀錄讀不出來，已經清空，從這裡重新開始。」在 2.0–9.9 秒的 dump 都看得到；抽屜「還沒有其他對話。」；搜舊詞「找不到…」；`session.xml` 前後 `diff` 為空；送出、停止、再送出都正常；再冷啟動續接到新那段、沒有任何提示。

## 任務 5：歷史清單查資料庫
- **驗收 1**：pass。`HistoryRowsTest`／`a row shows the title and preview the whole thread would`：八段對話（含條文列的六種；截短上限 26、測資第 26 個 UTF-16 單位落在 👨‍👩‍👧 中間）逐段 `assertEquals(whole.title, entry.title)` 與 preview，另有四個字面值斷言。
- **驗收 2**：pass。你說的每一句都刪掉並切走 → 「（訊息已刪除）」＋預覽是最後一則對方的話；再把對方每一則（含開場白）刪掉 → 標題同上、預覽那一行沒有節點、那一列仍在；冷啟動後兩種列都不變（為了同時看到兩種，第二種狀態另用一段對話，見偏離）。
- **驗收 3**：過程條文，不判。

## 任務 6：搜尋
- **驗收 1**：pass。`SearchAssemblyTest` 13 支：(i) `a saved thread is matched by the very rules…`（三個查詢的 hits 與 `searchConversations` 直接算的相等、標示取出 `Hello World`、kiwi 1／1、`台` 無命中）；(ii) 五項性質各一支＋生成中一支；(iii) 晚到的 `h` 不蓋 `he`、清空後晚到的不顯示；(iv) 449ms 時 `askedAt` 為空且結果仍是初始、450ms 恰一次且是 `hel` 並含開著那段、`""` 與三個空白清空時 `currentTime` 不前進、100ms 清空從未問過來源、儲存變動不必等輸入。既有比對測試（`ConversationSearchTest` 51 支）除被搬走的 3 支外沒有改動。
- **驗收 2**：pass。S 不是開著的、冷啟動後：`hello world` → 1 段 1 則，截圖上粗體變色的是「Hello World」；`HELLO` → 命中；`hello,world` →「找不到…」。全形：英文版面打 `plain`、切注音版面送 `!`，欄位逐碼位 `…0xff01` → 命中 `plain!`。換行：第二個提示得到 `1. Mango\n2. Papaya`，查 `mango 2. papaya` → 命中，標示落在「Mango 2. Papaya」。正向對照（S 開著）三個查詢結果相同。
- **驗收 3**：pass。(a) 見任務 8 驗收 4。(b) 刪 `zqw-gone now` 後立刻進搜尋 →「找不到…」；復原後立刻再查 → 1 段 1 則、一個分組。
- **驗收 4**：pass。一個指令連送五個 keycode 與一次 `input text hello` 的結果完全相同（3 段 6 則）；整個刪掉後約 1 秒與再 2 秒都是引導文案；送鍵後約 0.2 秒的截圖是「hello＋引導文案」，沒有載入中的樣子；全程鍵盤開著、沒有按任何鈕。
- **驗收 5**：過程條文，不判。
- **驗收 6**：pass。灌 300 段×40 則（每則 300–512 字元，共 12,003 列訊息；`updatedAt` 各不相同）、恰 3 則含 needle。(a) 冷啟動正常；點抽屜後約 0.3–0.5 秒畫面穩定（遠低於 5 秒），最上三列 21:59／20:58／19:56；捲到底（9/6）再捲回頂，無 ANR；(b) 輸入結束後約 0.63 秒出現「3 段對話・3 則訊息」與 3 個分組（<3 秒）；(c) 點命中列打開 bulk-077，邊捲邊蒐集對到儲存裡的 40 則；(d) `am_crash`／`am_anr` 都是 0。判完清空。

## 任務 7：架構文件
- **驗收 1–2、4–9**：pass（冷讀）。元件圖與無障礙標籤寫「對話與訊息各一張表」；儲存節說出一則一列、刪除狀態是欄位、多指令包在一筆交易、沒有自動化測試與缺口；分類表第二列含「結構版本對不上」，並寫明每次改結構要升版附搬遷、漏寫的後果；搜尋節三句都改寫、保留「只有一個地方說了算」、說明逐批讀、為什麼不交給資料庫比對、250 毫秒與其理由；清單與不變條件與程式相符；升級一段齊全；模組地圖新增 `SearchAssembly.kt`、`HistoryRows.kt`、`LegacyThreadRows.kt`、`ConversationMigrations.kt`，路徑都存在；沒有派生欄位，文件也這樣寫。
- **驗收 3**：pass。六件都照著做出來了（任務 2 驗收 1、任務 3 驗收 2b／3、任務 4 驗收 1／2／3 實際用到）；`git show e7de9fd:plan/architecture.html` 查得到舊結構那一節。
- **驗收 10**：過程條文，不判。

## 任務 8：回歸驗證
- **驗收 1**：pass（同任務 1 驗收 6）。
- **驗收 2（MCD 裝置端）**：全部 pass，除註明者。
  - 任務 2：1 pass（你說的／正常回覆／開場白／已停止回覆各一次，選單恰為「複製」「刪除」；「出錯了：…」造不出來 → 本環境無法執行，由 MCD 任務 1 驗收 7 守）；2 pass（長按後截圖仍「輸入中…」且後兩張截圖持續變大，無選單；結束後長按有選單）；3 pass（兩種關法後畫面不變、儲存 `diff` 為空；複製 X 後對 Y 開關選單兩次，貼上＝X）；4 pass；5 (i) pass、(ii) pass（第二個提示得到 `- **apple** - banana - cherry`，C＝S）；6 pass（135 字元，C＝S，以標記結尾）；7 pass（3,010 字元，開頭捲出畫面）；8 pass。
  - 任務 3：1 pass（含截圖：佔位是外框空心斜體、一般泡泡是實心；深色模式讀得出來）；2、3、4、5、6、8、9、10 pass；7 pass（這一次算數：刪開場白後仍「輸入中…」、回覆自己跑到長度上限）。
  - 任務 4：1、2、3（含反向對照）、5 pass；4 由本規格任務 2 驗收 2 取代。
  - 任務 5：2、3、4、5 pass。
- **驗收 3（SDB 任務 7）**：1–11、13、14 全部 pass。13：算數的 5 次為 Qwen→SmolLM、Qwen→Gemma、Gemma→Qwen、Gemma→SmolLM、Qwen→SmolLM（另有 3 次回覆先結束、不算），每次 pid 不變、`am_crash` 0、頂端換模型、之後收得到回覆。14（Qwen）：不停止 0.45 秒；停止後立刻再送兩次約 1.0 秒、1.9 秒。
- **驗收 4**：SDB 任務 2 驗收 3 pass（四個入口各一次＋零文字變體；每次切換前清單已捲過第一列；A 立刻在最上面且恰一列、10 秒後仍是、查 `sea` 有 A、A 的回覆以切換前截圖上的文字為開頭並以「…（已停止）」結尾、開另一段時 B 不在清單；每次切換時約 200–350 字。模型 Qwen2.5 0.5B）；驗收 4 pass；SDB 任務 3 驗收 1 pass（查「什麼」→ 孫子那段只有開場白的分組出現）；驗收 2 pass（算數：回到聊天時仍「輸入中…」）。
- **驗收 5（CS 任務 9）**：7 pass（預覽逐碼位含 U+3000，片段是 0x20）；9／CS 任務 7 驗收 6 pass（Gboard 注音版面以按鍵事件組字選字；兩端「…」、🍞 在片段內、標示在「天氣」、單行）；10 pass（臺→只命中「臺北車站」；台→只命中「台北車站的天氣」）；11 pass（`天氣 很好` 命中 U+3000 與 U+00A0 兩則，標示蓋住整個詞組）；13 pass（判讀時鍵盤都開著）；8 → CS 任務 11 驗收 2 pass（「3 段對話・9 則訊息」、3+2+2 列、「還有 2 則符合」）、CS 任務 7 驗收 2／3／5 pass；5 → CS 任務 6 驗收 1 (i)–(vi) pass；4 → CS 任務 1 驗收 6 各項由上面幾條涵蓋（「對話檔存在／不存在」「損毀的對話檔」改以儲存的內容判）。**綁舊儲存形式而未執行的：沒有。**

## 任務 9：features.md
- **驗收 1–4**：pass。保存-12、13 存在且內容齊；兩個節名在 `spec.md` grep 得到；保存-08／09／11 與 `e7de9fd` 相比沒有改動；搜尋-02 同時指向 CS §1.7「即時，沒有送出鈕」與本規格 §6.7，兩個節名都 grep 得到。
- 本輪在裝置上確認過、可以拿掉 `※未經查證` 的列：**保存-12、保存-13**；搜尋-02 的「停下來才更新」在裝置上只看得到「打完之後結果才出現」，250 毫秒本身只有 JVM 測試在守。

# 邊界情況檢查
- 舊資料沒有 `deleted` 鍵（§3.1）：JVM (i)＋裝置上 B。解不開的三種列與「連讀都讀不出來」：前者裝置上驗過；後者只看到程式碼逐列 `runCatching`，裝置上沒有造。
- 搬遷不得重建檔案（既有事實 4）：升級後沒有「已經清空」，重建記號沒被觸發。
- 開著的那段同時在儲存裡（§6.2）：搜尋結果只有一個分組（F-2、F-5）。
- 還沒存過的對話在生成中刪別則（既有事實 9）：MCD 任務 3 驗收 7 那次正是這種情形，佔位出現、回覆照常結束。
- 清空查詢後晚到的結果（§6.5）：JVM＋裝置上 3 秒內沒有結果冒出來。
- 知情留白的（§2.2 存檔途中被結束、§3.4 前半）：沒有驗，也驗不到。

# 如果 fail：問題歸屬
沒有 fail。下面是條文層面的問題，**打回 PM**：
1. **任務 8 驗收 5 自相矛盾**：它要求照做 CS 任務 9 的裝置端條文，但那一任務的驗收 4、5、8 自己寫「由別的任務驗，本條不另外跑」，而本條的「QA 驗不到的」又寫「那份規格其餘任務的裝置端條文本輪沒有重跑」。照本檔「指向的那一條就是要執行的條文」，我把被指到的幾條也做了；請 PM 寫明到底要不要。
2. **任務 5 驗收 2 第三點「上面兩列」**：前兩點是同一段對話的先後兩個狀態，冷啟動時不可能同時存在兩列。
3. **任務 1 驗收 4「一段…含一則生成中訊息的對話」**：實作拆成兩支測試，性質都守到；條文若真的要求同一段，請寫明它獨自抓得到什麼。
4. **CS 任務 9 驗收 8–11 的查詢必須是中文**，連帶讓 MCD 任務 3 驗收 6（查「已刪除」）與 SDB 任務 3 驗收 1（開場白都是中文）也得走注音組字；這不是缺陷，但每輪成本高，請 PM 知道。

# 驗證方式說明
1. 標準測試指令：做了（`clean testDebugUnitTest`，讀結果 XML）。
2. 指名測試的條文：逐條讀了斷言（任務 1、5.1、6.1）。
3. 邊界情況：逐條，見上。
4. 實機條文：全部在模擬器上實際操作；沒有任何一條降級成靜態閱讀。
5. 隨機抽測三項：做了，見下。
6. 真實資料：**走「使用者已同意清空」那一種。** 清掉的：`databases/`、`shared_prefs/`、`files/conversations`（情境 A、B、I、J 之前與收尾各一次）；保留的：`files/*.task` 三個模型檔與快取。沒有做快照／還原。
- 指定的突變建置做了一個（任務 4 驗收 2），沒有做突變全掃。

**偏離 `test-cases.md` 的地方**：
- 觀察 D-4 的「已刪除」查詢與觀察 F-4 都需要中文輸入，延到情境 J 的注音版面一起做（F-4 的詞改用新對話開場白裡真的有的「什麼」）。
- 情境 C 的 3,010 字元訊息清欄位失敗一次，殘留文字混進下一段測資；那一段作廢，另開新對話重做情境 D。
- 觀察 D-6 用了兩段對話，才能在冷啟動後同時看到兩種列。
- 情境 H 沒有另造 P1、P2，直接拿當時儲存裡的 30 多段（多段含已刪除訊息）當基準。
- 情境 J 的測資（含一般中文訊息）全部直接寫資料庫檔，沒有用注音一則一則打；判的是算繪與比對，讀取走的是同一條路。
- 串流中切換：第一次「切換人格」點到同一個人格，重做；Qwen 偶爾直接拒答使回覆先結束，那幾次照條文不算、重做。
- SDB 任務 3 驗收 2 與 MCD 的兩條「生成中」條文改用同一個 `adb shell` 批次加連續截圖判，因為 dump 一次約 2 秒、整段生成只有數秒。

# 本輪驗證範圍
全驗。

# 環境文件的處置
`docs/qa-environment.md`：實際跑過確認方式的有——第 1 節（JDK 與 clean 測試的判準）、第 2 節四條裝置指令、第 3 節 (c) 與串流安裝、第 5 節 `ui.py`／`uitext.py` selftest、第 5a 節手勢區、第 6.1 節「送 x 判版面」、第 6.2 節非 ASCII 送不進去（本輪是無聲失敗：欄位不變、沒看到例外輸出）、第 13 節 `sqlitecontent.py` selftest、第 14 節的清欄位／貼上順序／建置舊版；都成立或已在第 15 節補正。其餘各節本輪沒有依賴，也沒有重跑它們的確認方式。第 14 節「clone 不帶被忽略的檔」實際踩到一個沒寫到的變形，補在新增的第 15 節。新增第 15 節九條（clone 的資產目錄、唯讀開 WAL 複本、checkpoint 後再推、連送多個 keycode、批次內 tap 的耗時、深色模式、清欄位要讀回、等待條件的字串、從位元組數看畫面何時開始變）。沒有發現越界內容，沒有刪除任何東西。`docs/qa-tools/` 沒有改動。

# 本輪隨機抽測的三項功能
- **保存-05**：從抽屜刪掉一段 → 列立刻消失；停止 App 讀儲存，那個 id 的對話列與訊息列都是 0。與描述相符。
- **模型-06**：打開一段 SmolLM 的對話，頂端顯示「SmolLM 135M」；歷史列與搜尋分組標頭都帶模型名稱。相符。
- **比較-08**：從抽屜進比較畫面，還沒跑過時畫面上有「一次只有一個模型能常駐記憶體，所以是依序執行——每換一個模型的載入時間會分開列出。」相符。
- 沒有拿這三項改變上面的 pass／fail。另外兩件操作途中看到、不屬於任何一份受驗條文的事：SmolLM 的回覆偶爾露出 `<|im_start|>user`；輸入框裡沒送出的字在切換對話後仍留著。是不是問題由呼叫者判斷。

# 隨機挑選記錄的處置
`docs/qa-random-coverage.md` 追加三行：`2026-09-18 保存-05`、`2026-09-18 模型-06`、`2026-09-18 比較-08`。
