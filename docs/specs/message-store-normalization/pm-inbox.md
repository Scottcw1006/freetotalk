# PM 收件匣（訊息儲存正規化）

**目前 1 筆待處理。**

## [2026-09-18] QA 在 v1 驗收報告裡指出的四處條文問題
- 來源：QA 報告（`v1/test-report.md`；RD 轉錄摘要，細節以報告為準）
- 內容：
  1. 任務 8 驗收 5 自相矛盾：它要求照做 `conversation-search` 任務 9 的裝置端條文，但那一任務的驗收 4、5、8 自己寫「由別的任務驗」，而本條的「QA 驗不到的」又說其餘任務沒有重跑。QA 這一輪把被指到的幾條也做了；請寫明到底要不要。
  2. 任務 5 驗收 2 的「上面兩列」：前兩點是同一段對話先後的兩個狀態，冷啟動時不可能同時存在兩列。
  3. 任務 1 驗收 4 寫「一段…含一則被標成生成中的訊息的對話」，實作拆成兩支測試（來回那一支、生成中那一支）；兩個性質都有守到。條文字面與做法要不要對齊，請判斷。
  4. 中文查詢的需求使 `message-copy-delete` 任務 3 驗收 6 與 `conversation-store-db` 任務 3 驗收 1 每輪都得走注音組字，成本高，請 PM 知道。
- 另：QA 回報保存-12、保存-13 本輪在裝置上確認過，可以拿掉 `※未經查證`（任務 9 說明）。
- 相關條目：`spec-tasks.md` 任務 8 驗收 5、任務 5 驗收 2、任務 1 驗收 4、任務 9
- 狀態：待處理（不急：不影響本輪判定，下次修訂時處理）

---

- **這個檔是意見信箱，不是檔案櫃。** 有事情要交給 PM 時，在這裡加一筆（標題含日期、來源是「使用者裁決」還是「RD 觀察」／「QA 報告」、相關條目、內容）。**PM 處理完就把那一筆刪掉。**
- **被刪掉的是通知，不是理由。** 每一筆的實質內容在刪之前都已落在版號最大的目錄裡的 spec 與 `spec-appendix.md`；哪一輪處理並移除了哪幾筆，列在該輪的 `changes-v<N>.md`（v1 沒有變更說明，記在 `v1/spec-appendix.md` 的修訂記錄）。原文在版本控制歷史裡。
- **RD 要寫在這裡的事**：決定不照 `spec-architecture.md` 的某一條建議走；判斷非改存檔的時機或次數不可（先寫、先不要做）；決定在資料庫裡存任何從訊息算得出來的東西；`spec-tasks.md` 任務 5 驗收 3、任務 6 驗收 5 要的那一筆說明（清單與搜尋各自從資料庫取什麼、什麼留在記憶體）；任務 7 驗收 10 要的那一筆（架構文件裡發現而沒改的不符，沒有也寫「沒有」）。
