# PM 收件匣（對話儲存改為資料庫）

## [2026-09-17] QA 打回 PM：測資不得等於讀取失敗時退回的值（v3 驗收）
- 來源：QA 報告（`v3/test-report.md`，任務 1 驗收 1 與「問題歸屬」；請直接讀原文）
- 內容摘要：任務 1 驗收 1 的讀回測試，測資的模型剛好是 `ModelSpec` 的預設值（Gemma1B），而讀取遇到認不得的名稱會退回預設值。讀取端把模型漏掉也會通過，條文宣稱「獨自抓得到模型轉錯」做不到。QA 判 fail，打回 RD 改測資，並建議 PM 在條文裡補上：每個欄位的測資都不得等於讀取失敗時會退回的值（模型、人格都有這種退回）。
- 相關條目：spec-tasks.md 任務 1 驗收 1（可能也適用驗收 3、4 與其他以「完全相等」判定的條文）
- 狀態：待處理

## [2026-09-17] RD 觀察：v3 驗收之後改了測試（commit b0ad0fa）與兩件 QA 順帶看到的事
- 來源：RD 觀察（記錄，不是請示）
- 內容：
  1. **改了既有測試**（流程規則要求講出來）：`ConversationRecordsTest` 的共用測資把模型從 `Gemma1B`（預設值）改成 `Qwen05B`；人格原本就是非預設的 `Teacher`，沒動。斷言本身沒有改。理由就是上一筆。RD 驗證過：把讀取改成一律回傳預設模型時，這個測試類別有 4 支會變紅；改回正確實作後全綠（148 支）。
  2. **過期註解**：`chat/TextRepair.kt` 的註解還寫著「讀取舊紀錄時也會修復」，已改成現況。不影響行為。
  3. **QA 順帶看到、規格沒有規定的**：從抽屜刪除一段對話而刪除失敗時，App 不做任何事（不提示、那一列也還在）。要不要規定，由你判斷；RD 沒有改。
  4. **任務 7 驗收 12 的觸發條件**：QA 依條文又提出一次「改動停止那一段程式」成立。那指的是 commit `6b284d1`，使用者已在 v3 知情裁決不重跑；`b0ad0fa` 沒有再動那段程式。RD 判斷不是新的觸發，沒有再問使用者；你若判斷不同，請升級。
- 相關條目：spec-tasks.md 任務 1 驗收 1、任務 7 驗收 12、「明確不做」
- 狀態：待處理

---

- **這個檔是意見信箱，不是檔案櫃。** 有事情要交給 PM 時，在這裡加一筆（標題含日期、來源是「使用者裁決」還是「RD 觀察」／「QA 報告」、相關條目、內容）。**PM 處理完就把那一筆刪掉。**
- **被刪掉的是通知，不是理由。** 每一筆的實質內容在刪之前都已落在 spec 與 `v<N>/spec-appendix.md`／決策歷程；哪一輪處理並移除了哪幾筆，列在該輪的 `changes-v<N>.md`。原文在版本控制歷史裡。
- **（2026-09-17）v2 處理並移除了 7 筆**（D1–D4、D4 子題、指定 Room、RD 的單一指令存檔），清單見 `changes-v2.md`／「本輪處理並移除的收件匣項目」。
- **（2026-09-17）v3 處理並移除了 3 筆**（QA 打回的四點、`stop-generation` 仍不重跑的裁決、RD 在 `6b284d1` 的修正紀錄），清單見 `changes-v3.md`／「本輪處理並移除的收件匣項目」。
- **RD 若決定不照 `v3/spec-architecture.md` 的某一條建議走，寫在這裡**；要改存檔的時機或次數、或再動取消生成那段程式（`stop-generation` 的重驗觸發條件），也要先寫在這裡。
- **任務 8 驗收 10 的「發現的不符」不寫在這裡**，寫在本目錄的 `findings.md`（v3 起）。
