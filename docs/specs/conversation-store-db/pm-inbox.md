# PM 收件匣（對話儲存改為資料庫）

**目前 2 筆待處理。**

## [2026-09-18] `message-store-normalization`：本規格有八處字面被覆蓋（使用者裁決：現在不改，下次修訂時照清單改）
- 來源：**使用者裁決**（2026-09-18，`message-store-normalization` 的 X1 選 (乙)、X4 選 (乙)）＋ 該輪 PM 整理的清單
- 內容：`message-store-normalization` 把訊息改成資料庫裡自己的一列（刪除狀態是欄位）、加了第一個結構搬遷、拿掉「結構對不上就清掉」、歷史清單與搜尋改查資料庫。使用者裁決本規格**現在不改**，下次修訂時照清單改；在那之前以那一份規格、`docs/product/features.md` 與 `plan/architecture.html` 為準。逐列清單（含為什麼不成立）在 `docs/specs/message-store-normalization/spec-appendix.md`「跨規格覆蓋清單」的 `conversation-store-db` 那一張表，摘要：
  1. **（規則，字面相反；清單第 1、8 列）**`spec.md` §5 開頭的分類表與 §5.1「儲存整份讀不起來時，告知一次後重新開始」把「結構不認得」歸在丟掉重來，其中**結構版本對不上**（漏寫搬遷、版本比建置新）→ 使用者裁決改歸「開不起來、不動」（那一份的 §4.1）；「改了結構卻沒升版本號」不在那一份的要求裡，仍照本規格的字面；連帶「這次會踩到的既有事實」第 10 條後半、`spec-architecture.md` §G.1 的 (ii)。
  2. **（規則）**`spec.md` §1.1 的訊息欄位列舉缺刪除狀態；任務 1 驗收 1 的測資沒有已刪除的訊息。（與下面那一筆的第三件是同一件事。）
  3. §1.4 的「由哪一層守」仍宣稱「存檔是單一指令」的結構保證；「明確不做」D1 例外的第二個觸發（改成多個指令要補回交易與測試）**已在那一輪成立，使用者知情裁決仍不補測試**。
  4. 功能總覽的模組表與資料流：搜尋「不讀儲存」、儲存「沒有搜尋或比對能力」。§3.1、§3.2 的規則不受影響。
  5. `spec-architecture.md` §B.1、§C.1、§D.1（建議，方向相反）。
  6. `spec-tasks.md`「測資與讀取的約定」、任務 6、任務 8 驗收 9 以「訊息那一欄裡的格式」為前提；那一份的「測資與讀取的約定」已寫明取代關係。
  7. 「明確不做」的「資料庫結構的版本升級機制」：觸發條件就是那一輪，該收掉並指過去。
- 相關條目：§1.1、§1.4、§5、§5.1；`spec-architecture.md` §B.1、§C.1、§D.1、§G.1；`spec-tasks.md`「測資與讀取的約定」、任務 6、任務 8 驗收 9、「明確不做」
- 狀態：待處理（不急：本規格下一次被修訂時一併處理，不為此單獨開一輪）。**使用者裁決：下面 `message-copy-delete` 那一筆不動，兩筆並存**；兩筆有一處重疊（§1.1 的刪除狀態），處理時一起看。

## [2026-09-18] `message-copy-delete` 新增一個存檔時機（刪除與復原當下）
- 來源：RD 觀察（代 `message-copy-delete` 的 PM 轉達；使用者已裁決 D4：別份規格現在不改，下次修訂時一併處理）
- 內容：`message-copy-delete` 那一輪**新增**一個存檔時機 —— 訊息被刪除或復原的當下就存；沒有拿掉或移動既有的任何一個時機。新增的這次存檔與既有的存檔走同一條單一指令的路。兩個後果：(1) 同一段對話可能同時被存兩次相同內容，由本規格 §1.4 涵蓋；(2) 對方還在生成時刪除別的訊息，會第一次讓「生成中的對話」被存下來，存下的那則回覆沒有中斷標記 —— `message-copy-delete` 把「那個瞬間行程被結束時留下什麼」列為明確不做。另外 §1.1 列舉的訊息欄位裡沒有刪除狀態。完整的跨規格衝突清單在 `docs/specs/message-copy-delete/v1/spec-appendix.md`「跨規格衝突清單」。
- 相關條目：§1.1、§1.4；本檔下方「要改存檔的時機或次數要先寫在這裡」那一條
- 狀態：待處理（不急：依 D4，本規格下一次被修訂時一併處理，不為此單獨開一輪）

---

- **這個檔是意見信箱，不是檔案櫃。** 有事情要交給 PM 時，在這裡加一筆（標題含日期、來源是「使用者裁決」還是「RD 觀察」／「QA 報告」、相關條目、內容）。**PM 處理完就把那一筆刪掉。**
- **被刪掉的是通知，不是理由。** 每一筆的實質內容在刪之前都已落在 spec 與 `v<N>/spec-appendix.md`／決策歷程；哪一輪處理並移除了哪幾筆，列在該輪的 `changes-v<N>.md`。原文在版本控制歷史裡。
- **（2026-09-17）v2 處理並移除了 7 筆**（D1–D4、D4 子題、指定 Room、RD 的單一指令存檔），清單見 `changes-v2.md`／「本輪處理並移除的收件匣項目」。
- **（2026-09-17）v3 處理並移除了 3 筆**（QA 打回的四點、`stop-generation` 仍不重跑的裁決、RD 在 `6b284d1` 的修正紀錄），清單見 `changes-v3.md`／「本輪處理並移除的收件匣項目」。
- **（2026-09-17）v4 處理並移除了 2 筆**（QA 打回的測資條文、RD 在 `b0ad0fa` 之後的觀察四點），清單見 `changes-v4.md`／「本輪處理並移除的收件匣項目」。
- **（2026-09-17）v5 處理並移除了 1 筆**（刪除 `history-missing-after-switch` 目錄前改引用的使用者裁決），清單見 `changes-v5.md`／「本輪處理並移除的收件匣項目」。
- **（2026-09-18）v6 處理並移除了 1 筆**（QA 在 v4 驗收報告裡給的三點條文建議），清單見 `changes-v6.md`／「本輪處理並移除的收件匣項目」。
- **RD 若決定不照版號最大的目錄裡 `spec-architecture.md` 的某一條建議走，寫在這裡**；要改存檔的時機或次數、或再動取消生成那段程式（`stop-generation` 的重驗觸發條件），也要先寫在這裡。
- **任務 8 驗收 10 的「發現的不符」不寫在這裡**，寫在本目錄的 `findings.md`（v3 起）。
