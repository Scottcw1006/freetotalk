# 這一輪改了什麼

處理收件匣裡 QA 給的三點條文建議（v4 驗收判 pass，那三點是給 PM 的參考，不擋完成）。**這一輪只動驗收怎麼判**：`spec.md` §1–§5 的行為規則一個字都沒有改，沒有新增或移除任何任務，**RD 沒有程式工作**。

本輪的規格在 `docs/specs/conversation-store-db/v6/`（入口 `v6/spec.md`）。v5 原樣留著，兩個目錄 `diff` 得出本輪的全部改動。

**要不要重驗由使用者拍板。** PM 的判斷是**不需要**，逐條分類與最小範圍寫在 `v6/spec-decisions.md`／「本輪（v6）我自己做的判斷」第 1 項。

# 這一輪要做的任務（完整清單）

- **RD：本輪沒有任務。** 沒有程式改動，也沒有文件交付物（任務 8 的結論是「本輪無需更新」）。
- **QA：開不開由使用者決定。** 使用者若決定要驗，最小範圍是下面第一項；其餘各條上一輪已經成立，而本輪沒有任何程式改動。

| 項目 | 狀態 |
|---|---|
| 任務 2 驗收 3（串流中切換，被切走的那段立刻在清單上） | 〔修改：新增「預期 4 的判法」；擺設與預期 1、2 的判法只清掉版本標註，文字未變〕 |
| 任務 3 驗收 1（從未存下的新對話搜得到） | 〔修改：拿掉「排在最前」，改為寫明排序由哪一份守〕 |
| 任務 7 驗收 13（串流中切換模型不閃退） | 〔未變動；條文寫著每輪都跑 —— 只要開了 QA，它會被一起要求〕 |
| 任務 8（更新架構文件） | 〔修改：結論改寫成本輪的「無需更新」，移除前兩輪的歷史結論；驗收 1–9、11–14 一字未變〕 |
| 任務 9（`docs/product/features.md` 與本規格一致） | 〔修改：說明改寫成現況；驗收 1–7 一字未變〕 |
| 任務 1、4、5、6，任務 2 驗收 1、4，任務 3 驗收 2，任務 7 其餘各條 | 〔未變動〕 |

# 本輪移除或失效的任務／驗收

- 沒有任務或驗收被移除。
- **任務 3 驗收 1 的「而且排在最前」那半句被拿掉**（條文本身保留）。它守的性質沒有變成沒人守：本規格沒有排序規則，排序由 `docs/specs/conversation-search/spec.md` §3.2「排序」與 §8.7「新對話的起點」守。

# 改動的規格章節（RD 與 QA 要讀原文的地方）

- `v6/spec-tasks.md`
  - 新增「攔住還在生成中的回覆：現場提示」（不是要求、不判 pass/fail）—— 位置在「測資與讀取的約定」之後、任務 1 之前。
  - 任務 2 驗收 3：新增「預期 4 的判法」。
  - 任務 3 驗收 1：改寫判定範圍。
  - 任務 8：本輪結論。任務 9：說明。
- `v6/spec.md`：**§1–§5 未改動。** 只動「這份文件怎麼讀」裡兩句（拿掉逐版列改動的那句、拿掉指名某一版判斷的那句）。
- `v6/spec-decisions.md`：改寫成本輪的狀態（仍 0 項待決），加上重驗的判斷。
- `v6/spec-appendix.md`：決策歷程新增「v6：QA 在 v4 驗收報告裡給的三點條文建議」；修訂記錄加一列。
- `v6/spec-architecture.md`：**一字未改。**

# 未改動但與本輪改動互相牽動的章節

- **`spec.md` §2.2「回覆還在生成時切換，被切走的那段一打開抽屜就在清單上」**：預期 4 的規則文字一字未動，改的只是任務 2 驗收 3 怎麼判它。讀新判法之前先讀這一節，才知道判法為什麼可以與規則不逐字相同（理由在 `v6/spec-appendix.md`／決策歷程「v6：QA 在 v4 驗收報告裡給的三點條文建議」）。
- **`docs/specs/stop-generation/spec.md` R2**（中斷標記的定義）：§2.2 預期 4 引用它，本輪沒有動它，也沒有要求它改。
- **`docs/specs/conversation-search/spec.md` §3.2「排序」與 §8.7「新對話的起點」**：任務 3 驗收 1 拿掉排序判定之後，排序改由那兩節守。本輪沒有動那一份。

# 本輪處理並移除的收件匣項目

- **[2026-09-17] QA 給 PM 的三點條文建議（v4 驗收，判定已通過，不擋完成）** —— 三點的結論與理由落在 `v6/spec-appendix.md`／決策歷程「v6：QA 在 v4 驗收報告裡給的三點條文建議」；改出來的條文在 `v6/spec-tasks.md` 任務 2 驗收 3、任務 3 驗收 1、以及新增的「攔住還在生成中的回覆：現場提示」。

# 規則 → 條文的反向核對（本輪交付，不進 spec）

**本輪判定為「被改動」的章節**：`spec.md` §2、§3。判準：本輪改寫的驗收（任務 2 驗收 3、任務 3 驗收 1）指名的規則落在這兩節，而且本輪新寫的文字明文引用了 §2.2 與 §3.1。**其他章節（§1、§4、§5）本輪明確宣告不需要核對** —— 沒有任何一條規則的文字被動到、沒有任何指名它們的驗收被改寫、本輪也沒有實作改動會經過它們。

| 規則 | 被違反時，哪一條驗收會發現 |
|---|---|
| §2.1 歷史清單＝儲存 − 開著的那段，隨時最新 | 任務 2 驗收 1（(i)(ii)(iii)）、任務 2 驗收 3、任務 2 驗收 4 |
| §2.2 串流中切換，被切走的那段一打開抽屜就在清單上（含預期 1–5） | 任務 2 驗收 3（預期 4 本輪補上判法） |
| §2.3 其他會改變儲存的動作，清單也立刻跟著變 | 任務 2 驗收 4 |
| §3.1 還沒存下的東西仍然搜得到（第一項：從未存下的新對話） | 任務 3 驗收 1 |
| §3.1（第二項：已送出、還沒收尾的那句） | 任務 3 驗收 2（盡力） |
| §3.1（第三項：生成中的回覆已顯示的部分） | 沒有獨立條文，理由寫在該規則旁（與第二項同一段記憶體裡的對話，打破它的錯法會同時讓第二項搜不到） |
| §3.2 同一段兩邊都有時以記憶體那份為準 | 本規格沒有獨立條文，理由寫在該規則旁（`conversation-search` 的純邏輯測試，在任務 1 驗收 7 底下每輪都跑） |

**核對結果：沒有缺口，本輪沒有因此補寫任何條文。**
