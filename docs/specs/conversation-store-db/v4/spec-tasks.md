# 任務與驗收標準、明確不做（對話儲存改為資料庫 spec 的第五、六節）

> 入口與檔案地圖見 `spec.md`。行為規格（什麼算對）在 `spec.md`；架構建議（QA 不驗）在 `spec-architecture.md`；待決事項在 `spec-decisions.md`。
> **這兩節是不同性質的東西**：上面是這次要做的，下面是這次刻意不做的。

# 任務與驗收標準

## 先看這裡

**（v4）沒有任何條文在等決定。** v4 是 v3 驗收之後的修訂；這一輪要做哪些、改了哪幾條，列在 slug 根目錄的 `changes-v4.md`（之前各輪的在 `changes-v2.md`、`changes-v3.md`）。

**標準測試指令**：`./gradlew testDebugUnitTest`。**本輪所有新的自動化測試都只用現有的 JUnit、不碰資料庫**（D1）。

## 測資與讀取的約定（v2，依 D2 裁決）

**造測資分兩條路：**

1. **App 自己做得出來的資料，一律用 App 的正常操作建立**（送出訊息、切換、刪除…）。**每一個測試開一段新的對話**，那一條判讀完之後，**從 App 裡把那段刪掉**。
   - **清理排在判讀之後**：凡是要比「操作前後儲存裡的對話數或內容」的條文（任務 7 驗收 3、4、9 等），清理不得落在基準與判讀之間。
   - **開著的那段刪不掉**（刪除鈕只在抽屜清單上）：要清理時先開一段新的對話，再從抽屜刪。
   - **可以直接清空 App 資料的時候**（使用者已同意，模型檔保留），清空也算清理。
2. **App 做不出來的資料，才直接改資料庫檔**：只含助理訊息的對話、讀不起來的一段、整份壞掉的儲存。另外兩件同一性質的前置也走這一條：把**舊格式的對話檔**放進舊位置（新建置做不出它們）、把「上次停在哪」的偏好寫成一個指定值（任務 4 驗收 2）。
   - **一律在 App 停止時改、改完再冷啟動**：Room 看不到 App 以外的修改（`spec.md`「這次會踩到的既有事實」第 13 條）。
   - 位置與結構照任務 6 的交付物（`plan/architecture.html` 儲存那一節）。

**產品程式碼不加任何測試專用的匯入、匯出或造資料入口**（D2 的 (b) 不採用）。

**讀出「儲存的內容」**（任務 5、6、7 裡比對操作前後的條文）：**App 停止時，唯讀地直接讀資料庫檔**。比的是**內容**（每一段對話的每一個欄位、每一則訊息），不是檔案位元組 —— App 把同一段內容重寫一次不算改動。
- **這一點是 PM 的判斷，不是裁決的原文**：裁決講的是「怎麼造資料」，沒有直接講「怎麼讀」。「唯讀地讀資料庫檔」不寫入任何東西、不需要產品程式碼，屬於裁決第二條路的延伸；**而且只有它看得到儲存裡精確的最後更新時間**（畫面上只顯示到分）。理由記在 `../v2/spec-decisions.md`「本輪（v2）我自己做的判斷」第 1 項。

**比對的值，不得等於 App 讀不懂時會退回的值（v4 新增，自動化測試與裝置上都適用）。** 凡是以「讀回來與存進去（或記住的）相同」判定的條文，拿來比的每一個欄位，測資都要選一個**與 App 讀取時遇到認不得的值所退回的那個值不同**的值。
- **為什麼**：讀取端把那個欄位漏掉或讀錯時，會退回同一個值；測資剛好就是那個值，兩邊相等，條文照樣通過 —— 它宣稱抓得到的錯誤其實抓不到（v3 驗收時，任務 1 驗收 1 就是這樣：測資的模型剛好是退回值）。
- **判法**：查讀取端遇到認不得的名稱時退回什麼，測資不得等於它。**現行有這種退回的是人格與模型**（對話上記的、與「上次停在哪」記住的都是）；訊息的作者沒有退回（認不得就整段讀不起來，歸 `spec.md` §1.5）。日後新增有退回的欄位，本條同樣適用。
- **不適用**：直接唯讀資料庫檔比對的條文（讀到的是原始字串，沒有經過 App 的退回），以及本來就要驗「讀不懂時退回什麼」的條文。

---

## 任務 1：對話儲存換成資料庫

- **說明**：把對話的儲存從舊位置的 JSON 檔換成資料庫，提供 `spec.md` §1 的全部性質，並讓儲存能通知「有變動」（§2 要用）。**用哪個程式庫、結構長什麼樣、怎麼切類別，由 RD 決定。** 存檔的**時機與次數**本輪不要求改動；若判斷非改不可，**先寫進本 slug 的 `pm-inbox.md`**（那兩件事受另兩份規格保護，改了會變成跨輪待決事項）。
- **對應功能模組**：對話儲存。
- **資料庫程式庫**：Room（使用者指定，`spec-architecture.md` §I）。
- **測試（v2，依 D1 改寫）**：**資料庫層不寫自動化測試**（使用者開的例外，見「明確不做」）。從資料庫層拆出來的規則 —— 讀回不加工（§1.1）、空白對話的處置（§1.2）、一筆解不開只丟那一筆與兩條讀取路徑同一個答案（§1.5）—— **照常用 JUnit 測，而且測試不得碰資料庫**。
  - **可驗證性要求**：上面那幾條規則必須放在一個**不依賴 Android、不依賴資料庫**、能被測試直接驅動的地方。怎麼切由 RD 決定；RD 回報的做法（Room 只交出每一列的原始字串，解析與容錯在純邏輯裡做，`spec-architecture.md` §B.1、§C.1）滿足這一點。
- **驗收標準**：
  1. **讀回來的就是存進去的（§1.1，不碰資料庫）**。一支在標準測試指令下執行、不碰資料庫的測試：一段對話含 `spec.md` §1.1 第一條可判定列出的每一種文字，轉成**存進資料庫的形式**、再經由**「依 id 取」與「取全部」共用的讀取規則**轉回來，每一個欄位、每一則訊息都與原本的**完全相等**（不是「不為空」、不是 `contains`）。同一支測試另轉一則被標成「生成中」的訊息 → 轉回來不是生成中。
     - **（v4）測資的人格與模型都不得是讀取時的退回值**（本檔「測資與讀取的約定」／「比對的值，不得等於 App 讀不懂時會退回的值」）；訊息要同時含使用者與助理兩種作者。
     - **鑑別力**：把讀取端的模型改成一律給退回值，本條的測試必須變紅；把人格改成一律給退回值，同樣必須變紅。冷讀判法：測資的人格與模型各自不等於退回值，就判得出這兩種改法會讓斷言不相等。
     - *獨自抓得到什麼*：讀取時對文字再加工（現行的讀取修復會把兩個字元的 `\n` 變成換行、去掉前後空白）；任一欄位漏轉或轉錯（例如人格、模型、建立時間、訊息順序）。
     - **這一條管不到的**：資料庫那一端有沒有把字串原樣存好、原樣交回。那一半由裝置上的任務 7 驗收 8（冷啟動後內容都在）承接，並列在「明確不做」的 D1 例外裡。
  2. **（v2 依 D1 移除，編號保留不遞補。不要再驗它。）** 原本要求：並發存同一段對話時讀取不會讀不到它的資料庫層測試（§1.4，H2），並由 QA 以突變模式連跑 5 次。
     - **§1.4 的規則本身保留。** 現在的守衛是裝置上的任務 2 驗收 3（每輪都跑），以及 RD 的單一指令存檔（`spec-architecture.md` §B.1）。已知缺口與觸發條件在「明確不做」的 D1 例外那一條。
  3. **一段解不開不連累其他段，兩條讀取路徑同一個答案（§1.5，H3；不碰資料庫）**。一支不碰資料庫的測試，給讀取規則一組「從資料庫取出的原始列」：其中一列是解不開的 X（例如某則訊息的作者是不存在的值、或那一列的訊息內容根本不是合法格式），其餘是正常的 E、F。斷言：
     - 「取全部」的結果**恰為 E、F**，而且在測試時限內結束；
     - 「依 id 取」對 X → 找不到；對 E → 取得 E 且內容相等（E 的測資同樣適用「比對的值，不得等於 App 讀不懂時會退回的值」）；對一個從來不存在的 id → 找不到；
     - **對上面每一個 id，「依 id 取」與「取全部再挑出同一個 id」的答案相同。**
     - *獨自抓得到什麼*：一列壞資料讓整份讀取失敗（使用者看到整份歷史不見）；「依 id 取」走了另一套解析而繞過容錯（兩條路答案不同）；把「解不開」當成「稍後再試」而卡住。
     - **這一條管不到的**：「依 id 取」在資料庫那一端查的是不是同一列。由裝置上的任務 7 驗收 6 承接一部分。
  4. **空白對話的處置（§1.2；不碰資料庫）**。一支不碰資料庫的測試，驅動「存一段對話時該寫入還是刪除」的那個判斷：只有開場白的對話 → 判成**刪除那個 id**（不寫入）；只含助理訊息、id 為 X 的對話 → 判成刪除 X；訊息清單為空的對話 → 判成刪除。**正向對照**：有使用者訊息的對話 → 判成寫入。
     - *獨自抓得到什麼*：空白對話被寫進儲存（歷史長出空殼）；「外面來的只含助理訊息的那段」打開再離開後沒有被清掉（`conversation-search` 決策 J 的行為被默默改掉）。**這一條同時是 `docs/specs/conversation-search/spec-tasks.md`／「明確不做」裡「任務 16 的行為不要求自動化測試」那條觸發條件所要求的存取層那一半。**
     - **這一條管不到的**：「判成刪除」之後資料庫真的刪了。由裝置上的任務 7 驗收 4、5 承接。
  5. **（v2 依 D1 移除，編號保留不遞補。）** 原本要求：刪除只刪那一段、同一段存兩次仍是一段的資料庫層測試（§1.3、§1.6）。現在由裝置上的任務 7 驗收 7（刪除）、任務 2 驗收 3（恰好一列）與任務 7 驗收 4、9（對話數不變）承接。
  6. **（v2 依 D1 移除，編號保留不遞補。）** 原本要求：訂閱儲存變動的資料庫層測試。現在由裝置上的任務 2 驗收 3、4 承接（每一種會改變儲存的動作之後，清單都立刻跟著變）。
  7. **除本輪新增之外，專案既有的測試全部通過**（標準測試指令；一支都不得變紅、停用或刪除）。

## 任務 2：歷史清單跟著儲存走

- **說明**：讓抽屜上的歷史清單＝儲存裡的所有對話 − 開著的那段，並隨兩者任一變動更新（`spec.md` §2）。現行「存完之後讀一次全部」的做法要不要留，由 RD 決定，只要 §2 成立。
- **對應功能模組**：歷史清單的組合、聊天狀態。
- **驗收標準**：
  1. **組合本身可被直接驅動（可驗證性要求；不碰資料庫）**。一支不依賴 Android、在標準測試指令下執行的測試，給這個組合一個可控的「儲存裡的全部對話」來源與一個可控的「開著的 id」，斷言：
     - (i) 每一份輸出都等於**最新**的來源扣掉**最新**開著的那段；
     - (ii) 來源變動（新增、改內容、刪除）與開著的 id 變動，**各自**都會讓輸出更新；
     - (iii) **重現這個 bug 的順序**：開著 A（來源裡沒有 A）→ 開著的換成 N → 之後來源才出現 A → 最新輸出含 A、不含 N。
     - **鑑別力**：把組合改成「只在開著的 id 變動時重算、來源變動不重算」，(ii) 與 (iii) 必須變紅；把「扣掉開著的那段」拿掉，(i) 必須變紅。
     - *獨自抓得到什麼*：清單在某一刻讀一次就存起來（晚於那一刻的存檔永遠不會出現）；忘了扣掉開著的那段（`conversation-search` §5.2 的「歷史列表不含目前開著的那段」會被打破）。
  2. **（v2 依 D1 移除，編號保留不遞補。）** 原本要求：把組合接到真的儲存的自動化測試。「組合接錯來源（例如接成一次性讀取）」現在由裝置上的驗收 3、4 承接 —— 接錯時，切換或刪除之後清單不會跟著變。
  3. **在裝置上：串流中切換，被切走的那段立刻在清單上（§2.2，H1）。這一條每一輪都要跑**，不論那一輪有沒有動到儲存 —— 它是「重現案例進測試套件」那條規則的**替代守衛**（使用者開的例外，見「明確不做」的 D1 例外）。照 `spec.md` §2.2 的前置與預期：
     - **（v3 新增）每一次切換之前的擺設**：歷史清單要長到一個畫面放不下（不夠時先用正常操作多建幾段）；打開抽屜，把清單往下捲到**最上面那一列已經不在畫面上**，再關上抽屜。之後才送出訊息、做切換。
       - *為什麼*：v2 驗收時，被切走的那段在資料上是第一列，卻被捲在畫面上方看不到（`spec.md`「這次會踩到的既有事實」第 17 條）。上一次的畫面若剛好停在最上面，這個錯法照不出來。
     - **（v3）預期 1、2 的判法**：打開抽屜之後**先不做任何捲動**，以當下畫面上看得到的清單判定「最上面那一列是 A」。要捲動才看得到 A → 預期 1 不成立。「A 恰好一列」可以捲動整份清單確認。
     - 四個入口（切換人格、切換模型、開始新的對話、開啟另一段對話）**各做一次**，每一次預期 1–5 全部成立；
     - 零文字變體用「切換人格」做一次，預期 1–3 成立；
     - **這一次算不算數**：切換之後打開 A，那則回覆結尾是中斷標記才算「切換時還在生成」；結尾是完整回覆或其他收束標記 → 那一次不算，重做。零文字變體則看那則空回覆沒有留下、使用者那句還在。**切換之前的擺設沒有做到**（清單不夠長、或最上面那一列仍在畫面上）→ 那一次也不算。
     - 模型與提示由驗的人選（`spec.md` §2.2 給的提示加上 Gemma3 1B 是一組起點，不是要求）；報告寫明用了什麼、每次切換時回覆大約出現幾個字。**攔不到「還在生成」時**記「本環境無法觀察」，並寫明試過的模型、提示與串流大約持續多久 —— 不得只寫這幾個字。
     - **次數的理由**：這個 bug 在新做法下不再是競態，而是「每個入口有沒有接上」—— 每個入口一次就照得出來。**「並發不會讓讀取漏掉」這一條證明不了**；v2 起它沒有自動化測試，靠單一指令存檔從結構上排除，是使用者知情接受的缺口（「明確不做」的 D1 例外）。
  4. **在裝置上：其他會改變儲存的動作，清單也立刻跟著變（§2.3）**。同一次執行裡依序：
     - 不在生成時從抽屜開另一段 → 打開抽屜，剛才那段在清單上（可以捲動找到）；
     - 刪掉清單上的一段 → 那一列立刻消失，不必關再開；
     - **（v3 改寫）** 先照驗收 3 的擺設把清單往下捲過、關上抽屜；送出一則訊息、等回覆結束、切走 → 打開抽屜，**不做任何捲動**，畫面上最上面那一列就是那段，預覽是完整回覆的開頭。
     - *獨自抓得到什麼*：只有串流中那條路接上了、一般的切換或刪除仍靠舊的重讀（或根本沒更新）。驗收 3 只走串流中那條。

## 任務 3：還沒存下的東西仍然搜得到

- **說明**：`spec.md` §3。本輪改了清單的來源，搜尋不得跟著變成只看儲存。
- **對應功能模組**：聊天狀態、搜尋（不改搜尋本身）。
- **驗收標準**：
  1. **在裝置上：從未存下的新對話搜得到**。在一段有內容的對話裡切換人格，產生只有開場白的新對話 N → 從抽屜進入搜尋，查 N 開場白裡的一個詞 → N 的分組出現，而且排在最前。
     - *獨自抓得到什麼*：搜尋改成讀儲存（N 永遠不會被存下，所以永遠搜不到）。
  2. **在裝置上（盡力）：已送出、還沒收尾的那句搜得到**。在一段新開的對話送出一句含有獨特詞（例如 `harborlight`）的訊息，**回覆還在生成時**進入搜尋、查那個詞 → 那段的分組出現。
     - **（v4 改寫）這一次算不算數**：看到搜尋結果之後**立刻**回到聊天 ——
       - 回覆**仍在增加**（仍顯示生成中）→ 算數：查詢的整段期間都還在生成，那句一定還沒存下；
       - 回覆結尾是中斷標記（進入搜尋會停止生成的實作）→ 算數；
       - 回覆已經結束（完整回覆或長度上限等其他收束標記）→ **不算**，重做（換生成較久的模型、或更長的提示）。
       - *為什麼不再接受「進入搜尋前的截圖」*：v3 驗收時指出，那只證明進入搜尋前約 1 秒還在生成；回覆若在查詢之前就結束，那句已經存下，這一次照不出本條要抓的錯。
     - 攔不到「還在生成」時記「本環境無法觀察」，寫明試過的模型、提示、串流持續多久。**不判 fail**：驗收 1 已經判得出「搜尋改讀儲存」這一型。
     - *獨自抓得到什麼*：搜尋來源是「儲存 ＋ 開著的那段在儲存裡的版本」而不是記憶體裡的即時版本 —— 驗收 1 的 N 整段不在儲存，照不出這一型。

## 任務 4：舊資料：不搬遷、刪掉舊位置

- **說明**：`spec.md` §4。
- **對應功能模組**：舊資料清理、聊天狀態（啟動）。
- **驗收標準**：
  1. **清理的範圍（不經過裝置、不碰資料庫）**。一支不依賴 Android、在標準測試指令下執行的測試，在暫存目錄裡模擬 App 的檔案目錄：舊位置底下有三個 `.json`；舊位置旁邊有一個名稱以 `.task` 結尾的檔、一個以 `.task.part` 結尾的檔、一個名為 `profileInstalled` 的檔、另一個含檔案的子目錄。對它執行清理 → 舊位置不存在；**旁邊每一個檔的位元組都與清理前相同**，子目錄與其中的檔仍在。另外三個案例：舊位置本來就不存在 → 不出錯、旁邊的東西不變；舊位置裡有一個刪不掉的檔（把舊位置設成不可寫）→ **清理不丟出錯誤**、旁邊的東西不變；舊位置恢復可寫之後再清理一次 → 舊位置不存在。
     - **可驗證性要求**：清理必須能對任意一個目錄執行，才寫得出這支測試。
     - *獨自抓得到什麼*：刪錯一層（模型檔被刪，使用者要重新等 GB 級的解壓；偏好被刪，忘記上次停在哪）；刪不掉時讓啟動失敗。
  2. **在裝置上：升級情境（§4.1、§4.2）**。新建置已安裝、至少一個模型已經解壓過。**這一條的前置走「測資與讀取的約定」的第二條路**（新建置做不出舊格式的檔，也做不出指向舊對話的偏好）。結束 App 的行程，把三個**舊格式**的對話檔放進舊位置（格式照舊版程式：commit `a4c7b20` 的 `app/src/main/java/com/example/demo/data/ConversationStore.kt`，可用 `git show a4c7b20:<路徑>` 查），其中一個的 id 寫進「上次停在哪」的偏好，人格與模型也寫進偏好（各選一個非預設值）。冷啟動：
     - (i) 抽屜顯示「還沒有其他對話。」；搜尋那三段裡的詞都找不到；
     - (ii) 聊天畫面是一段只有開場白的新對話，頂端的人格與模型是寫進偏好的那一組；
     - (iii) 舊位置不存在；
     - (iv) 沒有出現「正在把 AI 模型解壓到裝置上」的橫幅，App 檔案目錄裡原有的模型檔仍在（以檔案清單與大小判定）；
     - (v) 送出一則訊息、等回覆結束，再冷啟動一次 → 續接到那段、內容都在、舊位置仍不存在。
     - **做不到「把檔放進舊位置」時**：記「本環境無法觀察」並寫明原因；(iii) 與 (iv) 的範圍由驗收 1 承接，(i)(ii) 仍可用「清空資料後第一次啟動」的畫面判一半，報告要明講只判了一半。
     - *獨自抓得到什麼*：新建置把舊檔讀進來（搬遷或顯示了）、升級後因為「上次停在哪」指到不存在的對話而卡住或閃退、清理在真的裝置目錄上刪錯東西。

## 任務 5：儲存壞掉或寫不進去

- **說明**：`spec.md` §5。§5.1 依 D3 選 (b) 寫定：告知一次、丟掉壞的那份、重新開始。**Room 的預設行為只做到一半**（`spec-architecture.md` §G）。**（v3）新增 §5.4**：開不起來、但判斷不出內容壞了時，不刪、告知、下次啟動再試（`spec-architecture.md` §G.2）；§5.1 只剩內容本身壞了的情況。
- **對應功能模組**：儲存故障的呈現、對話儲存、聊天狀態。
- **驗收標準**：
  1. **在裝置上：整份讀不起來（§5.1）**。先用正常操作存好一段對話 P。結束 App 的行程，照「測資與讀取的約定」第二條路把資料庫檔整份覆寫成無法讀取的內容（例如亂碼），冷啟動：
     - 不閃退；聊天畫面出現；沒有出現解壓橫幅；可以送出訊息、收到回覆、按停止；
     - 畫面上出現**一次**「之前的對話紀錄讀不出來，已經清空，從這裡重新開始。」，至少停留 3 秒或直到關掉它；
     - 抽屜是「還沒有其他對話。」、搜尋 P 裡的詞找不到（壞掉的那份不保留）；
     - 「上次停在哪」的偏好內容與冷啟動前相同；
     - 在一段新對話送出訊息、等回覆結束、再冷啟動 → 那段在，**而且提示不再出現**。
     - *獨自抓得到什麼*：只靠 Room 預設的刪檔、沒有提示（使用者看到抽屜突然空掉）；每次啟動都跳提示（沒有真的重新開始）；之後的對話存不下來。
     - **使用途中才發現損毀的那一種**在裝置上造不出可靠的時機，**本條不驗**；規則仍在 `spec.md` §5.1，建議在 `spec-architecture.md` §G.1。
  2. **在裝置上（盡力）：開得起來、但寫不進去（§5.2）**。先存好一段對話 P。用任何能讓儲存**開得起來、但寫入失敗**的方式（例如讓手機的可用空間用完）→ 在另一段對話送出訊息、等回覆結束 → 不閃退；出現「有一段對話沒能存下來。」且至少停留 3 秒；那段內容仍在畫面上、可以再送出；**寫入失敗期間打開抽屜，P 在清單上**、內容不變。恢復可寫之後再送出一則並等回覆結束 → 冷啟動後那段在。
     - **（v3）不要用「把資料庫檔設成唯讀」造**：v2 驗收實測，那在 Android 上造成的是整個開不起來（`spec.md`「這次會踩到的既有事實」第 14 條），那是驗收 4 的情境。
     - **這一次算不算數**：寫入失敗期間打開抽屜，**顯示的是 §5.4 的「目前讀不到之前的對話紀錄。」** → 造出來的是「開不起來」，這一次不算，換一種造法。P 不在清單上、而抽屜也沒有那句 → **fail**。
     - **P 用正常操作建立**；讓儲存寫不進去的那一步屬於「測資與讀取的約定」第二條路。造法若動到這個 App 以外的東西（例如填滿裝置空間），做完要還原，並在報告寫明做了什麼、怎麼還原的。
     - **（v4）填滿空間時的已知情況**（v3 驗收實測）：空間用完時，系統會自己清掉各 App 的快取，可用空間隨即回升，那一次存檔可能其實成功了。**填滿之後的那次存檔沒有出現「有一段對話沒能存下來。」、而且儲存裡有那段 → 這一次不算**，再填滿再試。**系統自己清掉的其他 App 快取不必還原**（那是系統行為，驗收裝置是模擬器），報告寫明有發生即可。
     - **環境做不出「開得起來、但寫入失敗」時**：記「本環境無法觀察」並寫明試過的方式與結果，**不得寫成 pass**。這時不閃退、提示、內容仍在畫面上、恢復後存檔成功這幾件，由驗收 4 在「開不起來」的狀態下判到；**「寫入失敗期間 P 仍在清單上」這一輪沒有判到**，報告要明講。
     - *獨自抓得到什麼*：寫入失敗讓 App 閃退；失敗時沒有任何告知；一次寫入失敗讓已存的對話從清單上消失，或被弄丟。
  3. **（v2 依 D1 移除，編號保留不遞補。）** 原本要求：資料庫層「寫入失敗不毀掉已存的」自動化測試。**已知缺口**，由驗收 2 的「P 仍在」部分承接，以及單一指令存檔的結構保證；見「明確不做」的 D1 例外。
  4. **（v3 新增）在裝置上：開不起來、但內容沒壞（§5.4）**。前置：用正常操作存好兩段對話 P1、P2（其中一段的訊息含 emoji 與換行），再開始一段新的對話（讓 P1、P2 都不是開著的那段）；結束 App 的行程，照「測資與讀取的約定」第二條路**讀一次儲存的內容當基準**。然後在 App 停止時，用下面的造法讓 App 開不起來儲存（**不改檔案內容**），冷啟動。兩種造法**各做一輪**：
     - **造法 (i)**：資料庫的檔案（主檔與它旁邊的附屬檔）都改成 App 只能讀、不能寫（v2 驗收時用過，已知會讓整個開不起來）；
     - **造法 (ii)**：資料庫主檔改成 App 連讀都不能讀。
     
     每一輪，冷啟動之後：
     - (a) 不閃退；聊天畫面出現；沒有出現解壓橫幅；可以送出訊息、收到回覆、按停止；
     - (b) 畫面上出現**一次**「目前讀不到之前的對話紀錄（紀錄沒有被刪除），這段期間的對話也可能存不下來。」，至少停留 3 秒或直到關掉它；**整輪都沒有出現**「之前的對話紀錄讀不出來，已經清空，從這裡重新開始。」；
     - (c) 打開抽屜：歷史區顯示「目前讀不到之前的對話紀錄。」，**沒有**「還沒有其他對話。」；
     - (d) 送出一則、等回覆結束 → 出現「有一段對話沒能存下來。」，至少停留 3 秒；那段內容仍在畫面上，可以再送出；
     - (d2) **（v4 新增，造法 (i) 那一輪做即可）** 結束 App 的行程，**權限先不要恢復**，再冷啟動一次 → (b) 的提示**再出現一次**、(c) 仍成立，仍沒有清空的提示。
       - *獨自抓得到什麼*：「已經提示過」被記到下一次啟動（例如寫進偏好），阻礙還在、提示卻不再出現，使用者以為問題已經排除（`spec.md` §5.4「阻礙沒有排除時，每一次冷啟動都照上面再來一次」）。(f) 是阻礙排除之後的啟動，照不出這一型。
     - (e) 結束 App 的行程，把檔案權限恢復原狀，讀一次儲存的內容 → **與基準相同**（P1、P2 每一個欄位、每一則訊息都在，沒有多也沒有少）；
     - (f) 冷啟動 → P1、P2 在抽屜上、內容與前置時相同；(b) 的兩種提示**都沒有出現**；再送出一則、等回覆結束、冷啟動 → 那段在。
     - **造法 (ii) 在環境裡做不出來時**（例如權限改了 App 仍讀得到）：記「本環境無法觀察」並寫明試過什麼、結果如何；造法 (i) 仍要做。**這時「判斷不出內容壞了的其他開檔失敗不得刪資料」這一輪只判到唯讀那一種**，報告要明講。
     - *獨自抓得到什麼*：
       - 開不起來時抽屜默默變空、沒有任何說明（v2 實作的樣子）；提示消失之後，抽屜仍寫「還沒有其他對話。」讓使用者以為紀錄不見了；
       - 把「開不起來」當成「內容壞了」走 §5.1，把完好的紀錄刪掉 —— (e) 讀到的內容少了、或 (b) 出現了清空的提示。造法 (ii) 是衝著「開不了檔的那一類錯誤被算成壞了」（`spec.md`「這次會踩到的既有事實」第 15 條）；
       - 開不起來讓 App 閃退、或讓聊天也不能用。
     - **正向對照**：驗收 1（內容真的壞了）必須走清空重來、出現清空的提示。兩條合起來，一個「永遠不刪」或「永遠刪」的實作都會被抓到。

## 任務 6：驗收環境看得到儲存（v2 依 D2 寫定）

- **說明**：「測資與讀取的約定」的第二條路（直接改資料庫檔）與讀取方式，需要知道**資料庫檔在裝置上的位置與結構** —— 包含訊息那一欄裡的格式（RD 的做法是把一段對話的訊息整包存在一個文字欄位裡）。**交付物是 `plan/architecture.html` 儲存那一節裡的說明**（任務 8 驗收 9）。**產品程式碼不為此加任何東西。**
- **對應功能模組**：文件。
- **驗收標準**（由 QA 照交付物實際做一次；**全程在 App 停止時讀寫，改完再冷啟動**）：
  1. **讀得出來**：先用**正常操作**建立兩段對話（其中一段的訊息含 emoji 與換行）。照交付物，在不改專案原始碼的前提下，從 App 以外列出儲存裡每一段對話的每一個欄位與每一則訊息；列出的結果與 App 畫面上看得到的一致。判讀完照約定清理那兩段。
  2. **寫得進去**：照交付物寫入一段**只含助理訊息**的對話 X → 冷啟動後 X 出現在抽屜上，標題是「還沒說話」，點開看得到那幾則助理訊息。
     - **只驗 App 做不出來的那一種**（v2 改寫）：含使用者訊息的對話屬於第一條路，用正常操作建立，不必從外面寫。
  3. **弄得壞**：照交付物，讓儲存裡**一段**對話解不開（其餘照常，冷啟動後其餘都在清單上），以及讓**整份**儲存讀不起來（冷啟動後走 `spec.md` §5.1）。
  - 三件事任一做不到即 **fail**，並寫明卡在哪一步。**這一條驗的是交付物夠不夠用，不是環境能力** —— 若卡住的原因確實是環境（例如主機上沒有需要的工具），寫明原因，由 PM 判斷交付物要不要補。
  - **不歸這裡**：QA 的工具與 `docs/qa-environment.md` 怎麼改，歸 QA。

## 任務 7：回歸驗證（每輪必做）

- **說明**：本輪換掉的是每一條存檔與讀取都會經過的那一層，並改了清單的來源。下面是另兩份規格裡**會因為換儲存而受影響**的行為，**以不綁儲存形式的寫法**在本輪各驗一次。**測資與「儲存的內容」照本檔「測資與讀取的約定」**：App 做得出來的對話用正常操作建立、判讀完才清理；判「儲存的內容」是 App 停止時唯讀資料庫檔。
- **對應功能模組**：所有會存檔、讀歷史的路徑。
- **驗收標準**：
  1. **停止當下就存下（`docs/specs/stop-generation/spec.md` R5「停止當下就必須落盤」）**：回覆生成中按停止，**不做任何其他操作**，立刻結束 App 的行程、冷啟動 → 續接到那段，那則回覆的文字與停止時相同、以中斷標記結尾。
  2. **存下的是被停止的那段（同一份規格 R6「落盤的必須是「被停止的那一段」」）**：生成中從抽屜開另一段既有對話 B → 儲存裡 A 那段的那則回覆以中斷標記結尾；**B 在儲存裡的內容（每一個欄位、每一則訊息）與切換前相同**。
  3. **切走沒有改到內容的對話，儲存裡的它不變（`docs/specs/conversation-search/spec.md` §8.4「可判定（儲存體裡的時間不動）」）**：一段有內容、最後更新時間早於現在至少 1 分鐘的對話 A 開著，不做任何事，從抽屜開另一段 → 儲存裡 A 的內容（含最後更新時間）與切換前相同。
  4. **新開沒說過話的對話不留（同一份規格 §9.1「儲存一段空白對話等於刪檔，不是寫檔」）**：換人格產生只有開場白的對話，分別用開另一段、開始新的對話、換人格、換模型四種方式離開 → 每一次前後，儲存裡的對話數與每一段的內容都相同。**正向對照**：在一段新開的對話送出訊息、等回覆結束再離開 → 儲存裡多了那一段。
  5. **外面來的空白對話打開再離開就清掉（同一節的決策 J 那一項）**：先用**正常操作**建立一段含使用者訊息的對話 Y 並切走；結束行程，從 App 以外寫入只含助理訊息的 X（v2：只有 X 走第二條路）→ 冷啟動 → 點 X、再從抽屜開 Y → 儲存裡沒有 X、冷啟動後抽屜與搜尋都找不到 X；再開一段新的對話 → **儲存裡 Y 仍在、內容不變**。判讀完照約定清理 Y。
  6. **一段讀不起來不連累其他段（同一份規格 §9.2「一個讀不起來的對話檔，不得讓其他對話消失」）**：從 App 以外讓儲存裡一段對話讀不起來，其餘兩段正常 → 冷啟動後抽屜列出那兩段，App 不閃退。
  7. **刪掉就是真的刪掉（同一份規格 §9.4「使用者從歷史清單刪掉一段對話，就是真的刪掉」）**：開著 A，從抽屜刪 B → 儲存裡少一段、沒有 B、A 的內容不變；冷啟動後抽屜與搜尋都找不到 B。
  8. **回覆結束後直接關 App 仍在；冷啟動回到上次那段**：送出一則訊息、等回覆結束、不做任何事、結束行程再冷啟動 → 續接到那段，使用者那句與完整回覆都在，**頂端的人格與模型與結束前相同**。再換人格產生一段沒說過話的對話、結束行程再冷啟動 → 以記住的人格與模型開新的一段。
     - **（v4）兩半用的人格與模型都不得是讀取時的退回值**（本檔「測資與讀取的約定」／「比對的值，不得等於 App 讀不懂時會退回的值」）：前半那段對話本身的人格與模型、後半換到的人格與當時的模型，各選非退回值。
     - *獨自抓得到什麼*（v4 補明）：資料庫那一端把人格或模型的欄位存錯、交錯（任務 1 驗收 1 管不到資料庫那一端）；「上次停在哪」記住的人格或模型讀不回來、退回預設值。
  9. **搜尋與點命中列不改動儲存（同一份規格 §1.10「搜尋全程不觸發推論、不產生檔案讀寫」與 §6.2–§6.4）**：取一次儲存的內容當基準（**前置擺設都做完之後才取**，被點到的那段要含使用者訊息；**測資的清理排在判讀之後**）→ 進搜尋、打字到有結果、點另一段的命中列開啟它、再進搜尋點回原本那段 → 儲存裡的對話數與每一段的內容都與基準相同。
  10. **聊天流程照常**：送出、串流、停止、刪除、切換人格／模型、冷啟動續接，每一項做得完並出現直接結果（送出後使用者那句與回覆出現；串流時回覆逐步增加直到結束；停止後回覆不再增加；刪除後那一列消失；切換後開了一段新的、頂端換成選的人格或模型；冷啟動後回到上次那段）。任一項做不完、出錯或閃退即 fail。
  11. **除本輪新增之外，專案既有的測試全部通過**（標準測試指令）。
  12. **（v2 依使用者裁決移除，編號保留不遞補。不要再驗它。）** 原本要求：`docs/specs/stop-generation/spec.md` 的任務 2、3、4 在本輪整組重跑。
      - **（v4，PM 判斷）v3 驗收之後沒有新的觸發。** QA 在 v3 驗收報告裡依本條提出一次「觸發條件成立」，指的是 commit `6b284d1` —— 下面 v3 的裁決已經處理過它。v3 裁決之後的兩個程式 commit —— `4ad41b1`（v3 的實作：開不起來時的提示與抽屜文字、判定範圍）與 `b0ad0fa`（一支讀回測試的測資、文字修復的一段程式碼註解）—— PM 讀過差異，**都沒有碰停止、回覆收尾、中斷標記那一段程式**，所以不算新的一次成立，不再請使用者裁決。
      - **裁決（v3，2026-09-17，現行）**：v2 驗收之後，RD 為了修串流中切換模型的閃退，改了引擎取消生成的流程（`spec.md`「這次會踩到的既有事實」第 16 條）—— 那正是下面「之後什麼時候跑」寫的「改動停止…那一段程式」。**使用者知道條件又成立一次，仍決定本輪不重跑。**
      - **裁決（v2，2026-09-17，歷史）**：那一輪的 S3 觸發條件（程式再被動到）在 v2 **確實成立**，使用者知道這一點，決定不重跑。
      - **現在誰在守**：與儲存有關的那幾件 —— 停止當下就存下、存的是被停止的那段、切換觸發的停止也存下 —— 由本任務驗收 1、2 與任務 2 驗收 3 以不綁儲存的寫法驗。v3 這次改動直接碰到的兩個後果，由本任務驗收 13（切換模型不閃退）、14（停止後立刻再說話不被卡住）驗；**那兩條不是 `stop-generation` 的重跑**。
      - **知情接受的缺口**：`stop-generation` 其餘的裝置端行為（停止不是錯誤、中斷標記逐字、停止後不再變動、零文字、兩種自動中斷，以及「立刻可再說話」除了驗收 14 量到的那一段以外的部分）沒有在模擬器上重驗，而 v2 動到了它們所在的聊天狀態、v3 動到了取消生成的流程。
      - **之後什麼時候跑**：`stop-generation` 那份規格下一次被修訂時；或任何一輪改動停止、回覆收尾、中斷標記那一段程式時。屆時照那一份自己的 R3 註記處理時序競爭的歸類。**這個條件已經兩次成立、兩次被使用者略過；下一次成立時仍要提出來，不因前兩次的裁決自動略過。**
  13. **（v3 新增）串流中切換模型，App 不閃退**（v2 驗收撞到的閃退，RD 已修，`spec.md`「這次會踩到的既有事實」第 16 條）。這是那個閃退的重現案例，**每輪都跑**（原生推論引擎在不經過裝置的測試裡載不起來，所以只能在裝置上）。
      - 回覆生成中（已經出現文字、而且還在增加）從抽屜點另一個模型，**做 5 次**；其中**至少 3 次是從 Qwen2.5 0.5B 串流中切走**（RD 修正前在「Qwen 串流中切到 SmolLM」上 3 次閃退 2 次）。其餘的模型組合由驗的人選，報告寫明每一次的組合。
      - 每一次：App 沒有閃退（行程仍在、沒有新的閃退紀錄）；頂端換成選的模型；之後送出一則能收到回覆。**任一次閃退即 fail。**
      - **這一次算不算數**：切換之後打開被切走的那段，那則回覆結尾是中斷標記才算（同任務 2 驗收 3）；不算的那次重做。
      - *獨自抓得到什麼*：那個修正被拿掉或失效。任務 2 驗收 3 只切換模型一次，而修正前一次切換不一定會閃退，所以要多做幾次。
      - **它證明不了的**：閃退不會再發生。這是時序競爭，次數只能提高照到的機會。
  14. **（v3 新增）停止之後立刻再說話，回覆不被卡住**。同一個模型下：
      - 先**不停止**、送出一句並等回覆結束，記下「送出到回覆第一個字出現」大約幾秒；
      - 再送出一句，回覆生成中按停止；**停止鈕一變回送出鈕就立刻**送出下一句 → 使用者那句出現，回覆開始出現並照常結束，App 沒有閃退。記下這一次「送出到回覆第一個字出現」大約幾秒。
      - 停止再送出的部分做 2 次。**任一次比不停止的那一次多出 5 秒以上，即 fail。** 報告寫明用的模型與每一次的秒數。
      - *5 秒從哪來*：取消之後的收尾正常時不該讓人察覺；引擎卡在等待上限時，會多出約 10 秒。5 秒是 PM 取在兩者之間的界線。
      - *獨自抓得到什麼*：v3 的修正讓引擎等被取消的推論真的停下來才讓出；取消之後推論若遲遲不結束，下一句的回覆要多等好幾秒才出來。驗收 10 只要求停止與送出各自做得完，不量這段等待。
  - *這一組為什麼不直接指向那兩份規格的條文*：那些條文的可觀察物寫的是 JSON 檔，本輪之後照字面做不出來（`spec-appendix.md`／「跨輪衝突清單」）。這裡驗的是**同一件事的使用者後果**，不是另寫一份規則。

## 任務 8：更新架構文件

- **本輪（v4）結論：本輪無需更新。** v3 的更新已通過驗收 1–14。v3 驗收之後唯一的程式改動（`b0ad0fa`）是一支測試的測資與一段程式碼註解，文件描述的行為與模組都沒有變；本輪新增的「比對的值不得等於退回值」是驗收的約定，不是系統的性質。**驗收 1–9、11–14 的要求不變，本輪仍要成立**（程式沒有動到那幾處，冷讀一次即可）；**驗收 10 本輪不要求新的一筆**（它記的是「更新文件時發現的不符」，本輪不更新文件）。
  - **本輪 RD 若另外改了程式**，而改動讓文件某處不再相符，這個結論就翻成「需要更新」，照驗收 1–14 做，並在 `findings.md` 補 v4 的一筆。
- **（v3 的結論，歷史）需要再更新一次。** v2 的更新已通過驗收 1–9、11，但 v2 驗收之後的改動（§5.4 的新行為、RD 在 `6b284d1` 對取消生成與整份重來的修正）讓「儲存」那一節對資料庫出問題的分類過期了；另有兩處本來就不準的地方（驗收 14）。驗收 1–9、11 的要求不變，本輪仍要成立。
- **（v2 的結論，歷史）** 需要更新：`plan/architecture.html` 有多處把「一段對話一個 JSON 檔」寫成現行儲存方式，另有兩處在 v2 之前就已經過期。
- **對應功能模組**：文件。
- **驗收標準**（冷讀 `plan/architecture.html` 判）：
  1. **全檔不再有任何一處把「一段對話一個 JSON 檔」描述成現行的儲存方式** —— 包括圖裡的文字、圖的無障礙標籤、圖下方的說明、「幾條刻意維持的規則」、模組地圖。提到舊版曾經這樣存、以及升級時會刪掉，不算。
     - *為什麼寫成整份檔案的性質*：同一件事在這份文件裡有好幾個載體（圖中字、無障礙標籤、說明文字），逐處列舉會漏（`stop-generation` 撞過同型）。
  2. 「幾條刻意維持的規則」裡談「對話之間的隔離是結構性的」那一條，**改寫之後說的每一件事都與現行程式相符**（隔離不再來自「不在同一個地方」；刪除不再是「刪一個檔」）。
  3. 描述 `ConversationRepository`、標題為「什麼事都沒做，這是刻意的」的那一段：**若那一層不再只是轉發，就改寫成它現在做的事**；若那個檔案已不存在，移除或改寫成現在負責這件事的那一層。
  4. 「搜尋」那一節裡「資料層那邊也刻意沒有查詢方法」與「磁碟永遠不會比較新」兩句：**改寫之後仍與現行程式相符**，而且讀完仍然得出「搜尋看的是記憶體加歷史清單，不是只看儲存」（`spec.md` §3）。
  5. **新增**：歷史清單是由儲存的變動與開著的是哪一段持續算出來的（`spec.md` §2.1），以及舊位置在啟動時被刪除（`spec.md` §4.2）。用白話寫性質即可。
  6. 「文字修復」那一節說修復「生成時和讀取舊紀錄時都會經過」、以及「幾條刻意維持的規則」裡「舊紀錄不會被重新貼標」那一條：**改寫之後與現行程式相符**（`spec.md` §1.1 規定讀取時不得加工）。
  7. **模組地圖**：資料層每一列的路徑在專案裡存在、職責與該處程式碼現在做的事相符；本輪新增的模組（歷史清單的組合、舊資料清理、儲存故障的呈現，依 RD 實際的切法）各有一列或被併入某一列的職責描述。
  8. **（本輪之前就過期）** 「純邏輯」那一句寫著「五支測試各對應一塊」：改寫之後**不寫死任何測試支數**；若本輪新增的純邏輯（讀取規則、清單組合、舊目錄清理）各有測試，那句話列出的「純邏輯」要涵蓋它們，或改成不逐一列舉的寫法。頁首的「約 3,000 行 Kotlin」：**改成不會自己過期的寫法或移除**（本輪核對時 `app/src/main` 約 3,160 行，而這個數字每一輪都會變）。
  9. **（v2 依 D2 寫定）** 「儲存」那一節寫明：對話資料庫檔在裝置上的位置、表與欄位、以及訊息那一欄裡的格式，足以讓任務 6 的三件事做得到；並寫明**從 App 以外修改時要在 App 停止時做**（Room 看不到外部修改）。**夠不夠用由任務 6 判**，本條只判「那一節裡有這些說明」。
  10. **（v3 改寫：過程條文，QA 只驗得到痕跡）** 更新過程中發現文件其他地方與程式碼不符，**寫進本 slug 根目錄的 `findings.md`「架構文件更新時發現的不符」一節**：每一輪一筆，標明版號與日期；沒有發現也要寫「沒有發現」（本輪不要求一併修，除非改動極小）。**已知一處不修**：重複迴圈「收成兩輪」的敘述（實際保留三輪，`stop-generation` 的「明確不做」有觸發條件，本輪不碰那一段）。
      - **由誰執行**：RD。
      - **QA 驗得到**：那一節裡有 v3 的一筆。
      - **QA 驗不到**：RD 是否真的把全文與程式碼比對過、是否漏掉了別的不符。**這一條不構成「文件其他地方都與程式碼相符」的證據**；文件與程式碼相符與否，只有本任務其他各條指名的那幾處被驗到。
      - **檔案還不存在**：本輪由 RD 建立（跨輪的檔案，放在 slug 根目錄，不放進版號目錄；只增不改）。v2 那一次的回報（沒有新發現；模組地圖少了 `MainActivity.kt` 與 `ui/theme/`）記在 `spec-appendix.md`「v3：RD 在 v2 驗收之後的修正」，要不要一併補成 v2 的一筆由 RD 決定，QA 不驗。
  11. **（v2 依 D4 新增）** 「儲存」那一節裡有一句話說出：**`docs/specs/` 底下較早的規格若把對話寫成「一段一個 JSON 檔」（檔案數、檔案位元組、放進對話目錄），那些條文描述的是舊的儲存；修訂那些規格時以本文件與 `docs/product/features.md` 為準。** 以意思判。
      - *獨自抓得到什麼*：日後修訂 `conversation-search` 或 `stop-generation` 的人，從架構文件讀儲存時不會知道那兩份的檔案條文已過期。這句話的另一個落點在 `docs/product/features.md`（任務 9 驗收 6）；兩處各守一種讀者（架構文件給 RD、功能清單給 PM 與 QA）。
  12. **（v3 新增）** 全檔描述「資料庫出問題時會怎樣」的地方（「儲存」那一節、模組地圖資料層那幾列、以及其他提到這件事的段落），說的分類都與 `spec.md` §5 開頭那張表一致：**內容壞了** → 丟掉重來並提示；**開不起來、判斷不出內容壞了**（例如唯讀、權限不對）→ 不丟、提示、下次啟動再試；**開得起來但某次寫不進去**（例如空間不足）→ 不丟、提示。**不再有任何一處把「打不開」歸到丟掉重來，也不再把「唯讀」當成寫不進去的例子。** 以意思判。
      - *獨自抓得到什麼*：v2 更新後的「儲存」那一節仍寫著「打不開」要丟掉重來、「唯讀」屬於寫不進去 —— 日後讀文件改這段程式的人，會照著它把 §5.4 的行為改回去。
  13. **（v3 新增）** 描述「取消生成」的地方（「一則訊息從送出到存檔」那一節，或其他講到停止、切換時怎麼收掉生成的段落），說出兩件事：取消之後，**要等推論真的停下來才關掉 session、才讓出引擎**；不這樣做的後果是**串流中切換模型時，App 可能在原生程式裡閃退**。以意思判。
      - *獨自抓得到什麼*：日後有人為了讓切換或停止更快，把這段等待拿掉 —— 程式碼註解說了原因，但規劃改動的人讀的是架構文件。
  14. **（v3 新增，本來就不準的兩處）** (i) 模組地圖涵蓋 `MainActivity.kt` 與 `ui/theme/`（各一列，或併進某一列的職責描述）；(ii) 模組地圖與「純邏輯」相關的敘述，對 `data/ConversationRecords.kt` 的描述與它現在實際依賴的東西相符（v2 驗收時它被寫成完全不依賴資料庫程式庫，但它用了資料庫程式庫的標註）。改文件或改程式都可以，判的是兩邊說的一致。
      - *獨自抓得到什麼*：驗收 7 只要求資料層每一列與新增的模組；這兩處不在它的範圍內。

## 任務 9：`docs/product/features.md` 與本規格一致

- **說明**：本輪由 PM 在寫規格的同時改 `docs/product/features.md`：人格-04、模型-04、保存-04 的引用改指本規格；保存-02 的例外改成不綁檔案的說法；新增保存-07（升級不搬遷）、保存-08（儲存讀不起來，v2 依 D3 補上）；**v2 依 D4 在「這份文件怎麼維護」補上「較早的規格與全域文件不一致時以全域文件為準」**，並在指向那兩份舊規格、而條文仍綁 JSON 檔的幾列旁加註。**v3**：修正保存-02 例外句末 v2 驗收時仍留著的「檔」字（驗收 5 當時 fail）；新增保存-09（儲存開不起來但沒壞，§5.4）。**v4：PM 沒有改這份文件**（本輪沒有新增或改變使用者看得到的行為；「刪除失敗時不提示」決定不規定，見「明確不做」）；驗收 1–7 的要求不變。本任務驗那幾處的結果。**執行者是 PM**，本任務只驗結果。
- **對應功能模組**：文件。
- **驗收標準**：
  1. 人格-04、模型-04、保存-04 三列的「對應 spec」欄**各自含有**指向 `docs/specs/conversation-store-db/spec.md` §2.2「回覆還在生成時切換，被切走的那段一打開抽屜就在清單上」的引用；那個節名在本規格版號最大的目錄的 `spec.md` 裡 grep 得到，而且落在 §2.2 的標題上；引用不含版號目錄。
     - **正向對照**：保存-04 原本指向 `stop-generation` R6、R10 的兩個引用仍在。
  2. 三列裡**不再有**指向 `docs/specs/history-missing-after-switch/` 的引用。
     - *獨自抓得到什麼*：那個目錄日後被收掉時，功能清單留下追不到的引用。
  3. 保存-07 存在，說出兩件事：從舊版升級時舊的對話不會帶過來；而且會從手機上刪掉。它指向本規格 §4.1 與 §4.2，節名 grep 得到。
  4. 保存-08 存在，說出：儲存整份讀不起來時 App 不閃退、仍能聊天、會出現 `spec.md` §5.1 那句提示、舊的紀錄被清空、之後的對話照常存。它指向本規格 §5.1，節名 grep 得到。
  5. 保存-02 的例外不再寫「對話檔」「放進手機」這類只對舊儲存成立的字眼，意思仍是：從 App 以外寫進來、沒有任何一句使用者訊息的對話會出現在清單上，打開再離開就被清掉。它指向 `conversation-search` 的兩個引用仍在。
  6. **（v2 依 D4 新增）** 「這份文件怎麼維護」一節裡有一條，說出三件事：(i) `docs/specs/` 底下較早的規格可能仍用舊的儲存方式（一段對話一個 JSON 檔）描述條文；(ii) 修訂那些規格時，要以本文件與 `plan/architecture.html` 為準，把那些條文換掉；(iii) 改寫清單在 `docs/specs/conversation-store-db/spec-appendix.md`「跨輪衝突清單」。以意思判。
     - **另外**：保存-02、保存-03、保存-04、保存-05 這四列（它們指向的舊規格條文仍綁 JSON 檔）的「對應 spec」欄各自帶著一句指回那一條的提醒。
     - *獨自抓得到什麼*：D4 的規則沒有落在日後修訂那兩份規格的人會讀到的地方。那兩份規格本輪不能動；PM 每一輪都要改這份文件，而那四列正是從功能追到那幾條舊條文的入口。
  7. **（v3 新增）** 保存-09 存在，說出：對話紀錄暫時開不起來時（例如檔案權限不對），App 不閃退、仍能聊天、會出現 `spec.md` §5.4 那句提示；**紀錄不會被刪掉**，問題排除之後下次打開就回來；這段期間的對話可能存不下來。它指向本規格 §5.4，節名在本規格版號最大的目錄的 `spec.md` 裡 grep 得到。
     - **正向對照**：保存-08 仍在，仍指向 §5.1，節名仍 grep 得到（v3 改了 §5.1 標題括號裡的字）。
     - *獨自抓得到什麼*：功能清單只寫了「壞掉就清空」，讀的人會以為任何讀不到的情況都會清空紀錄。

---

# 明確不做

每條附「什麼時候該回來處理」。

**不搬遷舊對話。** 使用者裁決：這是資料庫的第一版，舊的 JSON 對話全部清空。
- **後果（使用者知情接受）**：**升級之後，使用者原本的對話會全部不見**；而且舊檔在第一次啟動時就被刪掉（`spec.md` §4.2），**之後也無法再救回**。
→ 觸發：使用者表示要保留或匯入舊對話。那必須在**發佈這一版之前**決定 —— 發佈之後舊檔已經刪了。

**資料庫結構的版本升級機制。** 這是第一版，沒有舊結構要升。
→ 觸發：下一次要改儲存的結構時。屆時「升級時不得弄丟既有對話」是那一輪的核心規則，不能沿用本輪「全部清空」的做法。

**同一段對話同時被存入「不同」內容時的先後保證。** 今天唯一實際會發生的並發是相同內容（停止與切換）。理論上，一次回覆收尾的存檔若慢到被下一次回覆的收尾超過，較舊的內容可能最後落地；兩次收尾之間至少隔一整次生成，本輪判斷走不到。
→ 觸發：新增任何「在前一次存檔可能還沒完成時，存下同一段的不同版本」的路徑（例如生成中定時存檔）；或有人回報重開 App 後少了最後一輪對話。

**生成途中每段文字即時存檔。** 生成中 App 被系統殺掉，那一輪的回覆會不見（使用者那句也還沒存）。這是既有行為。
→ 觸發：使用者回報「切出去再回來，剛才的回覆不見了」（與 `stop-generation`「明確不做」的同一條觸發一致）。

**寫不進去的那段，被切走之後不留在清單上。** `spec.md` §5.2 只要求告知。讓它在本次執行中仍留在清單上，需要一份「沒存成的對話」的記憶體清單，本輪不做。
→ 觸發：使用者回報「看到沒存成的提示之後，那段對話就找不到了」。

**某個模型或人格從 App 移除之後，舊對話上的名稱怎麼顯示。** 現行讀取遇到不認得的模型或人格名稱，會各自退回一個預設值（v4 更正：v3 以前這裡寫「模型會釘成 Qwen」，那是舊版讀取的做法；見 `spec.md`「這次會踩到的既有事實」第 18 條）；這一輪不定義儲存裡出現不認得的名稱時該怎樣。
→ 觸發：任何一輪要移除或改名一個模型或人格。

**（v4）從抽屜刪除一段對話、而刪除失敗時的告知。** 現行行為（v3 驗收時 QA 讀程式碼看到、PM 讀程式碼確認）：App 不閃退、不提示，那一列**仍留在清單上**（清單跟著儲存走，而儲存裡它還在）。使用者看到的是「按了刪除沒反應」，不會因此遺失資料，也不會誤以為已經刪掉。
- **為什麼這一輪不規定**：(i) 在模擬器上造不出「清單上有列、但刪除會失敗」的時機 —— 開不起來時清單上根本沒有列（§5.4），執行中改檔案權限不影響已經開著的資料庫；(ii) 「失敗之後要提示」寫在聊天狀態那一層，而那一層在不經過裝置的測試裡建不起來（T1 不在本輪）。規定下去會是一條沒有任何驗收守得到的規則。
- **現行「不閃退」也沒有條文守**，同樣是因為造不出來；知情留白。
→ 觸發：使用者回報「按了刪除沒反應」或「刪不掉」；或 T1 完成（聊天狀態可以在測試裡直接驅動）；或出現在模擬器上造得出刪除失敗的方法。屆時比照 `spec.md` §5.2 定一句提示。

**刪除之後的實體殘留。** 資料庫刪掉一段之後，那段的位元組可能在檔案的空閒區留一陣子，直到被覆寫；本輪只保證 App 讀不到它（`spec.md` §1.3）。
→ 觸發：出現「刪除必須不可還原」的隱私需求，或有人回報刪掉的內容還能從手機裡撈出來。

**匯出、備份、跨裝置同步、加密。** 全部不在範圍內。
→ 觸發：使用者提出。

**（v2，使用者開的例外）資料庫層不寫自動化測試；這個 bug 的重現案例不進測試套件。** 「bug 修正的重現案例必須成為永久測試」那條規則，由使用者 2026-09-17 對本輪明確開出例外（D1）。**它不是既有那種「決定維持現狀、所以不修」的例外 —— 這一輪是要修的。**
- **使用者的理由（原意）**：在電腦上跑沒有實機環境，可能測試過了、手機上卻壞了；叫 QA 在裝置上細測資料庫細節又太花時間。改由模擬器上以一般操作建立資料的驗收，確認使用者看到的結果正常。
- **被移除的條文**：任務 1 驗收 2、5、6，任務 2 驗收 2，任務 5 驗收 3（編號都保留不遞補）。**從資料庫層拆出來的純邏輯仍有測試**：任務 1 驗收 1、3、4，任務 2 驗收 1，任務 4 驗收 1。
- **守衛**：模擬器上的**任務 2 驗收 3**（四個切換入口 ＋ 零文字變體），**每輪都跑**。
- **知情接受的缺口**：
  - **並發寫入時讀取漏掉**（`spec.md` §1.4）沒有自動化測試；
  - **一次失敗的寫入毀掉已存資料**（原任務 5 驗收 3）沒有自動化測試；
  - 資料庫那一端有沒有把字串原樣存好、刪除有沒有只刪那一列、「刪除一個不存在的 id」不出錯，只有裝置上的結果在驗，或不驗。
  - 使用者問過前兩個在有資料庫的前提下還有沒有風險。RD 的回答：只剩「多步驟寫入沒有包在交易裡」這一種錯法，而 RD 的做法讓每次存檔只有一個指令，從結構上排除它（`spec-architecture.md` §B.1）。
- **實測紀錄保留**（`spec-appendix.md`／「查證：資料庫能不能在 JVM 單元測試裡跑」），當作觸發時的起點。
→ 觸發（使用者原意）：**出現意料外的資料庫相關 bug 時，把那個案例加成測試項目，屆時再決定跑在哪裡。** 另一個觸發：存檔或刪除改成不只一個資料庫指令時 —— 那時結構保證不再成立，要同時補回交易與測試。

**（v2、v3，使用者裁決）`stop-generation` 的裝置端驗收本輪不重跑。** 下面的觸發條件**已經成立過兩次**，兩次都是使用者知情決定不重跑（任務 7 驗收 12 已移除，缺口寫在原位）：
- **v3（2026-09-17）**：v2 驗收之後，RD 為了修「串流中切換模型時原生閃退」（v2 任務 7 驗收 10 的 fail），改了引擎取消生成的流程 —— 取消之後等推論真的停下來，才關掉 session、才讓出引擎，最多等 10 秒（commit `6b284d1`）。這就是「改動停止…那一段程式」。使用者知道條件成立，**仍決定本輪不重跑**。**裁決沒有附理由**（收件匣只記了裁決本身）。這一筆同時回答了 v2 驗收報告「問題歸屬」第 2 點「請 PM 評估要不要重跑」。
- **v2（2026-09-17）**：本輪動到停止與切換所在的聊天狀態（那一輪 S3 的「程式再被動到」）。
- **本輪另外加的兩條**（任務 7 驗收 13、14）只驗 v3 這次改動直接碰到的後果，**不是**那一份的重跑。
- **（v4）** v3 裁決之後的程式改動（`4ad41b1`、`b0ad0fa`）沒有碰那段程式，不算第三次成立（任務 7 驗收 12 的原位說明）。
→ 觸發：`stop-generation` 下一次被修訂時；或任何一輪改動停止、回覆收尾、中斷標記那一段程式時。**下一次成立時仍要提出來，不因前兩次的裁決自動略過。**

**（v3）§5.4 那種狀態在同一次執行中被排除時，不必重啟就恢復。** `spec.md` §5.4 只要求下一次啟動恢復。同一次執行裡，儲存開不起來的原因消失了，清單與存檔要不要自己回來，本輪不定義。
→ 觸發：使用者回報「權限（或空間）恢復之後，要重開 App 紀錄才回來」；或出現一種會在使用途中短暫開不起來、又自己恢復的情況。

**（v3）打開抽屜時要不要保留上一次的捲動位置。** `spec.md` §2.2、§2.3 只要求「剛移到最上面的那段，一打開就看得到」。RD 的做法是每次打開都回到最上面，這不是規則；改成別的做法，只要 §2.2、§2.3 成立就可以。
→ 觸發：使用者回報「往下捲找舊對話、關掉再打開又跳回最上面，很不方便」。

**（v3）「開得起來、但寫不進去」在模擬器上不容易造。** 任務 5 驗收 2 可能一直是「本環境無法觀察」；那時「寫入失敗期間已存的對話仍在清單上」沒有人判到（`spec.md` §5.2 的「由哪一層守」）。
→ 觸發：任務 5 驗收 2 連續兩輪記「本環境無法觀察」時，PM 要回頭找別的造法，或把它列成使用者知情接受的缺口。

**（v2 依 D4 改寫）不改寫 `conversation-search` 與 `stop-generation` 的條文，也不另排時間改。** 使用者裁決：本輪完成時，`docs/product/features.md` 與 `plan/architecture.html` 反映新的儲存方式（任務 8、9）；**那兩份規格日後有人修訂時，必須以這兩份全域文件為準**，順便把綁 JSON 檔的條文換掉。
- **改寫清單**留在 `spec-appendix.md`／「跨輪衝突清單」，就是那時候的清單。
- **這條規則放在哪裡**：`docs/product/features.md`「這份文件怎麼維護」一節（任務 9 驗收 6，連同四列的加註）與 `plan/architecture.html` 儲存那一節（任務 8 驗收 11）。那兩份規格本輪不能動，所以規則寫在修訂它們的人一定會經過的兩份全域文件裡。
- **代價，誠實寫**：在那之前，那兩份規格裡有一批條文照字面做不出來；誰去跑那兩份的全驗，會先撞到它們。
→ 觸發：**`conversation-search` 或 `stop-generation` 下一次被修訂時**（以全域文件為準改寫清單上的條文）；或有人要跑那兩份的全驗。

**刪除 `history-missing-after-switch` 目錄。** 本輪不得動它；建議在本輪回報裡。
→ 觸發：本輪驗收通過之後。刪之前先確認沒有現行文件指著它（`conversation-search` 現行版的 `spec-tasks.md`「明確不做」裡有兩處，要等那份規格下一次修訂時一併換掉）。

**T1：`ChatViewModel` 的相依改成可從外面給。** 本輪用「歷史清單的組合可被直接驅動」（任務 2 驗收 1）釘住那個 bug 的狀態層成因，不需要整個狀態層可測。
→ 觸發：使用者決定做 T1；或下一個 bug 只能在狀態層重現。

**比較畫面。** 它不存檔，本輪與它無關。
→ 觸發：比較結果開始被保存下來時。

**改寫 QA 的工具與環境文件（`docs/qa-tools/`、`docs/qa-environment.md`）。** 那歸 QA；D2 已拍板（兩條路），由 QA 自己依本檔「測資與讀取的約定」與任務 6 的交付物更新。
→ 觸發：本輪 QA 開始驗收之前。
