# conversation-store-db v4 驗收 test cases

- 觀察點數：36
- 涵蓋驗收條數：51（任務 1：1、3、4、7；任務 2：1、3、4；任務 3：1、2；任務 4：1、2；任務 5：1、2、4；任務 6：1、2、3；任務 7：1–11、13、14；任務 8：1–14；任務 9：1–7）

受測版本：`fa6da6b`（工作區乾淨）。判定一律對照 `spec-tasks.md` / `spec.md` 原文；下面引號內是原文摘錄。
「讀儲存」＝ App force-stop 後以 `docs/qa-tools/sqlitepull.sh` 拉 `databases/conversations.db`（含 -wal/-shm）在主機唯讀開啟，逐欄逐則訊息比對。
測資的人格與模型避開退回值（人格退回 Friend、模型退回 Gemma1B；見 `Persona.of` / `ModelSpec.of`）—— 適用於以「讀回相同」判的條文。

---

## 情境 J：JVM（不經過裝置）

J1 `./gradlew clean testDebugUnitTest`，讀全部結果 XML（tests/failures/errors/skipped）
- → 任務 1 驗收 7、任務 7 驗收 11：「專案既有的測試全部通過（一支都不得變紅、停用或刪除）」—— 另比對 75a90b3 與 HEAD 的既有測試檔有沒有被刪／加 @Ignore。
- → 任務 1 驗收 1、3、4，任務 2 驗收 1，任務 4 驗收 1 的「在標準測試指令下執行」。

J2 逐行讀 `ConversationRecordsTest`、`HistoryListTest`、`LegacyConversationsTest` 的測資與斷言
- → 任務 1 驗收 1：「§1.1 第一條可判定列出的每一種文字…每一個欄位、每一則訊息都與原本的完全相等」「另轉一則被標成生成中的訊息 → 轉回來不是生成中」「人格與模型都不得是讀取時的退回值」「訊息要同時含使用者與助理兩種作者」
- → 任務 1 驗收 3：「取全部的結果恰為 E、F，而且在測試時限內結束」「依 id 取對 X → 找不到；對 E → 取得 E 且內容相等；對不存在的 id → 找不到」「對上面每一個 id，依 id 取與取全部再挑出同一個 id 的答案相同」
- → 任務 1 驗收 4：「只有開場白 → 刪除那個 id；只含助理訊息 X → 刪除 X；訊息清單為空 → 刪除；有使用者訊息 → 寫入」
- → 任務 2 驗收 1：(i)(ii)(iii) 與鑑別力「只在開著的 id 變動時重算…(ii)(iii) 必須變紅；拿掉扣掉開著的那段，(i) 必須變紅」
- → 任務 4 驗收 1：三個 .json、`.task`、`.task.part`、`profileInstalled`、含檔案的子目錄；位元組相同；舊位置不存在／不可寫／恢復可寫三個案例

## 情境 DOC：文件冷讀

DOC1 `plan/architecture.html` 全檔 → 任務 8 驗收 1–9、11–14（逐條依原文判；「以意思判」者照原文）；同時當任務 6 交付物讀（位置、表與欄位、訊息欄格式、App 停止時修改）
DOC2 `docs/specs/conversation-store-db/findings.md` → 任務 8 驗收 10：「QA 驗得到：那一節裡有 v3 的一筆」（本輪不要求新的一筆）
DOC3 `docs/product/features.md` → 任務 9 驗收 1–7（節名以 `grep` 在 v4/spec.md 查、落在標題上；不含版號目錄）

---

## 裝置前置 E0（一次）
覆蓋安裝 HEAD 建置（不解除安裝）；清空 App 使用者資料（databases/、shared_prefs/、files/ 下非模型檔），模型檔保留；記錄模型檔清單與大小。

## 情境 A：升級（乾淨資料起點；第二條路造前置）
前置：App 停止；三個舊格式 JSON（格式照 `git show a4c7b20:app/src/main/java/com/example/demo/data/ConversationStore.kt`）放進 `files/conversations/`，含獨特詞；`shared_prefs/session.xml` 寫 last_thread_id＝其中一個 id、last_persona＝Teacher、last_model＝已解壓的非預設模型（Qwen05B）。記下模型檔清單與大小。
A1 冷啟動後一次看完
- → 任務 4 驗收 2 (i)「抽屜顯示「還沒有其他對話。」；搜尋那三段裡的詞都找不到」
- → (ii)「只有開場白的新對話，頂端的人格與模型是寫進偏好的那一組」
- → (iii)「舊位置不存在」 (iv)「沒有出現解壓橫幅，原有的模型檔仍在（以檔案清單與大小判定）」
A2 送出一則（Teacher＋Qwen）等回覆結束，記下畫面上的訊息全文，不做任何事 force-stop → 冷啟動
- → 任務 4 驗收 2 (v)「續接到那段、內容都在、舊位置仍不存在」
- → 任務 7 驗收 8 前半「續接到那段，使用者那句與完整回覆都在，頂端的人格與模型與結束前相同」（人格、模型皆非退回值）；並對照 §1.1 裝置判法（文字相同）
A3 換人格到 Grandchild（非退回值、模型仍 Qwen）不說話 → force-stop → 冷啟動
- → 任務 7 驗收 8 後半「以記住的人格與模型開新的一段」

## 情境 B：造一批對話（第一條路）
B0 以 SmolLM/Qwen 正常操作建立約 8 段對話（讓清單超過一個畫面）；其中 P1 的使用者訊息含 emoji 與換行（Gboard emoji 面板＋Enter）。結束時開一段新的對話。
B1 force-stop，讀儲存，列出每段每欄每則；冷啟動，對照抽屜每列與點開的內容
- → 任務 6 驗收 1「列出儲存裡每一段對話的每一個欄位與每一則訊息；列出的結果與 App 畫面上看得到的一致」

## 情境 C：搜尋與基本流程（沿用 B 的資料）
C1 在有內容的對話換人格 → N；抽屜 → 搜尋 N 開場白的一個詞
- → 任務 3 驗收 1「N 的分組出現，而且排在最前」
C2 （讀儲存基準：前置做完之後）→ 進搜尋、打字到有結果、點另一段的命中列、再進搜尋點回原本那段（含使用者訊息）→ force-stop 讀儲存
- → 任務 7 驗收 9「儲存裡的對話數與每一段的內容都與基準相同」
C3 開著 A（最後更新早於現在 ≥1 分鐘；讀儲存基準後冷啟動續接 A），不做任何事，抽屜開另一段 → force-stop 讀儲存
- → 任務 7 驗收 3「儲存裡 A 的內容（含最後更新時間）與切換前相同」
C4 開著 A，讀儲存基準後冷啟動，抽屜刪 B → 畫面上該列消失 → force-stop 讀儲存 → 冷啟動查抽屜與搜尋
- → 任務 7 驗收 7「儲存裡少一段、沒有 B、A 的內容不變；冷啟動後抽屜與搜尋都找不到 B」
C5 四次：（讀儲存基準 → 冷啟動 → 換人格產生只有開場白的對話 → 以 開另一段／開始新的對話／換人格／換模型 其一離開 → force-stop 讀儲存）；正向對照：新對話送出等回覆結束再離開 → 讀儲存
- → 任務 7 驗收 4「每一次前後，儲存裡的對話數與每一段的內容都相同」「正向對照：儲存裡多了那一段」
C6 生成中按停止，立刻 dump 記下回覆文字，force-stop，冷啟動
- → 任務 7 驗收 1「續接到那段，那則回覆的文字與停止時相同、以中斷標記結尾」
C7 同一模型：不停止送一句量首字秒數；再兩次「送出→生成中停止→停止鈕變回送出鈕立刻送出」量首字秒數（連續截圖＋裝置時鐘）
- → 任務 7 驗收 14「任一次比不停止的那一次多出 5 秒以上，即 fail」「使用者那句出現，回覆開始出現並照常結束，App 沒有閃退」
（任務 7 驗收 10 的六項在 A、B、C 各步驟裡順帶逐項記錄：送出、串流、停止、刪除、切換人格／模型、冷啟動續接）

## 情境 D：串流中切換（清單須超過一個畫面；每次切換前把抽屜清單往下捲到最上面那一列不在畫面上再關上）
每一次：長提示（`Write a very long detailed story about the sea, at least 600 words`）→ 回覆出現文字且在增加時切換 → 立刻開抽屜截圖＋dump（不捲動）→ 捲動全清單數 A 的列數 → 關抽屜 `adb shell sleep 10` → 再開抽屜判 → 進搜尋查 `sea` → 從抽屜開 A 看結尾。
D1 切換人格 → 任務 2 驗收 3 預期 1–4
D2 切換模型（從 Qwen 串流中切走）→ 任務 2 驗收 3 預期 1–4；同時算任務 7 驗收 13 的第 1 次
D3 開始新的對話 → 任務 2 驗收 3 預期 1–4
D4 開啟另一段 B（切換前讀儲存基準；切換後 force-stop 讀儲存）→ 任務 2 驗收 3 預期 1–5；任務 7 驗收 2「A 的回覆以中斷標記結尾；B 在儲存裡的內容與切換前相同」
D5 零文字變體（切換人格，回覆尚無字時）→ 任務 2 驗收 3 預期 1–3，「那則空回覆沒有留下、使用者那句還在」
D6 再 4 次串流中切模型（至少再 2 次從 Qwen 切走），每次查行程、閃退紀錄、頂端模型、之後送出收得到回覆、被切走那段結尾是中斷標記
- → 任務 7 驗收 13「任一次閃退即 fail」
「預期 1」原文：「打開抽屜的第一個畫面上、不做任何捲動，歷史清單最上面那一列就是 A；清單裡 A 恰好一列」。「預期 4」：「那則回覆留下切換當下已經顯示的文字，結尾是中斷標記」。

## 情境 E：非串流的清單更新（同一次執行）
E1 不在生成時從抽屜開另一段 → 開抽屜看剛才那段（可捲動）
E2 刪掉清單上的一段 → 不關抽屜看那一列消失
E3 捲動擺設 → 關抽屜 → 送出等回覆結束 → 切走 → 開抽屜不捲動
- → 任務 2 驗收 4 三步原文：「剛才那段在清單上」「那一列立刻消失，不必關再開」「畫面上最上面那一列就是那段，預覽是完整回覆的開頭」

## 情境 F：生成中搜尋
F1 新對話送 `harborlight` ＋長提示 → 生成中進搜尋查 `harborlight` → 看到結果立刻返回聊天，截圖判回覆狀態
- → 任務 3 驗收 2「那段的分組出現」＋「算不算數」判法

## 情境 G：從外面寫的對話（第二條路）
G1 先正常操作建立 Y（含使用者訊息）並切走；force-stop；在 DB 寫入只含助理訊息的 X；冷啟動
- → 任務 6 驗收 2「X 出現在抽屜上，標題是「還沒說話」，點開看得到那幾則助理訊息」
G2 點 X → 抽屜開 Y → force-stop 讀儲存 → 冷啟動查抽屜與搜尋 → 開新對話 → force-stop 讀儲存
- → 任務 7 驗收 5「儲存裡沒有 X、冷啟動後抽屜與搜尋都找不到 X」「儲存裡 Y 仍在、內容不變」
G3 force-stop；把一段的 messages 欄改成非法內容（另有兩段正常）；冷啟動
- → 任務 6 驗收 3 前半「其餘都在清單上」；任務 7 驗收 6「冷啟動後抽屜列出那兩段，App 不閃退」

## 情境 H：開不起來（§5.4）
前置：P1、P2（P1 含 emoji 與換行，B0 建立的）；開一段新的對話；force-stop；讀儲存基準＋記下原權限。
H(i) chmod 444 主檔與 -wal/-shm；H(ii) chmod 000 主檔（先證明造法生效）。每一輪：
H-a/b/c/d 冷啟動 → 截圖序列看提示次數與停留；抽屜文字；送出、收到回覆、停止；送出等回覆結束看「有一段對話沒能存下來。」與停留、內容仍在可再送出
- → 任務 5 驗收 4 (a)(b)(c)(d)
H-d2（只 (i)）force-stop，權限不動，冷啟動 → 任務 5 驗收 4 (d2)
H-e force-stop，還原權限，讀儲存 → (e)「與基準相同」
H-f 冷啟動 → 抽屜 P1、P2、無兩種提示；送出等結束、冷啟動 → (f)

## 情境 I：寫不進去（§5.2，盡力）
I1 P 已存；填滿 /data → 另一段送出等結束 → 提示、內容在畫面上可再送出、抽屜有 P 且非「目前讀不到…」；還原空間 → 送出等結束 → 冷啟動那段在
- → 任務 5 驗收 2（含「算不算數」判法；做不出來記「本環境無法觀察」並寫明）

## 情境 K：內容壞了（§5.1；會清掉全部，排最後）
K1 P 已存；記下 session.xml；force-stop；把 DB 主檔覆寫成亂碼（刪 -wal/-shm）；冷啟動
- → 任務 5 驗收 1 前四點；任務 6 驗收 3 後半「整份儲存讀不起來（冷啟動後走 §5.1）」
K2 新對話送出等結束 → 冷啟動
- → 任務 5 驗收 1 末點「那段在，而且提示不再出現」

## 隨機抽測（功能總覽，本輪範圍外）
R1 搜尋-08　R2 搜尋-18　R3 保存-02
