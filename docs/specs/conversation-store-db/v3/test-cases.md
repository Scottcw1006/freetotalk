# conversation-store-db v3 — test cases（2026-09-17，受測版本 4ad41b1）

- 觀察點數：31
- 涵蓋驗收條數：51

判定一律對照 `spec-tasks.md` 條文原文；下面的引號是條文原文節錄。報告照任務排。
「儲存的內容」＝ App `force-stop` 後以 `docs/qa-tools/sqlitepull.sh` 拉出三個檔，在主機複本上 `sqlite3` 唯讀查詢。

---

## 情境 S：不經過裝置（靜態＋標準測試指令）

- O1 `./gradlew clean testDebugUnitTest`，讀每個結果 XML
  → 任務 1 驗收 7、任務 7 驗收 11：「除本輪新增之外，專案既有的測試全部通過（一支都不得變紅、停用或刪除）」（另核 `git diff --name-status` 測試目錄只有新增、無 `@Ignore`）
- O2 讀 `ConversationRecordsTest.kt` 斷言
  → 任務 1 驗收 1：「每一個欄位、每一則訊息都與原本的完全相等（不是「不為空」、不是 `contains`）…另轉一則被標成「生成中」的訊息 → 轉回來不是生成中」；測資清單照 §1.1；「獨自抓得到：…任一欄位漏轉或轉錯（例如模型、建立時間、訊息順序）」
  → 任務 1 驗收 3：「「取全部」的結果恰為 E、F，而且在測試時限內結束；「依 id 取」對 X → 找不到；對 E → 取得 E 且內容相等；對一個從來不存在的 id → 找不到；對上面每一個 id，兩條路答案相同」
  → 任務 1 驗收 4：「只有開場白 → 判成刪除那個 id；只含助理訊息、id 為 X → 判成刪除 X；訊息清單為空 → 判成刪除。正向對照：有使用者訊息 → 判成寫入」；且該判斷是 repository 存檔實際用的那一個
- O3 讀 `HistoryListTest.kt`
  → 任務 2 驗收 1：(i)(ii)(iii) 與「鑑別力：只在開著的 id 變動時重算 → (ii)(iii) 變紅；拿掉扣掉開著的那段 → (i) 變紅」（靜態推演）
- O4 讀 `LegacyConversationsTest.kt`
  → 任務 4 驗收 1：三個 `.json`、`.task`、`.task.part`、`profileInstalled`、含檔子目錄；「旁邊每一個檔的位元組都與清理前相同」；不存在、不可寫、恢復後再清三個案例
- O5 冷讀 `plan/architecture.html`（含 aria-label）
  → 任務 8 驗收 1–9、11–14（逐條原文見 spec-tasks）
- O6 讀 `docs/specs/conversation-store-db/findings.md`
  → 任務 8 驗收 10：「QA 驗得到：那一節裡有 v3 的一筆」
- O7 讀 `docs/product/features.md`，對 v3/spec.md grep 節名
  → 任務 9 驗收 1–7

---

## 裝置前置（一次）
覆蓋安裝工作區建置；確認安裝時間；清空 App 使用者資料（databases、shared_prefs、files/conversations 等），保留 `files/*.task`；記下模型檔清單與大小。

## 情境 A：清空後第一次啟動＝升級情境（需要空的儲存，排最前）
前置：App 停止；照 `a4c7b20` 的舊格式放三個 JSON 進 `files/conversations/`；偏好寫入其中一個 id、非預設人格與非預設（已解壓的）模型。
- O8 冷啟動後一次看完 → 任務 4 驗收 2 (i)「抽屜顯示「還沒有其他對話。」；搜尋那三段裡的詞都找不到」(ii)「只有開場白的新對話，頂端的人格與模型是寫進偏好的那一組」(iii)「舊位置不存在」(iv)「沒有出現解壓橫幅，原有的模型檔仍在（檔案清單與大小）」
- O9 送出一則、等回覆結束、不做事、force-stop、冷啟動
  → 任務 4 驗收 2 (v)「續接到那段、內容都在、舊位置仍不存在」
  → 任務 7 驗收 8 前半「續接到那段，使用者那句與完整回覆都在」
- O10 換人格產生沒說過話的對話 → force-stop → 冷啟動
  → 任務 7 驗收 8 後半「以記住的人格與模型開新的一段」

## 情境 B：建清單（正常操作建多段，讓清單超過一個畫面；其中 P1 含 emoji 與換行）
- O11 force-stop，照 architecture.html 的交付物讀出每段每欄每則 → 與畫面比對
  → 任務 6 驗收 1「列出的結果與 App 畫面上看得到的一致」

## 情境 G：不在生成時切走
- O17 取基準（A 最後更新 ≥1 分鐘前）→ 冷啟動（續接 A）→ 抽屜開另一段 → 打開抽屜看 A → force-stop 讀儲存
  → 任務 7 驗收 3「儲存裡 A 的內容（含最後更新時間）與切換前相同」
  → 任務 2 驗收 4 第一點「打開抽屜，剛才那段在清單上（可以捲動找到）」

## 情境 F：刪除
- O16 取基準 → 開著 A，抽屜刪 B（當場看列消失）→ force-stop 讀儲存 → 冷啟動、抽屜與搜尋找 B
  → 任務 7 驗收 7「儲存裡少一段、沒有 B、A 的內容不變；冷啟動後抽屜與搜尋都找不到 B」
  → 任務 2 驗收 4 第二點「那一列立刻消失，不必關再開」
  → 任務 7 驗收 10（刪除）

## 情境 H：空白對話四種離開＋正向對照
- O18 每一種離開前後各讀一次儲存（開另一段／開始新的對話／換人格／換模型），最後正向對照
  → 任務 7 驗收 4「每一次前後，儲存裡的對話數與每一段的內容都相同。正向對照：…儲存裡多了那一段」
  → 任務 7 驗收 10（切換人格／模型、冷啟動續接）

## 情境 I：回覆結束後切走
- O19 先把清單往下捲到最上面那列不在畫面、關抽屜；送出、等回覆結束、切走 → 打開抽屜不捲動
  → 任務 2 驗收 4 第三點「畫面上最上面那一列就是那段，預覽是完整回覆的開頭」

## 情境 J：串流中切換（每一次之前都做擺設：清單下捲到最上列不在畫面、關抽屜）
每一次判：「1. 不捲動，最上面那一列是 A；A 恰好一列（可捲動確認）2. 關上、等 10 秒再開，仍是 A、一列 3. 搜尋 `sea` → A 的分組出現 4. 打開 A → 回覆留下文字、結尾是中斷標記 5.（開另一段）B 不在清單上」；算不算數看中斷標記與擺設。
- O20 切換人格 → 任務 2 驗收 3；同一狀態下查 N 的開場白詞 → 任務 3 驗收 1「N 的分組出現，而且排在最前」
- O21 切換模型 → 任務 2 驗收 3
- O22 開始新的對話 → 任務 2 驗收 3
- O23 開啟另一段 B（切換前 force-stop 讀儲存取 B 基準、冷啟動後再擺設）→ 任務 2 驗收 3；切換後 force-stop 讀儲存 → 任務 7 驗收 2「A 那則回覆以中斷標記結尾；B 的內容與切換前相同」
- O24 零文字變體（切換人格）→ 任務 2 驗收 3「那則空回覆不留下、使用者那句還在，預期 1–3 成立」

## 情境 K
- O25 新對話送出含 `harborlight` 的句子，串流中進搜尋查 → 任務 3 驗收 2「那段的分組出現；回聊天確認回覆結尾是中斷標記（或截圖證明當下還在增加）」

## 情境 L
- O26 串流中按停止 → 立刻 force-stop → 冷啟動 → 任務 7 驗收 1「續接到那段，那則回覆的文字與停止時相同、以中斷標記結尾」；任務 7 驗收 10（停止）

## 情境 M
- O27 擺設完取儲存基準 → 搜尋、點另一段命中列、再搜尋點回原段 → force-stop 讀儲存 → 任務 7 驗收 9「對話數與每一段的內容都與基準相同」

## 情境 N
- O28 串流中從抽屜切模型 ×5（≥3 次從 Qwen2.5 0.5B）；每次看行程、閃退紀錄、頂端模型、之後送出一則有回覆、被切走那段結尾是中斷標記 → 任務 7 驗收 13

## 情境 O
- O29 同一模型：不停止的首字延遲 ×1；停止後立刻送出的首字延遲 ×2 → 任務 7 驗收 14「任一次多出 5 秒以上即 fail」；任務 7 驗收 10（送出、串流、停止）

## 情境 D：從外面寫入只含助理訊息的 X
- O14 正常操作建 Y 並切走 → force-stop → 寫入 X → 冷啟動 → 抽屜看 X（標題「還沒說話」）→ 點 X 看助理訊息 → 抽屜開 Y → force-stop 讀儲存 → 冷啟動、抽屜與搜尋找 X → 開新對話 → force-stop 讀儲存
  → 任務 6 驗收 2「X 出現在抽屜上，標題是「還沒說話」，點開看得到那幾則助理訊息」
  → 任務 7 驗收 5「儲存裡沒有 X、冷啟動後抽屜與搜尋都找不到 X；再開一段新的對話 → 儲存裡 Y 仍在、內容不變」

## 情境 E：一段讀不起來
- O15 force-stop → 把一列改成解不開（其餘至少兩段正常）→ 冷啟動
  → 任務 6 驗收 3 前半「讓一段對話解不開（其餘照常，冷啟動後其餘都在清單上）」
  → 任務 7 驗收 6「冷啟動後抽屜列出那兩段，App 不閃退」

## 情境 P（盡力）：開得起來、寫不進去
- O30 填滿 /data（做完還原）→ 另一段送出、等回覆結束 → 看提示、內容、抽屜 → 還原 → 再送出 → 冷啟動
  → 任務 5 驗收 2（算不算數：抽屜出現 §5.4 那句 → 不算）

## 情境 C：開不起來、內容沒壞（P1、P2 在，開著的是新的一段）
每一輪 (a)–(f) 原文見任務 5 驗收 4。
- O12 造法 (i)：主檔與附屬檔改成 App 只能讀 → 任務 5 驗收 4
- O13 造法 (ii)：主檔改成 App 連讀都不能讀 → 任務 5 驗收 4

## 情境 Q：整份壞掉（最後做，會清掉儲存）
- O31 force-stop → 記偏好 → 主檔覆寫亂碼、刪 -wal/-shm → 冷啟動
  → 任務 5 驗收 1（不閃退、無解壓橫幅、可送出／收到／停止；提示一次且 ≥3 秒；抽屜「還沒有其他對話。」、搜尋 P 的詞找不到；偏好不變；新對話存下、再冷啟動提示不再出現）
  → 任務 6 驗收 3 後半「讓整份儲存讀不起來（冷啟動後走 §5.1）」

---

## 隨機抽測（不計入上面兩個數字）
搜尋-13、搜尋-14、比較-01。
