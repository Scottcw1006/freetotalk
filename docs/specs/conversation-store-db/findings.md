# conversation-store-db：RD 的發現紀錄

跨輪的檔案，只增不改。每一輪在對應的節底下加一筆。

## 架構文件更新時發現的不符

`plan/architecture.html` 與程式碼不一致、但不在該輪要改的範圍內的地方。依 `spec-tasks.md` 任務 8 驗收 10，每一輪一筆，沒有發現也要寫。

### v2（2026-09-17）

- 沒有發現新的不符。
- 本來就有的遺漏：模組地圖沒有列 `MainActivity.kt` 與 `ui/theme/`。當輪沒有補，v3 由任務 8 驗收 14 補上。
- 已知不修：「一則訊息從送出到存檔」那一節說重複的尾巴「收成兩輪」，程式實際保留三輪（`stop-generation` 的「明確不做」有觸發條件）。

### v3（2026-09-17）

- 沒有發現新的不符。本輪逐節比對了元件圖、一則訊息從送出到存檔、文字修復、儲存、對話儲存、啟動、搜尋、刻意維持的規則、模組地圖與已知限制。
- 已知不修（同 v2）：重複的尾巴「收成兩輪」，程式實際保留三輪。
