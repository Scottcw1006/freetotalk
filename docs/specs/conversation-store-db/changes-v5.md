# changes-v5（對話儲存改為資料庫）

> 這是索引，不是規格。規則的原文在 `v5/` 底下；這裡只告訴你這一輪要做哪些、要回去讀哪幾節。

# 這一輪改了什麼

使用者決定刪除 `docs/specs/history-missing-after-switch/`（那個 bug 已被本規格吸收並驗收通過），刪之前要先把本規格現行版裡指向它的引用改掉。**這一輪只改引用：沒有改動任何規則，也沒有改變任何驗收要驗的性質；依使用者裁決不跑 QA。** `spec-decisions.md` 仍是 0 項。

指向那個目錄裡檔案的路徑，全部改成版本控制的讀法，集中在 `v5/spec-appendix.md`「參考資料」：

```
git show 6b6dbbf:docs/specs/history-missing-after-switch/<路徑>
```

`6b6dbbf` 是 PM 處理時的 HEAD，目錄完整在裡面。**RD 在這一輪之後刪目錄。**

# 這一輪要做的任務（完整清單）

**這一輪沒有 RD 的程式工作，QA 不驗（使用者裁決）。** 逐條走過任務 1–9，答案都是「這一輪不做」：

- 任務 1–6　〔未變動〕本輪不做（沒有程式或條文改動）
- 任務 7　回歸驗證　〔未變動〕**條文寫「每輪必做」，這一輪依使用者裁決不跑**：這一輪沒有任何程式改動，改的只是文件裡的引用
- 任務 8　更新架構文件　〔未變動〕本輪無需更新：`plan/architecture.html` 裡沒有指向那個目錄的引用（PM 以 grep 確認），這一輪也沒有程式改動
- 任務 9　`features.md` 與本規格一致　〔未變動〕PM 本輪沒有改 `docs/product/features.md`（它裡面沒有指向那個目錄的引用）；驗收 2 要的「不再有指向那個目錄的引用」目錄刪掉之後仍然成立

**RD 這一輪要做的一件事（不是任務，是使用者交辦）**：刪除 `docs/specs/history-missing-after-switch/`。

# 本輪移除或失效的任務／驗收

- 沒有移除任何任務或驗收，也沒有任何驗收失效。

# 改動的規格章節（RD 要讀原文的地方）

**`v5/spec.md`**
- 「這份文件怎麼讀」：指向變更說明的那一行改成「與本目錄同版號的 `changes-v<N>.md`」（原本寫死 `changes-v4.md`）；v4 的內容摘要原樣保留
- 代號索引「H1–H4」：補一句那個 bug 的中文描述，並說明目錄已決定刪除、原文讀法在附錄「參考資料」

**`v5/spec-tasks.md`**
- 「先看這裡」：同上，指向變更說明的地方改成不綁版號；拿掉「（v4）」與「v4 是 v3 驗收之後的修訂」
- 「明確不做」**「刪除 `history-missing-after-switch` 目錄」**：改成現況（使用者已決定、由 RD 執行、不是本規格的任務；需要原文時照「參考資料」的指法）；觸發條件改成「之後任何文件要引用那個 bug 的原文時，照那個指法寫」

**`v5/spec-architecture.md`**
- §E「與 `conversation-search` 的架構建議方向相反的地方（知情）」開頭：出處「`history-missing-after-switch` 收件匣第一筆第 5 點」後補上原文讀法的指路

**`v5/spec-appendix.md`**
- 「吸收 history-missing-after-switch 的對照」：表頭說明不再寫那個目錄的路徑；表後「重現資料」那段改成現況，**刪掉已過期的「`conversation-search` v3.24 有兩處現行引用」**
- 決策歷程：新增「只改引用的一輪：被吸收的 bug 規格要刪掉之前」（理由、commit 的選法、沒改的地方為什麼仍然成立、跨輪分辨）
- 「參考資料」：那個目錄的三項引用改成 `git show 6b6dbbf:<路徑>`
- 修訂記錄：新增 v5

**`v5/spec-decisions.md`**：整份改寫（0 項待決，附 v5 的判斷五項；v4 的判斷改指 `../v4/`）。

**節名沒有改**：附錄「吸收 history-missing-after-switch 的對照」保留原名（`spec.md` 開頭以它指過去；`conversation-search` v3.25 也描述到它）。沒有任何節號或條文編號變動。

# 未改動但與本輪改動互相牽動的章節

- **v5 裡仍然提到 `history-missing-after-switch` 的地方**（刻意保留，目錄刪掉之後仍然成立，理由在附錄決策歷程那一節）：`spec.md` 開頭的說明與代號索引 H1–H4、附錄「吸收 history-missing-after-switch 的對照」的節名、`spec-architecture.md` §A.1「讓歷史清單由「儲存的變動通知」與「開著的是哪一段」持續組合出來」的理由與 §E 開頭的出處、`spec-tasks.md`「明確不做」那一條的標題、`spec-appendix.md` 跨輪衝突清單「二」、決策歷程「本輪的使用者裁決」、修訂記錄 v1。這些是 bug 的名字或歷史敘述，不是路徑。
- `v5/spec-tasks.md` **任務 9 驗收 2**：沒動，它守的「`features.md` 不指向那個目錄」在目錄刪掉之後仍然要成立。
- **收件匣第二筆**（QA 給 PM 的三點條文建議，指向 §2.2、任務 2 驗收 3、任務 3 驗收 1）：依使用者指示本輪不處理，原樣留在收件匣；這一輪沒有碰那三處。

# 本輪被判定為「被改動」的章節，與規則 → 條文反向核對

**判定為被改動**：
- **§1**：代號索引 H1–H4 那一格本輪有改寫，格內引用 §1.4、§1.5、§1.6（判定條件第 2 項；引用本身是原有文字，但那一格被改過，判不出來就算）。§1 的規則文字沒有改。
- **§2**：同一格引用 §2.2；本輪改寫的附錄段落也明文引用 §2.2（判定條件第 2 項）。§2 的規則文字沒有改。

**判定為未被改動**：**§3**、**§4**、**§5**（沒有規則文字改動；本輪新寫的文字沒有引用它們的節號；沒有驗收改寫；沒有實作改動）。架構 §A–§J 是建議，不做反向核對。

| 規則 | 會發現違反的條文 |
|---|---|
| §1.1 | 任務 1 驗收 1；任務 7 驗收 8 |
| §1.2 | 任務 1 驗收 4；任務 7 驗收 4、5 |
| §1.3 | 任務 7 驗收 7。「刪除不存在的 id」無條文，規則旁已寫明知情不驗 |
| §1.4 | 任務 2 驗收 3（只照得出「沒接上」）。規則旁已寫明是使用者開的例外 |
| §1.5 | 任務 1 驗收 3；任務 7 驗收 6 |
| §1.6 | 任務 2 驗收 3；任務 7 驗收 4、9 |
| §2.1 | 任務 2 驗收 1（組合本身）；接錯來源由任務 2 驗收 3、4 承接（驗收 2 依 D1 移除時已寫明） |
| §2.2 | 任務 2 驗收 3 |
| §2.3 | 任務 2 驗收 4 |

# 本輪處理並移除的收件匣項目

1 筆處理完，已從 `pm-inbox.md` 移除：

- **[2026-09-17] 使用者裁決：刪除 history-missing-after-switch 目錄，刪之前把指向它的引用改掉** → 改法落在 `v5/spec-appendix.md`「參考資料」與上面「改動的規格章節」列出的各處；裁決內容、理由與沒改的地方在 `v5/spec-appendix.md` 決策歷程「只改引用的一輪：被吸收的 bug 規格要刪掉之前」；PM 的判斷在 `v5/spec-decisions.md`「本輪（v5）我自己做的判斷」。

**沒有處理、原樣留著的**：[2026-09-17] QA 給 PM 的三點條文建議（使用者決定留到下一次真正修訂本規格時）。
