# 需要你決策的事項（對話儲存改為資料庫）

> **這一份是整套文件裡唯一寫給使用者看的。** 只放還沒拍板的事；拍板之後連同選項與代價移進 `spec-appendix.md`／決策歷程。
> 入口見 `spec.md`；這一版改了什麼見 slug 根目錄的 `changes-v5.md`。

## 目前狀態（v5）：**本次沒有需要決策的事項。**

**這一輪只改引用**：依你的裁決，刪除 `history-missing-after-switch` 目錄之前，把本規格現行版裡指向它的地方改掉。沒有改動任何規則，也沒有改變任何驗收要驗的性質；**依你的裁決不跑 QA**。RD 沒有程式工作；刪目錄由 RD 在這一輪之後執行。

**處理收件匣時的三步檢查**（收件匣有 2 筆，依你的指示只處理第一筆）：

- **彼此沒有衝突。** 第二筆（QA 在 v4 驗收報告裡給的三點條文建議）指的是 `spec.md` §2.2「回覆還在生成時切換，被切走的那段一打開抽屜就在清單上」的判法、`spec-tasks.md` 任務 2 驗收 3、任務 3 驗收 1；這一輪一個字都沒有碰它們。第二筆原樣留在收件匣，等下一次真正修訂本規格時處理。
- **沒有相依。**
- **合起來沒有改變根本假設。**
- **跨輪分辨過**：`conversation-search` v3.25 已先做了同一件事，它指進本規格的地方（任務 1 驗收 4「空白對話的處置」、「明確不做」裡的 T1 那一條、`spec.md` §1.6 與 §2.2 的節名、附錄的吸收對照）這一輪都沒有改編號或標題，對得起來。`docs/product/features.md` 與 `plan/architecture.html` 裡沒有指向那個目錄的引用（PM 以 grep 確認），這一輪沒有動它們。

---

## 本輪（v5）我自己做的判斷（不擋工，你可以推翻）

1. **`history-missing-after-switch` 這個名字留著，只改路徑。** 本規格開頭、代號索引的 H1–H4、架構建議 §A.1 的理由、附錄的節名與歷史敘述，都把它當成那個 bug 的名字在用，不是當路徑。`conversation-search` v3.25 選了把名字也換成「切換後歷史清單漏掉對話」；本規格不跟，因為本規格就是吸收它的那一份，代號索引裡已定義這個名字、並說明原文去哪讀。**你要兩邊寫法一致的話，說一聲，我把名字也換掉**（不影響任何規則或驗收）。
2. **附錄「吸收 history-missing-after-switch 的對照」這個節名不改。** `spec.md` 開頭以這個節名指過去；`conversation-search` v3.25 以「H1–H4 的吸收對照」描述它，不是逐字節名。改名沒有收益，只會多一處要同步。
3. **刪除前的 commit 用 `6b6dbbf`**（處理時的 HEAD，目錄完整在裡面，四個檔都以 `git show` 確認讀得到）。原文的讀法集中寫在附錄「參考資料」，其他地方指過去，不各寫一份。
4. **跨輪衝突清單「二」裡引用 `conversation-search` 舊觸發條件原文的那一句沒有改。** 它記的是 v1 核對時的樣子；`conversation-search` v3.25 已把那條改寫成現況，那句說的「那一條的改寫由那一輪決定」也已經發生。這一輪只改引用，所以留到下一次真正修訂時一併更新。
5. **`spec.md`「這份文件怎麼讀」與 `spec-tasks.md`「先看這裡」原本寫死 `changes-v4.md`**，這一輪會變成指錯版。改成「與本目錄同版號的 `changes-v<N>.md`」，之後各輪不必再改這兩處；v4 那段內容摘要原樣保留。

---

## 歷史：之前各輪的判斷

- v4 的判斷（刪除失敗時不規定提示、`b0ad0fa` 不算 `stop-generation` 的第三次觸發、「測資不得等於退回值」寫成通則、QA 報告另外兩點、§5.4 補 (d2)）原文在 `../v4/spec-decisions.md`。
- v3 的判斷原文在 `../v3/spec-decisions.md`；v2 的在 `../v2/spec-decisions.md`。
- **它們在 v5 仍然有效**，沒有被推翻。
