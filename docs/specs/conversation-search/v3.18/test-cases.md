# conversation-search v3.18 — test cases（執行分組）

> 判定依據永遠是 `spec-tasks.md` / `spec.md` 的條文原文；本檔只決定執行順序。每個觀察點引用它對應條文的預期。
> 受測版本：工作區 HEAD 39d6418（工作區乾淨）。使用者已同意清空驗收裝置上的 App 資料（保留模型檔）。

## S0 環境準備（每條都先跑 qa-environment.md 的確認方式）
- JDK / gradle -v、adb devices、wm size/density、ui.py selftest、手勢區、Gboard 版面
- `./gradlew installDebug` 覆蓋安裝（不解除安裝），記 lastUpdateTime 與 HEAD 提交時間
- force-stop → 只刪 `files/conversations` 與 `shared_prefs`，確認 `files/*.task` 仍在
- 記下 accelerometer_rotation / user_rotation / density 原值

## S1 自動化與靜態（不需裝置）
- `./gradlew clean testDebugUnitTest`，逐檔讀 XML
  - 任務 3 驗收 1–26：逐條找出對應測試方法並讀斷言（例：驗收 16「第一個 code unit 不得是落單的低位代理…且片段裡仍然完整含有查詢詞」「兩個案例必須各自獨立」—以測資索引推算）
  - 任務 2 驗收 2–6、9、11；任務 4 驗收 2、3、6（「id `same` 只出現一次、訊息數為 2、且排在 `other` 之後」）；任務 12 驗收 6（U+00A0 開頭移除、U+001C 開頭保留）、驗收 9（空查詢契約測試仍存在且通過）、驗收 12 不經畫面那層（「(1 個空白)北京」→「北京」）
- 讀程式碼
  - 任務 2 驗收 1（「沒有任何 Android / Compose / 持久化層的相依」）、7（整形與折疊可被單元測試直接呼叫）、10（整形後字串不得寫回欄位）、11（用整形判斷空查詢）
  - 任務 9 驗收 2（文字修復不引用整形/折疊）、3（模型上下文過濾不是搜尋共用的那一份）、7 程式碼面
  - 任務 13 驗收 6（「什麼算一次更新」只有一個判斷處）
  - 任務 1 驗收 10（§9.3 兩條路徑同一答案）
- 探針（env §12，非突變）：任務 2 驗收 8 —「U+0020 與 U+00A0 在四處都算空白，U+001C 在四處都不算」
- 文件：任務 10 驗收 1、2、3(i)(ii)(iii)、4（三列路徑存在 + 職責相符）、5、7（「命中長度剛好是 60 個字時，前面留幾個引子字元」答案唯一且為 0）
- 文件：任務 15 驗收 1、2、3；任務 16 驗收 5（保存-02 與 §9.1 一致）

## S2 空資料狀態（清空後第一次啟動）
- 觀察 → 任務 6 驗收 1 (v)「沒有其他對話時，顯示『還沒有其他對話。』」
- 觀察 → 任務 1 驗收 6「冷啟動續接（檔案不存在）」前提：無 prefs 時啟動正常

## S3 推測資後的搜尋主情境（推入 JSON，冷啟動；鍵盤彈出）
測資（全部推入 `files/conversations`）：
- G1（老師/Qwen，今天）：5 則含 `apple`；含「北京 天氣」「臺北」、長中文訊息（命中「天氣」前 20 字內含 emoji）
- G2（姊姊/Gemma，今天較舊）：2 則含 `apple`；含「北京　天氣」(U+3000)
- G3（朋友/SmolLM，昨天）：2 則含 `apple`；含「北京 天氣」(U+00A0)、「台中」
- 另 12 段填充對話（讓抽屜列表需要捲動），含一段英文長句標題/預覽、一段「你好」短句
- 觀察點：
  - 任務 8 驗收 7 / 任務 11 驗收 2：查 `apple` →「摘要列顯示『3 段對話・9 則訊息』，畫面上列出 3 + 2 + 2 筆」
  - 任務 7 驗收 4：「顯示 3 則 + 『還有 N 則符合』，N = 總數 − 3」
  - 任務 7 驗收 8（§7.1）：「直向、鍵盤彈出、不捲動…至少完整看到 1 個對話標頭 + 4 個命中列」
  - 任務 7 驗收 1、2、5、7、11：佔滿、可捲動；標示顏色取自主題；單行不破版；標頭無標題；「人格 emoji、人格名稱、模型名稱、時間」四樣且換一段會改變
  - 任務 8 驗收 6：「命中列沒有刪除鈕」
  - 任務 8 驗收 1、2；任務 11 驗收 3：引導文案、無「找不到」；「只出現『找不到…』一句，無殘留舊結果」；空查詢與無命中無摘要
  - 任務 6 驗收 3、4、9：聚焦+鍵盤；輸入框固定；「輸入『北』…1 秒內結果必須出現；接著輸入『京』…結果必須換成『北京』那一組」且無按下才搜的控制項（注音輸入法）
  - 任務 7 驗收 6、任務 9 驗收 9：注音輸入法查長中文中段片語，「片段前後有『…』、標示恰好蓋住輸入的那幾個字不多不少」、emoji 在視窗內
  - 任務 9 驗收 8：注音查「今天」（開場白常見詞）→ 多段、標示、未破版、摘要段數 = 分組數；任務 7 驗收 3：捲完不錯亂不崩潰
  - 任務 9 驗收 10：「臺」「台」各打一次 → 結果不同
  - 任務 9 驗收 11：注音打「北京」+ 半形空白鍵 +「天氣」→「兩則都命中，標示恰好蓋住整個詞組」
  - 任務 9 驗收 12：剪貼簿不可設 → 記「本環境無法執行」
  - 任務 9 驗收 13：8–11 在鍵盤彈出下判讀，並複驗 §7.1
- 重置點：每個查詢前清空欄位（叉叉或刪字）

## S4 查詢欄位空白規則（同 S3 資料；逐碼位讀欄位）
- 任務 12 驗收 1：空欄位 keyevent 62 →「欄位仍為空、顯示引導文案、不顯示『找不到』」；長按替代＝連送多次 62；貼上 U+3000/U+00A0 → 本環境無法觀察（由驗收 6 承接）
- 任務 12 驗收 2（替代做法：英文版面 `input text "%s%s%sbeijing"`）→ 開頭空白不留
- 任務 12 驗收 3：「北京(2 個空白)」刪掉「北京」→「欄位為空、回到未搜尋狀態」、不得顯示「找不到符合『  』的訊息」
- 任務 12 驗收 4：「北京」+ 空白 →「北京 」；再「天氣」→「北京 天氣」且命中
- 任務 12 驗收 5：TAB 失焦 → 尾端空白移除；重新聚焦不加回
- 任務 12 驗收 7 / 任務 8 驗收 10：`"  kiwi  "` →「欄位內容為 `"kiwi  "`、文案為 `找不到符合「kiwi  」的訊息`；失焦後都變成 `kiwi`」
- 任務 12 驗收 8：「北京(2 個空白)」記結果與摘要 → 失焦 →「一字不差」；開頭移除前後結果相同（`%s%sapple` vs `apple`）
- 任務 12 驗收 12：「北京」→ MOVE_HOME + 空白 → 仍「北京」；移回尾端 + 空白 →「北京(1 個空白)」

## S5 搜尋外殼：進入 / 離開 / 旋轉 / 行程死亡 / 偏好
- 任務 6 驗收 2：入口列不是輸入框，點擊抽屜關閉進搜尋
- 任務 6 驗收 5 / 任務 8 驗收 8：畫面返回鍵、系統返回鍵都回聊天「不是退出 App」，再進入輸入框為空、引導文案
- 任務 6 驗收 8 / 任務 5 驗收 5 / 任務 8 驗收 8：旋轉 →「仍停留在搜尋畫面、輸入框仍在頂端」「查詢與結果都在」；還原方向
- 任務 8 驗收 12：輸入查詢後讀 `shared_prefs` →「找不到那個字串，鍵的組成也沒有增加」；正向：冷啟動續接原本那段
- 任務 8 驗收 9：搜尋中 Home + am kill + am start →「停在搜尋畫面、查詢為空、顯示引導文案」
- 任務 8 驗收 11：冷啟動後立即進搜尋，觀察結果區只有三種樣子；載入完成後同查詢「必須包含歷史裡的對話」
- 任務 7 驗收 9（§7.2）：density 606，鍵盤彈出 →「至少 1 個對話標頭 + 2 個命中列」；還原 density

## S6 點擊命中列與磁碟不變（每步前後雜湊對話目錄）
- 任務 5 驗收 8：進入、打字到有結果、離開 →「狀態列不變、沒有模型載入或推論的紀錄；進入前後對話檔的數量與內容逐一比對必須一致」＋ 非空結果
- 任務 8 驗收 3：點另一段 →「回到聊天、內容是該對話、再次進入搜尋時輸入框是空的」
- 任務 8 驗收 4：點目前這段 →「回到聊天、內容不變、不閃爍、不重載模型」
- 任務 8 驗收 5 / 任務 5 驗收 7：切人格 → 搜開場白 → 該空白對話出現；點擊 →「不建立新對話、不把空白對話存檔」
- 任務 7 驗收 7：空白對話標頭「不出現『還沒說話』」
- 任務 8 驗收 13：推只含助理訊息的檔 X，開著不同 id → 搜 X 的字、點擊 →「必須切換到那段舊對話」
- 任務 8 驗收 14：三種情境走完回聊天顯示對應對話，「不得在磁碟上新增或改寫任何對話檔」

## S7 抽屜（§5.2）— 任務 6 驗收 1、任務 9 驗收 5（同一套觀察，各自判）
- (i) 上半部順序、只有歷史列表可捲動（列表需長到可捲）
- (ii) 三入口：比較畫面 / 新對話且人格模型不變 / 搜尋畫面；抽屜都關閉
- (iii) 每列七樣；「今天」→「時:分」、非今天 →「月/日」；英文長句「尾端都看得到『…』」；「你好」「尾端都沒有『…』」
- (iv) 點一列開啟並關閉抽屜；刪除鈕「從列表消失，沒有確認的步驟」
- (vi) 開 A（有內容）時「列表上有 B、沒有 A；從列表點 B 之後再打開抽屜，列表上有 A、沒有 B」
- 任務 9 驗收 7 實機：含 U+3000 的訊息，抽屜預覽逐碼位仍是 U+3000；搜尋片段收成半形空白
- 任務 6 驗收 7：比較畫面系統返回鍵退出 Activity、旋轉被踢回聊天（既有缺陷維持）

## S8 存取層既有行為（任務 1 驗收 6、任務 9 驗收 4、任務 16 驗收 2–4）
- 壞檔：推一個內容損毀的 json →「歷史列表仍然完整顯示其餘對話」
- 任務 16 驗收 2：X（只含助理）標題「還沒說話」、點開換成 X；四種離開（開另一段、開始新的對話、換人格、換模型）每次新 X →「X 的檔不在；冷啟動後歷史清單與搜尋都找不到 X」；冷啟動入口（prefs 指 X）；空訊息檔；正向 Y「檔仍在，訊息與放進去時相同」
- 任務 16 驗收 3：開 A 刪 B →「對話檔少一個、B 的檔不在；冷啟動後歷史清單與搜尋都找不到 B；A 的檔仍在、內容與刪除前相同」
- 任務 16 驗收 4：換人格空白對話四種離開，「每一次離開前後，對話檔的數量與內容都相同」；正向：新對話送出並等回覆結束再離開 → 檔存在
- 任務 1 驗收 6：冷啟動續接（檔存在/不存在）、空白對話不進歷史、歷史排序、開啟/刪除/送出後更新、壞檔

## S9 需要模型生成（排序、時間、串流、存檔）
- 任務 13 驗收 4（§8.5）：送出並收完整回覆 → 時間前進、搜尋與抽屜都最前
- 任務 13 驗收 1（§8.3）：A 新 B 舊共同字，開 A → 抽屜開 B 再開 A →「搜尋分組順序、分組標頭的時間文字、抽屜歷史順序都與操作前相同」
- 任務 13 驗收 2（§8.4）：先讓 App 重寫一次 A 取基準，再切走 →「存檔內容與切換前完全相同」
- 任務 13 驗收 5（§8.7）：人格 P 的 B 送出等回覆結束，等 ≥60 秒，開新對話 N，搜 P 開場白 →「N 的分組排在 B 前面；N 標頭上的時間晚於 B 標頭上的時間」
- 任務 13 驗收 3（§8.6）：串流中有文字時停止，等 ≥30 秒，切換 →「結尾是中斷標記」「最後更新時間比切換的時刻早 ≥30 秒」（時刻取裝置時鐘）
- 任務 13 驗收 7 可判定一：四種離開各一段新的 S，串流中切走 → 抽屜開 S：訊息在、回覆結尾「…（已停止）」；冷啟動後同上；結尾不是中斷標記的那次重做。可判定二：回覆結束後直接 force-stop → 冷啟動續接、訊息與完整回覆都在
- 任務 5 驗收 2：串流中進搜尋查回覆中的字，觀察命中數自行增加（記模型、提示、持續時間）
- 任務 5 驗收 6 / 任務 13 驗收 8 / 任務 9 驗收 6：搜尋用過之後送出、串流、停止、刪除、切人格/模型、冷啟動續接照常

## S10 隨機抽測（功能總覽，本輪範圍外，恰好三項）
- 聊天-09、啟動-05、比較-13（實際操作，不讀程式碼推論）

## S11 收尾
- 還原旋轉與 density 原值、Gboard 版面回到進來時的那一個
- 更新 `docs/qa-environment.md`、`docs/qa-random-coverage.md`
