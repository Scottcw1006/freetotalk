# conversation-search v3.18 驗收報告

- 開始時本版號目錄沒有既有的 `test-cases.md` / `test-report.md`，未刪除任何檔案。
- 受測版本：`39d6418`（開始時 `git status --porcelain` 為空）。建置：`./gradlew installDebug` 覆蓋安裝，`lastUpdateTime=2026-09-15 23:12:36`，晚於 HEAD 提交時間 `2026-09-15 17:43:46 +0800`。
- 驗收裝置：Pixel 9（1080x2424、density 420），Gboard（注音／英文）。驗收途中裝置時間跨過午夜（2026-09-16 00:00 前後），跨日後以日期顯示的標頭改變，已逐一留意，不影響判定。

# 驗證結果總覽

**Pass。** 全部仍有效的驗收條文在本環境做得到的部分都通過，沒有判 fail 的條文。有四處記「未能觀察／本環境無法觀察」（任務 5 驗收 2 的命中數自行增加、任務 12 驗收 1 的貼上 U+3000/U+00A0、任務 9 驗收 12、§1.9 載入未完成的那一瞬間），都依條文的處置寫明原因與承接方式。另有兩點規格問題回報 PM（見「問題歸屬」），不影響本輪 pass。

# 逐項驗收結果

## 任務 1：對話存取的分層抽出
- 驗收 1、2、3、4、5、7、8、9：v3.11 已移除，不驗。
- **驗收 6（既有行為逐項不變，實機）**：pass。
  - 冷啟動續接（檔案存在）：開 G1 後 `session.xml` 為 `qa-g1`，force-stop + am start 後畫面是 G1 的全部訊息。
  - 冷啟動續接（檔案不存在）：停在一段換人格／換模型後的空白對話上 force-stop，再啟動 → 仍是空白對話，人格（朋友）與模型（SmolLM 135M）與記住的一致，該 id 無檔。
  - 空白對話不進歷史：任務 16 驗收 4 的四種離開前後檔案雜湊皆相同；抽屜從未出現開著的空白對話。
  - 歷史排序：抽屜由新到舊（22:14 → 21:14 → … → 9/14）；送出回覆後該段排到第一列。
  - 開啟／刪除／送出後更新：點列開啟、刪除鈕刪除（檔少一個）、送出後檔案出現且 updatedAt 前進，皆照常。
  - 壞檔：推入內容損毀的 `qa-bad.json` 後冷啟動，抽屜捲動蒐集到 20 列，恰為其餘全部檔案。
- **驗收 10（§9.3）**：pass（靜態閱讀 + 實機間接佐證）。`ConversationRepository.find(id)` 實作為 `all().firstOrNull { it.id == id }`，與「取全部再挑同一個 id」結構上同一答案（含找不到）。實機：存在的 id（qa-g1）冷啟動續接成功；不存在的 id（未存檔的空白對話 id）冷啟動得到新空白對話，兩者都與檔案清單一致。存取層無自動化測試（明確不做已載明），故為靜態層次。

## 任務 2：比對與片段的純邏輯
- **驗收 1**：pass。`chat/ConversationSearch.kt` 無任何 import；依賴的 `Conversation.kt`、`ModelSpec.kt`、`Persona.kt` 無 Android／Compose／持久化 import；測試在 JVM 上直接呼叫並通過。
- **驗收 2**：pass。`an empty query is not a search` 涵蓋 U+3000、U+00A0（逐碼位確認測試原始碼內是 0x3000、0xa0）。
- **驗收 3**：pass。`conversations come back newest first and hits stay in the order they were said`。
- **驗收 4**：pass。`a conversation shows at most three hits but reports the true total`（3 / 5）。
- **驗收 5**：pass。`a conversation with no matching message is left out entirely`（含 `none { it.hits.isEmpty() }`）。
- **驗收 6**：pass。`highlights stay inside the snippet and never overlap`（範圍內、遞增、不重疊）。
- **驗收 7**：pass。`flattenToLine`、`normalizeForSearch` 為 `internal`，測試直接呼叫並單獨斷言（BMP 掃描、U+0130 長度）。
- **驗收 8（§2.11）**：pass。以探針（scratchpad 複本，`cmp` 確認與專案原始碼相同）直接呼叫四個使用點：
  - U+0020、U+00A0：訊息端收合＝true、查詢端整形後命中＝true、只有它的查詢 `isActive`＝false、欄位開頭／尾端移除＝true。
  - U+001C：以上四項全部相反（不收合、不命中、`isActive`＝true、開頭尾端都保留）。
  - 程式碼上四處都經由同一個 `isFlattenableSpace()`。
- **驗收 9**：pass。`every unicode separator is collapsed`（BMP 掃描）、`zero-width characters are not whitespace`、`a run of mixed whitespace becomes exactly one space`。
- **驗收 10**：pass。`ChatViewModel.onSearchQueryChange` 只套 `withoutLeadingSpace()`，失焦只套 `withoutTrailingSpace()`，沒有把整形後字串寫回欄位；實機逐碼位讀到「北京 」「北京  」「kiwi  」皆原樣保留。
- **驗收 11**：pass。`SearchUiState.isActive = query.flattenToLine().isNotEmpty()`；測試 `isActive agrees with flattening about what counts as empty` 含 BMP 掃描。
- 驗收 12、13：v3.11 已移除，不驗。

## 任務 3：比對語意的單元測試（全部在 `ConversationSearchTest`，`clean testDebugUnitTest` 49/49 通過；逐條讀斷言）
- 驗收 1：pass —`a message the user wrote is found`、`a message the assistant wrote is found`。
- 驗收 2：pass —`matching ignores case`（hello／HELLO）。
- 驗收 3：pass —`matching ignores full-width and half-width`（ABC→ＡＢＣ、ｈｅｌｌｏ→Hello）。
- 驗收 4：pass —`traditional and simplified are not folded together`。
- 驗收 5：pass —`surrounding whitespace in the query is ignored`：`assertEquals(1, bare.size)` 且 `assertEquals(bare, padded)`（完整結果相同）。
- 驗收 6：pass —五個案例 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0 俱在（逐碼位確認）。
- 驗收 7：pass —`a query that matches nothing returns nothing`。
- 驗收 8：pass —`normalising never changes the length of the text`（含 İ）。
- 驗收 9：pass —`a highlight points at the matched text even when the message had newlines`。
- 驗收 10：pass —`every match inside the window is highlighted`。
- 驗收 11：pass —`a hit deep inside a long message…`：命中在索引 100、開頭「…」、長度 62、標示取出「北京」。
- 驗收 12：pass —3 / 5。
- 驗收 13：pass —`the persona opener can be searched`：作者 Assistant、id 等於第 0 則。
- 驗收 14：pass —`a thread holding nothing but its opener still matches`：1 / 1，不命中時空。
- 驗收 15：pass —`a shared opener matches in every thread that has it`。
- 驗收 16：pass。兩個測試的測資形狀與條文一致（15 emoji + 1 BMP + 查詢詞 + 尾巴；30 BMP + 查詢詞 + 7 BMP + 20 emoji），兩者都斷言片段仍含「北京」並逐碼位檢查落單代理。**獨立性以索引推算（靜態）**：案例一視窗 [11→10, 70)，索引 69 為 BMP，尾端保護不觸發；案例二起點 10 為 BMP，起點保護不觸發，索引 69 為高位代理，尾端保護觸發 → 拿掉起點保護只影響案例一、拿掉尾端保護只影響案例二。未做突變執行（本輪未被要求突變全掃）。
- 驗收 17：pass —下限的斷言字面為 `"a${character}b".flattenToLine() != "a b"`；上限含 U+200B/200C/200D/FEFF 與 👨‍👩‍👧；收合案例 `"北京\r\n\t 　天氣"` → `"北京 天氣"`。
- 驗收 18：pass —訊息 U+3000、查詢半形空白。
- 驗收 19：pass —四個案例都走 `searchConversations`（U+3000 查詢、3 空白查詢、U+00A0 訊息、只有 U+3000 → 空）。
- 驗收 20：pass —查詢「北京(2 空白)天氣」標示長度 5、取出「北京 天氣」（逐碼位確認為兩個 0x20）。
- 驗收 21：pass。
- 驗收 22：pass —U+3000、U+00A0、`" \t\n"` 非搜尋、`"a"` 搜尋，另有 BMP 掃描。
- 驗收 23：pass —L=59／60／61 三案例，並斷言 59 與 60 片段不同、60 片段不含「甲」、61 與 60 相同。
- 驗收 24：pass —老師／Qwen／qwen 不命中，正向「想不通」命中。
- 驗收 25：pass —`ellipses appear only where text was actually cut` 同一方法內兩個方向。
- 驗收 26：pass —`searching finds message text rather than the derived title`。

## 任務 4：「所有對話」的定義與搜尋子狀態
- 驗收 1、4、5、7：v3.11 已移除，不驗。
- **驗收 2**：pass —`allThreads contains the open thread even when it is blank`、`all threads are ordered newest content first`。
- **驗收 3**：pass —`the same thread arriving twice appears once`、`allThreads keeps the in-memory copy when a thread arrives twice`（id 只一次、訊息數 2）。
- **驗收 6**：pass —`the in-memory copy wins even when the saved one looks newer`（ConversationSearchTest）：`same`(100, 2 則) / 歷史 `same`(900, 1 則) / `other`(500)，斷言分組順序 `["other","same"]` 且 `same` 命中數 2。

## 任務 5：狀態層的搜尋入口與衍生結果
- 驗收 1、3、4：v3.11 已移除，不驗。
- **驗收 2（盡力而為）**：**未能觀察**（不 fail）。
  - 模型：Gemma3 1B（聊天人格：朋友）。
  - 嘗試一：提示「Count from 1 to 400, one number per line.」，送出後 5 秒進搜尋查 `120`，連續 109 秒每秒讀一次結果列：始終「找不到符合「120」的訊息」。回覆實際是中文閒聊條列，767 字撞上長度上限，從未出現 `120`。
  - 嘗試二：提示「Please list the numbers from 1 to 50, one per line, and after that say kiwi.」，4 秒時進搜尋查 `kiwi`。回覆約串流 41 秒（以該段對話檔出現的時刻為準），759 字撞上長度上限，從未寫出 kiwi，命中數維持 1。
  - **可接受的間接佐證已取得**：7 秒時搜尋已顯示「1 段對話・1 則訊息」，命中那則使用者訊息；同一時刻那段對話的檔還不存在（41 秒才出現）。也就是尚未落盤的新訊息搜得到。
  - 為什麼攔不到：命中數以「則」計，一則串流中的回覆最多讓數字加 1，而且只有在查詢字首次出現在回覆裡時才會。這兩次模型都沒有照提示寫出那個字。
- **驗收 5**：pass —查「今天」時旋轉為橫向：仍在搜尋畫面，欄位 bounds 在頂端 `[321,64][2402,230]`，欄位內容仍為「今天」。轉回直向後摘要「9 段對話・9 則訊息」仍在；方向設定已還原（1/0）。
- **驗收 6**：pass —多次使用搜尋之後，送出、串流、停止、切換人格、切換模型、刪除都照常運作（見任務 13、16 的操作紀錄）。
- **驗收 7**：pass —換人格為孫子產生空白對話，查「阿嬤」：第一組就是這段空白對話（Gemma3 1B、23:37，其餘孫子對話較舊）。
- **驗收 8**：pass。
  - 進入搜尋、查 `apple` 得「3 段對話・9 則訊息」（正向對照非空），再離開。
  - 狀態列前後同為「Qwen2.5 0.5B・離線」。
  - 以 `hashdir.sh` 比對對話目錄 16 檔，前後完全相同。
  - 以 pid 過濾的 logcat 共 29 行，含 llm／mediapipe／inference／model 字樣的為 0 行。

## 任務 6：抽屜入口與搜尋畫面外殼
- **驗收 1（§5.2 逐項）**：pass。
  - (i) 上半部依序：模型清單（Gemma3 1B／Qwen2.5 0.5B／SmolLM 135M）→「並排比較所有模型」→「歷史紀錄」＋「每段對話各自獨立，不會互相影響」→「開始新的對話」→「搜尋對話內容」。在歷史列表上捲動後，上半部每個節點的 bounds 完全不變，只有列表內容移動（列表 20 列，需要捲動）。
  - (ii) 三個入口：
    - 「並排比較所有模型」→ 標題「模型比較」，抽屜關閉；
    - 「開始新的對話」→ 新對話，人格（姊姊）、模型（Gemma3 1B）不變，抽屜關閉；
    - 「搜尋對話內容」→ 搜尋畫面，抽屜關閉。
    - 三者都是 TextButton 列，不是輸入框。
  - (iii) 每一列都有 emoji、人格名、模型名、時間、標題、預覽、刪除鈕（desc「刪除這段對話」）。
    - 時間：今天顯示「22:14」等，非今天顯示「9/14」。
    - 英文長句列：標題「this is a very long first …」、預覽「and this is a very long last message use…」，尾端都有「…」。
    - 「你好」列：標題與預覽都是「你好」，沒有「…」。
    - 另見截圖 drawer1。
  - (iv) 點 G1 列 → 開啟 G1，抽屜關閉。按「你好」列的刪除鈕 → 列立即消失，沒有任何確認對話框，檔案 15→14。
  - (v) 清空資料後第一次啟動，抽屜顯示「還沒有其他對話。」。
  - (vi) 開著 G1 時，列表沒有「apple 一」、有「apple 甲」；點 G2 後再開抽屜，有「apple 一」、沒有「apple 甲」。
- **驗收 2**：pass —同 (ii)。
- **驗收 3**：pass —進入時 EditText `focused=true`、`mInputShown=true`。
- **驗收 4**：pass —查「今天」往下捲完全部結果（12 次 dump），12 次 dump 的 EditText bounds 全部是 `[148,174][1058,340]`。
- **驗收 5**：pass —畫面返回鍵與系統返回鍵（鍵盤開著時先收鍵盤再按一次）都回到聊天畫面（`mCurrentFocus` 仍是本 App、「換人」在）。
- **驗收 7**：pass —比較畫面按系統返回鍵：焦點跑到桌面（NexusLauncher），即沒有接住返回鍵。比較畫面旋轉為橫向：被踢回聊天畫面。兩個既有缺陷維持原樣。
- **驗收 8**：pass —同任務 5 驗收 5；橫向＋鍵盤的結果可見量不判。
- **驗收 9（§1.7）**：pass。
  - 注音輸入法打「北」，不按任何鈕，1 秒後得「3 段對話・4 則訊息」（含「我住在臺北」）。
  - 再打「京」，1 秒後變成「3 段對話・3 則訊息」（臺北那則消失）。
  - 畫面上可點的只有返回、欄位，以及有字時出現的清除鈕，沒有「按下才搜」的控制項。
- 驗收 6、10：v3.11 已移除，不驗。

## 任務 7：結果列表
- **驗收 1**：pass —結果列表從欄位下方開始佔滿，查「今天」可整片捲完 9 組。
- **驗收 2**：pass —命中字加粗且為主題色（截圖 apple、q_yubao、q_beijing_tianqi 標示都落在查詢字上）；程式碼 `SpanStyle(color = MaterialTheme.colorScheme.primary, …)`，沒有寫死色碼。
- **驗收 3（§3.6）**：pass —查「今天」（多段對話第 0 則開場白同時命中），分步捲完全部，蒐集到 9 個互異標頭，與摘要「9 段對話」相符，順序新→舊，行程 pid 前後相同（30620），沒有崩潰。
- **驗收 4**：pass —查 `apple`：G1 列 3 則＋「還有 2 則符合」。
- **驗收 5**：pass —長中文片段列單行、超出以「…」收尾，沒有換行破版（截圖 q_yubao）。
- **驗收 6（中文算繪）**：pass。
  - 輸入法 Gboard 注音；送鍵方式為 `adb shell input text` 送鍵帽英數鍵序，再點候選字。
  - 查「天氣預報」命中一則 90 字的長中文訊息，片段為「…正常，標點符號也要正常顯示。🌧️明天的天氣預報說會下雨…順便聊聊最近的工…」（62 code units，前後「…」）。
  - emoji 🌧️ 距命中 5 字、在視窗內，完整算繪；標示恰好蓋住「天氣預報」四字；全形標點正常。
- **驗收 7**：pass —查空白對話（孫子）的開場白，結果區沒有任何「還沒說話」節點（計數 0）。
- **驗收 8（§7.1）**：pass。
  - 直向、鍵盤彈出、不捲動，查 `apple`：英文版面鍵盤上緣 y=1459，其上完整可見 3 個標頭＋7 個命中列。
  - 注音版面鍵盤上緣 y=1359，查「北京 天氣」時 3 個標頭＋3 個命中列都在上緣之上（該查詢總命中只有 3 則）。
- **驗收 9（§7.2）**：pass —`wm density 606`、鍵盤彈出，鍵盤上緣 y=1033，其上完整可見 1 個標頭＋3 個命中列（「apple 三」下緣 994）。已 `wm density reset` 回 420。
- **驗收 11**：pass —標頭同時有 emoji、人格名、模型名、時間。三組各不相同：📘老師 Qwen2.5 0.5B 22:14／🧡姊姊 Gemma3 1B 21:14／🙌朋友 SmolLM 135M 9/14。
- 驗收 10：v3.11 已移除，不驗。

## 任務 8：空狀態、無結果、點擊與返回
- **驗收 1**：pass —進入時只有「搜尋所有對話的訊息內容」，沒有「找不到」、沒有結果列。
- **驗收 2**：pass —查 `apple` 有結果後接著打成 `applezzqq`，畫面上只剩「找不到符合「applezzqq」的訊息」一個節點。
- **驗收 3**：pass —開著 G1 查 `apple`，點 G2 的命中列 → 回聊天，內容是 G2（姊姊、「明天北京　天氣如何」等）。再進搜尋，欄位為空、顯示引導文案。
- **驗收 4**：pass。
  - 開著 G2 點 G2 的命中列 → 回聊天，訊息節點集合前後完全相同，`last_thread_id` 仍是 qa-g2。
  - 點擊後 0.4 秒與 2 秒各查一次，都沒有「正在載入模型／載入中」，狀態列仍「Gemma3 1B・離線」。
  - 目錄雜湊相同。
  - 「不閃爍」無法以結構化查詢判定：畫面只經過一次切換回聊天，沒有看到重新載入的橫幅；逐幀閃爍未擷取。
- **驗收 5**：pass —換人格（孫子）得空白對話，查「阿嬤」並點該組 → 回聊天、仍是同一段（`last_thread_id` 不變、開場白畫面）、沒有載入橫幅、目錄雜湊與 `session.xml` 前後完全相同（未存檔、未建立新對話）。
- **驗收 6**：pass —搜尋畫面沒有任何刪除鈕（可點節點只有命中列本身、返回、欄位、清除）。
- **驗收 7**：pass —「3 段對話・9 則訊息」，畫面列出 3 + 2 + 2 筆。
- **驗收 8**：pass —三種離開都清空：畫面返回鍵、系統返回鍵、點命中列（驗收 3），再進入皆為空欄位＋引導文案；旋轉不清空（任務 5 驗收 5）。
- **驗收 9**：pass —搜尋中欄位有「kiwi」→ Home → `am kill`（pid 空）→ `am start`（「brought to the front」）→ 停在搜尋畫面、欄位為空、引導文案。
- **驗收 10**：pass —英文版面送 `"  kiwi  "`：欄位逐碼位為 `kiwi` 加兩個 0x20，文案「找不到符合「kiwi  」的訊息」。TAB 失焦後欄位為 `kiwi`，文案「找不到符合「kiwi」的訊息」。
- **驗收 11（§1.9）**：pass（兩半都判）。冷啟動後 0.6 秒內連點抽屜、搜尋並打 `apple`，第一次 dump 已是「3 段對話・9 則訊息」（包含歷史對話，正向對照成立），全程沒有 ProgressBar、「載入」字樣。「載入尚未完成的那一瞬間」未能攔到（記未能觀察，不影響判定）。
- **驗收 12（§5.7 前半）**：pass —欄位有 `kiwi` 時讀 `shared_prefs/session.xml`，只有 `last_thread_id`、`last_model`、`last_persona` 三個鍵，沒有 `kiwi`。正向：開 G1 後偏好為 qa-g1，冷啟動續接到 G1 全部訊息。
- **驗收 13（§6.1）**：pass —推入只含助理訊息的 qa-x1（id 與開著的 G2 不同），查 `zebrax1` 點該列 → `last_thread_id`=qa-x1，聊天畫面為「💗 情人」＋「助理獨白 zebrax1」；點擊前後目錄雜湊相同。之後換人格離開，qa-x1 的檔被清掉（§9.1 規定的行為，非 fail）。
- **驗收 14**：pass —驗收 3、4、5、13 四種點擊情境都回到聊天並顯示對應對話，每一次點擊前後對話目錄雜湊都完全相同（無新增、無改寫）。驗收 3 為了讓位元組可比，先讓 App 自己寫過 G1、G2 一次再取基準。

## 任務 9：回歸驗證與中文算繪的手動清單
- 驗收 1、14：已移除，不驗。
- **驗收 2**：pass —`TextRepair.kt` 沒有 `flattenToLine`／`normalizeForSearch`；`ConversationSearch.kt` 也沒有引用 `repairModelText`（grep 確認）。
- **驗收 3**：pass —模型上下文過濾在 `ChatViewModel.send` 的 `.dropWhile { !it.fromUser }`，是 `send` 內的區域運算，沒有被 `searchConversations` 使用，也不是共用函式。
- **驗收 4（§9.1、§9.2 實機）**：pass —空白對話四種離開不留檔（任務 16 驗收 4）；壞檔不讓其他對話消失（任務 1 驗收 6）。
- **驗收 5（§5.2 逐項）**：pass —逐項結果同任務 6 驗收 1 (i)–(vi)，同一次觀察，此處各自判定，全部成立。
- **驗收 6**：pass（判定層次見「問題歸屬」第 1 點）—不經過搜尋的既有流程全部照常。觀察到的：送出、串流、停止（留字＋「…（已停止）」）、零字停止前再送、長度上限標記、切換人格／模型開新對話、抽屜開啟／刪除、冷啟動續接、比較畫面既有行為。§8 的時間排序改變不計入。
- **驗收 7**：pass —G2 最後一則「明天北京(U+3000)天氣如何」，抽屜預覽節點逐碼位仍含 `　`，搜尋片段該處為 0x20。G3 的 U+00A0 在抽屜預覽為 `\xa0`，片段為 0x20。
- **驗收 8**：pass —注音查「今天」：9 段對話各一則開場白命中，標示蓋在「今天」，片段未破版，摘要「9 段對話」＝分組數 9。
- **驗收 9**：pass —同任務 7 驗收 6（片段前後「…」、標示恰好四字、emoji 在 20 字內）。
- **驗收 10**：pass —注音打「台」只命中「我在台中」（G3）；打「臺」只命中「我住在臺北」（G2），兩者不同。
- **驗收 11**：pass —訊息端 G2「北京(U+3000)天氣」、G3「北京(U+00A0)天氣」由檔案推入。注音打「北京」＋ keyevent 62 半形空白＋「天氣」，欄位逐碼位為 `北 京 0x20 天 氣`；三則（含 G1 半形）全部命中，截圖中標示蓋住整個「北京 天氣」含中間空白。
- **驗收 12（選作）**：本環境無法執行 —`adb shell cmd clipboard` 回「No shell command implementation.」，而且輸入法打不出 U+3000。語意權威在任務 3 驗收 19。
- **驗收 13**：pass —8–11 都在 `mInputShown=true` 下判讀；§7.1 複驗見任務 7 驗收 8。
- 輸入法與送鍵方式（8–13）：Gboard 注音版面，`adb shell input text` 送鍵帽英數鍵序，`input tap` 點候選字，空白用 `input keyevent 62`。

## 任務 10：更新架構文件（`plan/architecture.html`）
- **驗收 1**：pass —「搜尋」節有「輸入框裡留下什麼，跟拿去比對的是兩回事」，寫明開頭空白一打就消失、尾端空白要等失去焦點才移除、拿去比對的一律是整形過的文字。
- **驗收 2**：pass —規則表「（只有空白）」列：「這是比對層的契約：搜尋框本身打不出這種輸入（見下），但比對層仍然必須答對」。
- **驗收 3**：pass。
  - (i) 不變條件第一條三件事俱在：只跟著內容走（新增、文字改變、移除才算）；切過去看一眼、切走都不算；理由是否則「最近」會變成「最近被看過」，而排序靠這個時間。
  - (ii) 「不算」那一側沒有「按下停止」。
  - (iii) 全文其他提到停止之處（第 102、105、109 行等）都在描述中斷分支，沒有宣稱停止不算一次更新。
- **驗收 4**：pass。
  - `ui/SearchScreen.kt`：路徑存在；無 ViewModel、只收 state 與回呼，不含比對。
  - `chat/ConversationSearch.kt`：路徑存在；比對、裁切、標示。
  - `data/ConversationRepository.kt`：路徑存在；轉發讀寫刪除，是 `ChatViewModel` 取得對話的唯一入口（grep：`ConversationStore` 只在 data 層內被使用）。
- **驗收 5**：pass —全文沒有「最後開啟時間」或其他第二個時間概念的描述。
- **驗收 7**：pass —「命中本身有 60 個字（含）以上時裝不下引子，視窗就從命中的第一個字開始」→ 命中長度剛好 60 時引子數唯一且為 0。
- 驗收 6：v3.9 已移除，不驗。

## 任務 11：摘要的兩個數字維持現狀
- 驗收 1、4：v3.12 已移除，不驗。
- **驗收 2**：pass —「3 段對話・9 則訊息」，畫面列出 3 + 2 + 2 筆。
- **驗收 3**：pass —空查詢（引導文案畫面）與無命中（找不到畫面）都沒有摘要列節點。

## 任務 12：查詢欄位的空白規則（全部逐碼位讀欄位）
- **驗收 1**：pass。
  - 空欄位按 keyevent 62 → 欄位 `''`，引導文案，沒有「找不到」。
  - 「長按空白鍵」→ **替代做法**：連送 5 次 keyevent 62（每次間隔 2 秒，避免注音版面把連續空白換成「。」）。原因：本裝置長按空白鍵是叫出切換鍵盤選單。結果欄位仍 `''`、引導文案。
  - 「貼上單一 U+3000／U+00A0」→ **本環境無法觀察**。原因：本環境設不了剪貼簿（`cmd clipboard` 無實作），也送不進非 ASCII。由驗收 6 承接：測試第 332 行以 U+00A0 開頭被移除，加上任務 2 驗收 8 的探針（U+00A0 與 U+3000 同屬 Zs、同一答案）。
  - 環境沒有改善（剪貼簿仍無法設定），無需回報回收。
- **驗收 2**：pass（**替代做法**）—原因：本環境設不了剪貼簿，「貼上」做不出來。實際方式：英文版面 `adb shell input text '%s%s%sbeijing'` 一次送入，欄位逐碼位為 `beijing`（重複兩次相同）。
- **驗收 3**：pass —欄位「北京(0x20 0x20)」時，MOVE_HOME＋FORWARD_DEL×2 刪掉「北京」→ 欄位 `''`、引導文案，沒有「找不到符合「  」的訊息」。
- **驗收 4**：pass —「北京」＋空白 → `北京 `（0x20）；再打「天氣」→ `北京 天氣`，命中含「北京 天氣」的訊息。
- **驗收 5**：pass（本環境可觀察）—欄位「北京  」按 TAB 後焦點移到清除鈕，欄位變 `北京`；點回欄位重新聚焦，仍是 `北京`。
- **驗收 6**：pass —`the field uses our whitespace rule, not the built-in one`：以 U+00A0 開頭 → 移除（第 332 行逐碼位確認為 0xa0）；以 U+001C 開頭 → 保留（第 334 行為 0x1c），並斷言與內建 `trim()` 不同（第 342–343 行）。
- **驗收 7**：pass —同任務 8 驗收 10。
- **驗收 8**：pass。
  - 欄位「北京(2 空白)」時記下全部結果節點與摘要（3 段對話・3 則訊息），TAB 失焦後除了欄位本身，結果與摘要逐字相同（diff 只有欄位那一行）。
  - 開頭移除：`%s%sapple` 與 `apple` 的結果列表與摘要 diff 相同。
- **驗收 9**：pass —`an empty query is not a search`（`""`、`"   "`、`"\n\t"`、U+3000、U+00A0）與 `a query of nothing but whitespace is not a search` 仍存在且通過。
- **驗收 12**：pass。
  - 畫面層：欄位「北京」→ MOVE_HOME＋空白 → 仍 `北京`；MOVE_END＋空白 → `北京 `（正向對照）。
  - 不經畫面那層：探針 `" 北京".withoutLeadingSpace()` 得 `北京`。
- 驗收 10、11：v3.11 已移除，不驗。

## 任務 13：對話「最後更新時間」的定義
- **驗收 1（§8.3）**：pass。
  - A＝hello 對話（朋友／SmolLM，00:00），B＝G3（朋友／SmolLM，9/14），共同字「哈拉」（開場白）。開著 A。
  - 操作前：搜尋分組依序為 00:00、9/15、9/15、9/14；抽屜前 5 列已記錄。
  - 從抽屜開 B 再開回 A 之後：分組順序與每個標頭的時間文字 diff 相同，抽屜順序 diff 相同。
- **驗收 2（§8.4）**：pass —G3 已先由 App 自己寫過一次，取 sha256 為基準；開 G3 再切回 A，G3 的 sha256 完全相同，`updatedAt` 仍為推入時的 1789377241972。
- **驗收 3（§8.6）**：pass。
  - Gemma3 1B 回覆串流中、畫面已有 53 字時按停止（停止後 177 字，含標記）。
  - 等 32 秒不做任何事，再從抽屜切到 G1。
  - 預期一：存檔最後一則結尾為「…（已停止）」。
  - 預期二：存檔 `updatedAt` 比切換時刻（裝置時鐘）早 38,221 ms，≥30 秒；與按下停止的時刻只差 82 ms。
- **驗收 4（§8.5）**：pass。
  - 空白對話（朋友／SmolLM）送出「hello」，回覆結束後檔案出現，`updatedAt`=1789488027900，距讀取時刻約 5.7 秒。
  - 查共同開場白「哈拉」時該段排第一組（00:00）。
  - 離開後抽屜第一列就是它。
- **驗收 5（§8.7）**：pass。
  - B＝朋友／SmolLM 那段，送出「hi」並等回覆結束（`updatedAt` 1789488516595）。
  - 等 65 秒後用「開始新的對話」建立 N（朋友／SmolLM）。
  - 查「哈拉」：第一組 N 標頭 00:09、第二組 B 標頭 00:08，N 排在 B 前面，而且 N 的時間晚於 B。
- **驗收 6**：pass（靜態）—`ChatViewModel.updateThread` 是唯一以 `hasSameContentAs` 判斷要不要蓋時間的地方。所有改變訊息內容的路徑都經由它：`send` 新增訊息、`updateMessage`（串流片段與結尾覆寫）、`settle` 的 Drop、`stop` 的留字或移除。其餘設定 `conversation` 的地方都是換成另一段對話，不是寫回同一段。
- **驗收 7**：pass。
  - **可判定一**：模型 Gemma3 1B，每種離開方式各用一段新的 S。

    | S | 提示 | 離開方式 | 切走當下畫面字數（增加中） | 存檔回覆長度與結尾 |
    |---|---|---|---|---|
    | S1 | 「S1 write a very long story about a cat.」 | 從抽屜開另一段（G2） | 53→160 | 659 字，「…（已停止）」 |
    | S2 | 「S2 write a 2000 word story about a bird with many details.」 | 開始新的對話 | 42→138 | 436 字，「…（已停止）」 |
    | S3 | 「S3 write a 2000 word story about a fish with many details.」 | 切換人格 | 40→123 | 309 字，「…（已停止）」 |
    | S4 | 「S4 reply in Chinese: write a 2000 word story about a frog.」 | 切換模型 | 49→74 | 119 字，「…（已停止）」 |

    - 每次切走後從抽屜打開 S：使用者訊息在（往上捲找到節點），回覆結尾節點為「…（已停止）」。
    - force-stop 冷啟動後，四段逐一從抽屜打開：同樣四段都有使用者訊息節點與「…（已停止）」節點，存檔內容一致。
    - **不算數而重做的嘗試**（結尾不是中斷標記）：S2 第一次（回覆 134 字自然結束）；S4 前三次（兩次撞上「…（已達長度上限）」，一次在切走前已結束）。另有 4 次因為我的腳本解析錯誤而沒有切走，也不算。
  - **可判定二**：Gemma3 1B 送出「P2 say hello in five words.」，約 3 秒回覆結束（檔案出現）。不做任何操作直接 force-stop，冷啟動 → 續接到該段（`last_thread_id` 相符），畫面有使用者訊息與完整回覆「你好呀阿公阿嬤！／我愛你們！」，與存檔一致，沒有中斷標記。
- **驗收 8**：pass —送出、串流、停止、刪除、切換人格／模型、冷啟動續接全部照常（見驗收 3、4、7 與任務 16）。條文所指「任務 9 驗收 6 的清單」實際上沒有清單，以本條括號內列舉判定（見問題歸屬）。

## 任務 14：讓補上來的條文各自有東西在驗
- **驗收 1**：pass —清單上每一條都指得出驗法：
  - 任務 1 驗收 10：程式碼位置 `ConversationRepository.find`
  - 任務 3 驗收 25：測試 `ellipses appear only where text was actually cut`
  - 任務 5 驗收 8：實機雜湊＋pid logcat
  - 任務 6 驗收 9：實機注音北／北京
  - 任務 7 驗收 11：實機標頭四樣＋三組比較
  - 任務 8 驗收 11–14：實機步驟（見上）
- **驗收 2**：pass。
  - 任務 3 驗收 25：無條件補「…」的實作會讓 `assertEquals("我要去北京", whole.snippet)` 變紅；只在長訊息不補的實作會讓 `startsWith("…")` 變紅。
  - 任務 3 驗收 16 的鑑別力另見上方索引推算。
  - 本輪未做突變執行（未被要求）。
- **驗收 3**：pass —正向與否定兩半都執行到：
  - 任務 3 驗收 25：同方法兩方向
  - 任務 5 驗收 8：結果非空
  - 任務 6 驗收 9：北→北京結果改變
  - 任務 7 驗收 11：四樣都在且隨對話改變
  - 任務 8 驗收 11：載入後含歷史
  - 任務 8 驗收 12：冷啟動續接
- 驗收 4、5：已移除，不驗。

## 任務 15：`docs/product/features.md` 指向本規格的引用
- **驗收 1**：pass —表格內指向 `docs/specs/conversation-search/` 的引用逐處列出共 40 處（同一格多個節號分開算）。對 v3.18 的對應檔案判三件事：節號的標題行存在、節名 grep 得到、節名就在該節號的標題行上。40/40 成立。
- **驗收 2**：pass —搜尋-02（§1.7）、搜尋-13（§1.2）、搜尋-20（§2.8）都仍帶著引用。
- **驗收 3**：pass —沒有任何一處含 `v3.x/` 版號目錄。

## 任務 16：只含助理訊息的既有對話檔，打開再離開就清掉
- 驗收 1：v3.15 已移除，不驗。
- **驗收 2**：pass（每一步都以「列出目錄再比對檔名」判定存在與否）。
  - X 出現在歷史清單、標題「還沒說話」；點該列 → 頂端「💗 情人」，`last_thread_id`=X。
  - 四種離開各用一個新的 X，每次都「開啟前存在、離開後不在」：
    - 開另一段（qa-x2 → 開 G1）
    - 開始新的對話（qa-x3）
    - 換模型（qa-x4 → SmolLM）
    - 換人格（qa-x6 → 朋友）
  - 冷啟動入口：推入 qa-x5 並把 `session.xml` 指向它，冷啟動 → 畫面續接 X5（「助理獨白 zebrax5」）→ 不點 X5，直接從抽屜開 G1 → qa-x5 不在。
  - 訊息清單為空的 qa-e1：點開（`last_thread_id`=qa-e1）再開 G1 → 不在。
  - 正向 qa-y1（含使用者訊息）：點開再開 G2 → 檔仍在，parse 後訊息 (id, author, text) 與推入時完全相同，`updatedAt` 也相同。
  - 冷啟動後：抽屜捲完 9 次 dump 都沒有任何 `zebrax` 列；搜尋 `zebra` →「找不到符合「zebra」的訊息」。
  - 第一次執行時我的存在檢查寫錯（`adb exec-out` 不回傳結束碼，恆印「EXISTS」），那一次的結果作廢，全部用新的檔重做一次，以上是重做的結果。
- **驗收 3**：pass —開著 A（G2），刪 B（「你好」f02）：對話檔 15→14，雜湊清單 diff 只少 `qa-f02.json`（A 的雜湊不變）。冷啟動後抽屜蒐集的 20 列沒有「你好」列，搜尋注音「你好」→「找不到符合「你好」的訊息」。
- **驗收 4**：pass。
  - 換人格得空白對話，四種離開各一次，每次離開前後 `hashdir.sh` 都是 16 檔完全相同：開另一段（→G2）、開始新的對話、換模型（→SmolLM）、換人格（→朋友）。
  - 正向：新開的空白對話送出「hello」、回覆結束後檔案出現，離開（開 G1）後檔仍在。
- **驗收 5**：pass —保存-02 同時寫明「一句話都沒說過的對話不會被存起來，也不會出現在歷史清單裡」與「從 App 以外放進手機、沒有任何一句你說的話的對話檔…會出現在歷史清單裡（標題「還沒說話」），但打開再離開就會被清掉，不另外提示」，與 §9.1 一致。該格兩個引用（§9.1、§6.4）依任務 15 三條判定皆成立。

# 邊界情況檢查

本規格沒有名為「已知衝突與邊界情況」的節（v2 起拆進行為規格、明確不做、決策歷程）。以下逐一檢查行為規格裡的邊界規則與明確不做中影響判定的項目：

- **§2.2 零寬字元、§2.11 U+001C**：單元測試加探針確認（任務 2 驗收 8、9）。
- **§3.5 同 id 合併（畫面不可達）**：由直接測試守，測試存在並通過（任務 4 驗收 6），不因畫面不可達而刪除。
- **§2.4 全空白查詢在畫面不可達**：比對層契約測試存在並通過（任務 12 驗收 9）。
- **§4.2 L=59/60/61、§4.4 surrogate 兩個方向**：測試存在，測資形狀與條文一致（任務 3 驗收 16、23）。
- **§6.1 空白但非目前那段**：實機點擊判定為切換（任務 8 驗收 13）。
- **§6.4 空白對話走錯分支看起來無害**：以雜湊判定未寫檔（任務 8 驗收 5）。
- **§1.9 載入中**：沒有第四種樣子；瞬間未攔到。
- **§7.3 橫向＋鍵盤**：不判；橫向截圖只看得到一組結果，不計入。
- **明確不做「比較畫面兩個既有缺陷不修」**：兩者都維持（任務 6 驗收 7）。
- **明確不做「正在看的那段不能直接刪」**：抽屜列表不含開著的那段，聊天畫面可點節點只有歷史紀錄、換人、送出，沒有刪除入口，與規格一致。
- **明確不做「抽屜標題／預覽截短可能切開 emoji」**：**本輪在實機重現了**（條文寫「沒有實機重現過」）。
  - 測資：G1 最後一則在第 40 個 code unit 處是 🌧 的高位代理。
  - 抽屜預覽節點為落單 U+D83C 接「…」，uiautomator dump 因此崩潰（logcat：`IllegalArgumentException: Bad surrogate pair (U+d83c U+2026)`）。
  - 依明確不做，本輪不判；事實供 PM 參考觸發條件。重現後我調整了測資順序，其餘驗收不受影響。
- **明確不做「標題／預覽攤平規則與搜尋不同」**：U+3000／U+00A0 在抽屜保留、在片段收合（任務 9 驗收 7）。

# 如果 fail：問題歸屬

本輪沒有判 fail 的條文。以下是回報 PM 的規格面問題，不是實作問題：

1. **任務 9 驗收 6「不進入搜尋時，所有既有流程行為與改動前一致」—條文要求的證據不存在於產物裡（第 4 型）。**
   - 「改動前」拿不到：附錄記載版本控制第一個版本就已含搜尋，決策 I 的處理理由相同。
   - 本輪只能以「既有流程現在照常運作」判定，判不出「與改動前一致」。
   - 建議比照決策 I 改寫成「現在必須是什麼樣」，或指定可觀察的清單。
2. **任務 13 驗收 8 寫「同任務 9 驗收 6 的清單」，但任務 9 驗收 6 底下沒有清單**（`changes-v3.13.md` 已列為腐爛引用，現行版仍在）。本輪以驗收 8 括號內自己列的項目判定；這條任務因此不能完全單獨讀完。
3. （資訊）明確不做「抽屜截短切開 emoji」已在實機重現，而且會讓自動化 UI 查詢工具整個失效，見上一節。是否達到觸發條件由 PM 判斷。

`spec-architecture.md`：本輪讀過，沒有發現說得出使用者後果、卻沒搬進行為規格的條目。

# 驗證方式說明

六件事逐項：
1. **標準測試指令**：做了。`./gradlew clean testDebugUnitTest`，log 只有 `:app:clean` 與 `:app:testDebugUnitTest`，XML 時間戳為當下，9 個測試類別 122 個測試，0 failures／0 errors／0 skipped（ConversationSearchTest 49、ConversationTest 23）。
2. **指名了測試的條文**：做了。任務 3 驗收 1–26、任務 2、任務 4 驗收 2/3/6、任務 12 驗收 6/9 逐條讀斷言。含逐碼位檢查測試原始碼裡的 U+3000／U+00A0／U+001C／零寬字元確實存在（不是看起來像空白的普通空白）。
3. **邊界情況**：做了，見上一節。
4. **實機條文**：全部在 Pixel 9 上實際操作。建置證據見開頭。串流相關條文用 Gemma3 1B；§8.5、§8.7 用 SmolLM 135M。
5. **隨機抽測三項**：做了：聊天-09、啟動-05、比較-13（見下節）。
6. **真實資料**：走「使用者已同意清空驗收裝置上的 App 資料」那一種，不做快照、還原、逐位元組驗證。
   - 清掉的：`files/conversations/`（4 個檔）、`shared_prefs/session.xml`。
   - 保留的：`files/gemma3-1b.task`、`files/model.task`、`files/smollm.task`、`files/profileInstalled`。
   - 驗收結束時裝置上留著本輪的測試對話資料，未清除。`/data/local/tmp/qa` 已刪除。

**另外的靜態／探針層次**：任務 1 驗收 10、任務 13 驗收 6、任務 9 驗收 2/3 是讀程式碼判定；任務 2 驗收 8 是 JVM 探針（專案原始碼未改，複本以 `cmp` 確認相同）；任務 3 驗收 16 的獨立性是索引推算，未做突變執行。

**實際執行偏離 `test-cases.md` 的地方**：
- **S3 測資改動**：G1 最後兩則訊息對調。原排列讓抽屜預覽切開 emoji，UI dump 崩潰。含 emoji 的長中文訊息仍在 G1 內，任務 7 驗收 6 的條件不變。
- **S3/S4 合併執行**：同一段搜尋畫面裡依序做，沒有分成兩個情境。任務 12 驗收 3、8、12 用注音打「北京」；驗收 8 的「開頭移除」一半用英文 `apple`。
- **任務 16 驗收 2 做了兩次**：第一次的存在檢查因 `adb exec-out` 結束碼問題無效，全部以新檔重做（qa-x1 另於 S6 以換人格離開）。
- **S9 的 §8.3／§8.5／§8.7**：沒有另造專用資料，用執行中產生的 hello 對話與 G3，共同字改用開場白「哈拉」。
- **任務 13 驗收 7**：多次重做（見該條），另寫了取樣與座標點擊的腳本縮短切走延遲。
- **任務 5 驗收 2 與啟動-05**：各嘗試兩次。
- **S8 順序**：壞檔檢查排在任務 16 流程之前一併做。
- **偏好注入**：任務 8 驗收 12 的正向對照，與冷啟動入口（qa-x5）的偏好注入，放在 S6／S8 裡做。

**一次未能重現的觀察（未用於判定）**：一次 `input text '%s%sapple'` 讀回 `pple`（少一個字）。之後同樣輸入與其他開頭空白輸入重送 9 次都完整，無法重現，推測是注入按鍵的時序。任務 12 驗收 2 以重送結果判定。

# 本輪驗證範圍

全驗。呼叫者要求的範圍即為本功能全部，沒有縮小。

# 環境文件的處置

`docs/qa-environment.md`：
- **驗過並更新日期**：第 1、2、3、5（ui.py selftest）、5a、6.1、6.2、6.3、6.4、6.5、7、8、9、11、12 節的確認方式本輪都跑過，標上最後確認日期。
- **新增，皆附確認方式**：
  - 第 0 節：`adb exec-out` 不回傳結束碼；每次 Bash 呼叫是新 shell、要 `source` env 檔。
  - 第 4 節：推入偏好檔造冷啟動前提；指向新工具 `hashdir.sh`。
  - 第 5 節：文字節點含落單代理字元時 uiautomator dump 整個崩潰，含 logcat 確認法；raw XML 讀 `enabled`；awk 整理欄位會吃掉連續空白；清單蒐集的殘列剔除、步距放寬條件。
  - 第 6.1 節：不截圖判斷版面的方法。
  - 第 6.2 節：`input text` 偶發掉字的處理。
  - 第 6.3 節：點候選第一格加讀回守衛的省截圖做法。
  - 第 6.4 節：MOVE_END。
  - 第 10 節：以檔案出現當便宜的等待訊號。
  - 第 11 節：驗收跨午夜。
  - 第 13 節：守衛本身要先自測、工具清單。
- **修正**：第 6.3 節兩次 62 的敘述維持隔 2 秒的做法，本輪再確認。
- **新工具**：`docs/qa-tools/hashdir.sh`（含 `selftest`，本輪跑過 OK）。
- **刪掉越界內容**：沒有找到不通過判準的句子。只移除了兩處「本輪踩到」之類與當輪經歷相關、沒有環境資訊量的括號註記。

# 本輪隨機抽測的三項功能

依「最久沒被挑過優先、其餘隨機」，從 51 個從未被挑過、且不指向本規格的項目中隨機抽出：

- **聊天-09（停止之後可以馬上再說下一句）**：Gemma3 1B 回覆串流中（75 字）按停止，狀態列回到「離線」。立刻在同一段輸入「again」並送出，送出成功、畫面出現使用者訊息「again」、狀態列變成「輸入中…」、新回覆開始出現。不用等、不用切換、不用重開。**與功能總覽一致。**
- **啟動-05（模型還沒準備好時，字可以先打但送不出去）**：**本輪未能觀察**。兩次冷啟動後立即點輸入框打「abc」，第一次讀取時模型已就緒（狀態「Gemma3 1B・離線」、無載入橫幅），送出鈕 `enabled="true"`（欄位有字），攔不到未就緒的窗口。另一次冷啟動（任務 13 驗收 7 可判定二）雖在 3.5 秒時看到「正在載入模型，馬上就好。」，但當時沒有在打字。沒有看到與總覽不符的行為，只是沒攔到條件。
- **比較-13（比較畫面沒有長度上限）**：比較畫面取消勾選 Gemma3 1B 與 Qwen2.5 0.5B（晶片 checked 讀回為 false/false/true），問「Tell me about the ocean.」並執行。SmolLM 卡片 38 秒後狀態「完成」，回答 5,448 字，遠超聊天畫面的 800 字上限，沒有任何長度上限標記。**與功能總覽一致。**

以上三項都沒有拿來改變本規格的 pass/fail。

另外兩個本規格範圍外、非抽測的順帶觀察（未查證、不判定，由呼叫者決定是否處理）：
- SmolLM 在聊天畫面的回覆裡出現字面的 `<|im_start|>system` 字串（抽屜預覽可見）。
- 搜尋畫面轉為橫向的截圖左側有一條白色區塊。

# 隨機挑選記錄的處置

`docs/qa-random-coverage.md` 既有檔案，**追加**三行：
```
2026-09-16  聊天-09
2026-09-16  啟動-05
2026-09-16  比較-13
```
