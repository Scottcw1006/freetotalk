# conversation-search v3.20 — test cases（本輪整理，按環境狀態分組）

> 判定依據永遠是 `spec.md` / `spec-tasks.md` 的條文原文；這裡只是執行順序。每個觀察點後面標它對應的條文，並摘錄條文的預期。
> 受測版本：HEAD `a95f757`（工作區乾淨）。驗收裝置：Android 模擬器 `emulator-5554`（AVD Pixel_9，Android 17）。
> 使用者已同意清空驗收裝置上的 App 資料：清 `files/conversations/` 與 `shared_prefs/`，保留模型檔。

---

## H 主機端（不經過裝置）

### H1 專案標準測試（`./gradlew clean testDebugUnitTest`），並逐條讀斷言對照條文
- H1-1 → 任務 3 驗收 1–26：每條找出對應測試，讀斷言是否碰到條文要求的測資形狀與正反兩半（例：驗收 16「第一個 code unit 不得是落單低位代理…且片段裡仍然完整含有查詢詞」；驗收 23「L = 59 與 L = 60 的預期片段必須不同」；驗收 25「前後都不得出現『…』…同一個測試方法裡對照驗收 11」）。
- H1-2 → 任務 2 驗收 2–7、9、11（空查詢含 U+3000/U+00A0 回空；新→舊；最多 3 則且總數正確；無空分組；標示區間在範圍內遞增不重疊；整形與折疊可直接呼叫；§2.1–§2.3 集合；「算不算空查詢」用整形）。
- H1-3 → 任務 4 驗收 2、3、6（「所有對話」含空白的開啟中那段、新→舊；同 id 去重取訊息多者；§3.5 第二條：順序 `other`、`same`，`same` 命中數 2）。
- H1-4 → 任務 12 驗收 6（U+00A0 開頭被移除；U+001C 開頭不被移除）、驗收 9（`""`、`"   "`、`"\n\t"`、U+3000、U+00A0 契約測試存在且通過）。
- H1-5 → 任務 17 驗收 1、2、3、6（🌧 截點落在代理對中間、剛好放得下不補「…」、不得修過頭、組合 emoji 三類＋前一個字＋剛好放得下＋極端輸入）。
- H1-6 → 任務 18 驗收 1–4（起點／終點落在組合 emoji 中、已在字界不移動、§4.2 三界線與 §4.3、下限、「e」＋80 個 U+0301）。
- H1-7 → 條文宣稱的鑑別力以靜態推演確認（任務 17 驗收 1「把保護拿掉，這兩個案例必須變紅」、驗收 6「只留代理字元的保護，三類字的案例必須變紅」、任務 18 驗收 2「只讓終點那一側的保護失效時，只有這一條變紅」、任務 3 驗收 16 兩案例各自獨立）。

### H2 靜態閱讀
- H2-1 → 任務 1 驗收 10（§9.3：依 id 取一筆 == 取全部再挑）。
- H2-2 → 任務 2 驗收 1（比對純邏輯無 Android/Compose/持久化相依）、驗收 10（整形後字串不寫回欄位）。
- H2-3 → 任務 9 驗收 2（文字修復程式碼不含、不引用搜尋整形／折疊）、驗收 3（送進模型上下文的過濾與搜尋不共用）。
- H2-4 → 任務 13 驗收 6（「什麼算一次更新」只有一個判斷處，所有寫回對話的路徑都經過）。
- H2-5 → 任務 7 驗收 2 的「顏色取自主題，不寫死色碼」那一半。

### H3 探針（scratchpad 複本編譯，不改專案）
- H3-1 → 任務 2 驗收 8 / §2.11：U+0020、U+00A0「兩端整形都收合成單一 U+0020；只有它的查詢是未搜尋狀態；放在欄位開頭時必須被移除」；U+001C「兩端整形都不得收合；只有它的查詢算在搜尋；放在欄位開頭時必須留著」。
- H3-2 → 任務 12 驗收 12 不經過畫面那一層：「對輸入『(1 個空白)北京』必須給出『北京』」。

### H4 文件
- H4-1 → 任務 10 驗收 1、2、3（三件事＋不含「按下停止」＋無他處宣稱停止不算）、4（模組地圖三列路徑存在、職責相符）、5（只有一個時間概念）、7（命中長度剛好 60 時引子為 0）。
- H4-2 → 任務 15 驗收 1（每處節號存在、節名 grep 得到、同一節）、2（搜尋-02、-13、-20 仍帶引用）、3（不含版號目錄）。
- H4-3 → 任務 16 驗收 5（features.md 保存-02 與 §9.1 一致）。
- H4-4 → 任務 14 驗收 1–3（列出的條文各自指得出驗法；可自動化的說得出什麼實作會變紅；正向對照兩半都被執行）。

---

## 裝置前置（非條文）
覆蓋安裝工作區建置 → 記 `lastUpdateTime` → 清使用者資料（保留模型檔）→ 記裝置日期、`accelerometer_rotation`／`user_rotation`／`wm density` 原值。

---

## 情境 A：乾淨資料冷啟動（無任何對話檔、無偏好）— 準備一次
- A-1 → 任務 1 驗收 6「冷啟動續接（檔案不存在）」：啟動後是一段新對話，不崩潰。
- A-2 → 任務 6 驗收 1 (v)／任務 9 驗收 5：「沒有其他對話時，顯示『還沒有其他對話。』」
- A-3 → 任務 6 驗收 2「搜尋入口列（不是輸入框），點擊後抽屜關閉、進入搜尋畫面」；驗收 3「進入時輸入框已聚焦、鍵盤自動彈出」。
- A-4 → 任務 8 驗收 1「剛進入時顯示引導文案，沒有『找不到…』、沒有空的結果列表」；任務 8 驗收 11 否定那一半（無進度指示／骨架／載入中）；任務 11 驗收 3（空查詢無摘要列）。
- A-5 → 任務 12 驗收 1「空欄位按空白鍵 → 欄位仍為空、顯示引導文案、不顯示『找不到』」（逐碼位）；先試真正的長按空白鍵與貼上單一 U+3000／U+00A0，做不出來才走條文替代做法並記錄。
- A-6 → 任務 12 驗收 2「貼上『(3 個半形空白)北京』→ 欄位內容為『北京』」：先試真正貼上，做不出來才用一次送入整串的替代做法並記錄。
- A-7 → 任務 8 驗收 2「輸入保證不存在的字串，只出現『找不到…』一句」；任務 11 驗收 3（無命中無摘要）。
- A-8 → 任務 8 驗收 10／任務 12 驗收 7：輸入 `"  kiwi  "` →「欄位內容為 `"kiwi  "`、文案為 `找不到符合「kiwi  」的訊息`；使輸入框失焦後，欄位與文案都變成 `kiwi`」；任務 12 驗收 5「失去焦點後尾端空白被移除、重新聚焦不得加回」。
  - 重置點：kiwi 必須在任何含 kiwi 的測資推入之前做完。

## 情境 B：主資料集（推入對話檔，冷啟動）— 準備一次，底下多個觀察點
測資（全部是推入的 JSON；時間戳可控）：5/2/2 命中三段；「北」「北京」則數不同；長英文標題／預覽與「你好」短句；非今天更新的一段；不同人格／模型的數段；U+3000 與 U+00A0 夾在中文詞之間的訊息；「臺」「台」；命中 20 字內含 emoji 的長中文訊息；「北京 天氣」訊息；任務 17 驗收 4（25 BMP＋🌧、39 BMP＋🌧）；任務 17 驗收 7（👨‍👩‍👧 標題形狀、🇹🇼 預覽形狀，兩段）；任務 18 驗收 5（👨‍👩‍👧 起點形狀＋🇹🇼 終點形狀，同一段兩則）；多段同人格只有開場白＋內容的對話（開場白命中）；一個損毀檔；足以讓抽屜列表捲動的數量。

### B1 抽屜
- B1-1 → 任務 6 驗收 1 (i)／任務 9 驗收 5：上半部依序「模型清單、『並排比較所有模型』、『歷史紀錄』標題與『每段對話各自獨立，不會互相影響』、『開始新的對話』、『搜尋對話內容』」；捲動歷史列表時上半部不動。
- B1-2 → 任務 6 驗收 1 (iii)／任務 9 驗收 5／任務 17 驗收 5：每列 emoji、人格、模型、時間、標題、預覽、刪除鈕；今天「時:分」、非今天「月/日」；長英文句「尾端都看得到『…』」，「你好」「整句、尾端沒有『…』」。
- B1-3 → 任務 17 驗收 4：「標題、預覽尾端是完整的字接『…』…emoji 之前的字都在」（逐碼位）；dump 崩潰即 fail 證據。
- B1-4 → 任務 17 驗收 7：「標題『…』前面不是落單的 👨，預覽『…』前面不是落單的 🇹；兩者都是驗收 6 預期的其中一種」（逐碼位，寫明碼位）。
- B1-5 → 任務 9 驗收 7：同一則含 U+3000 的訊息，抽屜預覽逐碼位仍是 U+3000（搜尋片段那一半在 C 做）。
- B1-6 → 任務 1 驗收 6「放一個內容損毀的對話檔後歷史仍完整顯示」「歷史排序」；任務 9 驗收 4（§9.2）。
- B1-7 → 任務 6 驗收 1 (vi)／任務 9 驗收 5：開著 A 時列表有 B 沒 A；點 B 後列表有 A 沒 B。另 (iv)「點一列 → 開啟那段對話、抽屜關閉」。
- B1-8 → 任務 13 驗收 1（§8.3）：A 新 B 舊含共同字；從抽屜開 B 再開回 A →「搜尋分組順序、分組標頭的時間文字、抽屜歷史順序都與操作前相同」。（操作前先記一次搜尋與抽屜）
- B1-9 → 任務 6 驗收 1 (ii)：「並排比較所有模型」進比較畫面、「開始新的對話」人格模型不變、「搜尋對話內容」進搜尋，三者抽屜都關閉。

### B2 搜尋畫面（同一資料集）
- B2-1 → 任務 8 驗收 7／任務 11 驗收 2：5/2/2 →「3 段對話・9 則訊息」，列出 3 + 2 + 2 筆；任務 7 驗收 4「顯示 3 則 + 『還有 2 則符合』」。
- B2-2 → 任務 6 驗收 9：輸入「北」不按鈕不收鍵盤 1 秒內出結果；再輸入「京」結果換成「北京」那一組；畫面沒有「按下才會搜」的控制項。
- B2-3 → 任務 6 驗收 4／任務 7 驗收 1：捲動結果時輸入框不被捲走；結果佔滿輸入框以下並可捲動。
- B2-4 → 任務 7 驗收 3：兩段以上對話的第 0 則同時命中，捲完全部結果不錯亂、不崩潰。
- B2-5 → 任務 7 驗收 5：命中列單行省略、不破版（截圖）；任務 7 驗收 2 視覺那一半（命中字視覺明顯不同、位置正確）。
- B2-6 → 任務 7 驗收 7「分組標頭不顯示對話標題；空白對話的標頭不出現『還沒說話』」；驗收 11「標頭同時看得到人格 emoji、人格名稱、模型名稱、時間；換一段人格或模型不同的對話，標頭上那幾個字必須跟著改變」。
- B2-7 → 任務 8 驗收 6：命中列沒有刪除鈕。
- B2-8 → 任務 12 驗收 4「輸入『北京』+ 空白 → 欄位為『北京 』；再輸入『天氣』→ 欄位為『北京 天氣』，且命中含『北京 天氣』的訊息」。
- B2-9 → 任務 12 驗收 3「欄位內容為『北京(2 個空白)』時刪掉『北京』→ 欄位為空、回到未搜尋狀態」。
- B2-10 → 任務 12 驗收 8：輸入「北京(2 個空白)」記結果與摘要 → 失焦 →「結果列表與摘要數字完全相同」。
- B2-11 → 任務 12 驗收 12：欄位「北京」，游標移最前按空白 → 仍「北京」；移回尾端按空白 →「北京(1 個空白)」。
- B2-12 → 任務 9 驗收 7 另一半：同一則 U+3000 訊息，搜尋片段逐碼位收成 U+0020。
- B2-13 → 任務 18 驗收 5：查「kiwi」→ 起點形狀那列「…」之後不是 ZWJ 或 👩；終點形狀那列「…」之前不是落單 🇹；都是驗收 1、2 預期的其中一種（逐碼位，寫明碼位）。
- B2-14 → 任務 7 驗收 8（§7.1）：直向、鍵盤彈出、不捲動，至少完整看到 1 標頭 + 4 命中列（以 IME frame 上緣判）。
- B2-15 → 任務 7 驗收 9（§7.2）：顯示大小調到最大，重跑 → 至少 1 標頭 + 2 命中列。還原 density。
- B2-16 → 任務 6 驗收 8／任務 5 驗收 5／任務 8 驗收 8 後半：搜尋中旋轉 → 仍在搜尋畫面、輸入框在頂端、查詢與結果都在。還原旋轉設定。
- B2-17 → 任務 6 驗收 5／任務 8 驗收 8 前半：畫面返回鍵、系統返回鍵都回聊天、不退出 App；再次進入時輸入框為空、顯示引導文案。
- B2-18 → 任務 8 驗收 3：點另一段對話的命中 → 回聊天、內容是該對話、再進搜尋輸入框為空。
- B2-19 → 任務 8 驗收 4：點目前開啟對話的命中 → 回聊天、內容不變、不閃爍、不重載模型。
- B2-20 → 任務 8 驗收 12：輸入一個查詢後，偏好檔找不到該字串、鍵組成只有游標三鍵；正向：冷啟動仍續接原對話。
- B2-21 → 任務 8 驗收 9：搜尋中 Home → `am kill` → 回來 → 停在搜尋畫面、查詢為空、引導文案（或回聊天也不判 fail 的那部分以條文為準）。
- B2-22 → 任務 8 驗收 11 正向一半：冷啟動後立即進搜尋（攔得到就記），等歷史載入後同查詢結果含歷史對話。
- B2-23 → 任務 5 驗收 8：模型就緒後，進入搜尋、打到有結果、離開；全程狀態列不變、該 pid 無模型載入／推論紀錄；進入前後對話檔數量與內容逐一一致（hashdir diff）；正向：結果非空。

## 情境 C：中文輸入法（需要裝置上的中文輸入法）
- C-1 → 任務 7 驗收 6：中文輸入法組字選字產生查詢；命中位置 20 字內含 emoji 的長中文訊息 → 標示蓋正確的字、片段沒破版、標點正常。
- C-2 → 任務 9 驗收 8：查會命中多段開場白的詞 → 多段、標示在詞上、未破版、摘要與分組數一致。
- C-3 → 任務 9 驗收 9：長中文訊息中段片語 → 片段前後有「…」、標示恰蓋住輸入的字。
- C-4 → 任務 9 驗收 10：「臺」「台」各打一次，結果不同。
- C-5 → 任務 9 驗收 11：U+3000 與 U+00A0 各一則；打詞組中間按半形空白 → 兩則都命中，標示蓋住整個詞組含空白位置。
- C-6 → 任務 9 驗收 12（選作）：U+3000 放進剪貼簿貼上仍命中；做不到記「本環境無法執行」。
- C-7 → 任務 9 驗收 13：以上 8–11 在鍵盤彈出狀態下判讀，同時複驗 §7.1。
- 若本環境沒有可用的中文輸入法：C 全部記「本環境無法觀察」並寫明卡在哪一步。

## 情境 D：對話檔生命週期（每一小段自己重置測資）
- D-1 → 任務 16 驗收 2：只含助理訊息的 X（id 與開著的不同）→ 在歷史、標題「還沒說話」；點它 → 畫面換成 X；四種離開（開另一段、開始新的對話、換人格、換模型）各一次、每次重放 X → 離開後 X 的檔不在；冷啟動後歷史與搜尋都找不到 X。
- D-2 → 任務 16 驗收 2 冷啟動入口：偏好指向 X，冷啟動，不點任何東西直接開另一段 → X 的檔不在。
- D-3 → 任務 16 驗收 2 訊息清單為空的檔：放、點開、開另一段 → 檔不在。
- D-4 → 任務 16 驗收 2 正向：含使用者訊息的 Y 點開再開另一段 → Y 的檔仍在、訊息相同。
- D-5 → 任務 8 驗收 13：X 存在、開著另一段不同 id；搜 X 裡的字點該列 →「必須切換到那段舊對話，聊天畫面顯示的是它的內容」。
- D-6 → 任務 16 驗收 3：A、B 有內容、開著 A，刪 B →「對話檔少一個、B 的檔不在；冷啟動後歷史清單與搜尋都找不到 B；A 的檔仍在、內容與刪除前相同」；任務 6 驗收 1 (iv) 刪除無確認。
- D-7 → 任務 1 驗收 6：冷啟動續接（檔案存在）、空白對話不進歷史、開啟／刪除後更新。

## 情境 E：需要推論的操作（模型就緒）
- E-1 → 任務 13 驗收 4（§8.5）：送出並收到完整回覆 → 時間前進、搜尋結果與抽屜都排到最前；任務 1 驗收 6「送出後更新」。
- E-2 → 任務 13 驗收 5（§8.7）：B 送出等回覆結束，等 ≥1 分鐘，開始新的對話產生 N，搜開場白字 → N 與 B 都出現、N 在 B 前、N 標頭時間晚於 B。
- E-3 → 任務 5 驗收 7：切換人格後的空白對話可用開場白搜到（§1.8：排在最前）。
- E-4 → 任務 8 驗收 5：切人格產生空白對話 → 搜開場白點擊 → 回聊天、對話不變、不重載模型、不建立新對話、對話檔數量與內容不變。
- E-5 → 任務 8 驗收 14：三種點擊情境（另一段、目前這段、空白且未存檔那段）走完都回聊天顯示對應對話，且都不新增或改寫任何對話檔（配合 B2-18、B2-19、E-4、D-5 各自前後 hashdir）。
- E-6 → 任務 16 驗收 4：換人格產生空白對話，四種離開各一次，每次前後對話檔數量與內容相同；正向：新對話送出等回覆結束再離開 → 檔存在。
- E-7 → 任務 13 驗收 2（§8.4）：A 有內容且時間早於今天，開著 A、切到另一段 → A 存檔內容與切換前完全相同（先讓 App 自己寫過一次再取基準）。
- E-8 → 任務 13 驗收 3（§8.6）：串流中、已有文字時按停止；等 ≥30 秒；切換 → 存檔那則回覆結尾是「…（已停止）」；存檔 updatedAt 比切換時刻（裝置時鐘）早 ≥30 秒。
- E-9 → 任務 13 驗收 7 可判定一：四種離開各一次、每次新的 S，回覆出現文字且仍在增加時切走 → 抽屜打開 S：使用者訊息在、回覆結尾是中斷標記；結束行程冷啟動後同上。結尾不是中斷標記的那一次重做。報告寫模型、提示、切走時字數。
- E-10 → 任務 13 驗收 7 可判定二：送出等回覆結束，不做任何事 force-stop 冷啟動 → 續接、訊息與完整回覆都在。
- E-11 → 任務 13 驗收 8：送出、串流、停止、刪除、切換人格／模型、冷啟動續接各自「做得完並出現直接結果」。
- E-12 → 任務 5 驗收 6：搜尋之後送出、停止、切換人格／模型照常。
- E-13 → 任務 5 驗收 2（盡力而為）：串流未結束時進搜尋、輸入回覆中會出現的字，觀察命中數自行增加；寫明模型、提示、串流時長、有沒有攔到。
- E-14 → 任務 6 驗收 7：比較畫面，勾選預設、輸入一句話按「執行」→ 每個勾選模型各一張卡片依序跑完、最後「完成」且有回答文字；左上箭頭回聊天。

---

## 邊界情況（行為規格裡未被上面指名、需主動確認的可判定）
- §1.2 標題不參與比對（任務 3 驗收 26 測試）；§1.8 串流（E-13）；§1.9（B2-22）；§3.5（H1-3）；§3.6（B2-4）；§3.7 三段同人格開場白全部出現（B 資料集查開場白字）；§4.9 emoji 算繪（C-1、B2-5）；§5.7（B2-20、B2-21）；§6.1（D-5）；§7.3 橫向＋鍵盤不判；§8.2 串流中切走時間前進的原因是內容改變（E-9 旁證）；§9.4（D-6）。
