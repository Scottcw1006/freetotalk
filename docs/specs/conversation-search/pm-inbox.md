# PM 收件匣（對話內容搜尋）

**目前 1 筆待處理。**

## [2026-09-18] `message-store-normalization`：搜尋改查資料庫、輸入停下來 250 毫秒才開始搜尋 —— 本規格有七處字面被覆蓋（使用者裁決：現在不改，下次修訂時照清單改）
- 來源：**使用者裁決**（2026-09-18，`message-store-normalization` 的 X2 選 (乙)、D1 選 (a) 並加「輸入停下來 250 ms 才開始搜尋」）＋ 該輪 PM 整理的清單
- 內容：使用者裁決搜尋改成查資料庫（每次搜尋把存下來的訊息逐批讀出來、交給現有那一份比對規則、比完就放掉；開著的那一段仍取記憶體裡的版本），並且輸入停下來 250 毫秒才開始搜尋。**比對規則一條都沒有改，搜尋結果與之前相同。** 使用者裁決本規格**現在不改**；在那之前以那一份規格、`docs/product/features.md` 與 `plan/architecture.html` 為準。逐列清單在 `docs/specs/message-store-normalization/spec-appendix.md`「跨規格覆蓋清單」的 `conversation-search` 兩張表，摘要：
  1. **（規則）**`spec.md` §1.10「搜尋全程不觸發推論、不產生檔案讀寫」：「讀」現在是必然的；「不寫」「不觸發推論」照舊（那一份的 §6.4）。
  2. **（規則）**`spec.md` §1.7「即時，沒有送出鈕」：「每變動一次就重算一次」、延遲寫成「允許（非必要）」→ 延遲現在是必須的 250 毫秒，連續輸入的中間內容不各自重算。§1.7 的可判定（停 1 秒內出現、不必按任何鈕）照樣成立。
  3. `spec.md` §2.9「回顯用的是欄位當下的內容」：等待的 250 毫秒裡畫面停在上一個狀態，括號裡可能是上一刻的字；輸入停下來之後仍逐字元相同。
  4. 功能總覽：「沒有任何一條線通往磁碟」、對話存取層「刻意沒有查詢能力，搜尋路徑完全不碰它」、資料流那張圖。
  5. `spec-tasks.md`「明確不做」的「沒有索引、沒有快取」：寫著屆時「不是把比對搬去資料庫查詢」。它擔心的事（第二份比對規則、搜不到畫面上看得到的）由那一份的 §6.2、§6.3 守住了，但字面會讓人以為那一輪做錯了。
  6. `spec-architecture.md` §B.2、§B.6、§E.3、§E.5（建議，方向相反）。
  7. **順帶提醒**：`docs/specs/message-copy-delete/spec-appendix.md`「跨規格衝突清單」裡指向本規格 §1.1、§5.2、§8.1 的三條，使用者當時同樣裁決「下次修訂時改」—— 處理這一筆時一起改。
- 核對過、字面仍然成立的：§1.8、§1.9、§3.5、§5.2、§8、§9（§9 的「對話檔」早已在 `conversation-store-db` 的清單裡）。§3.5 在那一輪之後更承重：「開著的那一段取記憶體」是生成中的回覆、未存的訊息、只有開場白的對話搜得到的唯一原因。
- 相關條目：§1.7、§1.10、§2.9；功能總覽；`spec-architecture.md` §B.2、§B.6、§E.3、§E.5；`spec-tasks.md`「明確不做」
- 狀態：待處理（不急：本規格下一次被修訂時一併處理，不為此單獨開一輪）

---

- **這個檔是意見信箱，不是檔案櫃。** 有事情要交給 PM 時，在這裡加一筆（標題含日期、來源是「使用者裁決」還是「RD 觀察」／「QA 報告」、相關條目、內容）。**PM 處理完就把那一筆刪掉**，所以沒有待處理事項時它就是現在這個樣子。
- **被刪掉的是通知，不是理由。** 每一筆的實質內容在被刪之前都已經落在別的地方：規則進 `spec.md`／`spec-tasks.md`，選項、代價與裁決理由進 `spec-appendix.md`／決策歷程，而「哪一輪處理並移除了哪幾筆、各自落在哪一節」列在該輪的 `changes-v<N>.md`。**原文在版本控制歷史裡**（`git log -p -- docs/specs/conversation-search/pm-inbox.md`）。
- **（2026-09-16）本檔在 v3.24 依新規則清空。** 此前累積的、**全部已標為「已處理」**的那一批一併移除；它們的處理結果落在各輪的 spec 與變更說明裡，原文在版本控制歷史裡。**v3.24 這一輪處理並移除的三筆，列在 `changes-v3.24.md`／「本輪處理並移除的收件匣項目」；v3.25 那一筆列在 `changes-v3.25.md` 的同名一節。**
