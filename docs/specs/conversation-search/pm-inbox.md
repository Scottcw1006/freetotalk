# PM 收件匣

## [2026-09-04 17:20] §C.8 的舉證有一半是錯的：Kotlin 的 trim() 會移除 U+00A0

- **來源**：RD 觀察（實測，非推論）
- **相關條目**：§C.8、任務 12 驗收 6
- **狀態**：已處理

**內容**：

§C.8 寫著：

> Kotlin 的 `String.trim()` 依 `Char.isWhitespace()`，而那個集合與 §2.1 兩個方向都不相等 —— 它**不會**移除 U+00A0、U+2007、U+202F（不斷行空白，正是貼上文字裡最常見的那幾個），卻**會**移除 U+001C–U+001F。

**前半句是錯的。** Kotlin 的 `Char.isWhitespace()` 不等於 Java 的 `Character.isWhitespace()` —— 它是後者與 `Character.isSpaceChar()` 的**聯集**，而 `isSpaceChar` 涵蓋 General Category Zs，所以 U+00A0、U+2007、U+202F **都會**被 Kotlin 的 `trim()` 移除。

我是照 spec 寫測試才撞出來的：`assertNotEquals(" 北京".trim(), " 北京".withoutLeadingSpace())` 直接失敗，兩邊都是「北京」。

實測結果：

| 字元 | Kotlin `trim()` | §2.1 的定義 | 是否為差分案例 |
|---|---|---|---|
| U+00A0 | 移除 | 移除 | **否**（spec 說是，錯） |
| U+001C | 移除 | 不移除 | **是** ✓ |

**規則本身不受影響**，我沒有改實作 —— §2.8 仍然用 §2.1 的定義，而且 U+001C 那個方向就足以證明不能用內建 trim（它會吃掉在任何可見文字裡都不算間隔的 C0 分隔字元）。

**要請你處理的：**

1. **修正 §C.8 的舉證。** 差分是單向的，不是雙向。理由要重寫成站得住的版本 —— 現在的版本如果有人去查證，會發現一半是假的，那會連帶讓人懷疑整條規則。
2. **任務 12 驗收 6** 目前寫「差分案例，兩個都要有」，並在括號裡分別註明 trim 的行為。U+00A0 那一條的**要求**仍然成立（開頭的 U+00A0 必須被移除），但括號裡的理由是錯的。請調整措辭，不要讓 QA 拿著一個錯誤的理由去驗。
3. 附錄可以記一筆：**這是 spec 裡第一個被實作端實測推翻的事實宣稱**（先前 QA 推翻過一個關於輸入法的環境假設，這次是關於語言函式庫行為的）。兩者同類 —— 規格裡凡是引用「某個既有函式/環境的實際行為」，都是未經驗證的假設。

**我這邊的現況**：任務 12、13 已依 spec 實作完成，64 個單元測試全綠。測試裡對 U+00A0 的斷言已改成陳述實測到的真相（U+00A0 必須被移除、U+001C 必須不被移除、且只有後者與內建 trim 不同）。

---

- **狀態**：已處理
- **查證結論**：**RD 的觀察成立，全部採納。** 我獨立查證過，不是照單全收：Kotlin stdlib 的 `CharJVM.kt` 把 `Char.isWhitespace()` 定義成 `Character.isWhitespace(this) || Character.isSpaceChar(this)`，官方 API 文件的字面定義是「Zs / Zl / Zp，或落在 U+0009–U+000D、U+001C–U+001F 的 CONTROL 字元」；而 `String.trim()` 走的是 `CharSequence.trim(Char::isWhitespace)`，用的正是 Kotlin 這一份而不是 Java 的。所以 Kotlin 內建集合 = **§2.1 ∪ {U+001C–U+001F}**，是 §2.1 的**嚴格超集**。v3 §C.8 舉的三個例子（U+00A0、U+2007、U+202F）全部是 Zs，**三個都會**被 Kotlin 的 `trim()` 移除 —— 那半句不只是不精確，是三個例子全錯。落差只有 U+001C–U+001F 這一個方向，與 RD 實測一致。
- **附帶查出的一處內部矛盾（RD 沒提到，我在做相依檢查時發現）**：§C.6 主張「§2.1 的集合完全被 Kotlin 的 blank 集合包含」，而 v3 §C.8 主張「Kotlin 不會移除 U+00A0」。U+00A0 屬於 §2.1，這兩句話不可能同時為真。**正確的是 §C.6**，而且它之所以成立，靠的正是「Kotlin 的是聯集」這個事實。已在 §C.6 補寫成立的理由，並在決策歷程記下這次矛盾。
- **處理結果（改動的節，全部只動理由與舉證，規則字面一條未動）**：
  1. **§C.8** —— 第二點的理由整段重寫成「單向落差」，明說 v3 的宣稱錯在哪；可判定條文改寫成「兩個案例都要，但守的不是同一件事」。
  2. **§C.6** —— 補寫包含關係為何成立（Kotlin 的是聯集），並標明它與 v3 §C.8 曾經互相矛盾。原本的規則與失效模式論證未動。
  3. **§C.4 實作提醒** —— 把 Kotlin 的 `Char.isWhitespace()` 加進「沒有任何一個單獨等於規範集合」那份清單（它離得最近，正因如此最容易被順手拿來用）。
  4. **任務 12 驗收 6** —— 依收件匣第 2 點改寫措辭：U+00A0 的**要求**保留不動，理由改成「守的是 Zs 全部算空白這個下限，擋掉只認 ASCII 空白的實作」，並明寫它**不是**與 Kotlin 內建 trim 的差分；U+001C 標成唯一的差分方向。
  5. **任務 12 新增驗收 11** —— 這一項超出收件匣所列三點，理由：`ConversationSearch.kt` 的 KDoc（`withoutTrailingSpace` 上方）與測試 `the field uses our whitespace rule...` 的一行註解，**仍然原句照抄那個錯誤宣稱**（「it leaves U+00A0 and U+202F in place」／「one in each direction」）。只修 spec 而留著它們，等於把同一個假理由搬到更容易被相信的地方。該驗收是純文字修正、不改任何行為、不改任何斷言。**若認為這超出本輪範圍，刪掉這一條即可，其餘處理不受影響。**
  6. **附錄／決策歷程** —— 新增「第二次了：這次被實測推翻的是一個關於語言函式庫行為的宣稱」，含收件匣第 3 點要求的一般化（環境能力與函式庫行為同類，都是未經驗證的假設），以及「兩次推翻都沒有改掉任何一條規則」這個共同結局。同時修正 v3 在「同一個規則被回答了第二次」段落尾端那句括號註記（原文也寫著「兩個方向都不相等」）。
  7. **明確不做** —— 新增一條：本輪不把「引用既有函式／環境行為的宣稱必須有測試釘住」升格成 §F 的硬性條文，附觸發條件。
  8. **附錄／修訂記錄** 新增 v3.2 一列；卷首導讀補一句說明本輪沒有改動規則。
- **明確沒有做的事（依交辦，避免順手放寬規則）**：§2.8（四段欄位規則）、§2.10、§C.8 的**規則字面完全未動**；任務 12 原有的驗收 1–10 除了第 6 條的措辭之外未動；沒有因為「落差只剩單向」而放寬「不得使用語言內建 trim 或 blank」這條禁令 —— 集合不相等就足以支撐它，單向與雙向對這個結論沒有差別。RD 現有的實作與測試**不需要因本輪改任何行為**。

---

## [2026-09-07 19:05] 任務 10 已完成，請把核對結論與更動段落寫進該節

- **來源**：RD 觀察
- **相關條目**：任務 10 驗收 4、驗收 6
- **狀態**：已處理（v3.4）

**背景**：QA 全驗判 fail，主因是任務 10（更新架構文件）我漏做了。現在補做完成，但**驗收 4 與驗收 6 要求把結論寫進「本節」，也就是 spec 的任務 10 節** —— 那是你的檔案，我不能改。以下是你要記錄的內容。

### 驗收 6：實際更動的段落（`plan/architecture.html`）

1. **「搜尋」那一節，新增一個 note：「輸入框裡留下什麼，跟拿去比對的是兩回事」**（插在「攤平是唯一准許改變長度的一步」之前）。內容涵蓋 §2.8 三段：開頭空白立即移除、尾端空白打字中保留到失焦、中間不動；並說明兩次移除都不改變比對結果，以及判斷空白用的是自己那份定義而非語言內建 trim（含 U+001C–U+001F 的落差）。→ 驗收 1

2. **同一節規則對照表的「（只有空白）→ 不算搜尋」那一列**，補上一句：「這是比對層的契約：搜尋框本身打不出這種輸入，但比對層仍然必須答對」。→ 驗收 2

3. **「不變條件」清單新增一條，放在最前面**：「對話的『最後更新時間』只跟著內容走。新增訊息、訊息文字改變、訊息被移除才算一次更新；切過去看一眼、切走、按下停止都不算。否則『最近』會變成『最近被看過』，而歷史清單與搜尋結果的排序都靠這個時間。」→ 驗收 3

### 驗收 4：模組地圖三列的核對（核對日期 2026-09-07）

| 文件寫的 | 現況 | 結論 |
|---|---|---|
| 畫面 / `ui/SearchScreen.kt` / 搜尋輸入與命中列表。自己不持有狀態，也不決定什麼算命中 | 路徑存在；仍為無狀態、不含比對邏輯 | 相符，未改 |
| 純邏輯 / `chat/ConversationSearch.kt` / 什麼算命中、片段怎麼裁、標示要落在哪幾格 | 路徑存在；本輪新增的欄位空白規則也在這個檔案裡（與空白定義同源） | 相符，未改 |
| 資料 / `data/ConversationRepository.kt` / 對話從哪裡來、到哪裡去。聊天層唯一的入口 | 路徑存在；本輪未動 | 相符，未改 |

### 驗收 5：確認過

全檔搜尋不到「最後開啟時間」或任何第二個時間概念的描述。

### 一併請你考慮的條文問題

**驗收 4 與 6 要求 RD 把結論寫進 spec 的任務 10 節，但 RD 不得改 spec。** 這兩條照字面是 RD 做不到的。建議改寫成「RD 完成後提供更動清單與核對結論，由 PM 記錄於本節」，或明訂記錄位置改到架構文件自己的修訂區 —— 否則下一輪同樣會卡在這裡。

---

- **狀態**：已處理（v3.4）
- **查證結論**：**回報的部分全部成立，但這一筆漏掉了一條驗收。** 我沒有照單全收，逐條拿去 `plan/architecture.html` 對過：
  - 驗收 1 → note「輸入框裡留下什麼，跟拿去比對的是兩回事」確實存在，且位置就在「攤平是唯一准許改變長度的一步」之前，內容涵蓋 §2.8 三段與 U+001C–U+001F 的落差。**成立。**
  - 驗收 2 → 規則對照表「（只有空白）→ 不算搜尋」那一列確實補上了「這是比對層的契約…」。**成立。**
  - 驗收 3 → 「不變條件」清單第一條確實是最後更新時間那一條。**成立。**
  - 驗收 4 → 模組地圖三列的路徑我全部複核過確實存在；`chat/ConversationSearch.kt` 裡也確實同時放著欄位空白規則與空白定義（與該列描述相符）。**成立。**
  - 驗收 5 → 全檔搜尋不到「最後開啟時間」或第二個時間概念。**成立。**
  - **驗收 7 → 未完成，而這一筆完全沒有提到它。** 描述片段視窗的那一句仍是原文（只寫了引子那一邊），§4.2 的第二個分支沒有補上。**任務 10 因此仍未整體通過。**
- **處理結果**：
  1. **`spec-tasks.md`／任務 10 新增「目前狀態」與「執行紀錄」兩節，放在該任務的最前面** —— 執行紀錄收錄本筆的驗收 6 更動清單與驗收 4 核對表（含核對日期 2026-09-07）；目前狀態明寫「驗收 1–6 已完成、驗收 7 未完成、任務 10 不得判為通過，下一輪只需補驗收 7」。原有的 v2 / v3 / v3.2 / v3.3 核對結論全部保留，移到「歷次核對的歷史記錄」底下並標明不是現行狀態。
  2. **條文問題採納，照你建議的第一個方向改**：任務 10 驗收 4 與 6 改寫**執行主體** —— 核對與更動清單由做這個任務的人交出（交付形式不限，收件匣即可），寫進該節由 PM 完成；並各補一條可判定，讓 QA 對的是文件現況而不是任何人的口頭回報。**兩條要驗的東西一字未變**，只是把「誰做哪一半」寫進條文。不採用第二個方向（記錄位置改到架構文件自己的修訂區）的理由：那會讓「這個任務做完了沒有」的證據離開任務清單，QA 冷讀時要跨到另一份文件才判得出來。
  3. **附錄／決策歷程新增兩條**：一條記「第五種失效模式：條文指定的執行主體做不到那件事」（它與既有第 3 型不可達的差別在於不可達的是**分工**不是情境，並註明我沒有全面掃過還有沒有同型條文）；一條記「回報完成時逐條列出的條文，等於同時定義了沒被列到的那幾條」，也就是驗收 7 這次是怎麼漏掉的。
  4. **附錄／修訂記錄新增 v3.4 一列**；`spec.md` 卷首補一段本輪說明（含「任務 10 尚未整體完成」）。
- **沒有做的事**：沒有改動任何一條規則；沒有因為驗收 7 未完成而調整它的要求；沒有升級到「需要你決策的事項」（條文問題只有一個合理修法、核對結果是事實而非選項，見 `spec-decisions.md` 末段）。**依使用者要求，本輪不產 `changes-v*.md` 變更說明。**

---

## [2026-09-08 15:39] 使用者裁決：決策 B 拍 (d) + 一次性補掃

- **來源**：使用者裁決
- **內容**：

**決策 B（要不要把「規則 → 條文」的反向核對升格成 §F 的硬性條文）已拍板，結論是 `spec-decisions.md` 裡沒有列出的第四個選項：**

> **(d) 升格成硬性條文，但每一輪只對「這一輪被改動的章節」做規則 → 條文對照，不掃全文；並且先對現存的全部規則做一次性的全面補掃，把政策生效之前累積的歷史債清掉。**

**(d) 這個選項來自你自己在 `spec-decisions.md` 裡寫的「我的傾向」那一段**（偏向 (a) 但只在被改動的那幾節做）。它被獨立列成一個選項交給使用者，使用者選了它，並額外加上「一次性補掃」。

**為什麼要加補掃 —— 這是 RD 在整理選項時查到、而原本的傾向那段沒有涵蓋的一點：**

(d) 的邏輯是「一條規則一生只被寫下來一次，在它被寫下的那一輪核對它，每條規則就都會被核到一次」。但這個邏輯只對**政策生效之後**新寫的規則成立。對現存的規則，它有一個冷啟動的洞，而且證據就在附錄／修訂記錄裡：

**§4.2 的規則字面是原始版本，v3.3 只是替它補了可判定條文。** 也就是說，它在帶著 off-by-one 活著的那四輪裡，**從來不是「被改動的章節」** —— 純粹的 (d) 那四輪一次都不會打開它。而 §4.2 正是催生決策 B 的那條規則。

所以：不做補掃的 (d)，抓不到催生它的那個案例。補掃的一次性成本大約等於 (a) 的一輪成本，但只付一次，之後進入低成本穩態 —— 這正是 (d) 相對 (a) 的價值所在（(a) 是每輪付全表成本）。

**使用者同時裁決的另一件事**：QA 流程維持不變，**不採用**「第一次全驗之後改為只驗調整項與未通過項、段落結束再全驗」的分段驗收。理由已在主對話說明（實測縮小範圍反而更貴：294,337 tokens / 145 次工具呼叫 / 39.5 分，對比全驗的 270,916 / 135 / 33 分與 270,992 / 128 / 36 分；且 §4.2 這型缺口在分段驗收下每一輪都不會進範圍）。**這一條不需要你做任何事，寫在這裡是因為它是「為什麼補掃有價值」的另一半：既然每輪全驗會繼續做，補掃補上的覆蓋就會一直被複驗。**

- **相關條目**：
  - `spec-decisions.md`／決策 B（本項拍板後應移出，該檔回到「本次沒有需要決策的事項」的狀態，除非補掃過程中發現新的待決事項）
  - `spec-architecture.md`／§F.4 及其 v3.3 註記（「在裁決之前，這裡不加新的硬性要求」——這句話的前提已經消失）
  - `spec-tasks.md`／明確不做（v3.3）那一條：**§1.7（即時、無送出鈕）與 §6.5（不得為空白對話新增特例分支）兩處疑似同型缺口，明文寫著「決策 B 若裁決成 (a) 或 (c)，這兩條要一起處理」。(d) 是 (a) 的受限版本，且本次加了補掃，所以這兩條要一起處理。**
  - `spec-appendix.md`／決策歷程（決策 B 的選項、代價與裁決理由應完整移進來）

- **狀態**：已處理（v3.5）

---

- **狀態**：已處理（v3.5）
- **這是使用者裁決，我照辦，沒有重新爭論已拍板的事。** 只做了一件裁決沒有明說、但屬於落實範圍的判斷：把我自己在傾向那段提出的顧慮（「會不會退化成形式作業」）寫成 §F.5 裡三條防退化的硬性要求，而不是留成一句擔心。
- **處理結果（五件事逐項對照）**：
  1. **裁決寫成硬性條文** → `spec-architecture.md` 新增 **§F.5**：穩態義務（每輪只對被改動的章節做規則 → 條文對照，兩種結論擇一、不得留空、有覆蓋必須指名到「任務 N 驗收 M」）＋ **「什麼叫被改動的章節」的界線**。界線我照交辦認真處理，寫成**四項可判定的條件**（規則文字被動、被本輪新文字明文引用、有驗收條文指名它、本輪實作會經過它），並加兩條把界線往安全一側放大的規定：**以節為單位不以條為單位**、**判不出來就算被改動**。理由都寫在條文旁（規則之間會互相借用覆蓋；核對一節是分鐘級，漏一節的成本已量過是四輪）。
  2. **§F.4 底下那條 v3.3 註記** → 前提已消失，改寫成「§F.4 守後半段、前半段由 §F.5 守」，原註記降級為明確標示的歷史記錄放在後面。
  3. **一次性補掃已執行** → 101 條規則（`spec.md` 61 + 架構 40）逐條核對。結果：**補 13 條驗收條文**（任務 1 驗收 9、10；任務 2 驗收 13；任務 3 驗收 25；任務 4 驗收 7；任務 5 驗收 8；任務 6 驗收 9、10；任務 7 驗收 11；任務 8 驗收 11–14），**8 處明寫由哪一層守**（§1.2 的時間、§3.9、§7.3、§B.6、§F.1–§F.3、§G.4），新增**任務 14** 做收尾並寫下補掃的範圍與**沒掃到什麼**。補條文時逐條過了三型體檢，四條純否定斷言的規則各配了正向對照。
  4. **§1.7 與 §6.5 都補了條文**（任務 6 驗收 9、任務 8 驗收 14），另外連帶補了 §6.1（任務 8 驗收 13）。**我沒有維持 v3.3 的判斷**：其中 §6.5 那個理由（「已由任務 8 驗收 5 守住」）**經核對不成立** —— §6.4 自己的實作註記寫著走錯分支時「先存檔 → 刪檔 → 找不到 → 提早返回」看起來剛好無害，也就是特例分支存在時驗收 5 仍會通過。§1.7 的理由沒錯到那個程度，但「使用者立刻看得見」不是驗收機制。
  5. **決策 B 移出 `spec-decisions.md`**，選項 (a)(b)(c)、各自代價、(d) 的來歷、加補掃的理由（冷啟動的洞 + §4.2 的證據）、以及 QA 維持每輪全驗的實測數據，全部完整移進 `spec-appendix.md`／決策歷程。`spec-decisions.md` 回到「**本次沒有需要決策的事項**」，並寫明本輪為什麼沒有新的待決事項。
  6. **`spec-appendix.md`／修訂記錄新增 v3.5 一列**；`spec.md` 卷首補一段本輪說明；另產出 `changes-v3.5.md`。
- **沒有做的事（依交辦）**：**一條規則的實質內容都沒有改**（新增的全部是「可判定條文」與「由哪一層守」的註記，不是新要求）；**沒有碰任務 10 驗收 7**，任務 10 的「目前狀態」結論原樣保留（驗收 7 仍未完成、任務 10 仍不得判為通過），本輪的核對結論以「不改變目前狀態」的措辭放在歷史記錄區；**沒有修改 `plan/architecture.html`**，只讀。
- **一併回報兩件小事**：(a) 交辦寫的「131 條驗收」，我從頭數過是 **128** 條（任務 1–13 的編號條文）；補掃是對這 128 條做的。(b) 補掃**沒有掃到**的最大一塊是「既有 128 條條文與實際測試的對應」（也就是 §F.4 的方向），已明寫在任務 14，留給下一輪接。

---

## [2026-09-08 15:52] §8.2 第三點對「停止生成」的事實宣稱是錯的（第三次同型）

- **來源**：RD 觀察
- **內容**：

**§8.2 的第三點寫著：**

> 停止生成：把「還在生成」這個標記關掉，本身不算一次更新 —— 生成期間最後一個字出現時就已經記過一次了

**這句話對現行程式的描述不成立。** 按下停止時，程式做的不只是關掉標記 —— 它會**把整則回覆的文字覆寫成一段錯誤字串**。

證據在另一輪的階段 0 產出：`/Users/scotthuang/AndroidStudioProjects/demo/docs/specs/stop-generation/findings.md`。診斷是 `chat/ChatViewModel.kt` 的 `send()` 裡，`catch (e: Exception)` 接住了 `stop()` 觸發的 `CancellationException`（它是 `Exception` 的子類別），於是走進錯誤處理分支把回覆覆寫成「出錯了：StandaloneCoroutine was cancelled」。`stop()` 自己做的關標記動作隨即被這次覆寫蓋過。

**為什麼這是矛盾而不只是描述不精確**：§8.1 列舉「必須前進」的情況時，明文包含**「錯誤訊息寫入」**。所以同一個動作（按下停止）在 §8.1 底下要前進、在 §8.2 底下不得前進，兩條規則對它給出相反的答案。這個矛盾之所以一直看不見，是因為 §8.2 第三點的括號理由假定了停止只會關標記。

**§8.6 抓不到這個問題**：它的操作是「停止 → 等 ≥30 秒 → 切換」，預期存檔時間比切換時刻早 ≥30 秒。錯誤訊息寫入發生在停止當下，也滿足「比切換時刻早 ≥30 秒」，所以**不論這個 bug 在不在，§8.6 都會通過**。這條可判定對它想守的那件事沒有鑑別力。

**這一條的正確值取決於另一輪的裁決，請不要自己決定。** `stop-generation` 那一輪要 PM 回答的核心問題正是「按下停止之後這段對話應該留下什麼」。findings.md 指出現有程式在**長度上限**與**重複迴圈**兩種自動中斷時，走的是「留下部分文字 + 加一個標記」那條路（見 `send()` 裡的 `StopReason`）。若手動停止決定與它們一致，那就是一次內容改變，§8.1 要求時間前進，§8.2 第三點必須改寫；若決定停止完全不動內容，§8.2 第三點才站得住（但理由要重寫，因為「只是關掉標記」在修好之前都不是事實）。

**另外一處相鄰的相依**：`spec-architecture.md` §G.3 寫著「本輪只改『寫下去的時間值怎麼來』，不改『什麼時候寫檔』」。`stop-generation` 的第二個 bug（按停止那一次完全不會寫檔，內容只留在記憶體，直到後續某個動作才被存下來）要修的正是**什麼時候寫檔**。§G.3 不必然要改，但它劃的那條範圍線會被另一輪跨過，值得你確認兩邊的說法不會互相打架。

**這是第三次出現「規格裡的事實宣稱被實測推翻」。** 前兩次記在 `spec-appendix.md`／決策歷程（v3.2 的 §C.8 是其中一次）。而 `spec-tasks.md`／明確不做的 v3.2 那一條，對「要不要把『規格裡引用既有函式／環境行為的宣稱都必須有測試釘住』升格成硬性條文」寫的觸發條件，字面就是「**第三次出現**」。這一筆觸發了它。要不要因此升格是你的判斷 —— 但請注意這次與前兩次有一個差別：**前兩次被推翻時都沒有任何一條規則需要改，這次 §8.2 第三點很可能真的要改**，而那正是 v3.2 當初列出的另一個觸發條件（「某一次的推翻真的連帶改掉了一條規則」）。兩個觸發條件同時成立。

- **相關條目**：`spec.md` §8.1（「錯誤訊息寫入」那一項）、§8.2 第三點、§8.6；`spec-architecture.md` §G.1、§G.3；`spec-tasks.md`／任務 13、／明確不做（v3.2 那一條）；`spec-appendix.md`／決策歷程

---

- **狀態**：已處理（v3.5）
- **這一筆是在 v3.5 進行到一半時才進來的**（我開工時檔案裡只有上面那筆使用者裁決）。我把它與當輪那筆一起重看過，**兩筆之間沒有衝突，也沒有順序相依**；反而有一個交互作用：第一筆新增的 §F.5 成了第二筆所觸發那個提案的選項之一（決策 D 選項 (d)）。這一點寫在決策 D 的選項表裡，沒有被當成小事混過去。
- **查證結論：診斷成立，我自己讀過程式碼確認，不是照單全收。** `chat/ChatViewModel.kt` 的 `send()` 裡，回覆流程的 `catch (e: Exception)` 會接住 `stop()` 呼叫 `replyJob?.cancel()` 所觸發的 `CancellationException`（它是 `Exception` 的子類別），然後 `updateMessage(replyId, "出錯了：…", streaming = false)` 把整則回覆覆寫掉；`stop()` 自己那次關標記的更新隨即被它蓋過。**所以「按下停止只會關掉標記」對現行程式不成立。** RD 指出的第二件事（那一次的 `finally` 裡的存檔在工作已被取消的情況下不會真的落盤）我也看到了，方向一致。
  - **RD 說 §8.6 沒有鑑別力，這一點我也複核過並認同**：錯誤訊息的寫入發生在按下停止的當下，同樣滿足「比切換時刻早 ≥30 秒」，所以不論這個問題在不在，§8.6 都會通過。這是本輪補掃在找的那一型（第 1 型：一條會通過的錯條文），而它是被這一筆送上門的，不是被我掃出來的 —— **照實記在附錄。**
- **處理結果**：
  1. **升級成 `spec-decisions.md`／決策 C**（§8.2 第三點與 §8.1 的矛盾往哪一邊收）。依 RD 的要求**沒有自己決定**：選項表照兩種可能的裁決各寫一欄與各自代價，並註明正確值取決於 `stop-generation` 那一輪。
  2. **升級成 `spec-decisions.md`／決策 D**（第三次事實宣稱被推翻，要不要升格成硬性條文）。v3.2 當初寫的兩個觸發條件**同時成立**，所以它不再適合留在「明確不做」。四個選項含一個新變數：§F.5 剛上路，與這個提案部分重疊 —— 重疊到哪裡、以及它的洞在哪裡（只在該節被改動的那一輪才會問，而 §8 從 v3 之後沒被改過），都寫進了代價欄。
  3. **`spec.md` §8.2 底下加一個警示框**：說明矛盾在哪、§8.6 為什麼抓不到、**在裁決之前 QA 怎麼辦**（§8.6 與任務 13 驗收 3 照原樣執行，但「按下停止之後時間該不該前進」記「待裁決」、不判 pass/fail）。
  4. **任務 13 驗收 3 加註記**（要驗的東西未變）：這一條對 §8.2 第三點沒有鑑別力，不要拿它當「停止生成不算更新」的證據；它真正守住的是「切換那一刻不得蓋上新時間」。
  5. **`spec-tasks.md`／明確不做（v3.2 那一條）**：從「不做」改成「觸發條件已成立、已移交決策 D」，v3.2 當時的理由保留為歷史並標明它的哪一半已經失效。
  6. **`spec-tasks.md`／任務 14** 記下這是補掃找到卻補不了的缺口，並註明它是 §F.5 上路後的第一個實例（本輪新文字引用了 §8.1／§8.2／§8.6 → 依界線第 2 項 §8 整節算「被改動」→ 被核對 → 核出這個缺口）。
  7. **`spec-appendix.md`／決策歷程新增一條**：第三次事實宣稱被推翻，並寫出它與前兩次的關鍵差別（前兩次沒有任何規則需要改，這次讓兩條規則互相矛盾）。
- **沒有做的事**：**§8.1、§8.2、§8.6 的字面一字未動**，任務 13 的任何一條要驗的東西也沒有變。**沒有碰 §G.3** —— RD 指出 `stop-generation` 那一輪會跨過它劃的範圍線，我把這件事寫進決策 C 的「相鄰相依」提醒那一段，但不預先改一條屬於另一輪的範圍界線。

---

## [2026-09-08 16:35] 使用者裁決：決策 D 不升格；另附一條理由的寫法紀律

- **來源**：使用者裁決
- **內容**：

### 一、決策 D 的裁決

**不為「規格裡引用既有函式／環境行為的事實宣稱」另立硬性條文。** 使用者的原話是分工形式的：

> 如果是既定規則，那就需要測試；如果規則有衝突，那要請 QA 反應出來，PM 決定，如果有需要也可以升級到我這邊。

也就是：**規則**由 §F.4（條文 → 測試）與 §F.5（規則 → 條文）守住，這部分不變；**規則之間的衝突**走 QA → PM → 使用者這條升級路徑，不另外用條文去防。實質等於選項 (b)，但請把上面那個分工寫進決策歷程，因為它是裁決的理由本身，不只是結論。

### 二、裁決前 RD 與使用者查證出來的一件事：「三次同型」這個計次是錯的

**是使用者先看出來的** —— 他讀完三個案例後問「前兩次看起來不需要寫 spec 吧？只有第三次是 spec → 測試的問題」。我回去查證，附錄自己的文字就是證據：

- 第一次（輸入法打不出 U+3000）：附錄寫「**結論（查詢端必須整形）不變**，但支撐它的理由必須換掉」「**沒有任何一條比對規則需要改**」
- 第二次（Kotlin trim）：附錄寫「**規則沒有動，動的是理由**」「實作與測試在修正前就已經是對的（RD 依規則實作、64 個單元測試全綠），錯的只有規格裡的舉證」

**所以應該分成兩型，不是同一型：**

| | 次數 | 後果 |
|---|---|---|
| **A 型：理由錯了，規則沒事** | 2 次（U+3000 輸入法、Kotlin trim） | 只更正文件。產品毫髮無傷，什麼都不必修 |
| **B 型：前提錯了，規則因此自相矛盾** | **1 次**（§8.2 第三點 → §8.1／§8.2 打架） | 兩條規則對同一動作給出相反答案，而且看不見 |

**v3.2 當初寫的兩個觸發條件裡，只有第二個（「某一次的推翻真的連帶改掉一條規則」）是 B 型的標記；「第三次出現」那個把 A 型與 B 型混在一起數，因此把一次灌成了三次。** 決策 D 的問題敘述建立在「三次同型」上，而那個前提不成立 —— 這是 B 型的第一次。請把這個更正寫進決策歷程，並修正 `spec-tasks.md`／明確不做那一條裡的計次說法。

**這件事本身就是同一型錯誤的又一個實例**（規格裡的一個計次被當成既有事實使用，而它沒有被查證過），值得一併記下來。

### 三、附帶一條寫法紀律，請評估要不要寫進 §C 或 §F

**理由不該寫得比規則需要的更精確。**

診斷：A 型那兩次之所以會被推翻，不是因為「寫了理由」，而是因為**理由寫得比規則需要的更細**。

- §C.8 這條規則需要的事實只有一句：**兩個空白集合不相等**。§C.8 自己也這樣寫：「一個方向就足以支撐這條規則 —— 單向或雙向，對這個結論沒有差別」。但 v3 當初寫下的是五個具體斷言（三個字元不會被移除、四個會被移除、落差是雙向的），其中四個不撐任何東西，純粹是可被推翻的表面積。
- 第一次同理：規則需要的是「訊息端與查詢端都可能出現異常空白」，寫下的卻是「中文輸入法在全形模式下產出的空白就是 U+3000」——一個關於特定裝置特定輸入法的斷言。

**這條不是要少寫理由。** 附錄自己講得很好：如果 §C.8 只寫「必須用我們自己的定義」而不說為什麼，下一個 RD 會覺得那是潔癖然後順手改回 `trim()`。理由是這份 spec 能抓到問題的原因，要保留。

**它要的是：寫理由時只寫規則實際依賴的那個性質，不要為了顯得有把握而多寫幾個可被推翻的細節。**

**為什麼建議採納**：它**不增加任何每輪成本** —— 是寫法上的紀律，不是新的驗收義務，性質與決策 D 的 (a)/(c) 完全不同。使用者是在這個前提下同意把它交給你的。

**要不要寫成條文、寫進 §C 還是 §F、或只記在決策歷程當寫作提醒，由你判斷。** 如果你認為它會退化成形式作業或難以判定「規則實際依賴什麼」，明寫理由拒絕它也可以 —— 那同樣是有價值的結論。

- **相關條目**：`spec-decisions.md`／決策 D（本項拍板後移出）；`spec-tasks.md`／明確不做（v3.2 那一條的計次說法要修正）；`spec-appendix.md`／決策歷程（A 型／B 型的區分、以及第 138 行「與上一次的推翻是同一類」那句需要修正）；`spec-architecture.md` §C.8、§C.4、§F
- **狀態**：已處理（v3.6）

- **給 PM 的處理時機說明**：**這一筆不急著單獨處理。** 決策 C 仍在等 `stop-generation` 那一輪裁決「按下停止之後該留下什麼」，兩筆一起處理只需要跑一次 PM。RD 已確認兩項都不擋任務 1–9、11、12、14 的開工。

---

- **狀態**：已處理（v3.6）
- **這是使用者裁決，我照辦，沒有重新爭論已拍板的事。** 本輪三筆待處理事項（本筆、最末那筆 RD 觀察、以及 `stop-generation` 已完成的決策 C 裁決）我先一起讀完再動手：**彼此沒有衝突、沒有順序相依**，合起來也沒有動到任何架構決策的根本假設 —— 決策 D 明確把一類風險指派給人（升級路徑）而不是條文，這與 §F.4／§F.5 的分工相容，不推翻其中任何一條。
- **處理結果（三點逐項對照）**：
  1. **裁決寫進決策歷程，連同分工本身** → `spec-appendix.md`／決策歷程新增「決策 D 已拍板」一條，含使用者的原話、四個選項與各自代價（原樣保留）、以及「規則由 §F.4／§F.5 守、規則之間的衝突走 QA → PM → 使用者」這個分工 —— **它是裁決的理由本身，不只是結論**，所以寫在最前面。`spec-decisions.md` 回到「**本次沒有需要決策的事項**」。
  2. **計次更正，三處都改了** → (a) `spec-tasks.md`／明確不做那一條：現行結論改成「不升格（使用者裁決）」，並補上 A 型 2 次／B 型 1 次的表與「為什麼舊的計次會錯」；(b) `spec-appendix.md` 第 138 行那句「與上一次的推翻是同一類」：**原文保留**，補上「同一類只到這兩次為止，第三次不是 A 型是 B 型」的更正；(c) v3.5 那條決策歷程的**標題**（原本寫「第三次…」）加上一段前置更正，因為讀者對一節的判斷幾乎由第一句決定。另外把「**這件事本身就是同一型錯誤的又一個實例**」照你的建議記下來了 —— 一個計次被當成既有事實反覆引用，而它從來沒被查證過。
  3. **寫法紀律：採納，但刻意不寫成硬性條文。** 位置放在 `spec-architecture.md` §F.5 底下（那裡是這份 spec 唯一講「怎麼寫規格」的地方），明確標成**寫作紀律不是可驗收的義務**。不寫成條文的理由寫在旁邊：它沒有可觀察物（違反它的樣子就是一份寫得很有把握的規格），而且「規則實際依賴什麼」要靠判斷，正是 §C.4 警告的那種找不到理由的界線 —— 寫成條文只會多一條沒有鑑別力的義務。**它不增加每輪成本，這是採納它的前提。**
- **沒有做的事**：沒有改動 §C.8、§C.4 的任何規則或舉證（它們現行的說法與更正後的計次一致，不需要動）；沒有把寫法紀律追加成任何一條驗收。

---

## [2026-09-08 17:15] RD 觀察：任務 8 驗收 14 的結構條文與程式碼對不起來（字面上）

- **來源**：RD 觀察
- **內容**：

驗收 14 的結構那一半要求：

> 讀「點擊命中列之後開啟一段對話」那條路徑，從畫面把 id 交回去到對話被換掉為止，**沒有任何條件是以「這段對話是不是空白的 / 訊息是不是空的 / 有沒有被存過檔」為判斷依據；唯一的分支條件是 id 相等與否**。冷讀判定：指得出那條路徑，且指得出它只有那一個分支。

那條路徑是 `chat/ChatViewModel.kt:151` 的 `openThread(id)`，**它有兩個條件，不是一個**：

```kotlin
fun openThread(id: String) {
    if (id == _uiState.value.conversation.id) return        // 條件一：id 相等
    stop()
    viewModelScope.launch {
        repository.save(_uiState.value.conversation)
        val opened = repository.find(id) ?: return@launch   // 條件二：找不到就返回
        ...
    }
}
```

**我判斷這條實質上是通過的**：條件二是「目標對話還在不在」的空值防護，不是針對空白對話的特例分支。§6.1–§6.4 四種情境全部走同一條路，沒有任何一處問「這段是不是空白的」。

**但條文寫的是「只有那一個分支」，而冷讀的 QA 會數到兩個。** 這正是附錄記過的第 3 型失效（條文在改版後不可達）的近親：條文描述的形狀與程式碼的實際形狀對不起來，而正確的實作反而會被判 fail。

**建議的修法（由你決定）**：把條文從「唯一的分支條件是 id 相等與否」改成講**它禁止什麼**而不是**它允許幾個**，例如「沒有任何分支條件是以對話的空白狀態、訊息數量或存檔與否為依據；`find` 回傳空值的防護不屬於此類」。這樣要驗的東西不變，但可判定性回到「有沒有踩到禁止項」，而不是數分支數量。

**順帶一提，條件二正是 §6.4 實作註記描述的那條路**（「先存檔 → 對空白對話等於刪檔 → 找不到 → 提早返回」）。也就是說它同時是「看起來剛好無害」那個現象的所在地。你在寫修正條文時可能會想一併說清楚它為什麼是防護而不是特例 —— 否則下一輪還會有人在同一個地方卡住。

- **相關條目**：`spec-tasks.md`／任務 8 驗收 14（結構那一半）；`spec.md` §6.5、§6.4 的實作註記
- **狀態**：已處理（v3.6）

---

- **狀態**：已處理（v3.6）
- **查證結論：你的觀察成立，我自己讀過那條路徑確認，不是照單全收 —— 而且條件比你說的多一個。** 從畫面把 id 交回去到對話被換掉為止，實際上有**三個**條件：`ui/ChatScreen.kt` 搜尋畫面的 `onOpen` 回呼裡先有一次 id 相等的判斷，`chat/ChatViewModel.kt` 的 `openThread` 進去又有一次 id 相等的早返，然後才是你指出的 `repository.find(id) ?: return@launch`。**三個沒有一個是空白對話的特例**，§6.1–§6.4 四種情境走的是同一條路，所以規則本身沒有被違反 —— 你的「實質通過、字面不通過」判斷成立。
  - **這一點反而強化了你的建議**：連「有幾個分支」這個數字本身都不穩定，它會隨著防護加在哪一層而變。**數分支數量的條文，從一開始就不該存在。**
- **處理結果（採納你的修法方向）**：
  1. **`spec.md` §6.5 的結構可判定改寫**成「禁止哪幾種分支依據」：禁止以「這段對話是不是空白的／訊息是不是空的／有沒有被存過檔」為依據；並**明列兩類不算違反的條件**（以 id 相等與否為依據的判斷、目標對話取不到的空值防護），出現幾次都可以。冷讀判定改成「路徑上的每一個條件都歸得進這兩類其中之一」。
  2. **`spec-tasks.md`／任務 8 驗收 14 的結構那一半同步改寫**，並在底下寫出改寫的原因（正確的實作不只一個條件、舊寫法會把它判成 fail）。**要驗的東西一字未變。**
  3. **照你的順帶提醒，把「它為什麼是防護不是特例」寫清楚了**：它問的是「目標對話取不到嗎」，不是「這段對話是不是空白的」。同時明寫**這不放寬 §6.4 那句警告** —— 走錯分支時看起來剛好無害仍然是巧合、不得倚賴，那正是驗收 14 行為那一半存在的理由。
  4. **`spec-appendix.md`／決策歷程新增一條**：把這一型記成第 3 型失效（條文在改版後不可達）的近親 —— 差別是第 3 型不可達的是**情境**，這一型是**條文描述的形狀與正確實作的形狀對不起來，於是正確的實作被判 fail**。並記下它是任務 14 那句「結構驗收比行為條文更容易腐爛」的第一個實例，**只隔了一輪**。
- **沒有做的事**：沒有改動 §6.5 這條規則本身、也沒有改動禁止項的內容；沒有因為「實質通過」就把這條結構驗收刪掉 —— 它守的那個東西（不得為空白對話開特例分支）沒有別的條文在守。

---

## [2026-09-09 17:20] QA 全驗結果的後續：一條 RD 已修，一條要你補記，另三點條文問題

- **來源**：QA 報告（同目錄 `test-report.md`）＋ RD 處理結果
- **內容**：

### 一、任務 4 驗收 6 —— RD 已修，附突變驗證

QA 判 fail 是對的：§3.5 第二條可判定要求的三段測資（記憶體 `same`/100/兩則、歷史同 id `same`/900/一則、`other`/500/一則）在測試檔裡從來沒有存在過。既有的 `allThreads keeps the in-memory copy when a thread arrives twice` 兩份 `updatedAt` 都是 `500L`，那是驗收 3。

**這不是重建測試檔時弄丟的** —— 反編譯原始 class 得到的 41 個測試裡本來就沒有它，v3.3 補了條文、測試沒跟上。

**已新增** `the in-memory copy wins even when the saved one looks newer`，走 §3.5 可判定字面指定的搜尋入口那條路，斷言分組順序 `["other", "same"]` 與 `same` 那組命中數 2。

**並做了突變驗證**：把 `Conversation.allThreads` 改成條文點名的「先排序再去重」→ **80 個測試裡只有這一個變紅**。也就是說它確實抓得到那種寫法，而且先前沒有任何東西在守它。實作已還原，48 + 32 全綠。

### 二、任務 10 驗收 6 —— 要你補記，不是叫 RD 再動文件

`plan/architecture.html` 現況**已含**驗收 7 要求的第二個分支：「命中本身有 60 個字（含）以上時裝不下引子，視窗就從命中的第一個字開始。」

但 `spec-tasks.md`／任務 10 的執行紀錄只列到 v3.4 的三處，「目前狀態」還寫著「驗收 7 未完成」。條文明寫「寫進本節由 PM 完成」，所以請你補記並更新目前狀態。

**RD 補充**：那句話裡的「（含）」兩個字是我加的 —— 原文是「60 個字以上」，而驗收 7 要求「命中長度剛好 60 時前面留幾個引子」必須有**唯一**答案。「以上」在中文裡通常含界，但那是慣例不是保證，加兩個字就沒有第二種讀法。

### 三、QA 打回的三點條文問題（原樣轉交）

1. **任務 8 驗收 14 結構那一半的收尾句仍比開頭嚴。** 開頭列的是禁止項，收尾句卻寫「路徑上的**每一個**條件都歸得進上面兩類」。若把同一個回呼裡的清空查詢也算進路徑，它有一個以查詢字串是否為空為依據的提早返回 —— **沒踩任何禁止項，卻歸不進那兩類**。這正是 v3.6 想修掉的那一型（用「允許幾個」表達「禁止哪幾種」），只是換了位置。

2. **「一字未改」類條文在沒有版本控制的專案裡不可判定。** 任務 1 驗收 2／8、任務 9 驗收 1–4／7 都要求某個檔案「一字未改」，而這個專案沒有 git，QA 只能拿檔案 mtime 當旁證。這與 `stop-generation` 那輪被打回的「與修改前逐字相同」是同一型：**條文要求的證據不存在於產物裡**。

3. **spec 反覆指向同目錄的 `test-report.md`，而那個引用會週期性地斷掉。** `spec.md:28` 寫「任務 1–9 於 v2 已完成並通過驗收（見同目錄 `test-report.md`）」。

   **RD 補充成因**：流程要求每次重驗之前把舊報告移出 spec 目錄（避免新的 QA 讀到上一輪結論、破壞冷讀），所以**每當有 QA 在跑，這個引用就是斷的** —— 而那正好是有人會去追它的時候。這不是一次性的意外，是設計上的衝突。建議 spec 不要拿 `test-report.md` 當證據引用；「v2 已通過驗收」這個宣稱如果重要，它應該以別的形式留在附錄裡。

- **相關條目**：`spec-tasks.md`／任務 4 驗收 6、任務 8 驗收 14、任務 10（執行紀錄與目前狀態）、任務 1 驗收 2／8、任務 9 驗收 1–4／7；`spec.md`:28
- **狀態**：已處理（v3.7）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.7）
- **我先把四塊一起讀完再動手**：彼此**沒有衝突、沒有順序相依**，合起來也**沒有動到任何架構決策的根本假設** —— 四塊全部是紀錄與可判定性，**規則字面一條未動**。唯一需要留意的交互作用是第三塊的第 2、3 點與第一塊出自同一個成因（條文要求的證據不存在於產物裡），所以我把它們合成一型處理，而不是當成兩個獨立的措辭修正。
- **查證結論（我自己去核對過，不是照單全收）**：
  - **任務 4 驗收 6** → 我讀了 `app/src/test/java/com/example/demo/ConversationSearchTest.kt`。測試 `the in-memory copy wins even when the saved one looks newer` **確實存在**，測資是記憶體 `same`／100／兩則、歷史同 id `same`／900／一則、`other`／500／一則，走搜尋入口，斷言分組 id 清單**恰為** `["other", "same"]` 與 `same` 那組命中數 **2** —— 與 §3.5 第二條可判定條文的預期輸出逐項相符，**排序位置那一半也在**。突變驗證我沒有自己跑，但與程式碼推導一致（`allThreads` 現況是先去重再排序；改成先排序再去重會留下歷史那份，兩個斷言都會壞）。**成立。**
  - **任務 10 驗收 6／3／7** → 我讀了 `plan/architecture.html`（只讀，沒有動它）。片段視窗那句現況含第二個分支（驗收 7 成立）；「不變條件」那句的「按下停止」四個字已被拿掉、句首原則與句尾理由與 v3.4 記錄的版本逐字相同、全檔沒有第二處宣稱按下停止不算一次更新（驗收 3 成立）；模組地圖那三列路徑與職責描述與 v3.4 記錄的版本相同（驗收 4 複核成立）。**RD 多加的「（含）」兩個字我核對後保留**：驗收 7 要求「命中長度剛好 60 時留幾個引子」有唯一答案，「以上」是慣例不是保證，加這兩個字之後沒有第二種讀法，而且與 §4.2 的界線（≥ 60）逐字對得起來 —— **這是把條文要的唯一性做到，不是加新要求。**
  - **QA 的三點條文問題** → 三點都成立。第 1 點我對著 v3.6 改寫後的條文重讀，確認收尾句確實比開頭的禁止項嚴。第 2、3 點與另一輪被打回的「與修改前逐字相同」是同一型，**我把它記成第 6 型失效：條文要求的證據不存在於產物裡**（它與第 3 型不可達的是**情境**、第 5 型不可達的是**分工**都不同，這一型不可達的是**證據**）。
- **處理結果（逐塊對照）**：
  1. **任務 10** → `spec-tasks.md`／任務 10 新增「執行紀錄（v3.7 補記）」：補上**第四處**（片段視窗那句，含「（含）」兩個字的判斷）與**第五處**（「不變條件」那句拿掉「按下停止」，執行者是 `stop-generation` 任務 5 —— **執行者是誰與該不該列進本節是兩件事**，這是漏列的一半成因），另外寫出**哪些更動不屬於本任務**的界線（那份文件裡與停止生成有關的其餘內容），免得下一個冷讀的人把它們當成漏列。「目前狀態」整段改寫成 v3.7 現況（七條的成立條件都已具備、本輪不需要任何人再動那份文件），v3.6 的舊結論降級為明確標示的歷史。
  2. **QA 第 1 點（收尾句）** → `spec.md` §6.5 與 `spec-tasks.md`／任務 8 驗收 14 的**收尾句**同步改寫成「路徑上沒有任何條件踩到禁止項」，並把 (a)(b) 兩類降級成舉例（連同你提的「旁支狀態清理」）。**禁止項與要驗的東西一字未變**；附帶換到的是「那條路徑的邊界畫到哪裡」不再影響結論。
  3. **QA 第 2 點（一字未改）** → 七條（任務 1 驗收 2、8，任務 9 驗收 1–4、7）改成「未被本次改動觸及」，並在 `spec-tasks.md` **新增一節共用的判定基準**（時間／內容／行為三項，任一不成立即 fail 並指出是哪一項）。**要驗的東西沒有變**；換來的證據比「一字未改」弱，這一點誠實寫在該節與「已知取捨與風險」。**更強的做法（版本控制／基準雜湊）我沒有自己決定**，列進「明確不做」並附三個觸發條件 —— 那是專案層級的決定。
  4. **QA 第 3 點（報告引用）** → `spec.md` 卷首、`spec-tasks.md` 卷首與任務 14 的三處引用全部改掉；`spec-appendix.md` 新增「**歷次全驗的結論**」一節承接這個角色，本輪先記兩列（2026-09-09 全驗、v2 那一輪，後者誠實註明它現在沒有可查證的原始來源）。**往後每一輪的全驗結論都要在那裡留一列。**
  5. **升級一項給使用者：決策 E**（任務 10 的執行紀錄該放在哪裡）。理由：這一節已經腐爛兩次，而 v3.4 曾為**相反方向**寫過一個到今天仍然成立的理由，兩邊都對 —— 這是「在兩個都合理的做法裡挑一個」，不該我自己吞。四個選項與各自代價寫在 `spec-decisions.md`。**它不擋任何人開工，拍板前照現行寫法做即可。**
  6. **超出這一筆所列四塊的一項（照 §F.5 的義務做出來的，不是你交辦的）**：本輪把 §1 判定為「被改動的章節」之後逐條核對，發現 **§1.2 的第三條可判定（對話標題不參與比對）從來沒有被任何驗收指名** —— 任務 3 驗收 24 只覆蓋了人格與模型名稱那兩條。補成**任務 3 驗收 26**。**不需要任何新的實作或新測試**：測試 `searching finds message text rather than the derived title` 已經存在且兩半都在斷言，缺的只是一條指名它的條文。**若認為這超出本輪範圍，刪掉這一條即可，其餘處理不受影響。**
  7. **附錄** → 決策歷程新增三條（第 6 型失效；v3.6 修了開頭卻留下收尾句；任務 10 的紀錄第二次腐爛，這次成因是「改文件的人、記錄的人、知道它被改過的人是三個角色」）；修訂記錄新增 v3.7；「歷次收件匣升級判斷」補一列。`spec-architecture.md` §F.5 底下新增**第二條寫法紀律**（一條驗收只能要求產物裡拿得到的證據），**刻意不是硬性條文**，理由寫在旁邊。
- **沒有做的事**：**沒有改動任何一條規則的實質內容**（§6.5 的禁止項、七條驗收要驗的東西、任務 10 七條的條文字面，全部一字未動）；**沒有碰 `plan/architecture.html`**（只讀）；**沒有碰 `docs/specs/stop-generation/` 底下任何檔案**（那一輪正在被驗收，本輪只在自己的文件裡引用它的既有事實）；**沒有碰 `docs/qa-environment.md`**；**沒有把「證據要存在於產物裡」併進 §F.5 的三型體檢**（那會擴大一條硬性條文的要求，本輪範圍是補條文與改寫措辭）。`docs/product/features.md` **本輪無需更新**，理由與核對過程寫在 `changes-v3.7.md`。
- **另產出** `changes-v3.7.md`。

---

## [2026-09-09 20:10] 使用者裁決：決策 E ＋ 一條新的寫法規則

- **來源**：使用者裁決
- **內容**：

### 一、決策 E 的裁決 —— 使用者換了一個角度看它

**使用者原話**：

> 決策 E，現況算是兩個功能同時做的時候有細節內容衝突，這個在未來如果有衝突時讓我決策要用哪一邊的設計，然後再讓 PM 去同步兩邊的 spec

**他沒有從四個選項裡挑，而是指出你列的四個選項都在回答「紀錄該放哪裡」，而真正的問題是「兩輪同時進行時的衝突怎麼處理」。**

**定下來的規則**：

> **兩輪之間出現衝突（同一份文件、同一段程式碼、同一條規則被兩輪同時碰到）時 —— 不要自己決定用哪一邊的設計，拉給使用者判斷。使用者拍板之後，由 PM 去同步兩邊的 spec。**

**RD 的補充（我造成了這次的腐爛，所以講清楚成因）**：v3.7 那次腐爛不是你忘了看，是**你不知道有東西被動過** —— 拿掉「按下停止」那幾個字的是我，在 `stop-generation` 任務 5，而我沒有告訴你。上面那條規則正好對得上這個成因：它要求跨輪的衝突走使用者，而不是靠某個人記得傳話。

**我的解讀（請你確認，不確定就升級回去問）**：既然成因由新規則接住，紀錄的**位置**維持現狀（選項 (a)，留在 `spec-tasks.md`／任務 10）。但**這是我的推論，使用者沒有明說**。若你認為位置本身仍需要一個答案，把它留在 `spec-decisions.md` 繼續問，不要當成已拍板。

### 二、新的寫法規則：引用別的文件或程式碼時，不要留下會腐爛的東西

**使用者原話**：

> 這裡我想調整任務的寫法，如果有參照到其他文件、程式碼的部分，都不要留下會腐爛的東西

**已經寫進 `pm-agent` 的定義**（「引用別的文件或程式碼時，不要留下會腐爛的東西」那一節），所以之後每一輪都適用。摘要：

| 不要寫 | 改成 |
|---|---|
| 行號 | 那句話的內容或所在的小標題 |
| 數量（「既有的四支 JVM 測試」） | 「既有的 JVM 測試」或「除本輪新增之外的全部」 |
| 檔案清單、目錄內容 | 哪一層的檔案，或當場查得出來的位置 |
| 抄一段別份文件的內容 | 指向它，或只寫你真正依賴的那個性質 |

判準：**「這件事被別人在另一輪改掉時，我這條會不會自己變錯，而且沒有人會發現？」**

**這一輪請回頭掃一次本規格**，把已經存在的同型寫法改掉。已知至少有一處：`stop-generation` 那輪的 QA 回報，任務 4 驗收 7 寫死「既有的**四支** JVM 測試」，而本功能那一輪之後已經是五支 —— 那是別人改的，而那條條文自己變錯了。**本規格裡若有同型的（寫死數量、行號、檔案清單），一併處理。**

- **相關條目**：`spec-decisions.md`／決策 E；`spec-tasks.md` 全篇（掃寫死的引用）
- **狀態**：已處理（v3.8）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.8）
- **我先把本輪兩筆一起讀完再動手**：兩筆之間**沒有衝突、沒有順序相依**，合起來也**沒有動到任何架構決策的根本假設**（新增的 §F.7 規範的是 PM 怎麼做事，與 §F.4／§F.5 的分工不重疊，不推翻其中任何一條）。**有一個交互作用**：本筆的兩件事互相牽動 —— 新的寫法規則正好指向任務 10 的執行紀錄，而那就是決策 E 未決的那一半（見下方第 3 點）。
- **處理結果**：
  1. **裁決寫成規則** → `spec-architecture.md` 新增 **§F.7**：兩輪碰到同一份文件／同一段程式碼／同一條規則而**做法對不起來**時，PM 不得自己選一邊，該衝突必須以待決事項的形式出現在使用者面前；拍板之後由 PM 讓兩邊的 spec 說同一件事。條文旁一併寫了三件事：**什麼不算衝突**（兩邊說法對得起來就不是，前例是 §G.3 那次，但**分辨的動作仍要做**）、**這條的來歷**（你的自陳我採納了，成因是 PM 不知道有東西被動過）、以及**它沒有保證什麼** —— **它不是通知機制**，沒有人注意到的時候不會啟動。依 §F.5 的義務，條文旁明寫了「由哪一層守」：它沒有產品面的可觀察物，不另立驗收條文。
  2. **決策 E 我沒有當成已拍板，留在 `spec-decisions.md` 繼續問。** 你的推論（成因既然被接住，位置就維持現狀）**是推論不是裁決**，我沒有照它結案，理由兩個：(i) §F.7 不是通知機制，而這一次的失效正是「沒有人注意到」；(ii) **同一筆裁決裡的新寫法規則把這個問題往相反方向推** —— 任務 10 的執行紀錄正是「抄別份文件的整段內容」，照新規則該改成指位置，也就是選項 (d)，**而 (d) 是四個選項裡唯一會改到驗收 6 要驗的東西的那一個**。決策 E 的問題敘述與四個選項**一字未改**，只在最前面加了「v3.8 現況」與這個新輸入。
  3. **因此本輪刻意沒有動任務 10 的執行紀錄** —— 動它等於替使用者選了 (d)。這一點在 `spec-decisions.md`、`changes-v3.8.md` 與決策歷程三處都寫明了，免得下一個人把它當成漏掉。
  4. **新的寫法規則寫成 §F.5 底下的第三條寫法紀律**，並明寫**它與前兩條的來源不同**：前兩條是我的判斷、旁邊寫著「認為多餘就刪掉」，**這一條不適用那句話**；本節只記本規格依賴的那個性質，**不抄角色定義的內容**（抄過去就會出現第二個版本，正是這條要防的事）。
  5. **依它掃過五份檔案，改掉的地方**：`spec-architecture.md` §A.1 理由的「**三個**既有的 JVM 測試」（**我去看過專案的測試目錄，測試檔已經不只三個，多出來的正是別的輪次加的** —— 與你舉的「四支變五支」是同一個實例）；七條「未被本次改動觸及」相關的測試檔數量（判定基準第 3 項、任務 1 驗收 8、任務 4 驗收 5、任務 9 驗收 1），並在判定基準底下補上**範圍界線**（本功能開工時就存在的那幾個檔，別的輪次新增的不在範圍內）；任務 14 驗收 5 與「沒掃的範圍」裡寫死的既有條文數量；§8.6 與任務 13 驗收 3 對另一輪那個中斷標記的**字元數**，改成「以那一輪的現行定義為準」；`spec.md` 索引的標的個數；`spec-appendix.md` 指向一份**已經不在目錄裡**的舊 spec 檔名；任務 4 驗收 6 與任務 3 驗收 26 的現況記錄，補上「判斷依據是條文指名的預期輸出，**不是那個測試方法的名字**」。**每一處要驗的東西一字未變。**
  6. **界線是我畫的，寫在 `changes-v3.8.md`**：會被別人在另一輪改掉的數量全部拿掉；描述一件已結束的事、而且就地列得出來的數量（例如 v3.5 補掃掃了幾條）留著。**認為界線畫錯，改回來的成本是幾行字。**
  7. **附錄**：修訂記錄新增 v3.8；決策歷程新增「決策 E 被換了一個角度」；已知取捨新增一條（掃描的界線與殘留風險），並在跨輪相依那一條補上 §F.7 與它的關係（**有機制了，但不是零風險**）。
- **沒有做的事**：**沒有改動任何一條規則的實質內容、沒有改動任何一條驗收要驗的東西**；**沒有碰 `plan/architecture.html`**（只讀）、**`docs/specs/stop-generation/`**（那一輪已結案）、**`docs/qa-environment.md`**（歸 QA）；**沒有改 `docs/product/features.md`**（核對結論是本輪無需更新，過程寫在 `changes-v3.8.md`）；**沒有把新的寫法規則寫成硬性條文**（它是紀律，不增加每輪成本）。

---

## [2026-09-09 20:35] 使用者裁決：v3.7 那四項自主判斷全部接受

- **來源**：使用者裁決
- **內容**：

你在 `spec-decisions.md` 末段列出、並註明「不同意的話直接說」的四項，**使用者已逐項聽過詳細說明後全部接受，不需要退回**：

1. **§6.5 與任務 8 驗收 14 的收尾句改寫**（改成只問「有沒有踩到禁止項」）
2. **「一字未改」類條文改成三項判定基準**（時間／內容／行為），含你誠實寫出的「這比原本弱」與殘留風險
3. **§F.5 底下新增第二條寫法紀律**（一條驗收只能要求產物裡拿得到的證據），維持刻意不寫成硬性條文
4. **新增任務 3 驗收 26**（§1.2 的對話標題那一條可判定）—— 這一項超出當初交辦的範圍，使用者知情後仍接受

**第 4 項附帶一個沒有拍板的問題，留給日後**：RD 在說明時提出「要不要允許 PM 在交辦範圍外，照既有硬性條文的義務順手補零成本的洞」。使用者這次接受了結果，**但沒有對這個通則表態**。所以：**這次沒事，但不要當成通則已經成立** —— 下次遇到同樣情況仍然要在回報裡明講你多做了什麼、以及為什麼。

**這一筆不需要你做任何事**，寫下來是為了讓下一輪讀 `spec-decisions.md` 的人知道那四項已經定案，不必重新翻案。

- **相關條目**：`spec-decisions.md`／v3.7 那四項；`spec-appendix.md`／決策歷程
- **狀態**：已處理（v3.8）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.8）
- **這是使用者裁決，我照辦，沒有重新爭論已拍板的事。**
- **處理結果**：
  1. **四項的定案記進 `spec-appendix.md`／決策歷程**（「使用者接受 v3.7 那四項自主判斷」那一條），逐項列出，並寫明**是逐項聽過詳細說明之後接受的** —— 下一輪不必重新翻案。
  2. **`spec-decisions.md` 裡「v3.7 本輪的其餘判斷」那一節整節移出**（決策檔只放還沒拍板的事），在「已拍板的事項」補一列指向附錄。
  3. **第 4 項附帶的那個通則，我按你說的處理成「沒有成立」**：附錄裡明寫「**使用者接受了這次的結果，但沒有對這個通則表態，所以通則沒有成立**」，並寫下往後的做法 —— 同樣情況仍然要在回報裡明講多做了什麼、以及為什麼，讓使用者每一次都有機會說不。**它刻意沒有被寫成任何一條規則或紀律。**
  4. **一併記下另一輪的獨立確認**：`stop-generation` 的 PM 獨立得到了與 v3.7 相同的結論（「條文要求的證據不存在於產物裡」是獨立的失效型），**兩邊都是自己撞到的**；而**他刻意只記性質、不記編號**，理由是那個序號會隨本文清單的長度變動 —— 這一點記進了附錄與 §F.5 那條紀律旁邊，當成新寫法規則的正確用法。**本文這邊保留編號**（清單就在本文裡、由本文自己維護），但**不要求任何人用編號指它**。
  5. **與本輪另一筆的一處交會**：那四項裡的「一字未改」三項判定基準，本輪的掃描改了它第 3 項的措辭（拿掉測試檔數量、補上範圍界線）。**你接受的是那個判定的內容，不是它的字面**，要驗的東西未變 —— 這一點寫在附錄的收件匣升級判斷那一列。
- **沒有做的事**：**四項一項都沒有退回，也沒有替其中任何一項加上新的條件**；沒有把那個未拍板的通則當成慣例使用（本輪唯一的自主判斷是掃描的界線，而它是交辦事項本身的一部分，已在 `spec-decisions.md` 與 `changes-v3.8.md` 明講）。

---

## [2026-09-09 22:40] 全驗 pass，QA 回給你四點條文問題

- **來源**：QA 報告（同目錄 `test-report.md`）
- **內容**：

**這一輪結論是 pass** —— 任務 1–14 每一條、§A–§G 每一條、§1–§8 的邊界情況全部逐條判過，沒有一條 fail。三條依 spec 自己的但書記為「本環境無法觀察／無法執行」，不判 fail。

以下四點**不影響 pass**，是條文本身的問題：

**一、任務 10 驗收 6 是第 6 型的漏網之魚。** 它要求「該文件裡與本任務有關的每一處新增／改寫，本節都列到了」—— 在沒有版本控制的專案裡**拿不到證據**。這正是 v3.7 為「一字未改」類條文修掉的那一型（條文要求的證據不存在於產物裡），**同型的這一條被漏掉了**。

**二、「未被本次改動觸及」判定基準第 1 項的基準日已經被洗掉。** 那一節寫的基準是 2026-09-02，但 `ChatViewModel.kt` 在 09-08 被 `stop-generation` 那一輪動過、`Conversation.kt` 在 09-09 17:09 被 RD 動過（突變驗證後還原，內容相同但 mtime 變新）。條文的補救條款要求「說明它為什麼被動過、動了什麼」—— **那個說明同樣拿不到證據**。這是第 6 型在同一節裡的第二次現身。

**RD 補充**：`Conversation.kt` 那次 mtime 變動是我造成的（突變驗證改了再還原，內容逐字相同但檔案時間變新）。**這說明「mtime 早於基準日」這個判準本身會被正常的驗證活動打壞**，不只是被別輪改動打壞。

**三、任務 8 驗收 14 宣稱「路徑邊界不影響結論」，但那句話有反例。** 把邊界延伸到存取層就會撞到 `ConversationStore.save()` 裡的 `if (conversation.isBlank)` —— **字面上踩到禁止項**（以「這段對話是不是空白的」為分支依據），**實際上那是 §E.7 明文要求保留的既有語意**。建議在條文裡明文排除存取層那一處，否則下一個把邊界畫大一點的 QA 會判 fail。

**四、任務 12 驗收 2 指名的「貼上」在本環境做不到。** adb 無法設剪貼簿（前幾輪也撞過），QA 改走 UI 並把字串換成 ASCII，性質不變。**條文若要保留「貼上」這個字眼，需要註明可接受的替代做法**，否則每一輪都要重新臨場判斷一次。

- **相關條目**：`spec-tasks.md`／任務 10 驗收 6、「未被本次改動觸及」那一節、任務 8 驗收 14、任務 12 驗收 2
- **狀態**：已處理（v3.9）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.9）
- **我把本輪兩筆一起讀完再動手**：兩筆**沒有衝突、沒有順序相依**，合起來也**沒有動到任何架構決策的根本假設**。**有一個明顯的交集，而且兩邊指向同一個處置**：你的第一點說「任務 10 驗收 6 是第 6 型的漏網之魚」，使用者那一筆的裁決正好就是拿掉它 —— **兩邊各自撞到同一條條文，這讓那個處置比單邊回報可信。**
- **逐點處理（本輪只做了第一點，其餘三點一個字都沒動，理由見下）**：
  1. **任務 10 驗收 6 → 已移除**（依使用者裁決，編號保留不遞補）。你的判定理由與使用者的理由是同一個：更動的**結果**已由驗收 1、2、3、7 直接驗過，它唯一多出來的「有沒有漏掉某一處」在沒有版本控制的專案裡拿不到證據。**你這次是用自己畫的替代判準判它 pass 的**，那件事本身就是這條該拿掉的證據。**你建議的另一個方向（換一組拿得到的證據去問同一件事）我沒有採用**：使用者的裁決是拿掉，而剩下六條各自指名了一段具體文字，那就是現在的覆蓋範圍；缺口與觸發條件誠實寫在 `spec-appendix.md`／已知取捨與風險。
  2. **判定基準第 1 項（時間）→ 升級成 `spec-decisions.md`／決策 F，本輪沒有改條文。** **RD 補充的那半句是這次升級的關鍵**：`Conversation.kt` 的 mtime 是突變驗證改了再還原造成的、內容逐字相同 —— 也就是**這個判準連正常的驗證活動都會打壞**，不只是被別輪的改動打壞。我另外看出一件你沒明說的事：**修第 6 型的那一輪，自己又寫了一個同型的補救條款**（「必須說明動了什麼」在這個專案裡同樣拿不到證據）。四個可能的方向都會改到那七條**要驗的東西**，而使用者這次只裁決了任務 10 那兩條，所以我不自己選。**`spec-tasks.md` 那一節加了一個警示框，條文一字未動**，並寫明拍板前照現行字面判、把拿到的證據寫進驗收記錄（同 v3.5 在 §8.2 底下加警示框的處理）。
  3. **任務 8 驗收 14 的「路徑邊界不影響結論」有反例 → 升級成 `spec-decisions.md`／決策 G，本輪沒有改條文。** 你的反例成立而且指得很準：v3.7 那句「邊界畫到哪裡不再影響結論」是我寫的，它多說了一句規則不需要的話 —— **存取層那個 `isBlank` 分支字面上踩到禁止項，實際上是 §E.7 明文要求保留的既有語意**。修法只有一個方向（明文排除存取層那一處），**但它會動到既有驗收的邊緣，而那不在使用者這次裁決的範圍裡**，所以我列成待決事項等一句話，而不是順手做掉。
  4. **任務 12 驗收 2 的「貼上」在本環境做不到 → 同樣列進決策 G。** 你的處置（改走 UI、字串換成 ASCII、性質不變）與 §F.3 對「動作做不出來」的既有處理一致；要不要把可接受的替代做法寫進條文，同樣等使用者一句話。**在那之前，照你這次的做法做並在記錄裡寫明替代方式，不判 fail。**
- **一併記下的一件事**：這一輪的全驗結論（pass）已經記進 `spec-appendix.md`／「歷次全驗的結論」，連同這四點的去向。**往後每一輪都會在那裡留一列。**
- **沒有做的事**：**除了任務 10 那兩條（使用者明確裁決）之外，本輪沒有改動任何一條規則、也沒有改動任何一條驗收「要驗的東西」**；沒有碰 `plan/architecture.html`、`docs/specs/stop-generation/`、`docs/qa-environment.md`、`docs/qa-coverage.md`。

---

## [2026-09-09 23:30] 使用者裁決：任務 10 的驗收 4、6 要改，並附一條新的通則

- **來源**：使用者裁決
- **內容**：

### 一、任務 10 驗收 6：拿掉

**理由（RD 與使用者一起對照出來的）**：任務 10 有七條驗收，其中**五條（1、2、3、5、7）是「讀文件確認它現在長什麼樣」，直接判得出來**；只有驗收 4 與 6 要求「把過程寫進本節」。

驗收 6 問「改了哪幾段」，但**改的結果已經被驗收 1、2、3、7 直接驗過了**。它唯一多出來的是「有沒有漏掉某一處」—— 而**那正是它判不出來的部分**（沒有版本控制，無從得知還有沒有第三處）。

**它腐爛兩次、害全驗判 fail 一次，兩次成因相同**：更動由另一輪執行，沒有人回來補紀錄。**同一個任務裡的五條就是反證：不需要紀錄也驗得出來。**

### 二、任務 10 驗收 4：保留「核對」，拿掉「必須寫進本節」

模組地圖那三列照樣要被核對 —— 但 **QA 自己讀文件核對即可，不需要 spec 裡有一份紀錄**。

**「上次是什麼時候確認過的」這個資訊確實有一點價值**（多久沒人看過了），但它不該塞進 spec：**spec 要靠 PM 手動同步，而那正是它腐爛的原因。** 那種資訊屬於一份只記「哪一項、什麼時候」、由實際做檢查的角色自己維護的獨立帳（QA 現在有 `docs/qa-coverage.md` 就是這個形狀）。

**請你判斷**：改寫驗收 4 時，是只留「那三列的路徑與職責必須與現況相符」（QA 直接讀），還是要另外處理「多久沒核對過」那個資訊。**後者若你認為不值得，明寫理由拒絕它也可以。**

### 三、既有的執行紀錄怎麼辦

任務 10 現在有兩大段執行紀錄（v3.4 與 v3.7 補記）。條文拿掉之後那些內容就沒有驗收在要求它了。**保留或移進附錄由你判斷** —— 使用者沒有指示。**但不要直接刪掉**：那裡面記著「哪一處是由另一輪執行的」這類跨輪事實，那是決策 E 的來歷。

### 四、新的通則（已寫進 `pm-agent` 的定義，這裡是通知）

使用者要求：**PM 未來不要再寫這種沒有獨立價值的驗收。** 定義裡新增的判準是：

> 寫下一條驗收之前問兩個問題：
> **一、有哪一種錯誤，只有這一條會發現？**
> **二、那種錯誤，冷讀的人判得出來嗎？**
>
> 第一題答不出來 → 是別條的複述，刪掉。
> 第一題答得出、第二題答不出 → 實際效果是零，**而它看起來像有在守**，比沒有這條更危險。

並特別點名最常見的一型：**驗過程、不驗結果**。判準是「一條驗收如果在問『誰做了什麼、什麼時候做的』而不是『現在是什麼樣』，先確認那個過程資訊真的有獨立價值 —— 通常沒有，因為過程的結果已經在產物裡了」。

**這一輪請順便照這個判準掃一次本規格**，看還有沒有同型的條文。**掃出來的先列進變更說明報告，不要自己刪** —— 使用者只裁決了任務 10 那兩條。

- **相關條目**：`spec-tasks.md`／任務 10 驗收 4、6 與兩段執行紀錄；決策 E（它問的正是這兩條的紀錄該放哪裡，拿掉之後可能整個消失）
- **狀態**：已處理（v3.9）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.9）
- **這是使用者裁決，我照辦，沒有重新爭論已拍板的事。** 本輪兩筆先一起讀完再動手，判斷寫在上一筆的處理結果裡（兩筆沒有衝突、沒有順序相依，且**兩邊指向同一個處置**）。
- **處理結果（四點逐項對照）**：
  1. **驗收 6 拿掉了**，編號**保留不遞補**（別處對「驗收 7」的引用因此不會失效）。條文位置留下一段說明：原本要求什麼、為什麼移除、**以及它留下的缺口**（文件裡有一處與本任務有關、六條都沒指名到的更動，現在沒有條文會發現 —— 知情接受，觸發條件寫在附錄／已知取捨與風險）。
  2. **驗收 4 改寫**：核對保留，「核對日期與逐列結論必須出現在本節」拿掉，改成**那三列的路徑與職責必須與現況相符，QA 冷讀時直接讀文件與程式碼逐列判**（三列都相符才通過，任一列對不上即 fail 並指出是哪一列、哪一半）。**這一條要驗的東西因此變了**，這一點在條文旁明寫，沒有假裝是措辭調整。
  3. **「多久沒核對過」那個資訊：我拒絕它，理由明寫**（你說了不值得就可以拒絕）。兩個理由：那種帳要靠 PM 手動同步，而那正是這一節腐爛兩次的成因；而且**每一輪都會全驗**，所以「多久沒被看過」的上限本來就是一輪。真要記，它屬於做檢查的角色自己維護的那一份帳。
  4. **既有的兩段執行紀錄：一個字都沒刪**，原文移到任務 10 的**驗收標準之後**、標成「歷史紀錄，不是現行狀態，也不再被任何一條驗收要求」。**留在 `spec-tasks.md` 而不是移進附錄，是我的判斷，理由是驗收 3 的逐字基準就在那段紀錄裡**（它要求「句首原則與句尾理由與 v3.4 記錄的版本逐字相同」）—— 移進附錄會讓那個基準跨檔。你點名要保住的東西（「哪一處由另一輪執行」這類跨輪事實）同樣留著。
- **連帶處理的三件事**：
  - **決策 E 結案，而且不是被拍板 —— 是問題本身被這次裁決取消了。** 驗收 6 一拿掉、驗收 4 又不再要求紀錄，就**沒有任何一條驗收依賴那段紀錄的位置**，它降級成編輯判斷。四個選項與經過**完整移進 `spec-appendix.md`／決策歷程**（含一句值得記的觀察：使用者兩次都沒有從那四個選項裡挑，那是「問題問錯了」的訊號）。`spec-decisions.md` 換成新的兩項（決策 F、G，都源自同一輪 QA 回報）。
  - **照新通則掃過本規格，掃出來的同型條文一條都沒有動，全部列進 `changes-v3.9.md`**（連同「為什麼它可能不該被刪」的正反理由）。
  - **`docs/product/features.md` 本輪無需更新**：本輪沒有改動任何一條規則、沒有任何行為改變，而那份文件寫的是「使用者現在做得到什麼」、明文不寫某一輪改了什麼。核對過程寫在 `changes-v3.9.md`。
- **沒有做的事**：**沒有動 §A–§G 任何一條**（使用者調整了分工，但明說這一輪只做盤點、不重新分類 —— 盤點結果在 `changes-v3.9.md`）；**沒有把這條新通則寫進 §F**（§F 屬於 §A–§G，本輪不動；它的權威在角色定義裡，不因為 spec 沒抄而失效，理由記在附錄）；沒有碰 `plan/architecture.html`、`docs/specs/stop-generation/`、`docs/qa-environment.md`、`docs/qa-coverage.md`。

---

## [2026-09-10 00:20] 使用者裁決：決策 F、決策 G，以及 §6.5 結構條文降級

- **來源**：使用者裁決
- **內容**：

### 一、決策 F：選 (a)，維持現狀

**不動任何條文。** 使用者的理由是他要先想清楚版本控制要不要做、怎麼做 —— **這一題與那一題是同一個根**（引入版控之後，三項判定可以退回成真正判得出來的那一項）。

所以：那七條「一字未改」類驗收的三項判定**一字不改**，警示框留著。**決策 F 移進附錄／決策歷程並標明「因等待版控決定而暫緩，不是因為問題消失」** —— 這一點請寫清楚，免得下一輪有人以為它已經解決了。

### 二、§6.5 的結構條文降級 —— 這一項是使用者自己看出來的

**使用者的質疑**：「這條路上不准有『如果這段對話是空的，就走另一條路』這種特例」—— **這看起來像寫作偏好，並沒有實際的體驗問題。**

**他是對的，而且他用的正是你剛加進定義的那條通則**（每一條驗收都要說得出「它獨自抓得到什麼」）。逐項對照：

- **結構那一半抓得到什麼？** 今天什麼都抓不到。若有人寫了那個特例分支而四種情境行為全部正確，使用者感受不到差別 —— 而「行為全部正確」正是**驗收 14 行為那一半**已經在守的。
- **規格給的理由是「防止未來壞掉」**，但那是假設不是後果。對照 §C.4（空白定義只能有一處）：兩份定義**必然**分歧，分歧那天使用者會看到搜尋結果對不起來 —— **後果是確定的**。§6.5 則要有人**主動寫錯**才會壞，而「有人寫錯就會壞」對任何一段程式都成立。
- **三輪實績**：抓到違反 **0** 次；害正確的實作差點被判 fail **3** 次（v3.5「只能有一個分支」、v3.6 收尾句、v3.7 邊界）；為了修它花了 **3 輪**。三次都是同一個形狀 —— **想表達「不准做某件壞事」，卻寫成「只能長成某個樣子」**。

**裁決**：

- **任務 8 驗收 14 的結構那一半拿掉。**
- **行為那一半保留**（§6.2、§6.3、§6.4 三種情境都必須回到聊天畫面並顯示對應的對話，且三者都不得在磁碟上新增或改寫任何對話檔）—— 那是真的可觀察、真的該驗。
- **§6.5 本身留在 `spec.md` 裡當建議**（給下一個動那段程式的人看的提醒），**但不再有驗收條文，QA 不判它**。請把它的語氣從硬性規則改成建議，並保留它的理由與那三次修條文的來歷。

### 三、決策 G

**第一點（任務 8 驗收 14 的「路徑邊界不影響結論」有反例）：因為上面第二項而整個消失。** 結構那一半拿掉之後，就沒有東西需要為儲存層那個 `isBlank` 開例外。**移進附錄時請寫明它是被取消而不是被解決的**，並保留 QA 找到的那個反例（那是「條文寫得比需求滿」的好例子）。

**第二點（任務 12 驗收 2 指名的「貼上」在本環境做不到）：照建議修，但加兩個要求。**

使用者原話：**「可用替代方案，但要紀錄，且當他回來時你要跟我講」**。

所以條文要寫成三件事：

1. **允許替代做法** —— 走畫面輸入、字串換成 ASCII，性質不變（同 §F.3 對「動作做不出來」的既有處理）。
2. **每次用了替代做法都要記錄** —— QA 在報告裡寫明「這一條走的是替代做法，原因是本環境設不了剪貼簿」，不得默默替換。
3. **這個替代是暫時的，而且要有人盯著它回來** —— **若哪一輪 QA 發現這台裝置／這套工具鏈已經能設剪貼簿了，必須在報告裡明確回報**，因為那時就該改回驗真正的「貼上」。

**第 3 點請寫進條文本身，不要只寫在附錄** —— 使用者要求「回來時要跟他講」，而那件事若只依賴某個人記得，它就不會發生。寫進條文，QA 每輪讀到它就會順手檢查一次。

- **相關條目**：`spec.md` §6.5；`spec-tasks.md`／任務 8 驗收 14、任務 12 驗收 2；`spec-decisions.md`／決策 F、G（兩項都要移出）
- **狀態**：已處理（v3.10）（原文寫的是「待處理」，處理結果見下方）

---

- **狀態**：已處理（v3.10）
- **這是使用者裁決，我照辦，沒有重新爭論已拍板的事。** 本輪只有這一筆，但三項我先一起讀完再動手：**三項之間沒有互相抵觸**，合起來也**沒有動到任何架構決策的根本假設**（§A–§G 一條未動，§E.7、§F.3、§C.4 只是被引用）。**但有一個順序相依，而且它會改變結論**：第三項的第一半（決策 G 第一點「整個消失」）**成立的前提正是第二項**（結構條文被拿掉）。先做第二項，第三項的第一半才對；反過來單獨處理第三項，就會去修一條即將不存在的條文。這一點在收件匣原文裡是分開的兩項，所以特別寫出來。
- **處理結果（三項逐項對照）**：
  1. **決策 F 選 (a)，維持現狀** → 那七條「一字未改」類驗收的三項判定**一字不改**；`spec-tasks.md` 那一節的警示框留著，但**改寫成「現行結論 → 問題仍在 → QA 怎麼辦」的順序**（現行結論放第一句，避免掃過去的人只看到過期的那一句）。**「因等待版控決定而暫緩，不是因為問題消失」寫在三個地方**：警示框第一段、`spec-appendix.md`／決策歷程（決策 F 拍板那一條，含四個選項原樣保留）、以及 `spec-decisions.md` 的「已拍板的事項」。**指向 `spec-decisions.md`／決策 F 的連結改指附錄**（決策已移出，不改就是一個會斷的引用）。
  2. **§6.5 降級** → **任務 8 驗收 14 的結構那一半整條移除**（`spec-tasks.md`），**行為那一半原文一字未改**（連同它括號裡「分別對應驗收 3、4、5 與本條驗收 13」那半句）。**`spec.md` §6.5 改寫成建議語氣**：兩條可判定都拿掉、明寫「不再有任何驗收條文，QA 不判它」，並**保留它的理由與那三次修條文的來歷**（v3.5 數分支、v3.6 收尾句、v3.7 邊界 —— 三次同一個形狀），以及與 §C.4 的對照（後果確定 vs. 要有人主動寫錯）。**同時明寫兩件沒有被放寬的事**：§6.4 那句「看起來剛好無害是巧合、不得倚賴」仍然是規則、仍由任務 8 驗收 5 守；§E.7 的既有語意本來就不是靠這條結構條文守的。另外更正了 `spec-tasks.md`／明確不做那一條的現行結論（它原本寫著「§6.5 → 任務 8 驗收 14」，那句話已經不對）。
  3. **決策 G** → 第一點**移進附錄並寫明它是被取消而不是被解決**，**QA 找到的那個反例原樣保留**（存取層那個「這段對話是不是空白的」判斷是 §E.7 明文要求保留的既有語意 —— **我自己讀過那段程式碼確認反例成立**），並記下它是「條文寫得比需求滿」的例子：規則不需要 v3.7 多寫的那句「路徑邊界不再影響結論」，而那句話**一輪之內就被推翻了**。第二點**照修**，三件事全部寫進**條文本身**（`spec-tasks.md`／任務 12 驗收 2）：允許替代做法、每次用了都要在報告裡寫明原因且不得默默替換、**哪一輪發現環境已經能設剪貼簿必須在報告裡明確回報**。**第三件事刻意寫在條文而不是只寫在附錄**，理由照你說的：附錄要靠有人記得去翻，條文 QA 每輪都會讀到。
- **連帶處理**：`spec-decisions.md` 回到「**本次沒有需要決策的事項**」（決策 F、G 都移出，並在「已拍板的事項」各留一列指向附錄）；`spec-appendix.md` 新增 v3.10 修訂記錄、三條決策歷程（§6.5 降級的案例、決策 F 拍板、決策 G 結案）、收件匣升級判斷一列、已知取捨與風險兩條；另產出 `changes-v3.10.md`（含 §F.5 要求的「本輪被改動章節清單與規則 → 條文對照」：`spec.md` §2、§6 與 `spec-architecture.md` §C、§E、§F，**沒有缺口**）。
- **一件我判斷成「觀察」而不是「待決事項」的事，寫在這裡讓你看得到**：任務 8 驗收 14 行為那一半，上一輪被留下來的理由是「它是結構那一半的**正向對照**」；**結構拿掉之後那個理由不成立了**，它與驗收 3、4、5、13 的重疊因此更明顯。**你的裁決是保留它，我照辦、一字未改**，只把這個觀察記進附錄／已知取捨與風險並註明**那是我的觀察不是你的**。沒有升級成待決事項，是因為你這次已經對這一條表過態。
- **沒有做的事**：**除了上面三項之外，沒有改動任何一條規則、也沒有改動任何其他驗收「要驗的東西」**；**沒有重新分類 §A–§G**（依交辦，你還沒決定那件事）；沒有碰 `plan/architecture.html`（只讀）、`docs/specs/stop-generation/`、`docs/qa-environment.md`、`docs/qa-coverage.md`；**`docs/product/features.md` 本輪無需更新**（我逐項對過「搜尋-」那一區，沒有任何一項指向 §6.5 —— 而那正是它被降級的原因；核對過程寫在 `changes-v3.10.md`）。

---

## [2026-09-10 07:40] 目錄結構改了：spec 五份現在在 `v3.10/` 裡

- **來源**：使用者裁決
- **相關條目**：全部（這是路徑變更，不是規格變更）
- **狀態**：已處理（v3.11）—— 四筆的處理結果合寫在本檔最後一段

**內容**：

這個專案開始做**資料夾版控**：每一輪的 spec 定版留在自己的目錄裡。

```
docs/specs/conversation-search/
├── pm-inbox.md            ← 本檔，跨輪，留根目錄
├── changes-v3.5.md …      ← 跨輪，留根目錄
├── archive/               ← 沒有對應快照的歷史報告
├── v3.10/                 ← 現行：五份 spec + test-report.md
└── v3.11/                 ← 你這一輪要建的
```

**你要做的第一件事**：把 `v3.10/` 整份複製成 `v3.11/`，**在 `v3.11/` 裡改**。`v3.10/` 一個字都不准動。

規則已寫進你的角色定義與 `spec-flow` 流程文件，這裡只是告訴你現況。`stop-generation` 同時做了一樣的搬遷（`v3/`）。

**這件事解掉的是你自己的宣稱**：你每一輪都在寫「本輪一字未改」「要驗的東西未變」，而在這之前沒有人能查證。現在 `diff v3.10/ v3.11/` 查得到。

---

## [2026-09-10 07:45] §F 整節收掉，§A–§G 依准入判準重新分類

- **來源**：使用者裁決
- **相關條目**：`spec-architecture.md` §A–§G 全部；`spec.md` 行為規格；`spec-tasks.md` 驗收條文
- **狀態**：已處理（v3.11）—— 四筆的處理結果合寫在本檔最後一段

**內容**：

**這兩件事必須同一輪做完。** 分兩輪做會自己製造一次跨輪衝突：第二件會改動 §F.5 要求的「本輪被改動章節清單」。

### 背景：這份文件寫在規則之前

`spec-architecture.md` 現有 43 條規則、269 行。它成形的時候，下面這些規則還不存在：准入判準（說得出「使用者會怎樣」才寫）、架構是建議不是裁決、QA 不驗架構、引用不留腐爛物、每條驗收要說得出獨自抓得到什麼。

**當時沒有做錯。** 要處理的是規則生效前留下的存量。

### §F：整節收掉

| 條 | 處置 |
|---|---|
| F.1 比對語意的權威是單元測試 | **併進驗收條文本身**（本規格特有的事實，不是通則） |
| F.2 實機負責算繪不是算對 | 同上 |
| F.3 實機條文不得預設輸入法能力 | **已搬進 `pm-agent.md`（PM 半）與 `qa-agent.md`（QA 半）**，spec 端刪除 |
| F.4 條文 → 測試 | **已搬進 `pm-agent.md`**，spec 端刪除 |
| F.5 規則 → 條文反向核對 | **已搬進 `pm-agent.md`**（含被改動章節的四項界線、三個防退化），spec 端刪除 |
| F.6 實機判讀前先證明建置 | **`qa-agent.md` 早就有等價的一條**，spec 端刪除 |
| F.7 跨輪衝突不得自己選一邊 | **已搬進 `pm-agent.md`**，spec 端刪除 |

**刪除的那五條不要留摘要。** 留一句指路即可，形如：「這些通則的權威在角色定義裡；本規格依賴的性質是——一整類條文的權威放在實機上。」抄過來就會出現第二個版本，那正是要防的事。

### §A–§G：重新分類

判斷用你現行定義裡的三道關卡（准入判準 → 架構是建議 → 兩個例外）。

| 節 | 現在 | 去向 | 留在本檔 |
|---|---|---|---|
| §A | 6 條 | A.1 不會寫（你的定義已把它列成反例）／A.5 → 驗收條文／A.6 → 行為規格 | 3 條（A.2、A.3、A.4） |
| §B | 6 條 | B.1、B.3 的合併順序 → 行為規格／B.5 不會寫（使用者已判掉） | 3 條 |
| §C | 8 條 | C.1、C.2、C.3、C.6、C.7、C.8 → 行為規格 | 2 條（C.4、C.5） |
| §D | 5 條 | D.1、D.4、D.5 → 行為規格／D.2 不會寫（純技術選型） | 1 條（D.3） |
| §E | 7 條 | E.6、E.7 → 行為規格 | 5 條 |
| §F | 7 條 / 73 行 | 見上表 | 0 條 |
| §G | 4 條 | G.2 → 行為規格／G.3 → 明確不做／G.4 → 驗收條文 | 1 條（G.1） |
| 合計 | 43 條 / 269 行 | 20 條變行為規格或驗收條文、5 條進 agent 定義、3 條不寫 | **15 條，約 90 行** |

**這張表是主對話的判斷，不是逐條核對過的裁決。** 你自己讀原文重判，判出不同的結論就寫下理由。

**搬成行為規格的那 20 條，要依「規則 → 條文」的義務逐條核對** —— 這批規則過去靠「它在架構那一節」被讀到，搬家之後不能靠位置。

**留下來的 15 條要明講它們是建議**，QA 不驗。

### 一個可以拿來驗這件事有沒有做成的東西

`qa-agent.md` 執行步驟 1 現在有一段補丁：「『架構決策』那一節照樣要讀完 —— 因為那一節**常常混著行為要求**」。

那段補丁存在的唯一理由就是這份文件混了。**分類做完之後它應該可以刪掉。** 請在變更說明裡明講可不可以刪。

---

## [2026-09-10 07:50] 全面掃過 156 條驗收，砍掉追溯不到使用者後果的

- **來源**：使用者裁決
- **相關條目**：`spec-tasks.md` 全部 14 個任務、約 156 條驗收；「『未被本次改動觸及』怎麼判」整節；決策 F
- **狀態**：已處理（v3.11）—— 四筆的處理結果合寫在本檔最後一段

**內容**：

### 判準（一次性補掃，不是每輪義務）

> **每一條驗收條文都要指得出它守的那條規則，而那條規則要說得出使用者會遇到什麼。**
> 指不出來 → **改寫或移除**。

**驗證方式不設限。** 由單元測試判定不構成問題 —— 問的是它守的規則有沒有使用者後果，不是誰去驗它。任務 3 那 26 條全部是單元測試（§F.1：`adb shell input text` 送不了非 ASCII，而那些字元在畫面上看不出來），**全數保留**。

**這是 v3.5「101 條規則一次性補掃」的對偶**：那次問「規則有沒有條文守」，這次問「條文有沒有後果撐」。**同樣是一次性的**，之後由准入判準每輪擋住新的。

**對照表不進 spec**（同「對照表不進 spec」的既有處理），只作為本輪交付出現在變更說明裡。

### 已知一定會掃到的：「未被改動」那七條

**背景**：這七條要守的是「純結構搬移，不得夾帶對既有行為的偷改」。使用者的判斷是 —— **程式碼被動了而行為沒變，使用者什麼都感受不到；行為變了，那是別的驗收在守**（任務 1 驗收 6、任務 9 驗收 6）。所以「未被改動」那一半**答不出使用者會怎樣**。

**A. 五條關於產品程式碼**（任務 1 驗收 2；任務 9 驗收 2、3、4、7）

**拿掉「未被改動」那半句，保留後半句。** 這與使用者在 v3.10 對任務 8 驗收 14 的處理是同一個形狀。

| 條 | 拿掉 | 保留 |
|---|---|---|
| 1-2 | 下層檔案讀寫程式碼未被改動 | （無後半句 → 整條移除；它守的東西由驗收 6 承接） |
| 9-2 | 文字修復未被改動 | 該檔內不得出現搜尋的整形／折疊，也不得被它們引用 |
| 9-3 | 上下文過濾未被改動 | 指得出它在哪，且不是搜尋也在用的那一份 |
| 9-4 | 檔案讀寫層未被改動 | 空白對話刪檔與壞檔容錯仍成立（實機） |
| 9-7 | 標題與預覽未被改動 | 同一則含 U+3000 的訊息，抽屜預覽逐碼位仍是 `0x3000` |

**9-4 與 9-7 的後半句，2026-09-10 那一輪 QA 已經實際驗過且成立。**

**B. 兩條關於測試檔**（任務 1 驗收 8；任務 9 驗收 1）

spec 明寫它擋的是「**為了讓測試過而去改測試**」。那是**驗證流程規則**，依准入判準例外二屬於角色定義。**從 spec 移除** —— 對應的規則已經寫進 `spec-flow` 流程文件的 RD 硬規則，你不必再處理那一端。

**C. 連帶清掉**

- 整節「『未被本次改動觸及』怎麼判」（那三項判定基準）失去用途。
- 決策 F 的警示框失去用途。
- **決策 F 因此結案，而且是「被取消」不是「被解決」** —— 跟決策 E 同一個形狀。經過要寫進附錄：先裁決維持現狀、再因為「這七條根本不該存在」而整題消失。

### 一個要寫進附錄的事實

**這一題原本是「引入版本控制」的主要理由。** 使用者已決定這個專案會接 git，但**不是為了這七條** —— 那七條就算有 git 也仍然答不出使用者會遇到什麼。git 的理由換成純工程安全網（本 session 曾把一個含 41 支測試的檔案誤寫成 0 bytes，靠反編譯 build 產物救回）。

**不寫下來的話，下一個讀附錄的人會以為 git 是為了那七條。**

---

## [2026-09-10 07:55] 任務 3 驗收 16 的條文要指定測資形狀（RD 端已修，條文還沒）

- **來源**：QA 全驗（2026-09-10）+ RD 複驗
- **相關條目**：`spec-tasks.md`／任務 3 驗收 16；可比照任務 3 驗收 23 對 §4.2 的寫法
- **狀態**：已處理（v3.11）—— 四筆的處理結果合寫在本檔最後一段

**內容**：

**現象**：這條指名的測試 `cropping never splits a surrogate pair` **零鑑別力**。把 `ConversationSearch.kt` 的兩個保護整個改成 `= index`（直接回傳、不做任何事），**80 個單元測試仍然全綠**。

**原因**：舊測資形狀（101 個「測」＋「北京」＋50 個 emoji）在數學上永遠碰不到邊界 —— 視窗尾端固定落在「命中起點 + 40」，與 emoji 起點的距離恆為偶數；視窗起點又永遠落在 BMP 區間裡。

**RD 已經做的（測試端，不是條文端）**：

1. 把該測試的測資換成 `"😀"×15 + "測" + "北京" + "尾"×60` —— 命中在索引 31，引子想從 11 開始，那是第六對的低位半邊，**會碰到起點方向的保護**。
2. 新增 `cropping never splits a surrogate pair at the window end`，測資 `"測"×30 + "北京" + "尾"×7 + "😀"×20` —— 視窗想在 70 結束，那是第十六對的高位半邊，**會碰到尾端方向的保護**。
3. **逐一突變驗證，兩支各自獨立**：只拿掉 `avoidSplittingPairAt` → 只有第一支紅；只拿掉 `avoidSplittingPairBefore` → 只有第二支紅。原始碼已還原，SHA-256 與突變前相同，全套 81 支測試綠。

**要你做的**：**在條文裡指定會碰到邊界的測資形狀**，並決定驗收 16 是否要同時指名這兩支測試（現在只指名一支，而尾端方向沒有任何條文在守）。條文不說清楚，下一個人不會知道該用什麼形狀，而這正是它腐爛的方式。

**這一條是本輪三條隨機突變驗證抽到的其中一條。**

---

- **狀態**：已處理（v3.11）—— 以下是上面四筆（07:40／07:45／07:50／07:55）的**合併處理結果**，因為它們必須一起處理。
- **產出**：`v3.11/` 五份 spec（由 `v3.10/` 複製後修改，**`v3.10/` 一個字都沒有動，含它底下的 `test-report.md` —— 那份報告屬於 v3.10，沒有複製也沒有搬走**）＋ `changes-v3.11.md`。
- **三步檢查（四筆一起讀完才動手）**：
  - **衝突**：沒有。四筆全部是使用者裁決，方向一致（都在收縮「spec 該保管什麼」）。
  - **順序相依**：**有，而且你自己點名了它** —— 第 2 筆（重新分類）會改變「哪些規則還在、在哪裡」，而第 3 筆（掃驗收條文）問的正是「這條條文守的規則說不說得出後果」。**先分類再掃**；分兩輪做會讓第二輪面對一份「規則已經搬走、條文還指著舊位置」的文件。
  - **合起來有沒有動到架構決策的根本假設**：**有，而且這正是你要的** —— 架構決策從「必須成立的性質」變成「建議」，QA 不再驗它。**這件事沒有被當成幾個小修改混過去**，它在 `spec.md` 卷首、`spec-architecture.md` 開頭、`spec-decisions.md`、`changes-v3.11.md` 四處都明寫了。
- **逐筆處理結果**：
  1. **[07:40] 目錄結構** → 讀完照做。`v3.11/` 是 `v3.10/` 的副本再修改；`spec.md` 的檔案地圖補上版控說明。**核對 `docs/product/features.md` 時撞到一件這筆帶出來的事，見下面「一項升級」。**
  2. **[07:45] §F 收掉、§A–§G 重新分類** → 全部做完。**§F 整節收掉，只留一句指路、不留摘要**（本規格特有的那兩件事寫進任務 3、7、9、13 的說明）。**43 條逐條重判**，說得出使用者後果的搬進行為規格或驗收條文（`spec.md` 新增 **§2.11** 與 **§9**，**規則原文搬家、一字未改**），其餘留成建議並在每一條旁邊寫出「為什麼它只是建議」。**收件匣那張去向表我逐條重判過，六處判得不一樣**（§A.3、§C.4、§C.7、§C.1／§C.2、§E.1、§F.6），理由逐條寫在 `changes-v3.11.md` 與 `spec-decisions.md`。**其中最重要的一處是 §C.4**：表上說留在架構當建議，我把它搬進行為規格 §2.11 並補了可判定條文 —— 因為**你自己在 v3.10 就是拿它當「硬性 vs. 建議」的分界線用的**，把分界線的參考點放進一個 QA 不驗的檔案，那條線就沒有人在守了。**搬進行為規格的每一條都做了「規則 → 條文」核對，沒有一格留空**（對照表在 `changes-v3.11.md`，不進 spec）。
  3. **[07:50] 156 條驗收的一次性掃描** → 全部做完。**編號一律保留不遞補**；每一個被移除的編號底下都留著一行寫它原本守什麼、現在還有沒有東西在守。**你點名的七條全部照辦**（五條產品程式碼拿掉「未被改動」那半句／整條移除，兩條測試檔的整條移出 spec），**整節「『未被本次改動觸及』怎麼判」一併移除，決策 F 因此結案 —— 是被取消不是被解決**。**「git 不是為了那七條、理由換成純工程安全網」已寫進附錄。** **另外掃到一條同型、而不在你那七條裡的**：任務 4 驗收 5，依同一個理由一併移除（明講出來讓你有機會不同意）。
  4. **[07:55] 任務 3 驗收 16** → 條文改寫成**指定兩個方向的測資性質各一案例**（起點方向、尾端方向），各附一個可用的形狀，加上正向對照（片段仍須完整含有查詢詞），並要求兩支各自獨立（只讓其中一側保護失效時只有對應那支變紅）。**刻意不指名測試方法的名字**（名字會被改，判斷依據是測資性質與預期）。
- **一項升級（`spec-decisions.md`／決策 H，本檔從 0 項變 1 項，不擋任何人開工）**：`docs/product/features.md` 有三十幾處指向 `docs/specs/<slug>/spec.md`，**而兩份 spec 都已搬進版號目錄，那些路徑現在是斷的**。修法有四種、長期成本差很多，而且**同一題在 `stop-generation` 那一輪一模一樣** —— 依「兩輪之間的衝突不得由 PM 自己選一邊」，我沒有自己選，也**一個字都沒有動 `features.md`**。我的傾向寫在決策 H 裡。
- **回答收件匣問的那一題**：**`qa-agent.md` 執行步驟 1 那段補丁可以刪掉**，條件是等 v3.11 成為 QA 實際手上的那一版之後（在那之前 QA 可能還拿著 v3.10，而 v3.10 的架構那一節確實混著行為要求）。**刪掉會失去什麼也誠實寫了**：QA 從此讀不到架構條文旁邊那些理由 —— 那是刻意的分工，但它是一個真的損失，記在已知取捨與風險。
- **沒有做的事**：**沒有改變任何一條規則要求的行為**；**沒有動 `v3.10/`**；沒有碰 `plan/architecture.html`（只讀）、`docs/specs/stop-generation/`、`docs/product/features.md`、`docs/qa-environment.md`、`docs/qa-coverage.md`；**沒有動任務 8 驗收 14 剩下的那一半、任務 12 驗收 7、任務 13 驗收 8**（那三條是「重疊」不是「說不出後果」，本輪判準碰不到它們，而你上一輪才對其中一條表過態）；**沒有把這次的判準寫成 spec 裡的條文**（它是通則，權威在角色定義裡，本輪是它的一次性套用）。

---

## [2026-09-10 09:10] 決策 H 拍板：引用要帶「slug 目錄 + 檔名 + 節號 + 節名」，略過版號目錄

- **來源**：使用者裁決
- **相關條目**：`spec-decisions.md`／決策 H；`docs/product/features.md`（40 處）
- **狀態**：已處理（v3.12）（原文寫的是「待處理」，處理結果見本檔最後一段，與下一筆合寫）

**內容**：

格式定成這樣：

```
docs/specs/conversation-search/spec.md §5.1「入口是一個入口列，不是輸入框」
└──── slug 目錄 ────┘ └ 檔名 ┘ └節號┘ └────── 節名 ──────┘
```

**四段各自的理由：**

1. **slug 目錄**：穩定。
2. **檔名要留**：「哪一份檔案」跨版本是穩定的（`spec.md` 放行為規格、`spec-tasks.md` 放驗收，這個分工不會變）。而且 `features.md` 有指向 `spec-architecture.md` 與 `spec-appendix.md` 的引用，只寫目錄會讓這三份分不出來。
3. **版號略過**：那是唯一不穩定的一段，砍掉。讀的人自己挑版號最大的目錄。
4. **節號 + 節名一起帶** —— 這是這次真正的改變：
   - **節號指錯是靜靜失敗**：你落在一個真實存在的節，它講的是別的事，而你以為自己找對了。
   - **名稱指錯是大聲失敗**：grep 不到，當場就知道。
   - 節號給速度，節名給查核，兩者不一致時讀的人立刻看得到。**因此不需要另訂「節號不得重新編號」的規則**（那條原本要配著 (c) 一起上，現在不必了）。
   - 節名用那一條規則**粗體的那句話**，太長就取前半句，只要 grep 得到即可。

**要做的：**

1. **改 `docs/product/features.md` 那 40 處**（`conversation-search` 28 處、`stop-generation` 12 處）：拿掉版號、補上節名。
2. **同一趟核對節號在 v3.11 之後還對不對** —— §F 收掉、25 條驗收移除，有些引用可能指到已經不存在的東西。**指不到的不要猜**，記進變更說明。
3. **決策 H 移出 `spec-decisions.md`**，寫進附錄／決策歷程。
4. **`stop-generation` 那邊不必另外開一輪** —— `features.md` 是跨 slug 的單一份文件，一次改完兩邊都對。但若發現 `stop-generation` 的 spec 內部有跨檔引用寫了版號，**記進收件匣，不要自己去改別輪的 spec**。

**這條寫法已經進了你的角色定義**（「引用不留腐爛物」那張表多了一列），從此每一輪都適用，不只這一次。

---

## [2026-09-10 09:40] 任務 11 驗收 1、4 移除 —— 但那個裁決要留下來

- **來源**：使用者裁決
- **相關條目**：`spec-tasks.md`／任務 11 驗收 1、4；「明確不做」；`spec-appendix.md`／決策歷程（裁決一，摘要的兩個數字）
- **狀態**：已處理（v3.12）（原文寫的是「待處理」，處理結果見下方，與上一筆合寫）

**內容**：

**你在 v3.11 把這兩條留下來，理由是「它們是使用者一次裁決劃出的範圍界線，拿掉等於默默撤銷那次裁決」。使用者看過你的理由，判斷是移除。**

**他同意的是前半段**：照字面它們說不出使用者後果 —— 那兩個派生的量存不存在、畫面呼叫誰，使用者看到的摘要數字都一樣。**依本輪的掃描判準，它們該走。**

**但你擔心的那件事是真的，所以不是單純刪掉：**

1. **驗收 1、4 從驗收標準移除**，編號保留不遞補，底下照本輪的體例寫出它原本守什麼、現在由什麼承接。
2. **那個裁決的內容搬進「明確不做」** —— 內容不變，只是換一個身分：**它記的是「這一輪不做什麼」，不是「系統必須怎樣」**，而「明確不做」正是放這種東西的地方。要保留的三件事：
   - 比對層那兩個派生的量是**知情保留**的，不是沒清乾淨；
   - **不要順手刪掉它們，也不要順手把畫面改成呼叫它們** —— 那兩件事正是使用者在看過代價之後決定不做的（選項 (a) 與 (b)，他選 (c)）；
   - **想收斂就走 `spec-decisions.md` 重開一次**，不要在別的輪次順手做掉。這道門要留著。
3. **代價要誠實寫出來**：搬到「明確不做」之後，**沒有任何一條驗收每輪去檢查它了**。這是使用者知情的取捨 —— 他要的是「驗收條文裡不留追溯不到使用者後果的東西」，而不是「這個決定可以被忘記」。

**同型的一併照辦**：本輪掃描時若還有其他「記的是範圍界線、不是系統行為」的驗收條文，用同一個方式處理（移出驗收、內容進「明確不做」），並在變更說明裡列出來。

---

- **狀態**：已處理（v3.12）—— 以下是上面兩筆（09:10／09:40）的**合併處理結果**。
- **產出**：`v3.12/` 五份 spec（由 `v3.11/` 複製後修改，**`v3.10/` 與 `v3.11/` 一個字都沒有動**）＋ `changes-v3.12.md`＋ **`docs/product/features.md` 那 40 處引用**。
- **三步檢查（兩筆一起讀完才動手）**：
  - **衝突**：沒有。兩筆管的是不同的東西（一筆是「別人怎麼指這份 spec」，一筆是「驗收條文裡留什麼」）。
  - **順序相依**：**沒有真正的相依，但有一個交會，而且我是核對過才敢這樣說**：第二筆移出兩條驗收條文，而第一筆要核對的那 40 處引用**沒有任何一處指向驗收條文**（指的全是行為規格的節、另一輪的 R 代號，以及三處指向任務／明確不做／附錄的小標題）。所以移除條文不會讓那 40 處之中的任何一處變錯。
  - **合起來有沒有動到架構決策的根本假設**：沒有。`spec.md` 的行為規格 §1–§9 與 `spec-architecture.md` 的 §A–§G **一個字都沒有動**。
- **[09:10] 決策 H，逐項對照你交辦的四件事**：
  1. **`features.md` 那 40 處全部改完**（本規格 28 處、`stop-generation` 12 處）。**「拿掉版號」實際上是零改動** —— 那些路徑本來就寫成 `docs/specs/<slug>/spec.md`（v3.11 一個字都沒有動它），所以本輪真正做的是**補節名**。另外在該檔的「『對應 spec』欄怎麼看」底下寫了這個格式長什麼樣、以及**「這一欄的節名會過期，那時要改的是這裡」**。
  2. **節號核對完畢，結果是：本規格的 28 處全部仍然指得到，一處都沒有壞。** §F 收掉與 25 條驗收移除**沒有影響到任何一處** —— 因為那 28 處指的全是行為規格的節（§1–§8）與一處架構決策（§B.3），**沒有一處指向驗收條文**，也沒有一處指向 §F。逐處清單在 `changes-v3.12.md`。**唯一需要註記的是 §B.3**：它仍然存在，但那一整份檔案從 v3.11 起是建議、QA 不驗，所以我在該格補了這句話。
  3. **決策 H 已移出 `spec-decisions.md`**，四段格式的理由、被淘汰的三個選項與代價，全部移進 `spec-appendix.md`／決策歷程「決策 H 拍板」。`spec-decisions.md` 回到「**本次沒有需要決策的事項**」。
  4. **`stop-generation` 的 spec 內部我讀過了。沒有寫版號的跨檔引用，但有一處已經指不到的**：他們 `spec-appendix.md`／「跨輪相依」那一列列的三個標的，其中 §G.3 與決策 C **在 v3.11 各自搬了家**（§G.3 進了本規格的「明確不做」，決策 C 早已移進附錄）。**依你的交辦，我沒有動別輪的 spec，把事實寫進了 `docs/specs/stop-generation/pm-inbox.md`**，並告訴他們 `features.md` 已經一次改完、他們不必為它另開一輪。**這一處我判斷成「斷掉的引用」而不是「兩輪衝突」**（兩邊的說法沒有互相牴觸），所以沒有升級成待決事項。
- **[09:40] 任務 11 驗收 1、4，逐項對照你要的三件事**：
  1. **兩條移出驗收標準，編號保留不遞補**，底下照本規格既有的體例寫出它原本守什麼、以及**現在由什麼承接**（摘要顯示的數字由剩下的驗收 2、3 守，一字未改）。
  2. **那個裁決的內容原封搬進「明確不做」**，你點名要保留的三件事逐字都在：那兩個派生的量是**知情保留**的、**不要順手刪也不要順手改用**、**想收斂就走 `spec-decisions.md` 重開一次**（這道門留著）。**附錄／決策歷程「兩個沒有呼叫端的派生屬性」那一段也補了一句指向它**，免得下一個讀附錄的人以為那個裁決消失了。
  3. **代價誠實寫在三個地方**：條文原處、「明確不做」那一條、以及附錄／已知取捨與風險。寫的是同一句話 —— **搬過去之後沒有任何一條驗收每輪去檢查它了；發現它的唯一機會是有人去讀「明確不做」，而那不是每輪都會發生的事。**
  - **「同型的一併照辦」**：掃出**一條**同型的，**依同一個方式處理了：任務 14 驗收 4**（「這些條文不得要求任何行為改變；變紅時回報、不要改條文去遷就實作」）—— 它記的是那個任務的範圍界線與流程，不是系統行為。**另外三條看起來像、但我判定不同型而一字未動**：任務 6 驗收 7、任務 13 驗收 7、任務 9 驗收 6，它們的主體是**回歸行為**，被違反時使用者看得見（比較畫面壞掉、未存內容遺失、既有流程行為改變）。四條的判斷理由都列在 `changes-v3.12.md` 與 `spec-decisions.md`，**你要推翻其中任何一條，成本是幾行字**。
- **你在 v3.11 那幾項「我多做了什麼」，除了任務 11 那一項之外，本輪一律維持現狀、沒有翻案** —— `spec-decisions.md` 裡那一節已改標成「v3.11 當輪的判斷（保留為歷史）」，並註明你只回應了第 2 項。
- **本輪我自己做的判斷，寫在 `spec-decisions.md` 讓你有機會說不**：(a) 任務 14 驗收 4 判定同型並動了它；(b) **沒有**回頭替 spec 內部那數百處 `§X.Y` 引用補節名（新寫的文字都照新寫法，既有的列進「明確不做」並附觸發條件）；(c) `features.md` 只改「怎麼指」，不改任何一格的內容判斷。
- **沒有做的事**：**沒有改動任何一條規則、也沒有改動任何其他驗收「要驗的東西」**；**沒有動 `v3.10/` 與 `v3.11/`**；沒有碰 `plan/architecture.html`（只讀，本輪核對結論是**無需更新**，理由寫在任務 10 的「目前狀態」）、`docs/specs/stop-generation/` 底下的 spec（只讀，只寫了他們的收件匣）、`docs/qa-environment.md`、`docs/qa-coverage.md`。

---

## [2026-09-10 19:20] v3.12 唯一做的那件事，沒有任何驗收在守

- **來源**：QA 全驗（2026-09-10，驗的是 v3.12）
- **相關條目**：`docs/product/features.md` 的 40 處引用；任務 1–14 都沒有覆蓋它
- **狀態**：已處理（v3.13）（原文寫的是「待處理」，處理結果見本檔最後一段，四筆合寫）

**內容**：

v3.12 做了兩件事，第一件是把 `features.md` 那 40 處引用改成「slug 目錄 + 檔名 + 節號 + 節名」。

**沒有任何一條驗收在問「那些引用對不對」。**

QA 這一輪自己去核對了 29 處指向本規格的引用，**節號與節名逐處相符、0 個不符** —— 事情做對了。但那是它順手多做的一件沒被要求的事。**下一輪冷讀的 QA，沒有任何條文會叫它去看那 40 處；有人改壞了不會有人發現。**

**QA 點出的對比值得照抄**：任務 11 與任務 14 有把自己的缺口**誠實寫進「明確不做」**（白紙黑字說「這件事沒有人在守，我們知道」）。這一件沒有 —— 所以它不是知情的缺口，是一個沒人注意到的洞。

**還有一層諷刺要記下來**：v3.12 那一輪的主題正是「引用不能留下會腐爛的東西」，而那次改動本身變成了一個**會腐爛而且沒有人會發現**的東西。

**要你決定的是哪一種**（兩種都可以，但要選一個並寫下來）：

1. **補一條驗收** —— 例如「`features.md` 裡指向本規格的每一處引用，其節號與節名都要在現行定版裡找得到」。它說得出使用者後果嗎？**說得出**：那份文件的用途就是「從一個功能追到它的規格」，指錯了就追不到。
2. **寫進「明確不做」** —— 明講「這 40 處靠人工核對，沒有條文在守，我們知道」，並寫下觸發條件（什麼時候該補）。

**不要兩件都不做。** 現況是第三種：既沒有條文，也沒有承認缺口。

---

## [2026-09-10 19:25] §2.8(a) 兩個案例在這台裝置永遠不可達，但沒有替代做法

- **來源**：QA 全驗（2026-09-10，驗的是 v3.12）
- **相關條目**：`spec.md` §2.8(a) 的兩個判定案例；對照組是 `spec-tasks.md`／任務 12 驗收 2
- **狀態**：已處理（v3.13）（原文寫的是「待處理」，處理結果見本檔最後一段，四筆合寫）

**內容**：

§2.8(a) 底下列的判定案例裡，有兩個**在這台裝置上永遠做不到**：

- **長按空白鍵**
- **貼上單一個 U+3000 或 U+00A0**（這套工具鏈設不了剪貼簿，全形空白也送不進輸入法）

**規則本身有被守到** —— 同一條底下的其他案例（貼上「三個半形空白＋北京」、游標移到最前面按空白）做得到，而且驗的是同一個性質。**這一筆不是要放寬規則。**

**問題是那兩個案例的地位沒有寫清楚。** 每一輪 QA 都要重新判斷一次「這算不算 fail」，然後重新寫一次「本環境無法觀察」—— **同一個判斷被重做了不知道幾輪，而且下一個 QA 不知道前面的人也判過。**

**對照組就在隔壁**：任務 12 驗收 2 撞到同一件事（設不了剪貼簿），處理方式是三件事寫進**條文本身**：允許替代做法／每次用了都要在報告裡寫明原因且不得默默替換／哪一輪發現環境已經能設剪貼簿必須回報。

**請用同一個形狀處理這兩個案例。** 那三件事裡最重要的是第三件 —— **它是「這個替代是暫時的」唯一的回收機制**，沒有它，環境改善了也不會有人改回去。

---

## [2026-09-10 19:30] 任務 3 驗收 16 的 fail 已修（RD 回報）

- **來源**：RD 觀察
- **相關條目**：`spec-tasks.md`／任務 3 驗收 16
- **狀態**：已處理（v3.13）（原文寫的是「待處理」，處理結果見本檔最後一段，四筆合寫）

**內容**：

QA 判 fail 的那一條，**條文沒有問題，是我的測試漏了條文明文要求的一半**：兩支 surrogate pair 測試只斷言「片段裡沒有落單的代理」，沒有斷言「片段裡仍然完整含有查詢詞」。條文自己寫著漏掉會怎樣 ——「一個回傳空片段的實作也會通過」。

**已修**：兩支各補一行正向對照，並驗證過它真的擋得住那個失效模式（把片段強制改成空字串 → 兩支都變紅；改之前會通過）。原始碼已還原、SHA-256 相同、全套 120 支綠。

**這個漏洞是怎麼發生的，值得記進附錄**：我在 v3.11 還不存在的時候寫那兩支測試，然後請 PM 去把測資形狀寫進條文。**PM 照辦，並且多寫了一項正向對照 —— 而沒有人回頭拿新條文對一次我先寫好的測試。**

也就是說：**我先交作業、再請人出題，出完題沒有人對答案。** 這一型跟「規則改了、條文沒跟上」是同一個形狀，只是方向相反（條文改了、測試沒跟上）。要不要為它立規則由你判斷 —— 我這一端已經把「交付前跑全套、純邏輯自帶測試」寫進流程文件的 RD 硬規則，但那條擋不住這一型（測試當時是綠的）。

---

## [2026-09-10 20:05] 裁決：每一個任務要能獨立跑完，所以跨任務的重疊留著

- **來源**：使用者裁決
- **相關條目**：`spec-tasks.md`／任務 8 驗收 14（行為那一半）、任務 12 驗收 7、任務 13 驗收 8；`spec-decisions.md`／v3.11 自主判斷第 3 項
- **狀態**：已處理（v3.13）（原文寫的是「待處理」，處理結果見本檔最後一段，四筆合寫）

**內容**：

你在 v3.11 列出「沒有動那三條重疊條文」，並寫著**要不要合併取決於使用者希望每個任務能不能獨立跑完**。

**裁決：要能獨立跑完。所以那三條維持現狀，你的判斷成立。**

**這條從此是通則，不是這三條的個案處理**，而且已經寫進你的角色定義：

> **跨任務的重疊不是問題，而且那是刻意的：每一個任務都要能獨立跑完。** 同一件事在兩個任務裡各驗一次看起來是冗餘；但把其中一份改成「見任務 N 驗收 M」，那個任務就不能單獨交出去了 —— 驗它的人得先去跑另一個任務。**任務是可以被單獨指派、單獨回報的單位，那個性質比省掉幾行字重要。**

**要做的：**

1. **那三條一字不動。**
2. **把這條裁決寫進 `spec-appendix.md`／決策歷程**，並在 `spec-decisions.md` 的「已拍板的事項」留一列。重點是寫出它**解決了什麼疑問** —— `changes-v3.9.md` 那份附加報告把這三條列成「同一件事寫兩次」的問題，現在那個判定被推翻了：**它不是問題，是任務獨立性的代價，而那個代價是刻意付的。**
3. **順帶檢查有沒有反方向的殘留**：spec 裡如果有驗收條文寫成「見任務 N 驗收 M」而不是自己講完，那些現在是**違反這條通則**的。找到就列進變更說明，**但不要在本輪改**（那會擴大範圍）—— 讓使用者看見清單再決定。

**v3.11 與 v3.12 其餘的自主判斷，使用者看過了，沒有異議，全部維持現狀。** 包含 §C.4 搬進行為規格那一項 —— 他明確認可你的理由（分界線的參考點不能留在 QA 不驗的檔案裡）。**下一輪不要重新翻案。**

---

- **狀態**：已處理（v3.13）—— 以下是上面四筆（19:20／19:25／19:30／20:05）的**合併處理結果**。
- **產出**：`v3.13/` 五份 spec（由 `v3.12/` 以 `cp -r` 複製後修改，**`v3.10/`、`v3.11/`、`v3.12/` 一個字都沒有動**；`test-report.md` 沒有複製過去，它屬於它驗的那一版）＋ `changes-v3.13.md`。
- **三步檢查（四筆一起讀完才動手）**：
  - **衝突**：沒有。四筆分別管**驗收覆蓋**、**實機條文的可達性**、**一次已修完的 fail**、**任務之間的重疊**，方向互不牴觸。
  - **順序相依**：**沒有，但有一個交會，而且我是核對過才敢這樣說** —— 第四筆要我掃「寫成指路的驗收」，而第一筆新增了三條驗收。我在寫完任務 15 之後才掃，**新條文自己也過了同一把尺**（三條都自己講完了要判什麼，沒有一條把判定推給別的任務）。
  - **合起來有沒有動到架構決策的根本假設**：沒有。`spec-architecture.md` 一個字都沒有動，`spec.md` 的規則字面也一條未動。
- **[19:20] `features.md` 那 40 處沒有驗收在守 —— 我選了「補一條驗收」，理由寫在條文裡**：
  1. **新增任務 15**（`spec-tasks.md`）。**選擇的理由**：這件事**說得出使用者後果** —— 那份文件的用途就是「從一個功能追到它的規格」，指錯了就追不到，而追不到的時候讀的人手上正好有一個要判對錯的問題。**說得出後果的就該是條文**，這是准入判準本身，不是偏好。
  2. **條文有三條，而且刻意不只驗「指得對不對」**：驗收 1 判「節號存在、節名 grep 得到、兩者落在同一節」；**驗收 2 是正向對照**（那三列不得改成「尚無 spec」或留空 —— 否則把引用刪光會讓驗收 1 全數通過）；**驗收 3 禁止指進版號目錄**（指進去時驗收 1 仍會通過，但讀的人會停在一份不再更新的定版上）。三條各自說得出它獨自抓得到什麼。
  3. **我沒有把數量寫進條文**（「40 處」「28 處」都沒有寫）。條文寫的是「每一處路徑含 `docs/specs/conversation-search/` 的引用」—— **數量是別人在另一輪會改掉、而我不會知道的東西。**
  4. **誠實的那一半也寫了**：那份文件裡**指向 `stop-generation` 的引用不歸任務 15**，界線、理由與代價寫進「明確不做」並附觸發條件。**所以現在是「一半有條文守、一半是知情的缺口」，不是「都被守住了」。**
  5. **那個諷刺記進附錄了**：v3.12 的主題正是「引用不能留下會腐爛的東西」，而那次改動本身變成了一個會腐爛、沒有人會發現的東西。**你點出的對比（任務 11／14 把缺口誠實寫進「明確不做」，這一件沒有）我原句採納** —— 差別不在「有沒有條文」，在**是不是知情的缺口**。
- **[19:25] §2.8(a) 兩個不可達的案例，照任務 12 驗收 2 的形狀處理**：
  1. **處置寫進 `spec-tasks.md`／任務 12 驗收 1**，三件事：**替代做法／報告義務／回收機制**。
  2. **兩個案例的承接方式不同，這一點我寫明了**：**長按空白鍵**有替代做法（任何能讓空欄位連續收到多個空白輸入的方式，要驗的性質不變 ——「一個空白留不住，多個也留不住」）；**貼上單一 U+3000 / U+00A0 在畫面端沒有替代做法**，記「本環境無法觀察」、不判 fail，**改由同一任務的驗收 6 承接**（不經過畫面，U+3000 與 U+00A0 同屬 Zs、由同一份集合回答）。**這是換一層去問同一件事，不是放過它。**
  3. **第三件（回收機制）照你說的當成最重要的一件寫**：哪一輪發現這套工具鏈已經能設剪貼簿、或能把非 ASCII 送進輸入框，**必須在該輪報告裡明確回報**。條文裡寫著為什麼這句話要在條文裡。
  4. **`spec.md` §2.8(a) 底下加了一行**指明那兩個案例在目前環境做不出來、**規則不放寬**、處置在哪裡。**§2.8 的規則字面一個字都沒有動。**
- **[19:30] 任務 3 驗收 16：條文一字未改，成因記進附錄**：
  1. **照辦，沒有改條文。** 我讀過那條條文確認你的判斷成立：它明文要求「片段裡仍然完整含有查詢詞」，並自己寫著漏掉會怎樣（「一個回傳空片段的實作也會通過」）。**fail 的成因在測試，不在條文。**
  2. **成因記進 `spec-appendix.md`／決策歷程**，用你自己那句話當標題（「我先交作業、再請人出題，出完題沒有人對答案」），並寫出它與「規則改了、條文沒跟上」是**同一個形狀、方向相反**。
  3. **我判斷不為它立新的 spec 規則，理由也寫在那裡**：它問的是「條文寫下來之後有沒有人回頭對一次既有的測試」—— 那是**驗證流程規則**，屬於角色定義，不屬於任何一份 spec；而且它**說不出被違反時使用者會遇到什麼**（那兩支測試當時是綠的）。**這次的升級路徑是有效的**：缺口是全驗抓到的，與 v3.6 那次裁決的分工一致。
  4. **另外在「歷次全驗的結論」補了一列**（2026-09-10 那次驗 v3.12 的結果），含你已修這件事。
- **[20:05] 任務獨立性裁決**：
  1. **那三條一字不動**（任務 8 驗收 14 行為那一半、任務 12 驗收 7、任務 13 驗收 8）。
  2. **裁決寫進 `spec-appendix.md`／決策歷程**，並在 `spec-decisions.md`／「已拍板的事項」留一列。**重點照你說的寫出它解決了什麼疑問**：`changes-v3.9.md` 附加報告把這三條列成「同一件事寫兩次」的問題，**那個判定被推翻了 —— 它不是問題，是任務獨立性的代價，而那個代價是刻意付的。** v3.11 那節「你只回應了第 2 項」的標題也更新了。
  3. **反方向的殘留清單列在 `changes-v3.13.md`／附二，本輪一條都沒有改。** 分成三堆：**A 真的把判定推出去（三項）**、**B 指向 `spec.md` 的規則而非另一個任務（三項，我判定不違反通則）**、**C 看起來像但不是（兩項）**。
  4. **清單裡有一項超出「寫法問題」，值得你單獨看一眼**：**任務 13 驗收 8** 寫著「同任務 9 驗收 6 的**清單**」，而**任務 9 驗收 6 底下沒有清單** —— 那是一處指向不存在的東西的引用。**依交辦本輪沒有改**，寫進 `spec-decisions.md` 讓你看得到。
- **沒有做的事**：**沒有改動任何一條規則的實質內容**（`spec.md` §1–§9 規則字面、`spec-architecture.md` §A–§G 全份一字未動）；**沒有移除任何一條驗收**；**沒有改動那三條重疊條文與任務 3 驗收 16**；**沒有碰** `plan/architecture.html`（只讀，本輪核對結論是**無需更新**，理由寫在任務 10 的「目前狀態」）、`docs/product/features.md`（本輪只為它立驗收，**沒有改它一個字**）、`docs/specs/stop-generation/` 底下任何檔案、`docs/qa-environment.md`、`docs/qa-coverage.md`；**沒有動 `v3.10/`、`v3.11/`、`v3.12/`**。
- **`spec-decisions.md` 維持 0 項待決。** 四筆裡沒有一筆需要升級：一筆是你的裁決、一筆明說只需要我知道、一筆是**要我選一個並寫下理由**（升級回去問等於把交辦退回去）、一筆是把既有的處理形狀套到兩個新案例上。

---

## [2026-09-14 16:10] QA 驗 v3.13 的兩點條文觀察（補登，當時漏寫進收件匣）

- **來源**：QA 全驗（2026-09-10，驗的是 v3.13）
- **相關條目**：`spec.md` §2.8(a)「開頭：立即移除」；`spec-tasks.md` 任務 5 驗收 2
- **狀態**：待處理

**內容**：

**這兩點是 QA 在 v3.13 那一輪回報的，RD 當時沒有轉進收件匣** —— 那一輪結束後注意力轉到另一個新 bug，這兩點就掉了。**兩點都不影響 v3.13 的 pass/fail**，都是條文層的觀察。

### 一、§2.8(a) 第三條可判定沒有任何驗收指名它

§2.8(a) 底下有一條可判定：**把游標移到欄位最前面按空白鍵 → 欄位內容不變**。

**沒有任何一條驗收條文指名它。** QA 那一輪主動補測了、結果 pass，但**下一輪的 QA 冷讀時不會被任何條文要求去做** —— 這正是「規則有寫、條文沒覆蓋」那一型。

它說得出使用者後果：游標在最前面按空白，若欄位多出一個前導空白，使用者會看到搜尋結果變了或回顯字串多了空白。

**要你判斷**：補一條驗收（它屬於哪個任務、要不要併進任務 12 驗收 1 那組已經有替代做法機制的案例），或寫明它由哪一條已存在的驗收承接。

### 二、任務 5 驗收 2 在這台裝置上每一輪都在空轉

條文要求：送出一則會產生較長回覆的訊息，**在串流未結束時**進入搜尋並輸入回覆中會出現的字，觀察命中數自行增加；攔截不到則記「未能觀察」、不 fail。

QA 量到的現實：**最小的模型 8 秒內就回完，而必要的操作串（開抽屜 → 點搜尋 → 輸入）最快也要 8 秒。** 字面的動作幾乎必然攔不到。

條文已經寫了「不 fail」，所以**不會誤傷正確的實作** —— 但**每一輪都在做一件幾乎確定做不成的事**，而且沒有回收機制。

**QA 的建議**：比照任務 12 驗收 1、2 的形狀，把**間接佐證**升成正式的替代做法（例如用較大的模型、或較長的提示讓回覆拉長），並寫明每次用了替代做法要在報告裡記錄、以及哪一輪環境能攔到時要回報。
