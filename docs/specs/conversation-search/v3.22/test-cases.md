# v3.22 全驗 test case 清單（依「需要的環境狀態」分組）

> 這份清單在**第一次操作裝置之前**寫好，用途是把按任務排列的驗收條文，重排成「一個環境狀態準備一次、底下掛好幾個觀察點」。
> **判定依據永遠是條文原文**，不是這份清單裡的敘述；每個觀察點都引用它對應條文的預期輸出。
> **報告仍照任務排**。這裡只管執行順序。
> 受測版本：HEAD `6b9072c`，工作區乾淨。裝置：模擬器 `emulator-5554`（Android 17，1080x2424，density 420）。
> 呼叫者已明講「使用者已同意清空驗收裝置上的 App 資料」→ 走清空那一條路，不做快照／還原／逐位元組驗證。

---

## 情境 A：不碰裝置 —— 專案的標準測試指令（準備一次）

**環境狀態**：工作區乾淨、`./gradlew clean testDebugUnitTest`。

- A-1 → 全體：測試套件必須全綠（`failures=0 errors=0`），而且不是 UP-TO-DATE。
- A-2 → 任務 2 驗收 1：「這塊程式碼必須能在沒有 Android 環境的情況下被測試直接呼叫：它沒有任何 Android / Compose / 持久化層的相依。」→ 讀 `ConversationSearch.kt` 的 import。
- A-3 → 任務 2 驗收 7：「整形與折疊這兩個步驟能被單元測試直接呼叫並單獨斷言，不得只能由搜尋結果反推。」
- A-4 → 任務 9 驗收 2：「文字修復那一份程式碼裡不得出現搜尋的整形／折疊，也不得被它們引用。」
- A-5 → 任務 9 驗收 3：「指得出『送進模型的上下文』那道過濾在哪裡，而且它不是搜尋也在用的那一份。」

## 情境 B：不碰裝置 —— 逐條核對「條文指名的測試」是否存在且斷言正確（準備一次）

**環境狀態**：讀 `app/src/test/.../ConversationSearchTest.kt`、`ConversationTest.kt` 原始碼，對照條文。
**每一條都要指得出一個具體的測試方法**，找不到即 fail（行為對不算）。

- B-1 → 任務 3 驗收 1：「使用者訊息與助理訊息都能被搜到（各一個案例）。」
- B-2 → 任務 3 驗收 2：「`hello` 命中 `Hello world`；`HELLO` 亦然。」
- B-3 → 任務 3 驗收 3：「`ABC` 命中 `ＡＢＣ`；`ｈｅｌｌｏ` 命中 `Hello`。」
- B-4 → 任務 3 驗收 4：「『台』不命中僅含『臺』的訊息。」
- B-5 → 任務 3 驗收 5：「`"  北京  "` 與 `"北京"` 結果相同，**且兩者都不得為空**（斷言命中筆數 > 0）。」
- B-6 → 任務 3 驗收 6：「案例必須包含 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0，後兩者不得省略。」
- B-7 → 任務 3 驗收 7：「完全無命中回傳空結果。」
- B-8 → 任務 3 驗收 8：「直接呼叫折疊步驟，對含 U+0130 的字串斷言長度不變。」
- B-9 → 任務 3 驗收 9：「含換行的訊息命中時，片段中被標示的那段恰等於原文對應的字（§4.8）。」
- B-10 → 任務 3 驗收 10：「同一則訊息中落在視窗內的每一個命中都有標示（§4.6）。」
- B-11 → 任務 3 驗收 11：「命中在第 100 字時，片段以『…』開頭、長度符合 §4.1–§4.3、標示位置正確。」
- B-12 → 任務 3 驗收 12：「5 則命中時只顯示 3 則，回報總數為 5。」
- B-13 → 任務 3 驗收 13：「開場白可以被搜到，且命中的是該對話第 0 則、作者為助理。」
- B-14 → 任務 3 驗收 14：「只有開場白的空白對話也會產生命中：顯示 1 則、總數 1；不命中時完全不出現。」
- B-15 → 任務 3 驗收 15：「三段同人格的對話全部出現。」
- B-16 → 任務 3 驗收 16：兩個方向各一個案例，測資要真的碰得到邊界（起點形狀：15 個 emoji＋1 BMP＋查詢詞＋BMP 尾巴；終點形狀：30 BMP＋查詢詞＋7 BMP＋20 個 emoji）。預期：「第一個 code unit 不得是落單的低位代理，最後一個 code unit 不得是落單的高位代理；**且片段裡仍然完整含有查詢詞**」。兩案例必須各自獨立。
- B-17 → 任務 3 驗收 17：整形的三段掃描式斷言（下限 BMP 全掃 `整形("a"+c+"b")=="a b"` 字面形式；上限四個零寬字元＋ZWJ emoji 不變；收合與去除）。
- B-18 → 任務 3 驗收 18：「訊息端 U+3000 + 半形空白查詢 → 命中。」
- B-19 → 任務 3 驗收 19：四個案例都要走完整的搜尋入口。
- B-20 → 任務 3 驗收 20：「標示長度為 5，取出來恰為『北京(1 個空白)天氣』。」
- B-21 → 任務 3 驗收 21：「命中『我在北京 天氣很好』、不命中『北京很大，天氣很好』。」
- B-22 → 任務 3 驗收 22：「U+3000、U+00A0、`" \t\n"` 皆為非搜尋狀態，`"a"` 為搜尋狀態；並斷言不存在『畫面認為在搜尋、但整形後為空』的字元（BMP 掃描）。」
- B-23 → 任務 3 驗收 23：L = 59 / 60 / 61 三個案例，「L = 59 與 L = 60 的預期片段必須不同」。
- B-24 → 任務 3 驗收 24：查「老師」「Qwen」「qwen」都不得命中，**同一段對話用本文裡確實存在的字查必須命中**。
- B-25 → 任務 3 驗收 25：「短訊息命中時片段前後都不得出現『…』」＋同一測試方法裡對照長訊息。
- B-26 → 任務 3 驗收 26：「查『還沒說話』不得命中；查開場白裡確實有的字必須命中。」
- B-27 → 任務 2 驗收 2–11（逐條對應測試或直接呼叫觀察）。
- B-28 → 任務 4 驗收 2：「『所有對話』必含開啟中的那段（包含空白對話），排序為最後更新時間新→舊。」
- B-29 → 任務 4 驗收 3：「歷史內含與開啟中同 id 但訊息較少的項目 → 該 id 只出現一次，且是訊息較多的那份。」
- B-30 → 任務 4 驗收 6：§3.5 第二條可判定（memory 時間 100、history 同 id 時間 900 只含一則、other 時間 500）→「結果恰為兩個分組，順序是 `other`、`same`，且 `same` 這一組回報的命中訊息數為 2」。
- B-31 → 任務 12 驗收 6：兩個案例 —— 「以 U+00A0 開頭的輸入必須被移除」「以 U+001C 開頭的輸入必須不被移除」。
- B-32 → 任務 12 驗收 9：「比對層原有的空查詢契約測試（`""`、`"   "`、`"\n\t"`、U+3000、U+00A0）仍存在且通過。」
- B-33 → 任務 17 驗收 1：標題、預覽各一個案例，測資讓截點落在 🌧 兩個 code unit 中間。預期：「任何位置都沒有落單的代理字元；尾端是『…』；『…』前面那一段是原文的開頭一段，而且那個 emoji 之前的字全部都在」。
- B-34 → 任務 17 驗收 2：「標題 24 個 BMP 字 ＋ 🌧，共 26 個 code unit → 標題是整句、emoji 完整、尾端沒有『…』」。預覽同理。
- B-35 → 任務 17 驗收 3：不得修過頭的四件事（一般長句有「…」／剛好上限無「…」；換行變空白；U+3000 原樣保留；無使用者訊息 → 標題「還沒說話」）。
- B-36 → 任務 17 驗收 6：三類字（👨‍👩‍👧、🇹🇼、👍🏽）各一個案例，標題與預覽各做一次；＋「前面那個字也要在」＋「剛好放得下的組合 emoji」＋「極端輸入（e＋40 個 U+0301）」。「不得只剩 👨、🇹、或沒有膚色的 👍」「標題不得只剩『…』」。
- B-37 → 任務 18 驗收 1：三類字各一個案例，起點落在那個字裡且不在代理對中間。預期：「片段以『…』開頭；『…』之後是完整的那一個字接那一串『乙』，或者直接從第一個『乙』開始；片段完整含有『kiwi』，標示恰好蓋住『kiwi』」。
- B-38 → 任務 18 驗收 2：終點方向三類字各一個案例。預期：「片段以『…』結尾；『…』之前是完整的那一個字，或者是它前面的最後一個『乙』」。
- B-39 → 任務 18 驗收 3：不得修過頭四件事（截點本來在兩字之間不移動；§4.2 三條界線不變；§4.3 不該補的不補；下限仍成立）。
- B-40 → 任務 18 驗收 4：極端長字。預期：「片段不為空，含有命中起點那個字『k』，而且沒有把那個長字切成兩半」。
- B-41 → 任務 14 驗收 1、2、3：指名的九條各指得出怎麼被驗；可自動化的說得出「什麼樣的實作會讓它變紅」；配正向對照的兩半都執行到。

## 情境 C：不碰裝置 —— 文件冷讀（準備一次）

- C-1 → 任務 10 驗收 1：`plan/architecture.html`「搜尋」那一節補上查詢欄位的空白規則三件事。
- C-2 → 任務 10 驗收 2：規則對照表「（只有空白）→ 不算搜尋」那一列補一句「它現在是比對層的契約，在畫面上打不出來」。
- C-3 → 任務 10 驗收 3：(i) 那一條同時說出三件事（以意思判）；(ii)「不算」那一側不再含「按下停止」四個字；(iii) 文件裡沒有另一處仍宣稱按下停止不算一次更新。
- C-4 → 任務 10 驗收 4：模組地圖那三列（搜尋畫面、比對純邏輯、對話存取層）逐列判 (i) 路徑存在 (ii) 職責與現況相符。
- C-5 → 任務 10 驗收 5：文件裡描述的時間概念仍只有一個。
- C-6 → 任務 10 驗收 7：「命中本身長到裝不下時，視窗從命中的起點開始，不留引子」→「命中長度剛好是 60 個字時，前面留幾個引子字元」的唯一答案是 0。
- C-7 → 任務 15 驗收 1：`features.md` 每一處含 `docs/specs/conversation-search/` 的引用，判 (i) 節號存在 (ii) 節名 grep 得到 (iii) 兩者落在同一節。
- C-8 → 任務 15 驗收 2：`搜尋-02`、`搜尋-13`、`搜尋-20` 三列仍帶著指向本規格的引用。
- C-9 → 任務 15 驗收 3：每一處引用路徑不含版號目錄。
- C-10 → 任務 16 驗收 5：`features.md` 保存-02 的描述與 §9.1 一致（兩件事都要）；那一格的引用照任務 15 三條判。
- C-11 → 任務 19 驗收 1：冷讀「「※未經查證」是什麼意思」那一節 →「某一項如果有人在模擬器上開 App 確認過，它算不算已經查證？」必須答得出來且答案是「算」。
- C-12 → 任務 19 驗收 2：該節仍存在，且仍說出 (i) 標記的意思 (ii) 沒標的那些是怎麼來的。

---

## 情境 D：裝置 —— 乾淨起點（清空 App 使用者資料、保留模型資產）

**環境狀態準備**：`am force-stop` → 刪 `files/` 下的對話 JSON 與 `shared_prefs/`（模型檔保留）→ 冷啟動。
**這一組要的是「什麼都沒有」的起點**，所以排在最前面，不與別的情境共用。

- D-1 → 任務 1 驗收 6（一半）：「冷啟動續接（檔案**不存在**那一種）」。
- D-2 → 任務 6 驗收 1 (v)：「沒有其他對話時，顯示『還沒有其他對話。』。」
- D-3 → 任務 8 驗收 1：「剛進入時顯示引導文案，**沒有**『找不到…』、沒有空的結果列表。」
- D-4 → 任務 6 驗收 3：「進入時輸入框已聚焦、鍵盤自動彈出。」
- D-5 → 任務 6 驗收 2：「多一個搜尋入口列（不是輸入框），點擊後抽屜關閉、進入搜尋畫面。」
- D-6 → 任務 8 驗收 11：「結果區只有三種樣子，沒有第四種『載入中』。」（＋正向對照放到情境 F）

## 情境 E：裝置 —— 查詢欄位的空白規則（空欄位起點，鍵盤英文版面）

**環境狀態**：在搜尋畫面、輸入框聚焦、英文版面。每個觀察點之間把欄位清空。
**判定一律用 `ui.py field` 逐碼位讀，不看截圖。**

- E-1 → 任務 12 驗收 1：「空欄位按空白鍵 → 欄位仍為空、顯示引導文案、不顯示『找不到』。」
  - E-1a 真正的「長按空白鍵」：先試真的，做不出來才用「重複送出空白鍵事件」的替代做法。**報告要指名長按之後畫面上發生了什麼**，沒寫就是 fail。
  - E-1b 真正的「貼上單一 U+3000 / U+00A0」：先試真的（含怎麼產生那個字元）。**報告要指名那個字元用什麼方式試著產生、產生不出來還是產生得出來但進不了剪貼簿**，沒寫就是 fail。承接條文是任務 12 驗收 6。
- E-2 → 任務 12 驗收 2：「貼上『(3 個半形空白)北京』→ 欄位內容為『北京』。」**先試真正的貼上**（文字選取工具列）；做不出來才走替代做法，並**指名真正的貼上是在哪一步失敗的**，沒寫就是 fail。
- E-3 → 任務 12 驗收 12：「欄位內容為『北京』時，把游標移到最前面按一次空白鍵 → 欄位內容仍為『北京』」＋正向對照「游標移回尾端再按一次空白鍵 → 變成『北京(1 個空白)』」。
- E-4 → 任務 12 驗收 4：「輸入『北京』+ 空白 → 欄位為『北京 』；再輸入『天氣』→ 欄位為『北京 天氣』，且命中含『北京 天氣』的訊息。」（需要情境 F 的測資）
- E-5 → 任務 12 驗收 3：「欄位內容為『北京(2 個空白)』時刪掉『北京』→ 欄位為空、回到未搜尋狀態。」
- E-6 → 任務 12 驗收 5 / 任務 8 驗收 10 / 任務 12 驗收 7：輸入 `"  kiwi  "` → 「欄位內容為 `"kiwi  "`、文案為 `找不到符合「kiwi  」的訊息`」；失焦後「欄位與文案都變成 `kiwi`」。
- E-7 → 任務 12 驗收 8 / §2.10：「輸入『北京(2 個空白)』記下結果與摘要 → 使輸入框失焦 → 結果與摘要一字不差。」

## 情境 F：裝置 —— 一份「多段對話」的測資（推檔 + App 自己產生）

**環境狀態準備**：推入一組對話 JSON（含 5/2/2 命中、長英文標題、短標題、只含助理訊息的 X、含使用者訊息的 Y、U+3000 預覽、emoji 截點、組合 emoji 標題/預覽、搜尋片段組合 emoji 兩則），冷啟動讓 App 讀到。
**這一組準備一次，掛最多觀察點。**

- F-1 → 任務 6 驗收 1 (i)：上半部六項依序；捲動歷史列表時上半部不動；抽屜裡只有歷史列表可以捲動。
- F-2 → 任務 6 驗收 1 (iii)：每一列有人格 emoji、人格名稱、模型名稱、時間、標題、預覽、刪除鈕；時間兩種寫法（今天「時:分」、非今天「月/日」）；標題與預覽單行截斷。
  - F-2a 長英文句 → 標題與預覽尾端都看得到「…」；短句「你好」→ 整句、尾端沒有「…」。
- F-3 → 任務 6 驗收 1 (vi)：開著 A、另有 B → 列表上有 B、沒有 A；點 B 之後再打開抽屜 → 有 A、沒有 B。
- F-4 → 任務 6 驗收 1 (iv)：點一列 → 開啟那段對話、抽屜關閉；按刪除鈕 → 那段從列表消失、沒有確認步驟。
- F-5 → 任務 9 驗收 7：「同一則含 U+3000 的訊息，在抽屜歷史列預覽裡逐碼位讀出來仍是 U+3000，而搜尋片段把它收成一個半形空白。」
- F-6 → 任務 17 驗收 4：抽屜那一列的標題、預覽尾端是完整的字接「…」，沒有壞掉的字；emoji 之前的字都在。**讀畫面的工具在那一列可見時崩潰即為 fail 的證據。**
- F-7 → 任務 17 驗收 5：省略號兩個方向（與 F-2a 同一次觀察即可，但各自判定）。
- F-8 → 任務 17 驗收 7：抽屜那一列的 👨‍👩‍👧 標題、🇹🇼 預覽是完整的字；「與驗收 6 的預期不同即 fail」。
- F-9 → 任務 18 驗收 5：搜尋「kiwi」→ 起點形狀那一列「…」之後不是 ZWJ 或 👩；終點形狀那一列「…」之前不是落單的 🇹。
- F-10 → 任務 8 驗收 7 / 任務 11 驗收 2：「三段對話分別命中 5 / 2 / 2 → 摘要列顯示『3 段對話・9 則訊息』，畫面上列出 3 + 2 + 2 筆。」
- F-11 → 任務 7 驗收 4：「超過 3 則命中時顯示 3 則 + 『還有 N 則符合』，N = 總數 − 3。」
- F-12 → 任務 11 驗收 3：「空查詢與無命中時，摘要列不出現。」
- F-13 → 任務 7 驗收 1：「結果列表佔滿輸入框以下所有空間，可整片捲動。」
- F-14 → 任務 6 驗收 4：「輸入框固定頂端，捲動結果時不被捲走。」
- F-15 → 任務 7 驗收 3 / §3.6：「兩段對話的第 0 則同時命中時，捲動列表不錯亂、不崩潰。」
- F-16 → 任務 7 驗收 7 / §3.8：「分組標頭不顯示對話標題；空白對話的標頭不出現『還沒說話』。」
- F-17 → 任務 7 驗收 11：「任一有命中的分組，標頭上同時看得到人格 emoji、人格名稱、模型名稱、時間；換一段人格或模型不同的對話，那幾個字必須跟著改變。」
- F-18 → 任務 8 驗收 2：「輸入保證不存在的字串，只出現『找不到…』一句，無殘留舊結果。」
- F-19 → 任務 6 驗收 9 / §1.7：「輸入『北』…輸入停止 1 秒內結果必須出現；接著輸入『京』…結果必須換成『北京』那一組」＋「畫面上沒有任何『按下才會搜』的控制項」。
- F-20 → 任務 8 驗收 6：「命中列**沒有**刪除鈕。」
- F-21 → 任務 8 驗收 3：「點進另一段對話：回到聊天、內容是該對話、再次進入搜尋時輸入框是空的。」
- F-22 → 任務 8 驗收 13 / §6.1：放只含助理訊息的 X（id 與開著的那段不同）→ 搜 X 裡的字並點那一列 →「必須切換到那段舊對話」。
- F-23 → 任務 8 驗收 8：「三種離開都清空查詢；旋轉不是離開，查詢與結果保留。」
- F-24 → 任務 6 驗收 5：「畫面上的返回鍵與系統返回鍵都回到聊天畫面，不是退出 App。」
- F-25 → 任務 6 驗收 8 / 任務 5 驗收 5：「在搜尋畫面旋轉，仍停留在搜尋畫面、輸入框仍在頂端。」（橫向 + 鍵盤的可見量不判）
- F-26 → 任務 8 驗收 12 / §5.7：「輸入一個查詢字串之後，跨行程保存的偏好內容裡找不到那個字串，鍵的組成也沒有增加」＋正向對照「那組游標必須確實還在」。
- F-27 → 任務 8 驗收 9：`am kill` 後還原 →「停在搜尋畫面、查詢為空、顯示引導文案」，不 fail。
- F-28 → 任務 8 驗收 11 的正向對照：「等歷史載入完成後用同一個查詢再看一次，結果必須包含歷史裡的對話。」
- F-29 → 任務 1 驗收 6（其餘）：冷啟動續接（檔案**存在**那一種）、空白對話不進歷史、歷史排序、開啟／刪除／送出後更新、**放一個內容損毀的對話檔後歷史仍完整顯示**（§9.1、§9.2）。
- F-30 → 任務 1 驗收 10 / §9.3：「同一個 id，用『依 id 取一筆』與『取全部再挑出同一個 id』必須得到相同的結果，包含『兩邊都找不到』。」
- F-31 → 任務 16 驗收 2：X 出現在歷史清單、標題「還沒說話」、點得開；**四種離開各走一次**＋冷啟動入口＋訊息清單為空的檔；正向對照 Y 的檔仍在。
- F-32 → 任務 16 驗收 3 / §9.4：刪掉 B → 對話檔少一個、B 的檔不在；冷啟動後找不到 B；**A 的檔仍在、內容與刪除前相同**。
- F-33 → 任務 5 驗收 8 / §1.10：進入搜尋、打字到有結果、再離開，全程狀態列不變、無模型載入紀錄；**進入前後對話檔的數量與內容逐一比對一致**；正向對照該查詢確實有非空結果。
- F-34 → 任務 8 驗收 5 / 任務 8 驗收 14 / 任務 16 驗收 4 / §6.4、§9.1：切人格產生空白對話 → 搜其開場白 → 點擊 → 回到聊天、對話不變、不重載模型、不建立新對話、**對話檔數量與內容前後完全一致**；四種離開各一次；正向對照「送出一則訊息、等回覆結束再離開 → 那段的檔存在」。
- F-35 → 任務 8 驗收 4 / §6.3：點擊目前開啟對話的命中 → 回到聊天、內容不變、不閃爍、不重載模型。
- F-36 → 任務 5 驗收 7 / §1.8：「切換人格 / 模型後的空白對話，可用其開場白搜到。」

## 情境 G：裝置 —— 中文輸入法（注音版面）＋ 鍵盤保持彈出

**環境狀態**：搜尋畫面、注音版面、測資含長中文訊息且命中位置 20 字內有 emoji。
**任務 9 驗收 13：8–11 全部在鍵盤保持彈出的狀態下判讀** —— 不得為了看清楚先收鍵盤。

- G-1 → 任務 7 驗收 2：「命中字元視覺明顯不同且位置正確；顏色取自主題，不寫死色碼。」
- G-2 → 任務 7 驗收 6：中文算繪，查詢經過裝置上的中文輸入法產生；「標示蓋在正確的字上、片段沒破版、標點正常」；測資含「命中位置與 emoji 距離在 20 字以內」的中文訊息。
- G-3 → 任務 7 驗收 5：「命中列單行省略，不破版。」
- G-4 → 任務 9 驗收 9：與 G-2 同一次觀察，多判「片段的前後兩端都要看得到『…』」。
- G-5 → 任務 9 驗收 10：「『臺』與『台』各打一次：兩者結果不同。」
- G-6 → 任務 9 驗收 11：「訊息端一則放 U+3000、另一則放 U+00A0；用中文輸入法打該詞組、中間按一般半形空白鍵 → 兩則都命中，標示恰好蓋住整個詞組。」
- G-7 → 任務 9 驗收 12（選作）：能把 U+3000 放進剪貼簿就貼進搜尋框確認仍命中；做不到記「本環境無法執行」。與 E-1b 一起試。
- G-8 → 任務 7 驗收 8 / §7.1：直向、鍵盤彈出、不捲動、不收鍵盤 → 至少 1 個對話標頭 + 4 個命中列。
- G-9 → 任務 9 驗收 8：查一個會命中多段對話開場白的詞（四件事由別條驗，本條不另跑）。

## 情境 H：裝置 —— 小螢幕（density override）

**環境狀態**：先記下 `wm density` 原值，套 606，做完 reset。**這一組會讓 Activity 重建，所以單獨做。**

- H-1 → 任務 7 驗收 9 / §7.2：「至少 1 個對話標頭 + 2 個命中列。」

## 情境 I：裝置 —— 需要跑模型的那幾條（最貴，放最後）

**環境狀態**：模型可用、能送出訊息並收到回覆。每一條都要新的一段對話。

- I-1 → 任務 13 驗收 8：「送出、串流、停止、刪除、切換人格 / 模型、冷啟動續接全部照常」（六項逐項判，任一項做不完、出錯或崩潰即 fail 並指出是哪一項）。
- I-2 → 任務 5 驗收 6：「搜尋期間送出訊息、停止生成、切換人格 / 模型等既有行為不受影響。」
- I-3 → 任務 5 驗收 2 / §1.8：串流未結束時進入搜尋並輸入回覆中會出現的字，觀察命中數自行增加。攔不到記「未能觀察」，**不得只寫四個字**，要寫模型、提示、串流時長、有沒有攔到。
- I-4 → 任務 13 驗收 4 / §8.5：「送出訊息並收到完整回覆後，該對話的時間前進，且在搜尋結果與抽屜歷史都排到最前。」
- I-5 → 任務 13 驗收 1 / §8.3：「切走再切回之後，搜尋分組順序、分組標頭的時間文字、抽屜歷史順序都與操作前相同。」
- I-6 → 任務 13 驗收 2 / §8.4：「切換離開一段沒有內容改變的對話後，該對話的存檔內容與切換前完全相同。」
- I-7 → 任務 13 驗收 5 / §8.7：B 剛更新過 → 等 ≥1 分鐘 → 產生新對話 N → 「N 與 B 都出現；N 排在 B 前面；N 標頭時間晚於 B」。
- I-8 → 任務 13 驗收 3 / §8.6：串流途中按停止（當下已有文字）→ 等 ≥30 秒 → 切走。預期一：存檔中那則回覆**結尾是中斷標記**；預期二：存檔時間比切換時刻早 ≥30 秒。
- I-9 → 任務 13 驗收 7 可判定一：四種離開方式各做一次，每次用一段新的 S，**在回覆已出現文字且還在增加時切走**；每次當場讀存檔；四種做完後冷啟動一次（只做這一次）。起點：Gemma3 1B ＋「Write a very long detailed story about the sea, at least 600 words」。
- I-10 → 任務 13 驗收 7 可判定二：「送出、等回覆結束，不做任何其他操作，直接結束行程再冷啟動 → 續接到那段對話，使用者那則訊息與完整的回覆都在。」
- I-11 → 任務 13 驗收 6：「『什麼算一次更新』在程式碼中只有一個判斷處，所有寫回對話的路徑都經過它。」（冷讀程式碼）
- I-12 → 任務 6 驗收 7：比較畫面跑得出每個模型的回答（每張卡片最後顯示「完成」且有回答文字；左上箭頭回到聊天）。

## 情境 J：隨機抽測三項（本輪範圍以外的功能）

從 `docs/product/features.md` 挑三項**不指向 conversation-search 這份 spec** 的功能，優先挑 `docs/qa-random-coverage.md` 裡最久沒被挑過的。實際操作，不讀程式碼推論，不改任何程式碼。

---

## 重置點

- 情境 D 需要**乾淨起點**，不得與任何別的情境共用狀態 → 排第一，做完才推測資。
- 情境 F 的 F-31（四種離開各重新放一次 X）、F-32（刪除）、F-34（四種離開）會改變磁碟狀態 → 各自在自己的子步驟裡重新準備，不與 F 的讀取型觀察交錯。
- 情境 H 改 density 會重建 Activity → 單獨做，做完 reset 並確認讀回原值。
- 情境 I 每一條都要新的一段對話 → 不共用。
- F-23/F-25 的旋轉會改系統設定 → 先記原值，做完還原。
