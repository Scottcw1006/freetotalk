# 這一輪改了什麼

**沒有新功能、沒有 bug、沒有改變任何一條規則要求的行為。`spec.md` 的行為規格 §1–§9 與 `spec-architecture.md` 的 §A–§G 一個字都沒有動。**

起因是收件匣兩筆使用者裁決：**決策 H 拍板**（別的文件要怎麼指向這份 spec），以及**任務 11 驗收 1、4 移除**（但那個裁決要留下來）。動到的方面有三個：`docs/product/features.md` 的 40 處引用、`spec-tasks.md` 的兩處驗收條文與三條「明確不做」、以及 `spec-decisions.md` 回到 0 項待決。

**入口檔仍然是 `v3.12/spec.md`。** 本輪的定版目錄是 **`v3.12/`**（由 `v3.11/` 複製後修改），`v3.10/` 與 `v3.11/` 一個字都沒有動。

---

# 這一輪要做的任務（完整清單）

**篩選條件是「這一輪要不要做」，不是「有沒有變動」。**

| 任務 | 這一輪要做什麼 | 變動狀態 |
|---|---|---|
| 任務 9「回歸驗證與中文算繪的手動清單」 | **每輪必做**，照原樣全跑 | 〔未變動〕 |
| 任務 10「更新架構文件」 | **`plan/architecture.html` 本輪無需更新**（理由見下），但**六條驗收照原樣冷讀重驗一次** | 〔修改：「目前狀態」換成 v3.12 的結論，六條條文一字未動〕 |
| 任務 11「摘要的兩個數字維持現狀」 | 只驗**剩下的驗收 2、3**（摘要顯示的數字）。**驗收 1、4 不要再驗** | 〔修改：驗收 1、4 移除〕 |
| 任務 14「讓補上來的條文各自有東西在驗」 | 照驗收 1、2、3 做。**驗收 4 不要再驗** | 〔修改：驗收 4 移除〕 |
| 任務 1–8、12、13 | **條文一個字都沒有改**，全驗時照原樣判 | 〔未變動〕 |

**對 RD 而言，這一輪沒有任何實作工作** —— 沒有一條規則改變、沒有一條驗收「要驗的東西」改變。**對 QA 而言，變的只有「有兩條不要再驗了」。**

**任務 10 為什麼判「無需更新」**：本輪只動了「別的文件怎麼指向這份 spec 的寫法」與「兩處驗收條文的存廢」。`plan/architecture.html` 描述的是**系統的樣子**，它不描述 spec 的分節、不描述驗收條文；**我也核對過它裡面沒有任何一處指向 spec 的路徑**，所以決策 H 的新寫法碰不到它。條文本身仍要重驗，因為那六條驗的是文件現況、而文件可能被別的輪次動過。

---

# 本輪移除或失效的任務／驗收

**編號一律保留不遞補**，每一個被拿掉的編號底下都留著「原本要求什麼、為什麼移除、缺口在哪」。

| 條文 | 處置 | 內容去了哪裡 |
|---|---|---|
| **任務 11 驗收 1**（比對層那兩個派生的量仍然存在） | 移除（使用者裁決） | `spec-tasks.md`／「明確不做」的「**（v3.12…）比對層那兩個派生的量：留著，也不要順手改用它們**」那一條 |
| **任務 11 驗收 4**（本輪不得夾帶把畫面改成呼叫那兩個量的收斂改動） | 移除（使用者裁決） | 同上 |
| **任務 14 驗收 4**（這些條文不得要求任何行為改變；變紅時回報、不要改條文去遷就實作） | 移除（**同型，PM 依交辦一併照辦**） | `spec-tasks.md`／「明確不做」的「**（v3.12…）任務 14 的核對不得要求任何行為改變**」那一條 |

**三條的代價是同一句話，而且它寫在 spec 裡三個地方（條文原處、明確不做、附錄／已知取捨與風險）**：搬過去之後**沒有任何一條驗收每輪去檢查它們了**；發現它們被違反的唯一機會，是有人去讀「明確不做」。**這是使用者知情的取捨。**

## 「同型的一併照辦」：我掃了什麼、動了什麼、沒動什麼

判準（使用者的話）：**「記的是範圍界線、不是系統行為」的驗收條文。** 我把它寫成一個可判定的問句去掃全部 14 個任務：**這一條說的是「這一輪／這個任務不准做 X」，還是「系統必須是 Y」？**

| 條文 | 判定 | 理由 |
|---|---|---|
| 任務 14 驗收 4 | **同型 → 已移除** | 它問的是這個任務的範圍與「變紅時該怎麼辦」，冷讀時答不出「被違反時使用者會遇到什麼」 |
| 任務 6 驗收 7（比較畫面既有行為完全未變） | **不同型 → 一字未動** | 主體是**回歸驗收**：這一輪的改動會經過畫面切換那條路，把比較畫面弄壞時使用者看得見。**但它句尾那句「含它仍沒有接住系統返回鍵、仍會被旋轉踢回聊天」確實是範圍界線** —— 記在這裡，你要拆開它我再處理 |
| 任務 13 驗收 7（存檔時機未被改動） | **不同型 → 一字未動** | 它的可判定是「切走一段有未存內容的對話後再切回來，內容仍在」—— **那是使用者看得見的資料遺失** |
| 任務 9 驗收 6（不進入搜尋時既有流程行為與改動前一致） | **不同型 → 一字未動** | 回歸行為，被違反時使用者直接遇到 |

---

# 改動的規格章節（RD／QA 要讀原文的地方）

- **`spec.md`**：卷首「這份文件怎麼讀」（決策檔回到 0 項）、**檔案地圖新增決策 H 的引用格式**、卷首新增 v3.12 那一段、結尾指路。**行為規格 §1–§9 一個字都沒有動。**
- **`spec-tasks.md`**：卷首新增 v3.12 那一段；**任務 10「目前狀態」**（結論換成 v3.12）；**任務 11 的說明與驗收標準**；**任務 14 驗收 4**；**「明確不做」新增三條**（兩條是搬過來的內容、一條是本輪新的範圍宣告）。
- **`spec-decisions.md`**：目前狀態改成「**本次沒有需要決策的事項**」；決策 H 整節移出；新增「本輪我自己做的判斷」三項；v3.11 那一節改標成歷史並註明「你只回應了第 2 項」；「已拍板的事項」新增兩列。
- **`spec-appendix.md`**：修訂記錄新增 v3.12；決策歷程新增兩條（**決策 H 拍板**、**任務 11 驗收 1、4 被移除**）；「兩個沒有呼叫端的派生屬性」那一段補一句指向「明確不做」；歷次收件匣升級判斷新增 v3.12 一列；已知取捨與風險新增兩條。
- **`spec-architecture.md`**：**只改了結尾那句指路裡的「目前 1 項：決策 H」**。**§A–§G 一條規則都沒有動。**
- **`docs/product/features.md`**（不在 spec 目錄裡，但本輪動了它）：40 處引用補上節名；「『對應 spec』欄怎麼看」底下新增引用格式的說明與**「這一欄的節名會過期」**這個誠實的提醒。

---

# 未改動但與本輪改動互相牽動的章節

- **`spec.md` §3.4「摘要列顯示「N 段對話・M 則訊息」」**：任務 11 剩下的驗收 2、3 守的就是它，**一字未動**。任務 11 被拿掉的那兩條**從來不是在守 §3.4**，它們守的是一次裁決的範圍界線 —— 分清楚這件事，才不會以為摘要的數字現在沒人管了。
- **`spec-appendix.md`／已知取捨與風險「摘要的兩個數字保留了兩份寫法」**：那一條寫著「屆時要重新拍板一次，不要默默選一個」，**與新搬過去的「明確不做」那一條是同一件事的兩半**（一半記風險、一半記界線）。要處理那兩個量的人，兩條都要讀。
- **`spec-architecture.md` §C.5「什麼時候該共用程式碼，什麼時候不該」**：它底下那個「具名的例外」講的正是摘要那兩個數字。**它是建議、QA 不驗**，但它是理解「為什麼這個重複被保留」的最短路徑。

---

# 交付一：`docs/product/features.md` 那 40 處的核對結果

**結論先講：本規格的 28 處全部仍然指得到，一處都沒有壞；`stop-generation` 的 12 處也全部指得到。**

**為什麼 v3.11 那些大改動沒有弄壞任何一處** —— 這件事值得寫下來，因為它不是運氣：**那 40 處指的全是「使用者看得到什麼」對應的行為規格節、或另一輪的 R 代號，沒有一處指向驗收條文，也沒有一處指向 §F。** v3.11 收掉的是 §F 與 25 條驗收條文，兩者都不在被指的範圍裡。

| 來源 | 指向 | 核對結果 |
|---|---|---|
| 啟動-07、08 | `stop-generation` 任務 4「回歸驗證」驗收 4 | 在，補上任務名 |
| 聊天-06 | R3「停止之後那則回覆不再變動」 | 在；原本括號裡寫「必須變回送出鈕那一半」，改成引用該條真正的字句 |
| 聊天-07 / 08 / 09 / 10、11 / 12 | R2 / R4 / R9 / R8 / R7 | 全部在，補節名 |
| 人格-05 | §1.1「搜尋範圍是所有對話的每一則訊息本文」 | 在 |
| 模型-06、搜尋-06 | §3.8「分組標頭顯示人格（emoji + 名稱）、模型名稱與時間」 | 在 |
| 歷史-02、搜尋-01 | §5.1「入口在歷史抽屜的歷史區塊，是一個入口列」 | 在 |
| 歷史-11 | `spec-architecture.md` §B.3「「所有對話」值得只有一個組裝處」 | **在，但那一整份檔案從 v3.11 起是建議、QA 不驗** —— 已在該格補上這句話。這是本輪唯一需要加註的一處 |
| 歷史-12 | §8「對話的「最後更新時間」什麼時候前進」（整節） | 在 |
| 搜尋-01 | §5.1、§5.3、§5.4 | 全部在 |
| 搜尋-02 / 03 / 04 / 05 | §1.7 / §2.4 / §2.9 / §3.4 | 全部在 |
| 搜尋-06 / 07 / 08 / 09 | §3.1、§3.8 / §3.3、§4.9 / §3.3 / §4.1、§4.3 | 全部在 |
| 搜尋-10 / 11 / 12 / 13 / 14 / 15 | §1.4、§1.5 / §2.6 / §1.6 / §1.2 / §1.3 / §1.1、§1.8 | 全部在 |
| 搜尋-16 / 17 / 18 / 19 / 20 / 22 | §6.2、§6.3 / §5.5 / §5.5 / §5.6 / §2.8 / §1.10、§3.9 | 全部在 |
| 比較區的說明 | `stop-generation`／「明確不做」第一列 | 在；**「第一列」是會腐爛的指法**（別人加一列它就錯了），改成指那一列的內容：「比較畫面（`CompareScreen` / `CompareViewModel`）的停止行為」 |
| 比較-15、16 | `stop-generation` `spec-appendix.md`／「我查證過、值得記下來的事實」 | 在；**原本就是「小標題」形式，不必改** |
| 保存-02 | §6.4「命中屬於一段只有開場白、從未存檔的對話」 | 在 |
| 保存-03 / 04 / 06 | R5 / R6、R10 / §5.7 | 全部在 |

**核對方式**：每一個節名都直接在目標檔案裡 grep 過（`v3.12/spec.md`、`v3.12/spec-architecture.md`、`stop-generation/v3/spec.md`），**確認那句話真的存在、而且就在那個節號底下**。這正是這個格式的用途 —— 節名 grep 不到就是大聲失敗。

**順帶查到、但不屬於這 40 處的一件事**：`stop-generation` 自己的 `spec-appendix.md`／「跨輪相依」那一列指進本規格的三個標的，**其中兩個已經指不到**（§G.3 在 v3.11 搬進了本規格的「明確不做」、決策 C 早已移進附錄）。**依交辦沒有動別輪的 spec，事實寫進了 `docs/specs/stop-generation/pm-inbox.md`。** 它不是「兩輪做法對不起來」，所以沒有升級成待決事項。

---

# 交付二：本輪「被改動的章節」清單與規則 → 條文對照

**先講清楚本輪判定為「被改動」的章節，沒有被列進來的章節等於這一輪明確宣告它不需要核對。**

- **`spec.md` §1、§3、§5、§6、§7、§8、§9**：**未被改動。** 規則文字未動；本輪新寫的文字沒有引用它們的編號；沒有驗收被新增或改寫而指名它們；本輪沒有實作改動。
- **`spec-architecture.md` §A–§G**：**未被改動。** 唯一的編輯是檔案結尾那句指路（不在任何 §底下，也不是規則）。
- **`spec.md` §2 與 §4**：**判定為「被改動」，理由是界線的第 2 項** —— 本輪新寫的文字裡出現了 `§2.11` 與 `§4.2` 這兩個編號（在「明確不做」新增的那兩條裡，一處是舉引用格式的例、一處是抄自被移除條文的原句）。**判不出來就算被改動**，所以整節核對。

**§2 逐條核對（規則被違反時，哪一條驗收會發現）**：

| 規則 | 指名到的條文 |
|---|---|
| §2.1 「空白」的規範定義 | 任務 2 驗收 9；任務 3 驗收 17（下限，BMP 掃描） |
| §2.2 零寬與 Cf 不算空白 | 任務 3 驗收 17（上限） |
| §2.3 整形 | 任務 3 驗收 17、18、19；任務 2 驗收 9 |
| §2.4 空查詢等於「沒在搜尋」 | 任務 2 驗收 2；任務 3 驗收 6；任務 8 驗收 1；任務 12 驗收 9 |
| §2.5 「算不算空查詢」與整形同一個答案 | 任務 3 驗收 22；任務 2 驗收 11 |
| §2.6 中間的空白有意義 | 任務 3 驗收 18、19 |
| §2.7 前後空白不影響結果、且不得為空 | 任務 3 驗收 5 |
| §2.8 查詢欄位裡留下什麼 | 任務 12 驗收 1、2、3、4、5、8；任務 2 驗收 10 |
| §2.9 回顯用的是欄位當下的內容 | 任務 8 驗收 10；任務 12 驗收 7 |
| §2.10 兩次移除都不得改變結果 | 任務 12 驗收 8 |
| §2.11 「什麼算空白」只能有一個答案 | 任務 2 驗收 8；任務 12 驗收 6 |

**§4 逐條核對**：

| 規則 | 指名到的條文 |
|---|---|
| §4.1 片段長度上限 60 | 任務 3 驗收 11、23 |
| §4.2 視窗選取（含 = 60 的界線） | 任務 3 驗收 23（L = 59 / 60 / 61 三案例） |
| §4.3 省略號（該有與不該有兩個方向） | 任務 3 驗收 11、25 |
| §4.4 不得切開 surrogate pair | 任務 3 驗收 16（v3.11 起指定測資形狀，兩個方向各一案例） |
| §4.5 標示區間的語意 | 任務 2 驗收 6 |
| §4.6 視窗內每一個命中都要標示 | 任務 3 驗收 10 |
| §4.7 標示長度等於整形後查詢的長度 | 任務 3 驗收 20 |
| §4.8 含換行的訊息標示必須落在正確的字上 | 任務 3 驗收 9 |
| §4.9 算繪 | 任務 7 驗收 2、5、6；任務 9 驗收 9 |

**沒有一格留空，本輪沒有補任何新條文。**

---

# 一件我沒有做、但你應該知道的事

**本輪沒有回頭替這份 spec 內部那數百處 `§X.Y` 與「任務 N 驗收 M」的引用補上節名。** 新的引用寫法從本輪起適用，**本輪新寫的文字都照它寫**；但全面補齊是一次獨立的大整理，而這一輪交辦的是 `features.md` 那 40 處。

**它已經列進 `spec-tasks.md`／「明確不做」並附觸發條件**，不讓它變成一件沒有人知道的欠帳。**殘留風險誠實寫在那裡**：spec 內部若日後又搬動一條規則的編號，指向它的內部引用會**靜靜地指到別的節** —— 那正是決策 H 要防的事，只是這一輪它還留在門外。
