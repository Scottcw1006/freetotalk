# 這一輪改了什麼

**沒有新功能、沒有 bug、沒有要求任何實作改動。** 起因是收件匣一筆使用者裁決，針對 v3.15「本輪我自己做的判斷」第 1、2 項：

- **第 1 項（任務 16 驗收 1 移除）：接受。** 規格不需要再改，只記進決策歷程。
- **第 2 項（§5.2 抽屜清單）：多寫的兩件保留，另外補上「歷史列表不含目前開著的那段」。** 這是本輪唯一的條文改動。

**入口檔是 `v3.16/spec.md`。** 定版目錄 **`v3.16/`** 由 `v3.15/` 用 `cp -r` 複製後修改。不在 v3.15 裡改的理由同上一輪：v3.15 是使用者看過並裁決的那一版。**`v3.15/` 以前一個字都沒有動。**

**`docs/product/features.md` 本輪改了一格：歷史-11**，從「尚無行為條文；只寫在 `spec-architecture.md` §B.3」改成指向 `spec.md` §5.2「抽屜必須有下面這些東西，行為如下」。

`spec-decisions.md` 維持 **0 項**。

---

# 這一輪要做的任務（完整清單）

**篩選條件是「這一輪要不要做」，不是「有沒有變動」。** 全驗照舊跑全部十六個任務。

| 任務 | 這一輪要做什麼 | 變動狀態 |
|---|---|---|
| 任務 6「抽屜入口與搜尋畫面外殼」 | 照原樣驗；**驗收 1 多一項 (vi)：歷史列表不含目前開著的那段**（附正向對照） | 〔修改：驗收 1 新增 (vi)〕 |
| 任務 9「回歸驗證與中文算繪的手動清單」 | **每輪必做**；**驗收 5 補上同一件事**（附正向對照） | 〔修改：驗收 5 補一項〕 |
| 任務 10「更新架構文件」 | **`plan/architecture.html` 本輪無需更新**：它沒有描述抽屜的歷史列表含不含開著的那段，新寫的規則是既有行為。六條照原樣冷讀重驗 | 〔修改：「目前狀態」換成 v3.16〕 |
| 任務 15「`docs/product/features.md` 指向本規格的引用仍然追得到」 | **每輪必做**。歷史-11 那一格的新引用照三條判 | 〔未變動〕 |
| 任務 1–5、7、8、11–14、16 | 條文一個字都沒有改，全驗時照原樣判 | 〔未變動〕 |

**對 RD 而言，這一輪沒有任何實作工作。**

---

# 本輪移除或失效的任務／驗收

**沒有。**

---

# 改動的規格章節（RD／QA 要讀原文的地方）

- **`spec.md`**：卷首新增 v3.16 那一段；版號目錄；**§5.2 新增一項「歷史列表不含目前開著的那段」**（附理由）。**§5.2 的節名沒有變**，所以 `features.md` 指向它的兩格都照樣 grep 得到。
- **`spec-tasks.md`**：卷首新增 v3.16 那一段；**任務 6 驗收 1 新增 (vi)**；**任務 9 驗收 5 補上同一件事**；任務 10「目前狀態」。
- **`spec-architecture.md`**：§B.3 那一段補一句，指出它看得見的那一面已經寫成 §5.2 的規則。**建議內容沒有變。**
- **`spec-decisions.md`**：目前狀態換成 v3.16（0 項）；新增「本輪我自己做的判斷」三項；v3.15 那一節標上使用者的裁決並改標成歷史；「已拍板的事項」新增一列。
- **`spec-appendix.md`**：修訂記錄新增 v3.16；決策歷程新增一條（這次裁決，含使用者採納的理由，以及它指出 PM 在 v3.15 的切法漏看了什麼）；歷次收件匣升級判斷新增 v3.16 一列。

---

# 未改動但與本輪改動互相牽動的章節

- **`spec-tasks.md`／任務 4 驗收 3、6**（「所有對話」的合併與去重）：它們管的是搜尋結果裡同一段只出現一次。新增的 (vi) 管的是同一個合併在抽屜上的症狀 —— **兩邊驗的是同一個合併的兩個出口，不是重複驗同一件事。**
- **`spec-architecture.md` §B.3**：講「今天為什麼不會重複」靠的是什麼（重新整理歷史時的過濾、切換時兩次狀態更新的先後）。那是給實作者的建議；它看得見的後果現在由 §5.2 與上面兩條驗收守。

---

# 附一：規則 → 條文的反向核對

**這張表是本輪的交付，不進 spec。**

## 本輪判定為「被改動」的章節

| 章節 | 判定依據 |
|---|---|
| `spec.md` §5「進入、離開、旋轉與行程死亡」 | §5.2 新增一項；任務 6 驗收 1、任務 9 驗收 5 改寫 |
| `spec-architecture.md` §B「搜尋看的是畫面上有什麼，不是磁碟上有什麼」 | §B.3 補一句（依「判不出來就算」列入） |

**沒有被列進上面的章節，等於本輪明確宣告它不需要核對**：`spec.md` §1–§4、§6–§9，以及 `spec-architecture.md` §A、§C–§G。本輪新寫的文字沒有明文引用它們的節號，沒有改寫的驗收指名它們，也沒有經過它們的實作改動。

## 逐條核對結果

| 規則 | 被違反時，哪一條驗收會發現 |
|---|---|
| §5.1 入口是入口列 | 任務 6 驗收 2 |
| §5.2 版面（上半部固定、只有歷史列表可捲） | 任務 6 驗收 1 (i)、任務 9 驗收 5 |
| §5.2 三個入口 | 任務 6 驗收 1 (ii)、任務 9 驗收 5 |
| §5.2 每一列顯示的東西與時間格式 | 任務 6 驗收 1 (iii)、任務 9 驗收 5 |
| §5.2 點一列、按刪除鈕 | 任務 6 驗收 1 (iv)、任務 9 驗收 5；刪除的效果另由任務 16 驗收 3 |
| **§5.2 歷史列表不含目前開著的那段（本輪新增）** | **任務 6 驗收 1 (vi)、任務 9 驗收 5（本輪新增）** |
| §5.2 沒有其他對話時的文案 | 任務 6 驗收 1 (v)、任務 9 驗收 5 |
| §5.2 列表順序 | 任務 13 驗收 1（抽屜歷史順序那一半） |
| §5.2「沒有使用者訊息的對話標題是『還沒說話』」 | 任務 16 驗收 2 |
| §5.3 進入時聚焦、鍵盤彈出 | 任務 6 驗收 3 |
| §5.4 輸入框固定頂端 | 任務 6 驗收 4 |
| §5.5 三種離開行為一致 | 任務 6 驗收 5、任務 8 驗收 3、8 |
| §5.6 旋轉不算離開 | 任務 5 驗收 5、任務 6 驗收 8、任務 8 驗收 8 |
| §5.7 查詢不跨行程保存 | 任務 8 驗收 9、12 |
| `spec-architecture.md` §B 各條 | 全部是建議，那份檔案開頭已寫明 QA 不驗 |

**結論**：沒有一條落空。本輪新增的那一項有兩條驗收在守，兩條都附正向對照。
