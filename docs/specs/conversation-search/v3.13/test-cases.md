# 對話內容搜尋 v3.13 — 本輪 test case（依環境狀態分組）

> 寫於第一次操作環境（建置 / 裝置）之前。判定依據一律是 `spec.md` 與 `spec-tasks.md` 的條文原文；下面每個觀察點引用的是原文的預期輸出（節錄），判的時候回原文對照。
> 報告照任務排，每條各自 pass / fail。

---

## 情境 S1：JVM 單元測試全套（`clean testDebugUnitTest`，準備一次）

逐條讀斷言，確認「條文指名的測資與預期」真的在測試裡。

- S1-1 → 任務 2 驗收 1：「沒有任何 Android / Compose / 持久化層的相依」（讀 import + 測試在純 JVM 跑得起來）
- S1-2 → 任務 2 驗收 2–7、9、11（§2.4、§3.2、§3.3、§3.1、§4.5、整形與折疊可直接呼叫、§2.1–§2.3、「用整形判斷，不是語言內建的 blank」）
- S1-3 → 任務 3 驗收 1–26，每條找出對應測試並核對測資與斷言：
  - 5：「`"  北京  "` 與 `"北京"` 結果相同，**且兩者都不得為空**（斷言命中筆數 > 0）」
  - 6：「案例必須包含 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0」（逐碼位確認測試原始碼裡的字元）
  - 8：「對含 U+0130 的字串斷言長度不變」
  - 16：兩個方向各一案例、測資碰得到邊界、「片段裡仍然完整含有查詢詞」、「兩個案例必須各自獨立」
  - 17：BMP 掃描斷言字面形式 `整形("a" + c + "b") == "a b"`；U+200B/C/D、U+FEFF；`"北京\r\n\t (U+0020)(U+3000)天氣"`
  - 19：四個案例走完整入口
  - 22：U+3000、U+00A0、`" \t\n"` 非搜尋，`"a"` 搜尋，BMP 掃描
  - 23：L = 59 / 60 / 61 預期片段
  - 24、26：否定 + 正向對照
- S1-4 → 任務 4 驗收 2、3、6：「所有對話必含開啟中那段（包含空白對話）、新→舊」；同 id 訊息多者；`["other","same"]` 且命中數 2
- S1-5 → 任務 12 驗收 6：「以 U+00A0 開頭的輸入必須被移除」「以 U+001C 開頭的輸入必須不被移除」
- S1-6 → 任務 12 驗收 9：「`""`、`"   "`、`"\n\t"`、U+3000、U+00A0 仍存在且通過」

## 情境 S2：scratchpad 複本上的探針 / 定點突變（專案原始碼唯讀）

- S2-1 → 任務 2 驗收 8（§2.11）：U+0020、U+00A0「兩端整形都把它收合成單一 U+0020；只有它的查詢是未搜尋狀態；放在欄位開頭時必須被移除」；U+001C「兩端整形都不得收合它；只有它的查詢算在搜尋；放在欄位開頭時必須留著」
- S2-2 → 任務 3 驗收 16：「只讓起點那一側的保護失效時，**只有**第一個案例變紅；只讓尾端那一側失效時，**只有**第二個案例變紅」（條文本身要求獨立性，所以做這兩個定點突變；不是突變全掃）
- S2-3 → 任務 3 驗收 5 的產品面：`"  北京  "` 與 `"北京"` 的完整結果物件是否相等（補判測試斷言強度）

## 情境 S3：讀程式碼 / 讀文件（不需建置）

- S3-1 → 任務 1 驗收 10（§9.3）：「對一個確實存在的 id 與一個不存在的 id 各做一次，兩條路徑的答案相同」
- S3-2 → 任務 2 驗收 10：「整形後的字串不得被寫回查詢欄位」
- S3-3 → 任務 9 驗收 2：「文字修復那一份程式碼裡不得出現搜尋的整形／折疊，也不得被它們引用」
- S3-4 → 任務 9 驗收 3：「指得出送進模型的上下文那道過濾在哪裡，而且它不是搜尋也在用的那一份」
- S3-5 → 任務 13 驗收 6：「什麼算一次更新在程式碼中只有一個判斷處，所有寫回對話的路徑都經過它」
- S3-6 → 任務 10 驗收 1、2、3、4、5、7（`plan/architecture.html`；驗收 3(i) 逐字基準取 `spec-tasks.md` 任務 10 歷史紀錄 v3.4 第 3 點）
- S3-7 → 任務 15 驗收 1、2、3（`docs/product/features.md` 每一處指向本規格的引用：節號存在、節名 grep 得到、同一節；搜尋-02/13/20 仍帶引用；不含版號目錄）
- S3-8 → 任務 14 驗收 1、2、3（清單上每一條指得出驗法；可自動化者有測試且說得出什麼錯誤實作會變紅；正向與否定兩半都被執行 —— 後者在裝置情境裡實際執行）

---

## 裝置前置 D0（準備一次）

1. 從工作區建置並**覆蓋安裝**（不得解除安裝）；記 `git rev-parse --short HEAD`、`git status --porcelain`、`lastUpdateTime`。
2. 私有資料快照（檔案清單 + sha256 baseline；大檔裝置端就地雜湊）；記下 `accelerometer_rotation`、`user_rotation`、`wm density` 原值；截圖記下輸入法版面。
3. 推入測資（檔名前綴 `qa-`，不動既有檔）：
   - T1：三段對話命中某 ASCII 詞 5 / 2 / 2 則（其中一段另含「北」但不含「北京」的訊息，使「北」與「北京」命中數不同）
   - T2：訊息「北京(U+3000)天氣」為最後一則（抽屜預覽用）；T3：訊息「北京(U+00A0)天氣」；T4：「我在北京 天氣很好」
   - T5：一則長中文訊息，查詢片語位於中段、前後 20 字內含 emoji
   - T6：只含助理訊息的對話檔（獨特 ASCII 詞）
   - T7：內容損毀的 `.json`
   - T8/T9：兩段含共同詞的對話 A（較新）/ B（較舊），B 的 updatedAt 為昨天
   - 每段測資對話的第 0 則是該人格的開場白（多段同人格 → 開場白重複命中）

## 情境 D1：入口、外殼、空狀態、無結果（開啟中為一段非空白對話）

- D1-1 → 任務 6 驗收 1 / 任務 9 驗收 5：「抽屜既有內容的位置與行為完全不變；仍是上半部固定、只有歷史列表可捲動」「抽屜除了多一個搜尋入口列之外完全沒變」
- D1-2 → 任務 6 驗收 2：「多一個搜尋入口列（不是輸入框），點擊後抽屜關閉、進入搜尋畫面」
- D1-3 → 任務 6 驗收 3：「進入時輸入框已聚焦、鍵盤自動彈出」
- D1-4 → 任務 8 驗收 1：「剛進入時顯示引導文案，沒有『找不到…』、沒有空的結果列表」
- D1-5 → 任務 8 驗收 2：「輸入保證不存在的字串，只出現『找不到…』一句，無殘留舊結果」
- D1-6 → 任務 11 驗收 3：「空查詢與無命中時，摘要列不出現」
- D1-7 → 任務 6 驗收 5 / 任務 8 驗收 8（前半）：「畫面上的返回鍵與系統返回鍵都回到聊天畫面，不是退出 App」「三種離開都清空查詢」（重置點：每次離開後重新進入，確認欄位為空、顯示引導文案）
- D1-8 → 任務 8 驗收 11：「結果區只會是：引導文案、結果列表、或『找不到…』；不得出現進度指示、骨架列或『載入中』字樣」＋正向對照「結果必須包含歷史裡的對話」

## 情境 D2：查詢欄位的空白規則（每個觀察點之前清空欄位；逐碼位讀欄位）

- D2-1 → 任務 12 驗收 1：「空欄位按空白鍵 → 欄位仍為空、顯示引導文案、不顯示『找不到』」；長按空白 → 替代做法（重複送空白鍵事件）；貼上 U+3000/U+00A0 → 記「本環境無法觀察」（承接：任務 12 驗收 6）
- D2-2 → 任務 12 驗收 2：「貼上『(3 個半形空白)北京』→ 欄位內容為『北京』」；替代做法：一次送入整串前導空白＋ASCII 內容
- D2-3 → §2.8(a)「把游標移到欄位最前面按空白鍵 → 欄位內容不變」（由任務 12 驗收 1 的規則面觀察，記錄但以條文原文判）
- D2-4 → 任務 12 驗收 3：「欄位內容為『北京(2 個空白)』時刪掉『北京』→ 欄位為空、回到未搜尋狀態」
- D2-5 → 任務 12 驗收 4：「輸入『北京』+ 空白 → 欄位為『北京 』；再輸入『天氣』→ 欄位為『北京 天氣』，且命中含『北京 天氣』的訊息」（注音輸入法）
- D2-6 → 任務 12 驗收 5：「失去焦點後尾端空白被移除、重新聚焦不得加回」（TAB 移焦）
- D2-7 → 任務 12 驗收 7 / 任務 8 驗收 10：「`"  kiwi  "` → 欄位 `"kiwi  "`、文案 `找不到符合「kiwi  」的訊息`；失焦後欄位與文案都變成 `kiwi`」
- D2-8 → 任務 12 驗收 8：「開頭移除與失焦移除前後，結果列表與摘要數字完全相同」（輸入「(前導空白)詞(2 個空白)」→ 記下結果與摘要 → 失焦 → 比對）

## 情境 D3：結果列表與可見性（查 T1 那個詞，鍵盤彈出，直向）

- D3-1 → 任務 6 驗收 9：「輸入『北』…輸入停止 1 秒內結果必須出現；接著輸入『京』…結果必須換成『北京』那一組」＋「沒有任何按下才會搜的控制項」（中間狀態要看，不批）
- D3-2 → 任務 8 驗收 7 / 任務 11 驗收 2：「三段對話分別命中 5 / 2 / 2 → 顯示『3 段對話・9 則訊息』，畫面上列出 3 + 2 + 2 筆」
- D3-3 → 任務 7 驗收 4：「超過 3 則命中時顯示 3 則 + 『還有 N 則符合』，N = 總數 − 3」
- D3-4 → 任務 7 驗收 11：「標頭上同時看得到人格 emoji、人格名稱、模型名稱、時間；換一段人格或模型不同的對話，那幾個字必須跟著改變」
- D3-5 → 任務 7 驗收 7：「分組標頭不顯示對話標題；空白對話的標頭不出現『還沒說話』」（需一段空白對話命中 → 切人格後查開場白，見 D5）
- D3-6 → 任務 8 驗收 6：「命中列沒有刪除鈕」
- D3-7 → 任務 7 驗收 8（§7.1）：「鍵盤彈出、不捲動、不收鍵盤，必須至少完整看到 1 個對話標頭 + 4 個命中列」（以 `type=ime frame` 上緣比對 bounds）
- D3-8 → 任務 7 驗收 1 / 驗收 5：「結果列表佔滿輸入框以下所有空間，可整片捲動」「命中列單行省略，不破版」
- D3-9 → 任務 6 驗收 4：「輸入框固定頂端，捲動結果時不被捲走」
- D3-10 → 任務 7 驗收 3 / §3.6：「兩段對話的第 0 則同時命中時，捲動列表不錯亂、不崩潰」（查開場白共同詞，分步捲完）
- D3-11 → 任務 6 驗收 8 / 任務 5 驗收 5 / 任務 8 驗收 8（後半）：「旋轉，仍停留在搜尋畫面、輸入框仍在頂端」「查詢與結果都在」（改方向前記原值，做完還原）
- D3-12 → 任務 7 驗收 9（§7.2）：「顯示大小調到最大，至少 1 個對話標頭 + 2 個命中列」（density 改完還原）

## 情境 D4：中文輸入法（鍵盤保持彈出，同時複驗 §7.1）

- D4-1 → 任務 9 驗收 8：「查詢一個會命中多段對話開場白的詞：出現多段對話、標示蓋在該詞上、片段未破版、摘要數字與列出的分組數一致」
- D4-2 → 任務 9 驗收 9 / 任務 7 驗收 6 / 任務 7 驗收 2：「片段前後有『…』、標示恰好蓋住輸入的那幾個字不多不少」「命中位置 20 字內含 emoji」「顏色取自主題，不寫死色碼」（截圖 + 讀程式碼顏色來源）
- D4-3 → 任務 9 驗收 10：「『臺』與『台』各打一次：兩者結果不同」
- D4-4 → 任務 9 驗收 11：「訊息端 U+3000 / U+00A0 + 一般半形空白鍵 → 兩則都命中，標示恰好蓋住整個詞組」
- D4-5 → 任務 9 驗收 12（選作）：剪貼簿放 U+3000 → 預期記「本環境無法執行」
- D4-6 → 任務 9 驗收 13：「8–11 全部在鍵盤保持彈出的狀態下判讀，同時複驗 §7.1」

## 情境 D5：點擊命中列（每個觀察點前後各做一次對話檔雜湊）

- D5-1 → 任務 8 驗收 3：「點進另一段對話：回到聊天、內容是該對話、再次進入搜尋時輸入框是空的」
- D5-2 → 任務 8 驗收 4：「點擊目前開啟對話的命中：回到聊天、內容不變、不閃爍、不重載模型」
- D5-3 → 任務 5 驗收 7 / 任務 13 驗收 5：「切換人格 / 模型後的空白對話，可用其開場白搜到」「切人格產生的空白對話仍排在最前」
- D5-4 → 任務 8 驗收 5：「搜其開場白並點擊：回到聊天、對話不變、不重載模型、不建立新對話、不把空白對話存檔」
- D5-5 → 任務 8 驗收 13：「只含助理訊息的對話檔…搜該舊檔裡的字並點擊那一列 → 必須切換到那段舊對話」
- D5-6 → 任務 8 驗收 14：「三種情境走完之後都回到聊天畫面並顯示對應的對話，且三者都不得在磁碟上新增或改寫任何對話檔」
- D5-7 → 任務 5 驗收 8：「進入搜尋、打字打到有結果、再離開，全程：狀態列不變、沒有模型載入或推論的紀錄；進入前後對話檔的數量與內容逐一比對必須一致」＋正向「確實產生了非空的結果」

## 情境 D6：跨行程（偏好、行程死亡、冷啟動）

- D6-1 → 任務 8 驗收 12：「偏好內容裡找不到那個字串，鍵的組成也沒有增加…那組游標必須確實還在（冷啟動仍續接到原本那段對話）」
- D6-2 → 任務 8 驗收 9：「行程被殺後還原：停在搜尋畫面、查詢為空、顯示引導文案」（此為可接受狀態）
- D6-3 → 任務 1 驗收 6：「冷啟動續接（檔案存在／不存在兩種）、空白對話不進歷史、歷史排序、開啟／刪除／送出後更新、放一個內容損毀的對話檔後歷史仍完整顯示」
- D6-4 → 任務 9 驗收 4：「空白對話刪檔與壞檔容錯在分層之後仍成立（實機）」

## 情境 D7：最後更新時間（需要模型推論；時刻一律取裝置時鐘）

- D7-1 → 任務 13 驗收 1：「切走再切回之後，搜尋分組順序、分組標頭的時間文字、抽屜歷史順序都與操作前相同」
- D7-2 → 任務 13 驗收 2：「切換離開一段沒有內容改變的對話後，該對話的存檔內容與切換前完全相同」（基準取 app 自己寫過一次的那份）
- D7-3 → 任務 13 驗收 4 / 任務 5 驗收 6：「送出訊息並收到完整回覆後，該對話的時間前進，且在搜尋結果與抽屜歷史都排到最前」
- D7-4 → 任務 5 驗收 2（盡力而為）：「串流未結束時進入搜尋…觀察命中數自行增加」
- D7-5 → 任務 13 驗收 3：「預期一：存檔中那則被停止的回覆文字結尾是中斷標記」「預期二：存檔中的最後更新時間比切換的時刻早 ≥30 秒」（按停止時已有文字；等 ≥30 秒不動）
- D7-6 → 任務 13 驗收 7：「切走一段有未存內容的對話後再切回來，內容仍在」
- D7-7 → 任務 13 驗收 8 / 任務 9 驗收 6 / 任務 5 驗收 6：「送出、串流、停止、刪除、切換人格 / 模型、冷啟動續接全部照常」
- D7-8 → 任務 9 驗收 7：「同一則含 U+3000 的訊息，在抽屜歷史列預覽裡逐碼位讀出來仍是 U+3000，而搜尋片段把它收成一個半形空白」

## 情境 D8：比較畫面回歸

- D8-1 → 任務 6 驗收 7：「比較畫面既有行為完全未變 —— 含它仍沒有接住系統返回鍵、仍會被旋轉踢回聊天」

## 收尾 D9

- 還原方向 / density / 輸入法版面；刪除測資與 app 在測試中建立的檔；還原快照中位元組不同的檔；重算雜湊與 baseline diff 為空。

## 隨機抽測 R（本輪範圍以外，恰好三項，來源 `docs/product/features.md`）

- 以腳本從「未指向本規格、且最久未被挑過」的項目中隨機抽三項；在 D9 還原之前實際操作。
