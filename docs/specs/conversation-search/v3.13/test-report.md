# 驗證結果總覽

**pass。** 受測版本 `8dcc271`（產品程式碼最後一次變更 `7056f24` 2026-09-14 15:25:16；裝置 `lastUpdateTime=2026-09-14 16:11:40`，工作區乾淨）。任務 1–15 的每一條現行驗收都做到，或者照條文自己的處置記「未能觀察／本環境無法觀察」，沒有一條判 fail。另有兩件條文層面的事要打回 PM（見「問題歸屬」），它們不影響本輪的 pass。

# 逐項驗收結果

## 任務 1：對話存取的分層抽出
- **驗收 6（實機）：pass。**
  - 冷啟動續接，檔案存在的情形：推入測資後冷啟動，停在偏好記住的那段（📘 老師／Gemma3 1B）。
  - 冷啟動續接，檔案不存在的情形：切人格後產生的空白對話沒有檔案，`force-stop` 再冷啟動，仍開回同人格、同模型、只有開場白的對話。
  - 空白對話不進歷史：之後抽屜 23 列裡沒有空白對話。
  - 歷史排序：新到舊。
  - 開啟：從抽屜點列可以打開對話。
  - 刪除：從抽屜刪 qa13-B，那一列消失，檔案也不見了（24 → 23）。
  - 送出後更新：送出並收完回覆後，那段在抽屜排第一。
  - 損毀檔：放了一個 `{this is not json` 的檔之後，用兩種步距蒐集抽屜，都是 23 列，等於有效且非開啟中的檔數。
- **驗收 10：pass（靜態）。** `ConversationRepository.find(id)` 的實作就是 `all().firstOrNull { it.id == id }`，兩條路徑在結構上是同一個答案，也包含找不到時的 null。§9.3 沒有指名測試，存取層依賴 Android，本輪沒有另外執行它。

## 任務 2：比對與片段的純邏輯
- 驗收 1：**pass**。`ConversationSearch.kt` 沒有任何 import，只依賴同 package 的 `Conversation.kt`（它只 import `ModelSpec`、`Persona`，兩者也沒有 import）。整份測試在 JVM 跑綠，而且我把這幾個檔複製到 scratchpad 單獨編譯也成功。
- 驗收 2：**pass**。測試 `an empty query is not a search`（含 U+3000、U+00A0，已逐碼位確認）。
- 驗收 3：**pass**。測試 `conversations come back newest first and hits stay in the order they were said`。
- 驗收 4：**pass**。測試 `a conversation shows at most three hits but reports the true total`。
- 驗收 5：**pass**。測試 `a conversation with no matching message is left out entirely`。
- 驗收 6：**pass**。測試 `highlights stay inside the snippet and never overlap`。
- 驗收 7：**pass**。`flattenToLine`、`normalizeForSearch` 是 internal，測試直接呼叫並對它們單獨斷言。
- 驗收 8：**pass**。在 scratchpad 探針裡，拿 U+0020、U+00A0、U+001C 三個字元去問四個使用點：訊息端整形、查詢端（走搜尋入口）、`isActive`、欄位開頭與失焦尾端規則。19 項 0 違反：U+0020 與 U+00A0 在四處都算空白，U+001C 在四處都不算。
- 驗收 9：**pass**。三支測試：BMP 掃描測試的斷言字面就是 `"a${c}b".flattenToLine() != "a b"`；零寬字元測試用 U+200B/C/D、U+FEFF 與 👨‍👩‍👧；混合空白收合測試。
- 驗收 10：**pass**。`ChatViewModel.onSearchQueryChange` 只套 `withoutLeadingSpace`，失焦時只套 `withoutTrailingSpace`，沒有把整形結果寫回欄位。實機上也看到「北京␠」「qzk␠␠」的尾端空白在打字時留著。
- 驗收 11：**pass**。`isActive` 用的是 `flattenToLine().isNotEmpty()`，測試 `isActive agrees with flattening…` 裡含 BMP 掃描。

## 任務 3：比對語意的單元測試
我逐條找到了對應的測試並讀過斷言。看不見的字元（U+3000、U+00A0、U+001C、零寬字元、U+0130）都以 hex 確認過確實在測試字面裡。
- 驗收 1：**pass**，兩支（使用者訊息、助理訊息）。
- 驗收 2、3、4：**pass**，各一支，字面與條文相同。
- 驗收 5：**pass**。斷言 `bare.size == 1`，而且 padded 的數量等於 bare。
- 驗收 6：**pass**。案例 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0 都在。
- 驗收 7：**pass**。
- 驗收 8：**pass**，對含 U+0130 的字串斷言長度不變。
- 驗收 9、10、11、12：**pass**。驗收 11 的命中在索引 100，斷言以「…」開頭、長度 62、標示取出來是「北京」。
- 驗收 13：**pass**，命中的是 id 0、作者是助理的那則。
- 驗收 14、15：**pass**。
- 驗收 16：**pass**。
  - 兩個案例的測資形狀都符合條文：起點側是 15 個 emoji＋1 字＋查詢詞；尾端側是 30 字＋查詢詞＋7 字＋20 個 emoji。
  - 兩支都斷言沒有落單的代理字元，也都斷言片段裡含「北京」。
  - 條文要求「兩個案例各自獨立」，我在 scratchpad 複本上各做了一次突變：拿掉起點側保護時，49 支裡只有 `cropping never splits a surrogate pair` 變紅；拿掉尾端側保護時，只有 `…at the window end` 變紅。複本已經用 `cmp` 確認還原。
- 驗收 17：**pass**，下限（字面斷言）、上限、收合三段都在。
- 驗收 18、19：**pass**。驗收 19 的四個案例都走 `searchConversations`。
- 驗收 20：**pass**，標示長度 5，取出來是「北京 天氣」。
- 驗收 21：**pass**，兩個方向都有。
- 驗收 22：**pass**，U+3000、U+00A0、`" \t\n"`、`"a"` 都在，並有 BMP 掃描。
- 驗收 23：**pass**。L=59、60、61 三個案例都有，斷言與 §4.2 的預期輸出逐字相同，並另外斷言 59 與 60 的片段不同。
- 驗收 24：**pass**，查「老師」「Qwen」「qwen」都不命中，另有正向對照「想不通」。
- 驗收 25：**pass**，同一個方法裡同時驗短訊息不補省略號、長訊息要補。
- 驗收 26：**pass**，「還沒說話」不命中，另有正向對照「有事沒事」。

## 任務 4：「所有對話」的定義與搜尋子狀態
- 驗收 2：**pass**。測試 `allThreads contains the open thread even when it is blank`，斷言順序是 `["blank","older"]`。
- 驗收 3：**pass**。測試 `allThreads keeps the in-memory copy when a thread arrives twice`。
- 驗收 6：**pass**。測試 `the in-memory copy wins even when the saved one looks newer`，測資是記憶體 same 100（兩則）、歷史 same 900（一則）、other 500，斷言 id 清單恰為 `["other","same"]`，且 same 的命中數是 2。

## 任務 5：狀態層的搜尋入口與衍生結果
- **驗收 2：未能觀察（依條文不判 fail）。** 試了兩次都沒攔到。
  - 第一次：Qwen 送出後約 6 秒就完成，等我進搜尋時訊息已經落盤，所以間接佐證不成立。
  - 第二次：Gemma 生成中查 `qcat2`，12 次取樣都是 1 段 2 則，看不到數字自己增加。
- 驗收 5：**pass**。在搜尋畫面帶著查詢 `qzk` 轉成橫向，仍在搜尋畫面，欄位是 `qzk`，摘要「3 段對話・9 則訊息」還在，輸入框在最上方（y=68）；轉回直向後結果完全一樣。
- 驗收 6：**pass**。用過搜尋之後：送出（tw1、B、c2、long）、停止（c2）、切換人格（孫子、朋友）、切換模型（Gemma → Qwen，開出新對話，舊的那段還在歷史裡）都照常運作。
- 驗收 7：**pass**。切成孫子人格後產生空白對話，用開場白裡有的字元查 `!`（依 §1.5 會命中全形「！」），第一組就是那段新對話（🧒 孫子／Qwen2.5 0.5B／18:11）。
- 驗收 8：**pass**。
  - 進入前後狀態列都是「Qwen2.5 0.5B・離線」。
  - 25 個對話檔的 sha256 前後完全相同。
  - logcat 以 pid 過濾後有 28 行，沒有一行與模型或推論有關。
  - 正向對照：查詢確實有結果（3 段 9 則）。

## 任務 6：抽屜入口與搜尋畫面外殼
- 驗收 1：**pass**。兩種步距把歷史列表捲完，上半部 5 個節點（模型、並排比較、歷史紀錄、開始新的對話、搜尋對話內容）的 bounds 全程沒變。
- 驗收 2：**pass**。「搜尋對話內容」是一個 TextButton 入口列，不是輸入框；點下去抽屜的節點消失，進入搜尋畫面。
- 驗收 3：**pass**。進入時 EditText 是 `focused=true`，`mInputShown=true`。
- 驗收 4：**pass**。查 `e`（10 段 25 則），捲 3 次，摘要已經捲出畫面，輸入框的 bounds 仍是 (148,178,1058,336)。
- 驗收 5：**pass**。畫面上的返回鍵回到聊天。系統返回鍵：第一次只收鍵盤，第二次回到聊天，焦點一直在 App 裡，沒有退出。
- 驗收 7：**pass**。比較畫面按系統返回鍵直接回到桌面；在比較畫面轉成橫向，比較畫面消失、回到聊天。兩個既有缺陷都還在。
- 驗收 8：**pass**，同任務 5 驗收 5 的那次旋轉。橫向加鍵盤時結果區只看得到 1 列，依條文不在要求範圍內。
- 驗收 9：**pass**。
  - 注音打「北」，不按任何鈕、鍵盤開著，1 秒內出現「2 段對話・5 則訊息」。
  - 接著選「京」，同樣不做其他動作，結果換成「2 段對話・4 則訊息」。
  - 畫面上只有返回鍵與清除鍵，沒有任何「按了才搜」的控制項。

## 任務 7：結果列表
- 驗收 1：**pass**。結果列表從摘要一路到畫面底，可以整片捲動（見任務 6 驗收 4）。
- 驗收 2：**pass**。截圖上命中字是主題色加粗；程式碼用的是 `MaterialTheme.colorScheme.primary`，沒有寫死色碼。
- 驗收 3：**pass**。查「今天」得到 11 段 11 則，全部是各段對話 id 0 的開場白。從頂端慢速捲到底，蒐集到 11 個分組標頭，焦點仍在 App，沒有崩潰，也沒有錯位。
- 驗收 4：**pass**。qa13-a5 顯示 3 列，下方是「還有 2 則符合」。
- 驗收 5：**pass**。咖啡那則長片段是單行，尾端以「…」截斷，沒有破版。
- 驗收 6：**pass**。
  - 用裝置注音輸入法打「咖啡」。
  - 測資是命中位置前 20 字內有 🌸 的中文長訊息。
  - 片段為「…」＋ 60 個 UTF-16 單位＋「…」，🌸 在視窗內，「咖啡」前面正好 20 個引子字。
  - 截圖上標示蓋在「咖啡」上，emoji 與標點正常。
- 驗收 7：**pass**。在切人格產生的空白對話那一組，dump 裡任何地方都沒有「還沒說話」。
- 驗收 8（§7.1）：**pass**。英文版面鍵盤上緣 1459，查 qzk 時完整看得到 3 個標頭與 7 列命中，都在上緣之上。
- 驗收 9（§7.2）：**pass**。density 606、鍵盤開著，上緣 1033，完整看得到 2 個標頭（其中第二個的下緣剛好等於 1033）與 2 列命中，截圖目視也一致。
- 驗收 11：**pass**。三組標頭都同時有 emoji、人格名、模型名、時間：🧡 姊姊 Qwen2.5 0.5B 17:32、📘 老師 SmolLM 135M 17:22、🧒 孫子 Gemma3 1B 17:12，換了對話就跟著變。

## 任務 8：空狀態、無結果、點擊與返回
- 驗收 1：**pass**。剛進入時只有「搜尋所有對話的訊息內容」，沒有「找不到」，也沒有列表。
- 驗收 2：**pass**。從 qzk 的結果改成 `qzkzzz` 之後，只有「找不到符合「qzkzzz」的訊息」，摘要與命中列都是 0。
- 驗收 3：**pass**。點了另一段對話 qa13-a5 的命中列，回到聊天，畫面是 a5 的 5 則訊息；再進搜尋時欄位是空的，顯示引導文案。這一次點擊是誤點造成的，見「驗證方式說明」的事故紀錄，觀察本身有效。
- 驗收 4：**pass**。在 a5 開著時點 a5 的命中列，回到聊天：
  - 4 次取樣都沒有載入橫幅，狀態列一直是「離線」。
  - 聊天內容與點擊前相同。
  - 檔案雜湊相同。
  - logcat 以 pid 過濾後，模型相關行數是 0。
- 驗收 5：**pass**。切人格產生空白對話 → 用開場白查 → 點那一列 → 回到聊天，內容是同一段開場白，沒有橫幅，狀態列不變。點擊前後對話檔的 sha256 完全相同，也沒有多出檔案。
- 驗收 6：**pass**。所有搜尋畫面的 dump 裡都沒有「刪除這段對話」節點。
- 驗收 7：**pass**。摘要「3 段對話・9 則訊息」，畫面列出 3＋2＋2 列。
- 驗收 8：**pass**。三種離開都清空了查詢：畫面返回鍵、系統返回鍵、點命中列，之後再進入時欄位都是空的。旋轉時查詢與結果都保留。
- 驗收 9：**pass**。帶著查詢 `qzk` 按 Home → `am kill`（pidof 為空）→ `am start` 顯示 brought to front，停在搜尋畫面，欄位是空的，顯示引導文案。
- 驗收 10：**pass**。
  - 欄位逐碼位是 `6b 69 77 69 20 20`，文案是「找不到符合「kiwi␠␠」的訊息」。
  - TAB 失焦後欄位變 `kiwi`，文案同步變成「找不到符合「kiwi」的訊息」。
- 驗收 11：**pass**。本輪所有搜尋畫面的 dump 都只有三種樣子，沒有進度指示、骨架列或「載入中」字樣；行程還原後也一樣。正向對照：查詢結果含歷史裡的對話（qzk 那三段都在歷史裡）。「載入尚未完成的那一瞬間」**未能觀察**，依條文不影響判定。
- 驗收 12：**pass**。欄位裡是 qzk 時讀偏好檔：沒有 qzk，鍵仍然只有 `last_thread_id`、`last_model`、`last_persona` 三個。正向對照：隨後冷啟動，續接到偏好記住的 qa13-a5。
- 驗收 13：**pass**。在 qa13-a5 開著時查 `zqasst`，點 qa13-asst（只有助理訊息、不同 id）那一列，畫面切到那段舊對話，顯示「zqasst 這是只有助理訊息的舊紀錄」「第二則助理訊息」與 🙌 朋友。
- 驗收 14：**pass**。三種情境（驗收 3、4、5）加驗收 13 都回到聊天，顯示對應的對話。檔案方面：
  - 驗收 4、5 的點擊前後，雜湊完全相同。
  - 驗收 13 那次，被切走的 qa13-a5 檔位元組變了，但把它解析出來和我推入的原檔比，內容完全相同（`parsed-equal True`）。那是 App 重新序列化我手寫的 JSON，不是改寫內容。使用者自己的檔案在點擊切換時，位元組都沒變。

## 任務 9：回歸驗證與中文算繪的手動清單
- 驗收 2：**pass**。`TextRepair.kt` 裡找不到 flatten 或 normalize；`ConversationSearch.kt` 也沒有引用 `repairModelText`。
- 驗收 3：**pass**。那道過濾在 `ChatViewModel.send` 裡的 `.dropWhile { !it.fromUser }`，是 inline 寫的，沒有和搜尋共用函式。
- 驗收 4：**pass**。§9.1 見任務 8 驗收 5：空白對話存檔沒有產生檔案；另外，切走只有助理訊息的 qa13-asst 時，它的檔被刪掉了。§9.2 見任務 1 驗收 6。
- 驗收 5：**pass**。抽屜項目是：模型區、並排比較、歷史紀錄標題與說明、開始新的對話、搜尋對話內容，接著是歷史列表。上半部固定、只有列表可捲。
- 驗收 6：**pass**。見任務 5 驗收 6 與任務 1 驗收 6；本輪沒有看到任何非 §8 的行為差異。
- 驗收 7：**pass**。qa13-bj 在抽屜預覽的節點文字逐碼位含 `　`；同一則訊息在搜尋片段裡是 0x20。
- 驗收 8：**pass**。注音打「今天」得到 11 段 11 則，全是開場白；截圖上標示蓋在「今天」；分組標頭數 11 等於摘要的 11 段。
- 驗收 9：**pass**，同任務 7 驗收 6。
- 驗收 10：**pass**。注音「台」只找到「中部城市很熱，台中尤其」一則；「臺」只找到「我住在臺南很久了」一則。
- 驗收 11：**pass**。注音打「北京」，按半形空白鍵，再打「天氣」，欄位逐碼位是 `5317 4eac 20 5929 6c23`。三則都命中：一般空白那則、U+00A0 那則、U+3000 那則。截圖上標示連續蓋住「北京␠天氣」，含中間的空白位置。
- 驗收 12（選作）：**本環境無法執行**。`cmd clipboard` 不存在，輸入法也打不出 U+3000。
- 驗收 13：**pass**。驗收 8–11 全程 `mInputShown=true`；§7.1 見任務 7 驗收 8。

## 任務 10：更新架構文件
- 驗收 1：**pass**。`plan/architecture.html` 有「輸入框裡留下什麼，跟拿去比對的是兩回事」一段，內容涵蓋：開頭立即移除、尾端到失焦才移除、中間不動、比對用整形後的文字、不用內建 trim（U+001C–U+001F）。
- 驗收 2：**pass**。「（只有空白）」那一列補了「這是比對層的契約：搜尋框本身打不出這種輸入（見下），但比對層仍然必須答對」。
- 驗收 3：**pass**，(i) 附一項打回 PM 的條文問題。
  - (i) 句首原則與句尾理由的字詞與 spec-tasks.md 裡 v3.4 的紀錄一致。但現行文件用「」，紀錄裡是『』（紀錄本身被包在「」裡，所以內層引號換了字形），逐字元比對會有這兩處差異。見「問題歸屬」。
  - (ii) 「不算」那一側現在是「切過去看一眼、切走都不算」，不含「按下停止」。
  - (iii) 文件其他提到「停止」的地方（第 102、104、105、109 行一帶與模組地圖）都是在描述取消與存檔機制，沒有一處宣稱按下停止不算一次更新。
- 驗收 4：**pass**。三列的路徑與職責都和現況相符：
  - `ui/SearchScreen.kt` 存在；它是無狀態 composable，參數由外部傳入，標示區間也是算好才傳進來，本身不做比對。
  - `chat/ConversationSearch.kt` 存在，負責命中、裁切與標示。
  - `data/ConversationRepository.kt` 存在，是對話的來源與去處；`ChatViewModel` 只透過它存取對話檔。
- 驗收 5：**pass**。全文找不到「最後開啟」「開啟時間」這類詞，唯一描述的時間概念是「最後更新時間」。
- 驗收 7：**pass**。那一句寫的是「命中本身有 60 個字（含）以上時裝不下引子，視窗就從命中的第一個字開始」，所以「長度 60 時留幾個引子」的答案是唯一的 0。

## 任務 11：摘要的兩個數字維持現狀
- 驗收 2：**pass**，同任務 8 驗收 7。
- 驗收 3：**pass**。空查詢（引導文案）與無命中（kiwi、qzkzzz）時，摘要都不出現。

## 任務 12：查詢欄位的空白規則
- 驗收 1：**pass**。
  - 空欄位按一次空白鍵（`keyevent 62`），欄位仍是空的，有引導文案，沒有「找不到」。
  - **長按空白鍵：用了替代做法。** 原因是這台裝置上長按空白鍵會叫出切換鍵盤選單。實際做法是對空欄位連送 5 次 `keyevent 62`，結果同樣是欄位空、有引導文案、沒有「找不到」。
  - **貼上單一 U+3000／U+00A0：本環境無法觀察。** 原因是本環境設不了剪貼簿（`cmd clipboard` 回 `No shell command implementation.`），也送不進非 ASCII（`input text "中"` 當場拋 NullPointerException，欄位不變；兩者本輪都重新確認過）。依條文，這件事由驗收 6 承接，該條 pass。
  - **環境改善回報：** 本輪確認環境**沒有改善**，仍然無法設剪貼簿，也無法送非 ASCII。
- 驗收 2：**pass。用了替代做法。** 原因是本環境設不了剪貼簿。實際做法是英文版面下單一次 `adb shell input text "%s%s%sqzk"`（3 個半形空白＋qzk）；欄位逐碼位是 `71 7a 6b`，開頭空白沒有留下。
- 驗收 3：**pass**。
  - 用注音打出「北京」，再送兩次空白鍵，欄位是 `5317 4eac 20 20`，有結果（2 段 4 則）。
  - 用 MOVE_HOME 把游標移到最前，再送 FORWARD_DEL 兩次，欄位變空，顯示引導文案，沒有「找不到」。
- 驗收 4：**pass**，同任務 9 驗收 11：欄位打到「北京␠」時空白留著，再打「天氣」成為「北京 天氣」並命中。
- 驗收 5：**pass**。在 TAB 失焦前把 `qzk␠␠` 與 `kiwi␠␠` 打進欄位，失焦後尾端都被移除；以 class 定位點回欄位重新聚焦，欄位仍是 `kiwi`，空白沒有加回。
- 驗收 6：**pass**。測試 `the field uses our whitespace rule, not the built-in one`：
  - `" 北京".withoutLeadingSpace() == "北京"`。
  - `"北京".withoutLeadingSpace() == "北京"`（兩處 U+001C 以 hex 確認）。
  - 探針也確認同一個結果。
- 驗收 7：**pass**，同任務 8 驗收 10。
- 驗收 8：**pass**。`qzk␠␠` 在失焦前後，結果區的節點文字逐行比對完全相同，唯一差異是焦點標記的位置；摘要都是「3 段對話・9 則訊息」。
- 驗收 9：**pass**。空查詢契約測試（5 個案例）在，而且通過。

## 任務 13：對話「最後更新時間」的定義
- 驗收 1（§8.3）：**pass**。
  - A=qa13-A（9/13）、B=qa13-B（9/12），共同字 qsame。操作是從抽屜開 A → 開 B → 開回 A。
  - 前後搜尋的分組順序都是 A、B，兩個標頭的時間文字都是 9/13、9/12。
  - 抽屜順序：取基準時開著的不是 A（和條文前置不同），結束時 A 開著、不在歷史列表裡。因此另外在切到 B、A 回到歷史之後再比一次，與基準（扣掉 B）完全相同。
- 驗收 2（§8.4）：**pass**。A 被 App 寫過一次之後取位元組當基準，再切走一次，存檔逐位元組相同；`updatedAt` 仍等於原值 1789285338418。
- 驗收 3（§8.6）：**pass**，兩個預期都判了。
  - 在 c2（Gemma）串流中、畫面已有 153 字時按停止，什麼都不做等 31 秒，再切到 A。
  - 預期一：存檔最後一則助理訊息以「…（已停止）」結尾。
  - 預期二：`updatedAt`=1789382050713，按停止的時刻是 1789382050809，切換的時刻是 1789382085508，兩者相差 34795 ms，大於等於 30000。
- 驗收 4（§8.5）：**pass**。在 tw1 送出 `qsend1 hi`，`updatedAt` 落在送出（…637262）與完成（…671370）之間。用「?」搜尋時 tw1 是第一組；切走之後抽屜第一列是 tw1（18:27）。
- 驗收 5（§8.7）：**pass**，同任務 5 驗收 7，新空白對話排在第一。
- 驗收 6：**pass（靜態）**。產品程式碼寫入 `updatedAt` 的只有兩處：`newThread` 建立時、`updateThread`（用 `hasSameContentAs` 判斷）。送出、串流、結算、停止、刪訊息全部經過 `updateThread`／`updateMessage`，而 `updateMessage` 也是呼叫 `updateThread`。
- 驗收 7：**pass**。
  - 在 c2 串流中送 `qmount8` 的提問；切換前 qmount8 還不在檔案裡，屬於未存內容。
  - 立刻用「換人」切走。之後存檔裡有 qmount8 提問與一則 666 字、以「…（已停止）」結尾的回覆。
  - 從抽屜切回 c2，畫面上兩者都在。
  - 前兩次想用抽屜切走時，因為捲動太慢，回覆在切換前就已結束，那兩次不算數。
- 驗收 8：**pass**。送出、串流、停止、刪除、切換人格／模型、冷啟動續接本輪全部都做過，照常運作。

## 任務 14：讓補上來的條文各自有東西在驗
- 驗收 1：**pass**。清單上每一條都指得出驗證方式：
  - 任務 1 驗收 10：程式碼位置（`ConversationRepository.find`）。
  - 任務 3 驗收 25：測試 `ellipses appear only where text was actually cut`。
  - 任務 5 驗收 8、任務 6 驗收 9、任務 7 驗收 11、任務 8 驗收 11–14：本報告寫的實機步驟與判讀點。
- 驗收 2：**pass**。可自動化的只有任務 3 驗收 25。它會變紅的錯誤實作是「無條件在頭尾補『…』」（短訊息斷言 `assertEquals("我要去北京", …)` 會失敗）以及「從不補『…』」（長訊息的 `startsWith("…")` 會失敗）。本輪沒有實際把它突變成紅；突變實跑只做了任務 3 驗收 16。
- 驗收 3：**pass**。正向與否定兩半本輪都做了：
  - 任務 3 驗收 25：同一方法兩個方向。
  - 任務 5 驗收 8：結果非空。
  - 任務 6 驗收 9：北 → 北京 真的改變。
  - 任務 7 驗收 11：換對話後標頭跟著變。
  - 任務 8 驗收 11：結果含歷史對話。
  - 任務 8 驗收 12：冷啟動續接。

## 任務 15：`docs/product/features.md` 指向本規格的引用仍然追得到
- 驗收 1：**pass**。
  - 列出 38 處（同一格多個節號分開算），逐處判了三件事：節號存在、節名 grep 得到、兩者在同一節。全部成立。
  - 其中歷史-11 指向 `spec-architecture.md` §B.3，那個檔的標題寫成「**B.3（建議）「所有對話」值得只有一個組裝處…**」，沒有「§」；該檔第 28 行的對照表寫的是「§B.3」。節名和 B.3 標題在同一行，所以判成立。這個前綴不一致記在問題歸屬。
- 驗收 2：**pass**。搜尋-02、搜尋-13、搜尋-20 的「對應 spec」欄都還帶著指向本規格的引用。
- 驗收 3：**pass**。沒有任何一處引用含版號目錄（`grep 'docs/specs/conversation-search/v[0-9]'` 為 0）。

# 邊界情況檢查

spec 沒有名為「已知衝突與邊界情況」的單獨一節，以下是散在 §1–§9 與「明確不做」裡的邊界情況，逐一列出怎麼確認的：
- **空白家族（U+3000／U+00A0／U+001C／零寬字元／ZWJ emoji）**：單元測試（字面以 hex 確認）＋探針 3 字元×4 使用點＋實機的訊息端 U+3000／U+00A0。**已處理**。
- **查詢端打不出的輸入（全空白、U+3000）**：畫面上到不了，由比對層測試承接（任務 3 驗收 6、19、22；任務 12 驗收 9），測試都還在。**已處理**。
- **surrogate pair 切點**：兩側測試，加上兩次突變確認各自獨立。**已處理**。
- **視窗分支界線 L=59／60／61**：三個測試。**已處理**。
- **跨對話訊息 id 撞號（id 0）**：實機查「今天」得到 11 組開場白，從頂端捲到底，沒崩潰也沒錯位；程式碼的列表 key 是 `"$threadId#${message.id}"`。**已處理**。
- **同 id 在記憶體與歷史各一份**：畫面上到不了，由任務 4 驗收 6 的測試承接。**已處理**。
- **歷史尚未載入完成**：沒有攔到那一瞬間，記「未能觀察」；兩半的判定都成立（任務 8 驗收 11）。
- **行程死亡**：`am kill` 還原，停在搜尋畫面、查詢為空。**已處理**。
- **旋轉**：搜尋畫面保留；橫向加鍵盤的可見量依 §7.3 不判。**已處理**。
- **只含助理訊息的舊檔被讀成空白對話**：任務 8 驗收 13。**已處理**。
- **空白且未存檔的對話被點擊**：任務 8 驗收 5，雜湊前後相同。**已處理**。
- **損毀的對話檔**：任務 1 驗收 6，歷史完整。**已處理**。
- **串流中搜尋**：未能觀察（任務 5 驗收 2）。
- **串流中切走**：任務 13 驗收 7 的第三次嘗試有真的在串流中切走，內容保留，並加上中斷標記。**已處理**。
- **停止時已經有文字**：任務 13 驗收 3。**已處理**。零文字停止：依 §8.1 註記，本規格不另立條文，本輪沒有驗。
- **小螢幕**：density 606，任務 7 驗收 9。**已處理**。
- **比較畫面的兩個既有缺陷**：仍在，符合「刻意不修」。

# 如果 fail：問題歸屬

本輪沒有 fail。以下兩件是**打回 PM** 的條文層面問題，都沒有拿來改變上面的 pass／fail：

1. **任務 10 驗收 3 (i) 的「逐字相同」基準，在產物裡沒辦法逐字元比對（第四型：證據不完全存在於產物裡）。** spec-tasks.md 的 v3.4 紀錄把那句話包在「」裡，所以句中的引號寫成『』；現行文件用的是「」。字詞完全一致，但「逐字」若指字元，這兩處永遠不會相同。建議 PM 在紀錄旁寫明「引號層級依巢狀轉換，不列入比對」，或者換一種不需要巢狀的記法（例如程式碼區塊）。
2. **任務 15：features.md 寫的是「§B.3」，spec-architecture.md 的標題寫成「B.3（建議）」。** 本輪依「節名與 B.3 標題同一行」判為成立，但驗收 1 的 (i)「節號存在」照字面 grep `§B.3` 只會找到第 28 行的對照表，不是標題。建議 PM 決定其中一邊的寫法，讓這條的判定不必依賴 QA 的解讀。

另有兩件是**寫給呼叫者**的交付事項：
- **git 歷史會洩漏上一輪的判定。** 為了確認安裝之後產品程式碼沒有再動，我跑了 `git log --stat`，看到一則 commit 訊息寫著「QA 驗 conversation-search v3.13（commit fb61297）：pass」。我沒有打開任何報告或收件匣，但「上一輪判 pass」這件事我已經知道了。建議之後叫 QA 確認建置時，只給 `git log -- app/src` 這類不帶 QA commit 訊息的指令，或是別把判定寫進 commit 訊息。
- **test-cases.md 沒有寫進你指定的位置。** 你指定寫到 `v3.13/test-cases.md`，但我的角色規則明寫「整理出來的清單是這一輪的草稿，不要存進專案」（理由是沿用舊 test case 會把上一輪的理解帶進來）。所以本輪的分組草稿放在 scratchpad（`/private/tmp/claude-501/-Users-scotthuang-AndroidStudioProjects/9ae440a4-4314-4830-922d-41a257e18608/scratchpad/r1/test-cases.md`），分組內容寫在下一節。如果使用者確實要它進專案，請明講，我再照做。

# 驗證方式說明

## 每一輪都要做的六件事
1. **標準測試指令：做了。** `./gradlew clean testDebugUnitTest` 在背景跑完；log 同時有 `:app:clean` 與 `:app:testDebugUnitTest`，沒有 UP-TO-DATE 或 FROM-CACHE。9 個 XML 共 120 支，0 failure、0 error、0 skipped，其中 ConversationSearchTest 49 支。
2. **每一條指名了測試的驗收：做了。** 任務 2、3（1–26）、4（2、3、6）、12（6、9）、14 都找到了測試並讀了斷言，看不見的字元以 hex 確認。任務 3 驗收 16 的「各自獨立」是條文明文要求，所以在 scratchpad 複本上各做了一次突變實跑；這是那一條本身要求的，不是突變全掃，其他條文沒有做突變。任務 2 驗收 8 另外用探針執行。
3. **每一條邊界情況：做了**，見上一節。
4. **每一條實機條文：在裝置上做了。** Pixel 9／Android 16，建置時間已確認晚於產品程式碼。沒有降級成只看程式碼。未能觀察的有：任務 5 驗收 2、任務 8 驗收 11 的載入瞬間、任務 12 驗收 1 的貼上案例、任務 9 驗收 12（選作）。
5. **隨機抽測恰好三項：做了**，見下方專節。
6. **真實資料快照／還原／逐位元組驗證：做了。**
   - 開始前快照：16 個小檔（清單、baseline、裝置端計數都是 16），3 個大檔在裝置端算 sha256。
   - 推測資前確認裝置 `/data/local/tmp` 沒有別輪殘留、本機來源目錄只有本輪的 11 個檔；推完之後 `comm -13` 只多出本輪的 11 個檔。
   - 結束後刪除 9 個多出來的檔（qa13-asst、qa13-B 已在測試中被 App 刪掉），推回 `profileInstalled` 與 `session.xml`；檔案清單相同，16 個小檔逐位元組相同，3 個大檔雜湊相同。
   - 之後為了補確認又啟動了兩次 App（非 ASCII 限制、模型切換），每次都照協定再還原並重驗一次，最後一次仍然是 16／16、3／3 全部相同。`/data/local/tmp/qa` 已經刪除。

## test case 分組（本輪重新整理，沒有沿用任何先前的版本）
整理出 11 個情境：
- **情境 0（不經過裝置）**：任務 1 驗收 10；任務 2 全部；任務 3 全部；任務 4 驗收 2、3、6；任務 9 驗收 2、3；任務 10；任務 12 驗收 6、9；任務 13 驗收 6；任務 14；任務 15。
- **A（推測資＋壞檔後冷啟動）**：任務 1 驗收 6（續接檔案存在、損毀檔）、任務 9 驗收 4（§9.2）、5、7、任務 6 驗收 1。
- **B（qzk 5／2／2）**：任務 8 驗收 1、2、6、7、11；任務 11 驗收 2、3；任務 7 驗收 1、4、5、8、11；任務 6 驗收 2、3、4；任務 5 驗收 8。
- **C（注音輸入，鍵盤保持彈出）**：任務 6 驗收 9、任務 12 驗收 4、任務 9 驗收 8–11、13、任務 7 驗收 2、3、6。
- **D（欄位空白規則，ASCII）**：任務 12 驗收 1、2、5、7、8；任務 8 驗收 10。任務 12 驗收 3 因為要用注音，放到最後重新切回注音時才做。
- **E（離開、旋轉、行程死亡、偏好）**：任務 6 驗收 5、8；任務 5 驗收 5；任務 8 驗收 8、9、12。
- **F（點擊命中列）**：任務 8 驗收 3、4、13、14。
- **G（需要乾淨起點，單獨做：切人格產生空白對話）**：任務 8 驗收 5、任務 5 驗收 7、任務 13 驗收 5、任務 7 驗收 7、任務 9 驗收 4（§9.1）、任務 1 驗收 6（空白不進歷史、檔案不存在的續接）。
- **H（需要乾淨起點，單獨做：時間相關）**：任務 13 驗收 1、2、3、4、7、8；任務 5 驗收 2、6；任務 9 驗收 6；任務 1 驗收 6（開啟、刪除、送出後更新）。
- **I（單獨做：density 606）**：任務 7 驗收 9。
- **J（比較畫面）**：任務 6 驗收 7。

**需要乾淨起點而單獨做的**：G（雜湊基準必須在切人格之後、點擊之前取）、H 的 §8.3／§8.4（前後比對，不能夾別的操作）與 §8.6（按停止之後 30 秒內不得做任何事）、I（改系統設定）。

## 本輪的事故紀錄（照實寫）
1. **一次沒有守衛的批次點錯了節點。** 那一步想「點回輸入框」，helper 用文字 `qzk` 找節點，找到的第一個是命中列「第1則 qzk 測試」，於是點開了 qa13-a5、離開了搜尋畫面。之後的 `␠␠kiwi␠␠` 與 TAB 被送到聊天畫面上，其中一個按鍵事件打開了抽屜。
   - 事後唯讀稽核的結果：25 個對話檔雜湊與事前完全相同；沒有新增檔也沒有檔被刪；任何檔都不含 kiwi；聊天輸入框是空的；沒有送出訊息，也沒有在生成。偏好改指向 qa13-a5 是 App 自己寫的，已在還原時推回。
   - **沒有任何東西產生在受測 App 以外**，焦點全程在 App 內。之後所有批次都加上守衛，並改成以 class 定位輸入框。
2. **一次在鍵盤開著時從抽屜點列。** 從搜尋回到聊天後鍵盤仍開著，抽屜腳本的一次點擊落在鍵盤上。事後確認聊天輸入框是空的、App 內狀態沒有異常，沒有離開 App。抽屜腳本已經加上「鍵盤開著就中止」。

## 裝置設定的原值與還原
- `wm density`：原值 420、無 override → 測試中設成 606 → `reset` → 讀回 420、無 override。
- `accelerometer_rotation`：原值 1；`user_rotation`：原值 0。測試中改過 3 次，每次都還原並讀回 1／0。
- 輸入法：`default_input_method` 原值 LatinIME，全程未改。版面原值是**注音**，測試中切到英文，結束前切回注音並截圖確認。

# 本輪驗證範圍

全驗。任務 1–15 的每一條現行驗收標準與每一個邊界情況都驗了。你沒有給縮小範圍，所以不需要說明縮範圍的理由。

# 環境文件的處置

`docs/qa-environment.md`：
- **驗過的條目**：第 0、1、2、3（a）、4、5、5a、6.1–6.5、7、8、9、10、11、12、13 節。依賴到的每一條都先跑了它的確認方式；6.2 的非 ASCII 例外與 6.5 的剪貼簿，是在依賴之前補跑的。
- **確認失敗而修正的**：沒有條目失敗。只有第 11 節，把上一輪記下的具體時差數字改成「每輪都不同」，不再保留某一輪的具體值。
- **新增的**：
  - 第 0 節：zsh 的 `=` 展開、`--include=*.kt` 被 glob。
  - 第 1 節：`clean` 之後只花幾秒不代表沒跑。
  - 第 4 節：裝置端 toybox `ls` 不支援 `--time-style`；還原後只要再啟動一次 App 就要整個重跑；前後位元組比對要先讓 App 自己寫過一次再取基準。
  - 第 5 節：用文字找節點會找到清單列；content-desc 常常掛在子節點；富文字在 dump 裡被拆成多個節點；鍵盤高度隨版面變；在面板上 swipe 會落在鍵盤上；清單有重複文字時用 y 位移對齊；數列時不要只用文字。
  - 第 6 節：地球鍵的操作守則、雙音節一次送、MOVE_HOME／FORWARD_DEL。
  - 第 8 節：改 density 會讓 Activity 重建。
  - 第 12 節：突變要用「斷言原字串存在再取代」，並用 cmp 確認還原。
  - 第 13 節：helper 回報錯誤時批次要接 `|| exit 1`、打字前要守衛焦點，並附上自測方式。
- **刪掉的越界內容**：逐節用「換成完全不同的產品還成立嗎」檢查過，沒有找到產品觀察，所以沒有刪除。

# 本輪隨機抽測的三項功能

依 `docs/qa-random-coverage.md`，從總覽中不指向本規格、且從未被抽過的 62 項裡隨機抽出三項。依抽選規則，比較-18 標的是「尚無 spec」所以可以抽；但它描述的行為和本 spec 任務 6 驗收 7 的旋轉那一半重疊，這一點要讓你知道。三項都有實際操作，沒有讀程式碼推論，也沒有改任何程式碼。

- **聊天-04（回覆一個字一個字慢慢出現）**：在 qa13-long（Gemma3 1B）用注音送出「下雨」，收起鍵盤後在串流中每隔約 1.2 秒連拍截圖。第一張只有三個點的等待泡泡，第三張已經有好幾段文字，生成結束後文字又更長。**與總覽描述相符。**
- **比較-18（在比較畫面轉螢幕會被丟回聊天）**：從抽屜進入比較畫面（看得到「模型比較」），把系統方向改成橫向後，比較畫面的節點消失，「換人」出現，也就是停在聊天畫面。方向已經還原。**與總覽描述相符**；總覽把這一項標為 `※未經查證`，本輪實際操作過一次。
- **聊天-06（回覆中送出鈕變成轉圈圈的停止鈕，送不出下一句）**：同一次操作的截圖裡，閒置時輸入列右邊是箭頭形送出鈕；生成中那個位置換成轉圈的圓形進度，畫面上沒有送出鈕；生成結束後箭頭送出鈕回來了。**與總覽描述相符。**（第一次想用 dump 判讀按鈕種類的取樣不可靠，已經作廢，改用截圖判讀。）

三項都沒有發現與總覽不符的地方，也沒有拿它們去改變上面任何一條 pass／fail。

# 隨機挑選記錄的處置

`docs/qa-random-coverage.md` 已存在，本輪**追加**三行（日期 2026-09-14）：`聊天-04`、`比較-18`、`聊天-06`。那份檔案只記條目與日期，沒有寫任何結果。
