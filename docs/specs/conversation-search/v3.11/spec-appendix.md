# 附錄（對話內容搜尋 spec）

> 這是 `spec.md` 的附錄。入口與閱讀順序見 `spec.md` 開頭的「這份文件怎麼讀」，那裡也有索引與**五份檔案的地圖**。
> **這一節放的是歷史與取捨，不是你要做的事** —— 要做什麼看 `spec-tasks.md`，什麼算對看 `spec.md` 的行為規格。
> **本檔的節**：修訂記錄 → 舊 spec 代號對照表 → 決策歷程 → **歷次全驗的結論** → 已知取捨與風險。

## 修訂記錄

| 版本 | 內容 |
|---|---|
| v3.11（本輪） | **沒有新功能、沒有 bug、沒有改變任何一條規則要求的行為。** 處理收件匣四筆使用者裁決：(1) **資料夾版控** —— 每一輪的定版留在自己的目錄裡（本輪 `v3.11/`），上一輪原封不動；跨輪的東西留在上一層。(2) **架構決策 §A–§G 依准入判準重新分類，§F 整節收掉。** 說得出使用者後果的搬進行為規格或驗收條文（`spec.md` 因此新增 **§2.11「什麼算空白只能有一個答案」**與 **§9「對話存取層必須保留的既有行為」**，**規則原文搬家、一個字都沒有改**）；其餘留在 `spec-architecture.md` 當**建議**，**QA 不驗架構**；**§F 那七條是通則**，權威在 PM 與 QA 的角色定義裡，spec 端**只留一句指路、不留摘要**。`spec-architecture.md` 開頭新增一張「原本那 43 條各自去了哪裡」的對照表。(3) **驗收條文的一次性全面掃描**：判準是「每一條驗收都要指得出它守的那條規則，而那條規則要說得出使用者會遇到什麼」。**編號一律保留不遞補**；被移除的是**條文**不是**要求**，每一條底下都寫著它原本守什麼、現在還有沒有東西在守（**逐條清單在 `changes-v3.11.md`**）。其中使用者點名的「未被改動」那七條連同整節判定基準一起處理掉，**決策 F 因此結案，而且是被取消不是被解決**。(4) **任務 3 驗收 16 指定測資形狀** —— 2026-09-10 的全驗以突變證明它的鑑別力是 **0**，條文現在寫明兩個方向（視窗起點、視窗尾端）各要一個真的會碰到 surrogate pair 邊界的案例。**完整對照表在 `changes-v3.11.md`**（機械資訊不進 spec）。**另新增一項待決事項（決策 H）**：`docs/product/features.md` 那三十幾處指向 spec 的路徑因為版號目錄而全部斷掉，修法有四種、成本差很多，而且同一題在另一輪一模一樣，**依「兩輪之間的衝突不得由 PM 自己選一邊」等使用者拍板**。**沒有碰** `plan/architecture.html`（只讀）、`docs/specs/stop-generation/`、`docs/product/features.md`、`docs/qa-environment.md`、`docs/qa-coverage.md`。 |
| v3.10 | **沒有新功能、沒有 bug。處理一筆使用者裁決的三項，`spec-decisions.md` 回到 0 項待決。** (1) **決策 F 拍為 (a)：維持現狀** —— 七條「一字未改」類驗收的三項判定**一字不改**，警示框留著並改寫成「現行結論 → 問題仍在 → QA 怎麼辦」。**它是因等待版控決定而暫緩，不是因為問題消失。**（**v3.11 註**：這一題在 v3.11 整個消失了 —— 那七條本身被移除，理由是它們答不出使用者後果；版控仍然會做，但理由換了。）(2) **§6.5 降級成建議** —— **任務 8 驗收 14 的結構那一半整條移除**（抓到違反 0 次、害正確的實作差點被判 fail 3 次、修它花了 3 輪），**行為那一半保留、一字未改**。**這是使用者自己套用新的驗收通則看出來的**，也是那條通則篩掉的第一個東西。(3) **決策 G 結案**：第一點**因第二項而整個消失 —— 是被取消，不是被解決**；第二點**照修** —— 任務 12 驗收 2 現在明文允許替代做法、要求每次用了都在報告裡寫明原因、並要求**發現環境已能設剪貼簿時必須回報**。 |
| v3.9 | **沒有新功能、沒有 bug、沒有改動任何一條規則。** 收兩筆收件匣，做四件事：(1) **依使用者裁決動任務 10 的兩條驗收** —— **驗收 6 移除**（編號不遞補）、**驗收 4 保留核對、拿掉「必須寫進本節」**。(2) **任務 10 的兩段執行紀錄一個字都沒刪**，移到該任務的驗收標準之後、標明是歷史；**留在本檔而不是移進附錄**，理由是**驗收 3 的逐字基準就在那段紀錄裡**。(3) **決策 E 結案 —— 不是被拍板，是問題本身被取消了**；`spec-decisions.md` 換成新的兩項（決策 F、G）。(4) **依使用者新定下的驗收通則掃過本規格**，掃出來的同型條文**一條都沒有動，只列進 `changes-v3.9.md`**；同一份變更說明另附 **§A–§G 的一次盤點** —— **那份盤點就是 v3.11 重新分類的材料**。 |
| v3.8 | **沒有新功能、沒有 bug、沒有改動任何一條規則的實質內容，也沒有改動任何一條驗收「要驗的東西」。** (1) **決策 E 的裁決 —— 使用者換了一個角度看它**：真正的問題是**兩輪同時進行時的衝突怎麼處理**，定下的規則是「衝突時 PM 不得自己選一邊、拉給使用者判斷、拍板後由 PM 同步兩邊的 spec」，並誠實寫出**它不是通知機制**。(2) **依使用者新定下的寫法規則掃過五份檔案**：拿掉會被別人在另一輪改掉而自己變錯的東西（寫死的測試檔數量、寫死的條文數量、對另一輪那個中斷標記的字元數、指向已不存在檔案的引用）。**界線是 PM 畫的**，寫在 `changes-v3.8.md`。(3) **使用者已逐項接受 v3.7 那四項自主判斷**；其中附帶的那個通則（PM 能不能在交辦範圍外順手補零成本的洞）**沒有拍板，不得當成已成立**。 |
| v3.7 | **沒有新功能、沒有改動任何一條規則，也沒有改動任何一條既有驗收「要驗的東西」**（唯一新增的條文是任務 3 驗收 26）。全部是處理 2026-09-09 那次全驗回來的結果，四件事：任務 10 的執行紀錄補記與「目前狀態」更新；兩處可判定性改寫（§6.5／任務 8 驗收 14 的收尾句、以及「一字未改」類的七條 —— 專案沒有版本控制，**條文要求的證據不存在於產物裡**，記成**第 6 型失效**）；spec 不再引用同目錄的全驗報告（改在本檔新增「歷次全驗的結論」）；任務 4 驗收 6 補記現況。**新增決策 E。** |
| v3.6 | **沒有新功能，把三件懸而未決的事收掉；`spec-decisions.md` 回到 0 項待決。** (1) **決策 C 拍板** —— **停止生成從此算一次更新**：§8.2 那張清單裡「停止生成」**整條移除**，§8.1 點名它，**§8.6 換掉節名與判定方式**。**線沒有移動，是這件事從線的一邊換到另一邊。** (2) **決策 D 拍板為不升格**；一併更正「三次同型」這個沒被查證過的計次（A 型 2 次、B 型 1 次）；附帶採納一條**寫法紀律**（理由只寫規則實際依賴的性質）。(3) **任務 8 驗收 14 與 §6.5 的結構條文改寫**成「禁止什麼」而不是「允許幾個分支」。另新增 §F.6 與任務 9 驗收 14。 |
| v3.5 | **沒有改動任何一條規則。** 依使用者裁決把**決策 B 拍板為選項 (d) + 一次性補掃**，落成三件事：(1) 新增反向核對的硬性條文（範圍限於**被改動的章節**，含四項可判定界線與三條防退化要求）；(2) **執行一次性全面補掃**：對當時現存的 101 條規則逐條核對，**補上 13 條驗收條文**，並對 8 處**明寫「由哪一層守、為什麼不需要獨立條文」**；新增**任務 14**。(3) 決策 B 移出 `spec-decisions.md`。**(4) 當輪收件匣又進來一筆 RD 觀察**（§8.2 第三點對「按下停止只會關掉標記」的宣稱不成立），**升級成決策 C 與決策 D**。 |
| v3.4 | **沒有改動任何一條規則，只有記錄與搬家。** (1) 把任務 10 的執行結果記進該節；(2) **複核時發現 RD 沒有提到驗收 7，而它確實沒有做**；(3) 改寫任務 10 驗收 4 與 6 的**執行主體**（舊寫法要求 RD 把結論寫進 spec，而 RD 不得改 spec）；(4) 把「需要你決策的事項」拆成獨立的 `spec-decisions.md`。 |
| v3.3 | **只補驗收條文，一條規則都沒有改。** 依當輪的 QA 全驗指出的三項覆蓋缺口補上「會發現違反」的條文：§4.2 的視窗選取分支、§3.5 的「歷史版本時間較新時仍取記憶體那份」、§1.2 的元資料不參與比對。**§4.2 的 off-by-one 是實作沒照規則做，不是規則要改。** 同輪把本文依原有的六節切成四個檔案。 |
| v3.2 | **只修正舉證，沒有改動任何一條規則。** 處理一筆 RD 實測觀察：v3 宣稱「Kotlin 的 `trim()` 與 §2.1 兩個方向都不相等」，其中「不會移除 U+00A0 / U+2007 / U+202F」那一半是錯的。**我獨立查證後認定 RD 成立**（Kotlin 的 `Char.isWhitespace()` 是 Java `isWhitespace` 與 `isSpaceChar` 的聯集，所以 Zs 全部會被移除；落差只剩 U+001C–U+001F 一個方向）。 |
| v3.1 | 兩項使用者回覆的落實，**沒有新增任何規則**。(1) **決策 A 裁決 (a)（兩邊一起改）**；(2) **裁決一的原意經回問確認是 (c)（就只是留著，不動 UI）**，不是上一輪讀成的 (b)。 |
| v3 | **依三項使用者裁決改動規則，其中兩項會改變現行實作的行為。** (1) 摘要的兩個數字（**讀法已於 v3.1 更正為 (c)**）；(2) 查詢欄位的空白處理 —— **推翻舊條文「查詢欄位保存原樣輸入、不得 trim」**，改寫成 §2.8–§2.10；(3) 對話最後更新時間 —— 新增行為規格 §8，新增任務 13。 |
| v2 | 全面重寫組織方式：從「決策沉積層」改為「功能 → 行為 → 架構 → 任務」。**沒有改動任何一條已成立的規則**；代號從 64 個降到 13 個引用標的；把原本混在「已知衝突與邊界情況」一節裡的三種東西（必須處理的需求／範圍排除／事後檢討）分別移到行為規格、明確不做、決策歷程。新增「功能總覽」。刪去「現有架構觀察」一節。 |
| v1 第四輪 | 修正被實測推翻的事實前提（見決策歷程「查詢端理由的方向反了」）；空查詢判斷改用同一份整形；「所有對話」明訂合併規則與去重。 |
| v1 第三輪 | 補上查詢端管線、空白的規範定義（Unicode 空白類、明列排除 Cf）、共用與否的判準；旋轉語意定案；修正兩條不可達的條文。 |
| v1 第二輪 | 畫面位置從「抽屜內搜尋」改為獨立畫面（實機量測，見決策歷程）。 |
| v1 第一輪 | 「不排除任何訊息」經使用者裁決；對話存取分層經使用者裁決納入。 |

## 舊 spec 代號對照表

供人工比對用。**v1 那份累積式的舊 spec**（v2 重寫時被取代，**現在已經不在本目錄裡**）的**每一個** D / S / E 代號都在這裡。
（**v3.8 改寫**：這裡原本寫死了那份舊檔的檔名，而那個檔案已經不存在 —— 讀的人會去追一個追不到的路徑。這張表要的性質只有一個：舊代號到新位置的對照是完整的，那件事不依賴舊檔還在不在。）

> **（v3.11）下面「現在在新文件的哪一節」欄裡的 §A–§G 是 v3.10 之前的位置。** 本輪把 §A–§G 重新分類過，有的搬進行為規格、有的收掉、有的降成建議。**這張表刻意不逐格改寫** —— 逐格改寫會多出 30 幾個可以出錯的地方，而它們指的是同一件事。**要找某一條架構決策現在在哪裡，看 `spec-architecture.md` 開頭那張「原本那 43 條各自去了哪裡」的對照表**，那是唯一一份需要維護的。

### D 系列（架構決策，30 條）

| 舊代號 | 舊的內容摘要 | 現在在新文件的哪一節 |
|---|---|---|
| D1 | 比對邏輯放純 Kotlin 層、零 Android 相依、不新開套件、內部函式可測 | 架構決策 §A.1、§A.5 |
| D2 | 資料來源是記憶體狀態，不經存取層、不回硬碟；存取層不得有查詢方法 | 架構決策 §B.1、§B.2；行為 §1.8、§1.10 |
| D3 | 搜尋子狀態巢狀掛在聊天狀態下；「所有對話」的存在理由 | 架構決策 §B.1、§B.5 |
| D3.1 | 「所有對話」的合併規則：去重、開啟中那份優先、不變式要寫進註解 | 架構決策 §B.3；行為 §3.5；任務 4 驗收 3、4、6 |
| D4 | （傘節點）搜尋畫在獨立畫面、狀態留在聊天那份 | 架構決策 §D（整節） |
| D4.0 | 原本的「抽屜內搜尋」為什麼不成立（實機量到只剩 20px） | 附錄／決策歷程「搜尋為什麼變成獨立畫面」 |
| D4.1 | 定案：獨立畫面、入口列、旗標切換、無狀態、輸入框頂端自動聚焦 | 架構決策 §D.2、§D.3；行為 §5.1–§5.4 |
| D4.2 | 通則：即時過濾的輸入框必須在捲動容器頂端 | 架構決策 §D.1；行為 §7 |
| D4.3 | 旋轉語意：搜尋旗標必須撐過設定變更 | 架構決策 §D.4；行為 §5.6、§5.7 |
| D4.4 | 狀態歸屬：不新增第二個狀態來源；畫面不得自己去拿 | 架構決策 §D.3 |
| D5 | （傘節點）比對語意 S1–S10 | 行為 §1、§2、§4；架構決策 §C.7 |
| D6 | 命中的資料形狀、標示區間語意、畫面不得自己再算一次 | 架構決策 §A.2、§A.4；行為 §4.5 |
| D6.1 | 空查詢判斷必須用同一份整形，不得用內建 blank 判斷 | 架構決策 §C.6；行為 §2.5 |
| D6.2 | 查詢欄位存原樣輸入，不得寫回整形後的字串 | **v3 已改**：前半（原樣輸入）被 §2.8 取代；後半（不得寫回整形後的字串）仍成立，見 §2.8d 與任務 2 驗收 10 |
| D7 | （傘節點）查詢端與訊息端共用同一個座標系 | 架構決策 §C（整節） |
| D7.0 | 訊息端的五個步驟順序與片段參數（60 / 20 / 省略號規則） | 架構決策 §C.1；行為 §4.1–§4.6 |
| D7.1 | 查詢端套用完全相同的整形與折疊 | 架構決策 §C.3；行為 §2.3、§2.6 |
| D7.2 | 「空白」的規範定義：Unicode 空白類，明文排除 Cf | 行為 §2.1、§2.2、§2.3；架構決策 §C.4、§C.8 |
| D7.3 | 什麼時候該共用程式碼、什麼時候不該的判準 | 架構決策 §C.5（**v3.1 起附一個具名例外**：摘要的兩個數字） |
| D8 | 每對話最多 3 則命中但回報總數；上限在純邏輯層 | 行為 §3.3；架構決策 §A.3 |
| D9 | （傘節點）引入對話存取分層，使用者裁決 | 架構決策 §E（整節） |
| D9.0 | 定位：分層一致性，不是搜尋的需要；不得寫假理由 | 架構決策 §E.1 |
| D9.1 | 只包對話語料，不包「上次停在哪」那個游標 | 架構決策 §E.2 |
| D9.2 | 位置與建構方式：不做單例、不引入 DI | 架構決策 §E.4（改寫成「生命週期不得跨畫面共享」這個性質） |
| D9.3 | 純轉發，不加快取、不加狀態；沒有邏輯所以不需要測試 | 架構決策 §E.3 |
| D9.4 | API 面四個方法，不新增沒有呼叫端的方法；「不改成單檔讀取」 | 架構決策 §E.5、§E.6 |
| D9.5 | 這一層不寫 JVM 測試，前提是保持零邏輯 | 架構決策 §E.3；明確不做「對話存取層沒有自動化測試」 |
| D9.6 | 「當前對話 + 歷史」的組裝留在狀態層 | 架構決策 §B.6 |
| D10 | 比對用的內部函式用 `internal` 不要 `private` | 架構決策 §A.5（改寫成性質：「必須能被單元測試直接呼叫並單獨斷言」）；任務 2 驗收 7 |
| D11 | 驗證責任分工：語意歸單元測試、實機只驗算繪 | 架構決策 §F（整節）（**v3.11：整節收掉**，見 `spec-architecture.md` 開頭） |

### S 系列（比對語意，10 條）

| 舊代號 | 舊的內容摘要 | 現在在新文件的哪一節 |
|---|---|---|
| S1 | 即時搜尋、無送出鈕，允許 debounce | 行為 §1.7 |
| S2 | 子字串比對，不斷詞、不排序、不做多詞 AND | 行為 §1.3 |
| S3 | 大小寫不敏感 | 行為 §1.4 |
| S4 | 全形 ASCII 折疊；U+3000 不歸這裡、歸整形（且必須寫進註解） | 行為 §1.5；架構決策 §C.7 |
| S5 | 不做繁簡 / 注音 / 拼音 / 同義詞 | 行為 §1.6 |
| S6 | 正規化必須逐字元、長度不變 | 架構決策 §C.2；任務 3 驗收 8 |
| S7 | 空查詢 = 不顯示結果、顯示引導文案 | 行為 §2.4 |
| S8 | 查詢與訊息套用相同整形；連續空白算一個；中間空白仍有意義 | 行為 §2.3、§2.6 |
| S9 | 不排除任何訊息，包含開場白（**使用者裁決**）；不得與模型上下文過濾共用 | 行為 §1.1；架構決策 §C.5 |
| S10 | 不搜標題、人格名、模型名 | 行為 §1.2（**v3.3 起有對應的驗收條文**：任務 3 驗收 24） |

### E 系列（已知衝突與邊界情況，24 條）

舊文件把三種不同性質的東西混在同一節裡。這裡標明每條的真實性質，並指向它現在該在的地方。

| 舊代號 | 舊的內容摘要 | 現在在新文件的哪一節 |
|---|---|---|
| E1 | 訊息編號跨對話會撞號，跨對話列表要避開 | 行為 §3.6（它其實是**需求**，不是邊界情況） |
| E2 | 歷史不含開啟中的那段，只搜歷史會漏掉最可能被搜的那段 | 架構決策 §B.1、§B.2 理由 1；行為 §1.8 |
| E3 | 磁碟文字 ≠ 顯示文字（讀出時才做文字修復） | 架構決策 §B.2 理由 4 |
| E4 | 整形會改索引、正規化不能改索引，混在一起做必然錯位 | 架構決策 §C.1、§C.2 |
| E5 | 開場白造成大量重複命中，已接受，不得加去重或降權 | 行為 §3.7（**需求**，不是「不做」） |
| E6 | 串流中的訊息會被搜到；低階裝置未實測 | 行為 §1.8（需求）；附錄／已知取捨與風險（未實測的部分） |
| E7 | 初次載入前搜尋是空的，不為它做載入狀態 | 行為 §1.9 |
| E8 | 空白對話會出現在結果裡：五個子條（判準、標頭、點擊分支、巧合、不加特例） | 行為 §1.8、§3.1、§3.8、§6.1、§6.4、§6.5 |
| E9 | 標題對空白對話回傳「還沒說話」，標頭不得用標題 | 行為 §3.8 |
| E10 | 分層搬移帶進來的六項風險 | 架構決策 §E.6、§E.7（**v3.11：可觀察的那幾件搬進行為 §9**）；任務 1 驗收 6、10；附錄／已知取捨與風險 |
| E11 | 依 id 取一筆會讀完全部檔案（既有低效） | 明確不做（第 1 條） |
| E12 | 沒有索引、沒有快取；日後該加在哪一層 | 明確不做（第 2 條） |
| E13 | 旗標式導覽，不引入導覽框架 | 架構決策 §D.2；明確不做（第 3 條，含觸發條件） |
| E14 | 不捲動到命中的那一則訊息；保留完整訊息當接縫 | 明確不做（第 4 條）；架構決策 §A.4 |
| E15 | 篩選 / AND / 正則 / 繁簡互搜 / 拼音都不做 | 明確不做（第 5 條） |
| E16 | 比較畫面的結果不可搜尋 | 明確不做（第 6 條） |
| E17 | 查詢不進跨行程偏好、離開時清空；旋轉不算離開 | 行為 §5.5、§5.7 |
| E18 | 即時過濾的垂直空間是規格問題不是版面問題（曾只剩 20px） | 架構決策 §D.1；行為 §7；附錄／決策歷程 |
| E19 | 比較畫面兩個既有缺陷，這次刻意不修，但搜尋畫面要避開 | 明確不做（第 7 條）；架構決策 §D.5；行為 §5.5 |
| E20 | adb 送不了非 ASCII，語意歸單元測試 | 架構決策 §F.1（**v3.11：搬進任務 3 的說明**） |
| E21 | 驗收清單本身的四種失效模式（這次四種都踩到） | 附錄／決策歷程「驗收清單自己也會失效」 |
| E22 | 橫向 + 鍵盤彈出時結果區幾乎沒空間，已知限制不處理 | 明確不做（第 8 條）；行為 §7.3 |
| E23 | 目標裝置的預設中文輸入法打不出 U+3000，推翻了一個寫進規格的事實前提 | 架構決策 §F.3、§C.3；附錄／決策歷程；任務 9 驗收 11 |
| E24 | 空查詢判斷的舊寫法造成系統裡有第二個空白定義（已修，記錄成因） | 附錄／決策歷程「同一個規則被回答了第二次」（**事後檢討**，不是待辦）；**v3.11 起這條規則本身寫在行為 §2.11** |

### 舊文件其他章節的去向

| 舊章節 | 去向 |
|---|---|
| 現有架構觀察（分層慣例、資料流、文字處理、UI、測試環境、空白字元表） | **刪去**。系統層級的描述已由 `plan/architecture.html` 涵蓋；會影響本次判斷的既有事實併入相關決策的理由 |
| 修訂記錄（原本在開頭） | 附錄／修訂記錄 |
| 與原始需求描述的落差（8 點） | 附錄／決策歷程 |

## 決策歷程

**搜尋為什麼變成獨立畫面。** 原本定為「抽屜內搜尋」。實機（1080x2424、density 420）量到：搜尋框上方固定內容約 1343px、鍵盤約 1060px、螢幕 2424px，**剩給搜尋結果約 20px** —— 使用者打字時看不到任何結果，即時過濾在操作上失去意義：算出來了，但看不到。**這是規格漏洞不是實作偏差**：舊條文「鍵盤彈出時輸入框不被遮住」**確實通過了**，但它驗的是輸入框、不是結果。教訓一般化成 §7。
- 使用者可見的變更：入口仍在抽屜的歷史區塊（原需求「入口在既有的對話歷史列表」仍成立），只是點下去進入專屬畫面。若使用者對「留在抽屜裡就地展開」有明確偏好，這條要重新談。

**開場白排除 → 不排除（使用者裁決）。** 使用者裁決「『雙方的訊息都要能搜到』就是字面意思」。搜尋刻意不沿用「送進模型的上下文」那道過濾：後者丟掉開場白，是關於**推論品質**的判斷；搜尋回答的是「使用者在螢幕上看過的哪一句話裡有這幾個字」，而開場白是使用者真的看過、也真的會記得的一句話 —— 拿掉它等於讓搜尋對使用者說謊。**同一份訊息清單在兩種用途下有不同取捨，這不是不一致，而是兩個不同的問題各自答對。**

**不做存取分層 → 要做（使用者裁決）。** 技術分析保留在架構決策 §E.1：使用者是在知情下以**分層一致性**為理由納入，那是決策層級的取捨，不是技術結論被推翻。

**旋轉語意當初根本沒被規範。** 舊條文寫「與比較畫面**完全**同一個模式」是寫過頭了 —— 要沿用的是**導覽結構**，不是旗標的**存活範圍**，而存活範圍我漏掉沒規範，RD 照字面實作沒有偏差，於是兩條互相矛盾的條文（「離開要清空」與「旋轉後結果還在」）都被照做了。定案見 §5.6、§5.7。

**空白的邊界從字面清單改成窮盡定義。** 舊版只列了幾個字元，回答不了「那 U+00A0 為什麼不算」。改採 Unicode 空白類並**明文排除 Cf**：(a) 字面清單的界線沒有語意上的理由 —— **找不到理由的界線，下次還是會漏**；(b) 它不牴觸「搜得到什麼要能解釋」，因為 §1.6 拒絕繁簡轉換是因為「台」和「臺」是使用者**刻意打出來的不同字**，各自帶有內容，而所有空白帶的內容完全相同（「這裡有個間隔」）—— **§1.6 防的是「內容被改寫」，這裡統一的是「呈現形式」**；(c) 規則本身反而更好解釋；(d) 它只會增加命中、不會製造假命中，因為兩端對稱套用。

**同一個規則被回答了第二次。** 「這個查詢算不算空的」原本用語言內建的 blank 判斷回答，那是系統裡**第二個獨立的空白集合**。兩個答案今天相等是因為包含關係，而那個包含關係的前提正是「明文排除 Cf」—— 任何違反它去加入零寬字元的改動，都會同時打破包含關係，讓畫面顯示「找不到符合『（看不見的字元）』」而不是引導文案。修法是共用同一份整形。**這個模式很容易重演：一個規則被小心地定義在一個地方，然後在另一個看起來無關的地方被第二次、用不同的方式回答。**（v3 印證：欄位的空白規則正是它的第三次現身。內建 trim 的空白集合與 §2.1 **不相等**，若順手用它，第二個空白集合就又回來了。**v3.2 更正**：v3 這裡原本寫的是「兩個方向都不相等」，那一半是錯的，見下一條。**v3.11 註**：這條規則現在寫成行為規格 §2.11，有可判定條文與兩條驗收在守。）

**我寫進規格的一個事實前提是錯的。** 我曾在查詢端整形的理由裡斷言「中文輸入法在全形模式下產出的空白就是 U+3000」。QA 用逐字元讀碼位證明：目標裝置的預設注音輸入法**根本打不出 U+3000** —— 注音版面與兩個符號頁的空白鍵送出的都是 U+0020，長按空白鍵是切換鍵盤而非切換全半形。
- **這件事的意義比一條驗收標準大**：那句話不只支撐了一條實機條文，它還是我論證「這個缺口不對稱地懲罰中文使用者」的依據。**結論（查詢端必須整形）不變，但支撐它的理由必須換掉，而且主要方向是反的** —— 異常空白主要出現在**訊息端**（模型輸出、貼上、他家輸入法），使用者則用普通鍵盤打半形空白。
- **一般化**：規格裡凡是引用「使用者會怎麼操作」「環境有什麼能力」的句子，都是**未經驗證的假設**，不是已知事實。它們和技術判斷一樣需要證據，而且通常只有 QA 拿著真機才驗得到。**這也是「比對語意的權威放在不經過實機的測試上」這個分工值得維持的理由**：這次正因為語意的權威在單元測試，前提被推翻時**沒有任何一條比對規則需要改**。

**（v3.2）第二次了 —— 這次被實測推翻的是一個關於語言函式庫行為的宣稱。** v3 寫著「Kotlin 的 `trim()` **不會**移除 U+00A0、U+2007、U+202F，卻**會**移除 U+001C–U+001F」，並據此說落差是**雙向**的。RD 照這條寫測試時直接撞出反例：`" 北京"`（開頭是 U+00A0）用內建 `trim()` 與依 §2.1 去頭，**結果完全相同**，`assertNotEquals` 當場失敗。
- **我獨立查證過，RD 成立**：Kotlin 的 `Char.isWhitespace()` **不是** Java 的 `Character.isWhitespace()`，而是它與 `Character.isSpaceChar()` 的**聯集**（`String.trim()` 走的是 `trim(Char::isWhitespace)`，`isBlank()` 同源）。`isSpaceChar` 涵蓋 General Category Zs，所以那三個不斷行空白全部會被移除。v3 舉的三個例子**三個都錯**；真正的落差只有 U+001C–U+001F 這一個方向。
- **規則沒有動，動的是理由。** 這條規則要的是「欄位規則與比對共用同一份空白定義」，而**只要兩個集合不相等**這個要求就成立 —— 單向落差一樣封死內建 trim。所以實作與測試在修正前就已經是對的，錯的只有規格裡的舉證。**這也是為什麼「不要因為舉證被推翻就順手放寬規則」是對的**：舉證與結論是兩層東西。
- **它還揭穿了本文自己的一處內部矛盾。** 一處主張「§2.1 的集合完全被 Kotlin 的 blank 集合包含」，另一處主張「Kotlin 不會移除 U+00A0」—— U+00A0 屬於 §2.1，這兩句話不可能同時為真。**正確的是前者**，而且它之所以正確，靠的正是「Kotlin 的是聯集」這個事實。這種矛盾特別難被讀出來：兩處相隔數十行、分屬不同小節、而且各自看起來都很有把握。**同一份文件裡對同一個外部事實講了兩次，就有講成兩個版本的機會。**
- **與上一次的推翻是同一類 —— 但「同一類」只到這兩次為止（v3.6 更正）。** 這兩次確實同型（**A 型：理由錯了、規則沒事**），但這句話後來被延伸去把第三次（§8.2 第三點）也算成同一類，於是產生了「三次同型」這個錯誤計次。**第三次不是 A 型，是 B 型**，見下面「A 型與 B 型」那一條。
- 上一次是「輸入法會不會產出 U+3000」（環境能力），這次是「內建 trim 的空白集合長什麼樣」（函式庫行為）。一般化：**規格裡凡是引用「某個既有函式、某個環境的實際行為」的句子，都是未經驗證的假設，不是已知事實。** 兩次的結局也一樣 —— 因為結論本身另有理由撐著，**一條規則都不必改**。這是「理由要寫下來、而且要寫成可以被查證的形式」的價值：假的理由被抓出來時，真的那條規則毫髮無傷；反過來，如果當初只寫「必須用我們自己的定義」而不寫為什麼，這個錯誤永遠不會被發現，因為沒有人能去查證一句沒被寫下來的話。

**驗收清單自己也會失效 —— 這個功能四種都踩到了。**
1. **驗錯對象**：「鍵盤彈出時輸入框不被遮住」通過了，但功能的價值在**結果**看不看得見。**一條會通過的錯條文，比沒有條文更危險** —— 它產生「已驗過」的錯覺。
2. **規則有寫、條文沒有覆蓋**：決策裡明文列了 U+3000，但驗收清單原本沒有一條碰整形，所以一個只認 `\s` 的實作可以合法通過全部條文。**規則不等於驗收。** 這一型還會退化成「條文有寫、測試沒有覆蓋」—— 第二輪就發生了。
3. **架構改版後條文變得不可達**：畫面改版後，兩條舊條文描述的情境在畫面上做不出來。**每次改動架構決策，都要回頭掃一遍受影響的驗收條文還做不做得到。**（v3 又發生一次：§2.8 讓「欄位是全空白」變成不可達；v3.3 第三次：§3.5 的合併語意在畫面上同樣不可達。）
4. **條文預設了一個關於環境的事實，而那個事實是錯的**：見上面兩段。**實機條文只能要求該環境確實產生得出來的輸入**；而**寫進條文括號裡的「為什麼」如果是錯的，QA 會拿著錯的理由去驗** —— 任務 12 驗收 6 就是為此重寫的。

**（v3.3）第二型失效模式在這份 spec 裡的第幾次出現，以及這次是靠什麼發現的。**
- **計次。** 上面第 2 型分成兩種：
  - **純型（規則有寫、沒有任何驗收條文會踩到它）：這是第二次。** 第一次是 v1 的 U+3000。第二次就是 v3.3 的三項：§4.2 的視窗分支、§3.5 的「歷史較新時仍取記憶體那份」、§1.2 的人格名稱。
  - **退化型（條文有寫、測試沒有覆蓋或只有弱斷言）：另外發生過兩次。** v1 第二輪、以及 v3 的全驗。
  - **兩型合計，這是第四次。**「每條可自動化的驗收都要有對應測試」是為退化型寫的，**它擋不到純型** —— 純型發生在「條文根本不存在」的地方。
- **這次是靠什麼發現的：不是測試失敗，是一次反向核對。** QA 在全驗時拿行為規格的每一條規則回頭問「有沒有任何一條驗收條文會踩到它」。**這一型的定義就是「跑測試永遠不會發現它」**：三項都沒有讓任何一次執行變紅，它們存活了四輪全驗。
- **代價的量測。** §4.2 的 off-by-one 只在「命中長度恰為 60」時發作：片段會變成 20 個引子字元加命中的前 40 個字，而不是命中起點起算的 60 個字。它能活四輪的唯一原因，是沒有一條條文會造出「命中長度 ≥ 視窗」的輸入。
- **（v3.5 補記）純型的第三次，是靠一次性補掃一口氣找到 13 條的。** 它與前兩次的差別是：前兩次都是**先發生了某個具體症狀**才被回頭找出來，這一次是**主動全掃**找到的。**這一型的本質就是「不去找就永遠不會知道」。**
- **（v3.7 補記）純型的第四次**：§1.2 有三條可判定，而任務 3 驗收 24 **只覆蓋了前兩條**。**它的形狀是「規則寫了三件事，條文只覆蓋兩件」**，而讓它躲過 v3.5 補掃的原因很可能是驗收 24 的標題寫著「§1.2 的元資料不參與比對」，**看起來像是整條都被覆蓋了**。
  - **零成本這件事本身值得記**：那個測試早就存在、而且兩半都在斷言。**缺的從來不是驗證，是「有沒有一條條文指名它」。**
- **一併記下的驗收強度調整（PM 的判斷，不是使用者的指示）**：§1.2 的可判定條文原本只有否定斷言，一個永遠回空的實作也會通過它。比照 §2.7 的前例補上正向對照。

**（v3.4）第五種失效模式：條文指定的執行主體做不到那件事。** 任務 10 的驗收 4 與 6 要求「把核對日期與結論寫進本節」，而「本節」在 spec 裡，**做這個任務的是 RD，RD 不得改動 spec**。照字面，這兩條沒有任何人做得到；它們不是不可達的**情境**（第 3 型），而是不可達的**分工**。
- **修法是把執行主體寫進條文本身**，而不是放寬要驗的東西。
- **一般化**：一條驗收如果要求「產出物落在某個檔案裡」，就必須同時回答「誰有權寫那個檔案」。

**（v3.4）RD 回報「任務 10 已完成」，但複核發現驗收 7 沒有做。** 收件匣那一筆逐條交代了驗收 1–6，**完全沒有提到驗收 7**。
- **教訓**：**回報完成時逐條列出的那些條文，等於同時定義了「沒被列到的那幾條」**。核對的成本很低，漏掉的成本是整個任務再晚一輪。

**（v3.5）決策 B 已拍板：(d) 升格成硬性條文，但每輪只掃被改動的章節，並先做一次性全面補掃。**
（**v3.11 註**：這條規則本身已隨 §F 整節收掉而移出 spec —— 它是每份規格都適用的通則，權威在 PM 的角色定義裡。**規則沒有失效，只是不再由這份 spec 保管。** 下面的選項與代價保留，因為它解釋了為什麼「一次性補掃」這個做法在這個專案裡出現過兩次。）
- **問題是什麼**：「條文 → 測試」那個方向看不到「一條規則從來沒有任何條文」這一段。v3.3 一次抓出三條這種規則，其中 §4.2 帶著一個 off-by-one 活了四輪全驗。
- **當時列給使用者的三個選項與代價，原樣保留**：
  - **(a) 每一輪改動行為規格時都附一張全表對照** —— 得到：這一型缺口在寫規格的當下就會被抓到。**代價**：每輪多一次全表核對；對照表本身是機械資訊、會腐爛；而且會鼓勵「為了填滿表格而寫條文」，多出一批沒有鑑別力的驗收。
  - **(b) 不升格，只記在決策歷程** —— 得到：不增加固定成本。**代價**：這一型只能靠 QA 做全驗時的反向核對發現，既昂貴、也不保證每輪都做。
  - **(c) 折衷：只對「規則裡出現數字、邊界或分支」的條文要求指名覆蓋** —— **代價**：界線要靠人判斷，而「找不到理由的界線，下次還是會漏」。
- **使用者選的是 PM 的傾向並把它獨立成第四個選項 (d)，額外加上「一次性補掃」。** 加補掃的理由：**(d) 的邏輯「一條規則在它被寫下的那一輪被核到一次」只對政策生效之後新寫的規則成立。** 對既存的規則有一個冷啟動的洞 —— **§4.2 的規則字面是原始版本**，所以它在帶著 off-by-one 的那四輪裡從來不是「被改動的章節」。**不做補掃的 (d)，抓不到催生它的那個案例。**
- **使用者同時裁決 QA 流程維持每輪全驗**，不採用分段驗收。實測數據：分段 294,337 tokens / 145 次工具呼叫 / 39.5 分，對比全驗 270,916 / 135 / 33 分與 270,992 / 128 / 36 分 —— **縮小範圍反而更貴**。

**（v3.5）一次性補掃的結果，以及它推翻了我自己上一輪的一個判斷。**
- **規模**：101 條規則逐條核對（`spec.md` 61、架構 40）。結果：大多數有條文守著；**13 條沒有 → 補條文**；**8 處沒有獨立條文但有理由 → 明寫由哪一層守**。
- **13 條缺口的形狀，出乎意料地集中在三種**，而且三種都是這份 spec 已經記過的失效模式：
  1. **只有否定斷言、沒有正向對照**（§3.8 的標頭、§4.3 的省略號、§5.7 的偏好、§1.9 的載入中畫面）—— 一個「把東西整個拿掉」或「永遠加上」的實作會通過。
  2. **規則寫了兩件事，條文只覆蓋一件**。**被覆蓋的那一半會讓人以為整條都被覆蓋了。**
  3. **違反它不會讓任何行為改變**（搜尋狀態聚成一組、不引入導覽框架、說明要誠實、要交出訊息本身）—— 純結構規則，行為條文永遠看不到它們。
  - **（v3.11 回頭看第 3 型）**：當時把「行為條文永遠看不到它們」讀成「所以要補條文」；新的准入判準把同一個事實讀成「**所以它們不該是規則**」。**v3.11 把那一批全部移除了。** 兩個結論的差別不在事實，在判準 —— 這一點值得記，因為它說明**一條規則的去留取決於問題怎麼問**。
- **它推翻了我在 v3.3 寫下的一個判斷。** v3.3 說 §6.5「可觀察後果已經由任務 8 驗收 5 守住」。**那是錯的，而且證據就在 §6.4 自己的實作註記裡**：走錯分支時「先存檔 → 對空白對話等於刪檔 → 找不到 → 提早返回」**看起來剛好無害**。
  - **值得記一筆的原因**：v3.3 那段話的下一句自己寫了「這個判斷我沒有十足把握」。**我看見了那個模式，卻仍然套用了它。** 這是 (d) 需要「補掃」而不只是「往後每輪做」的具體理由之一：**當輪的自我核對會被當輪的判斷偏誤限制住。**

**（v3.5，標題的計次已於 v3.6 更正）「規格裡的事實宣稱被實測推翻」—— 這一次不一樣，它連帶讓兩條規則互相矛盾。**

> **（v3.6 更正，先讀這一句）標題原本寫的是「第三次」，那個計次是錯的。** 它把 A 型（2 次）與 B 型數在一起。**這一次是 B 型的第一次。**

- **被推翻的宣稱**：§8.2 第三點的括號理由「停止生成：把『還在生成』這個標記關掉，本身不算一次更新」。RD 回報、我讀 `chat/ChatViewModel.kt` 確認：按下停止會取消生成中的工作，取消丟出的例外被回覆流程的錯誤處理接住，於是**整則回覆的文字被覆寫成一段錯誤字串**。
- **與前兩次的差別，正是這一次值得單獨記的原因**：
  - 前兩次**結局都是「一條規則都不必改」**。
  - **這一次不是。** §8.1 明文把「錯誤訊息寫入」列為必須前進的情況，所以按下停止在 §8.1 底下要前進、在 §8.2 底下不得前進 —— **兩條規則對同一個動作給出相反答案**，而假的括號理由正是讓這個矛盾一直看不見的東西。
- **§8.6 為什麼抓不到**：它的預期是「存檔時間比切換時刻早 ≥30 秒」，而錯誤訊息的寫入發生在按下停止的當下，同樣滿足這個預期。**不論這個問題在不在，§8.6 都會通過** —— 這是第 1 型失效在這份 spec 裡的第二個實例。
- **兩件事都沒有自己決定**：升級成決策 C 與決策 D，**都已於 v3.6 拍板。**

**（v3.6）決策 C 已拍板：停止生成從此算一次更新。線沒有移動。**
- **裁決從哪裡來**：`stop-generation` 那一輪已由使用者裁決「按下停止要留下已顯示的文字，並在後面追加一個中斷標記」。所以停止當下必然改變內容。
- **落實方式（怎麼落位是 PM 的判斷，不是裁決的一部分）**：使用者的原話是「改成『本身也算一次更新』」，**但那一行不能原地改寫成肯定句** —— 在一張標題是「不算一次更新」的清單裡放一條「算一次更新」是把一個矛盾換成另一個矛盾。所以：**§8.2 那一項整條移除**，並在 §8.1 底下點名它。點名的理由是讀者體驗而不是正確性：整條移除之後「**刻意移除**」與「**不小心漏掉**」在讀者眼中分不出來。
- **這不是推翻使用者先前的裁決**：他裁決的是「時間只跟著內容走」，那條線**一個字都沒有變**。**線沒有移動，是這件事從線的一邊換到另一邊。**
- **這一輪不需要任何實作改動，而這件事本身值得記**：那個唯一的判斷處是以「**這次變換之後內容還是不是同一份**」判斷要不要蓋時間的，**沒有針對停止的特例**。**把規則寫成「內容有沒有改變」而不是「哪些動作要排除」，讓這次的規則改動成本變成零。**
- **移除那一項時浮出一個以前沒被規範過的複合情形**：**在回覆還在串流時切換走**。**答案是唯一的**：時間前進，原因是內容改變不是切換。**清單是判準的推論，不是第二套規則**，這句話現在寫進 §8.2 了。**同型的風險還在**：任何一條「某某動作不算一次更新」的條目，只要那個動作有可能順帶觸發別的事，就會再來一次。

**（v3.6）決策 D 已拍板：不升格；以及一個被當成既有事實使用、卻沒被查證過的計次。**
- **裁決內容**：不為「規格裡引用既有函式／環境／既有程式行為的事實宣稱」另立硬性條文。**使用者的原話是分工形式的**：「如果是既定規則，那就需要測試；如果規則有衝突，那要請 QA 反應出來，PM 決定，如果有需要也可以升級到我這邊。」也就是 —— **規則**由「條文 → 測試」與「規則 → 條文」兩個方向守；**規則之間的衝突**走 QA → PM → 使用者這條升級路徑。**這個分工就是裁決的理由本身**：它明確地把一類風險指派給人而不是指派給條文。
- **當時列給使用者的四個選項與代價，原樣保留**：
  - **(a) 全面升格：每一句引用既有函式／環境／既有程式行為的宣稱都要有一個會執行的斷言釘住** —— **代價**：本文的「理由」數量遠多於規則數量，等於替每條理由追加一項驗收；而且有些宣稱根本沒有辦法用斷言釘住。
  - **(b) 不升格** —— **代價（當時寫的）**：「已經連續三次了」——**這半句建立在錯誤的計次上**。
  - **(c) 縮小範圍升格：只有「規格宣稱某段既有程式此刻的行為是什麼」這一類要有斷言** —— **代價**：界線要判斷。
  - **(d) 不另設條文，信任反向核對已經接住它** —— **代價**：反向核對只在該節「被改動」的那一輪才會問，而 §8 從 v3 之後沒被改過。
- **A 型與 B 型：「三次同型」這個計次是錯的（使用者先看出來，RD 查證，PM 複核附錄自己的文字確認）。**

  | | 次數 | 後果 |
  |---|---|---|
  | **A 型：理由錯了，規則沒事** | **2 次**（U+3000 輸入法、Kotlin trim） | 只更正文件。產品毫髮無傷，什麼都不必修 |
  | **B 型：前提錯了，規則因此自相矛盾** | **1 次**（§8.2 第三點 → §8.1／§8.2 對同一動作給出相反答案） | 兩條各自正確的規則互相矛盾，而且看不見 |

  **v3.2 當初列的兩個觸發條件裡，只有第二個是 B 型的標記；「第三次出現」那一個把 A 型與 B 型混在一起數，於是把一次灌成了三次。**
- **這件事本身就是同一型錯誤的又一個實例**：一個**計次**被當成既有事實反覆引用，而**它從來沒有被查證過**。查證的方法很便宜 —— 回頭讀那兩條記錄各自的結局，兩分鐘。**規格裡的數字與統計，和規格裡引用的函式行為是同一種東西：未經驗證的假設。**
- **一併採納的一條寫法紀律：理由不該寫得比規則需要的更精確。** A 型那兩次被推翻，不是因為「寫了理由」，而是因為理由寫得**比規則需要的更細**：那條規則需要的事實只有一句（兩個空白集合不相等），但當初寫下的是五個具體斷言，其中四個不撐任何東西、純粹是可被推翻的表面積。**這不是要少寫理由** —— 理由是這份 spec 能抓到問題的原因。**它要的是：只寫規則實際依賴的那個性質。**

**（v3.6）一條結構驗收的形狀與正確實作的形狀對不起來 —— 第 3 型失效的近親。**
- **現象**：任務 8 驗收 14 的結構那一半要求「唯一的分支條件是 id 相等與否…指得出它只有那一個分支」。RD 讀程式碼時回報那條路徑上有**兩個**條件。
- **PM 自己讀過那條路徑查證，RD 的觀察成立，而且比他說的還多一個**：實際上有**三個**條件。**三個沒有一個是空白對話的特例**，所以規則本身沒有被違反。**這一點很說明問題：連「有幾個分支」這個數字本身都不是穩定的**，它會隨著防護加在哪一層而變。
- **修法是把條文從「允許幾個」改成「禁止什麼」。要驗的東西一字未變**，變的是可判定性。
- **它與第 3 型的差別**：第 3 型是**情境**做不出來，這一型是**條文描述的形狀**與正確實作的形狀對不起來，於是**正確的實作反而會被判 fail**。兩者都源自「條文寫了實作的樣子，而不是寫必須成立的性質」。**這是那句「結構驗收比行為條文更容易腐爛」的第一個實例，而且只隔了一輪。**

**（v3.6）實機驗收驗到的是舊建置 —— 那條「先證明建置」規則的來歷。**
- **發生了什麼**：RD 在實機驗收前發現，裝置上原本安裝的是 **2026-09-07 15:43** 的建置，而修正 §4.2 那個 off-by-one 的原始碼是同日 **16:35** 才寫進去的。也就是說，**在那之前於該裝置做過的實機判讀，驗的是還帶著那個缺陷的舊版**。
- **為什麼它值得被寫下來**：實機判讀有一個從來沒被要求證明過的前提 ——「畫面上這個行為出自這份程式碼」。那個前提被違反時，**全部實機條文都會給出看起來正常的 pass**。
- （**v3.11 註**：這條規則已從 spec 移出 —— 它規範的是 QA 怎麼判讀，屬於角色定義，而 QA 的角色定義裡已經有一條等價的。**經過留在這裡，因為它是那條規則存在的理由。**）

**（v3.7）第 6 型失效：條文要求的證據不存在於產物裡。**
- **現象**：2026-09-09 的全驗指出，**七條驗收要求某個檔案「一字未改」，而這個專案沒有版本控制、也沒有留下基準雜湊**。QA 判它們 pass，但只能附一句「無法直接判定，可得的佐證是檔案 mtime」。**那不是 pass/fail，是「看起來沒動」。**
- **同一輪一次撞到三個，而且是同一型**：(i) 上面那七條；(ii) spec 反覆指向同目錄的全驗報告，而流程要求每次重驗前把舊報告移出目錄，**所以每當有 QA 在跑，那個引用就是斷的 —— 而那正好是有人會去追它的時候**；(iii) 另一輪的 spec 裡有一條「與修改前逐字相同」。
- **它與既有五型的差別**：第 3 型不可達的是**情境**，第 5 型不可達的是**分工**，這一型不可達的是**證據** —— 情境做得出來、事實也成立，只是**沒有東西可以拿來證明它**。
- **當時的處理方式：不放寬要求，換一組拿得到的證據去問同一件事**（時間、內容、行為三項判定）。
- **（v3.11 的後續，這一段很重要）**：那三項判定在 2026-09-10 的全驗裡被判成**照字面不可能成立**（任務 9 驗收 3、7），而使用者最後的處置**不是再換一組證據，是問一個更前面的問題** ——「這條被違反時，使用者會遇到什麼？」答案是**什麼都不會**：程式碼被動了而行為沒變，使用者感受不到；行為變了，那是別的驗收在守。**於是那七條連同整節判定基準一起被移除。**
  - **值得記的一點**：v3.7 花了一整輪把一條**不該存在的條文**改成**可判定的**。**「證據拿不到」這個症狀，有時候是「這件事根本不值得驗」的徵兆**，而換證據會把那個徵兆蓋掉 —— 一條改好之後可判定的條文，看起來就沒有問題了。**下次遇到第 6 型，先問准入判準那一題，再問證據。**
- **一般化（一條寫法紀律）**：寫下一條驗收時多問一句「**冷讀的 QA 拿什麼判它**」。答案必須是一份看得到的東西。

**（v3.7）v3.6 修了那句條文的開頭，卻留下更嚴的收尾句 —— 同一型問題換了位置。**
- **現象**：v3.6 把結構條文從「唯一的分支條件是 id 相等與否」改寫成「禁止哪幾種分支依據」，**但收尾句仍是「路徑上的每一個條件都歸得進上面兩類其中之一」**。QA 指出：把同一個回呼裡順手做的清空查詢也算進那條路徑的話，它有一個以**查詢字串是否為空**為依據的提早返回 —— **沒踩到任何禁止項，卻歸不進那兩類**。
- **為什麼值得記**：一條條文的可判定性由**它的每一句**決定，改寫時只盯著被回報的那一句是不夠的 —— 尤其是「冷讀判定：…」那種收尾句，它往往才是 QA 真正照著判的那一句。

**（v3.7）任務 10 的紀錄第二次腐爛 —— 這次的成因是「改文件的人、記錄的人、與知道它被改過的人是三個不同的角色」。**
- **與 v3.4 那次的差別**：v3.4 那次是**分工不可達**（第 5 型）。這一次分工已經修好了，壞的是**傳話鏈**：兩處更動分別由**不同的人、在不同的輪次**做的，而只有 PM 能寫那一節。**沒有人做錯自己那一步，紀錄還是過期了。**
- **過期的樣子與「還沒做完」在讀者眼中一模一樣**，這才是它的代價。**擺在最前面的那句話錯了，傷害比藏在後面更大。**

**（v3.8）決策 E 被換了一個角度：問的不是「紀錄放哪裡」，是「兩輪衝突怎麼處理」。**
- **使用者沒有從四個選項裡挑。** 定下的規則是：**衝突時 PM 不要自己決定用哪一邊的設計，拉給使用者判斷；拍板之後由 PM 去同步兩邊的 spec。**（**v3.11 註**：這條規則已隨 §F 整節收掉而移出 spec，權威在 PM 的角色定義裡；**規則沒有失效。**）
- **成因的自陳來自 RD，PM 採納**：那次腐爛不是 PM 忘了看，是**PM 不知道有東西被動過**。**新規則對得上這個成因**：它要求跨輪的分歧走使用者，而不是靠某個人記得傳話。**但它不是通知機制** —— 沒有人注意到的時候它不會啟動。
- **值得記的一點：一條新規則會改變一個舊問題的選項配比，而不是自動關掉它。**

**（v3.8）使用者接受 v3.7 那四項自主判斷；附帶一個沒有拍板的通則。**
- **四項全部接受，不需要退回**：§6.5 與任務 8 驗收 14 的收尾句改寫、「一字未改」類條文改成三項判定基準、「一條驗收只能要求產物裡拿得到的證據」這條寫法紀律、以及新增任務 3 驗收 26。**使用者是逐項聽過詳細說明之後接受的。**
- **第四項附帶一個沒有拍板的問題，記在這裡免得日後被當成慣例**：RD 提出「要不要允許 PM 在交辦範圍外，照既有硬性條文的義務順手補零成本的洞」。**使用者接受了這次的結果，但沒有對這個通則表態** —— 所以**通則沒有成立**。往後遇到同樣情況，仍然要在回報裡明講多做了什麼、以及為什麼。
- **一併記下的一件事（來自另一輪）**：`stop-generation` 那一輪的 PM **獨立**得到了與 v3.7 相同的結論 —— 「條文要求的證據不存在於產物裡」是一個獨立的失效型。**兩邊都是自己撞到的，不是互相抄的。** 而**他在自己的附錄裡刻意只記性質、不寫編號**，理由是那個序號會隨本文這份清單的長度變動 —— **那正是「引用要指性質不要指位置」的正確用法**。

**（v3.9）新的驗收通則：每一條驗收都要說得出「它獨自抓得到什麼」。任務 10 那兩條是它的第一個實例。**
- **使用者定下的判準**：寫下一條驗收之前問兩個問題 —— **一、有哪一種錯誤，只有這一條會發現？二、那種錯誤，冷讀的人判得出來嗎？** 第一題答不出來 → 它是別條的複述，刪掉；第一題答得出、第二題答不出 → **它的實際效果是零，而它看起來像有在守**，比沒有這條更危險。
- **特別點名的一型：驗過程，不驗結果。** 判準是「一條驗收如果在問『**誰做了什麼、什麼時候做的**』而不是『**現在是什麼樣**』，先確認那個過程資訊真的有獨立價值 —— 通常沒有」。
- **任務 10 就是拿來對照的那個例子，而且它自己提供了反證**：七條裡**五條是「讀文件確認它現在長什麼樣」，直接判得出來**；只有驗收 4 與 6 要求「把過程寫進本節」，而驗收 6 **腐爛兩次、害全驗判 fail 一次**。**同一個任務裡的五條就是反證：不需要紀錄也驗得出來。**
- **「多久沒被核對過」這種資訊怎麼辦（使用者授權 PM 判斷，PM 拒絕了把它放進 spec）**：它確實有一點價值，但**它不該塞進 spec —— spec 要靠 PM 手動同步，而那正是它腐爛的原因**。那種帳屬於**做檢查的角色自己維護**的獨立帳。

**（v3.9）決策 E 結案 —— 不是被拍板，是問題本身被取消了。**
- **怎麼結案的**：決策 E 問「任務 10 的執行紀錄該放在哪一份文件裡」。這個問題之所以要緊，是因為**驗收 6 拿那份紀錄當證據**；使用者**拿掉了驗收 6**。**沒有任何一條驗收依賴那段紀錄的位置之後，它降級成編輯判斷。**
- **值得記的一點**：**一個待決事項的壽命，取決於有沒有東西依賴它的答案；依賴消失，問題就消失。**
- **四個選項（v3.7 寫下、v3.8 補充）保留為歷史，現在都不需要回答了**：(a) 維持現狀（冷讀在同一份文件裡判得完，但它會繼續腐爛）；(b) 只留結論、清單移到變更說明（機械資訊離開 spec，但可判定要跨檔）；(c) 維持位置但補一條「每輪必須重新核對」的義務（直接對著成因下手，但靠的仍是「有人記得做」）；(d) 只寫結論 + 指向文件裡那幾個小標題（不會因為文字被改寫而過期，但「更動了什麼」這個資訊會變淡）。
- **回頭看，這是一個訊號：當使用者兩次都不回答一個問題時，該懷疑的是問題問錯了，不是他還沒決定。**

**（v3.10）§6.5 的結構條文被降級成建議 —— 這是「每一條驗收都要說得出它獨自抓得到什麼」那條通則篩掉的第一個東西。**
- **是使用者自己看出來的。** 他讀到那條條文時的原話是：「這條路上不准有『如果這段對話是空的，就走另一條路』這種特例 —— **這看起來像寫作偏好，並沒有實際的體驗問題**。」
- **對照的結果**：
  - **它獨自抓得到什麼？** 今天什麼都抓不到。若有人寫了那個特例分支、而四種情境的行為全部正確，使用者感受不到任何差別。
  - **它給的理由是「防止未來壞掉」，那是假設不是後果。** 對照空白定義那條規則（現行的 §2.11）：兩份定義**必然**分歧，分歧那天使用者會看到搜尋結果對不起來 —— **後果是確定的**。§6.5 則要有人**主動寫錯**才會壞，而「有人寫錯就會壞」對任何一段程式都成立。**這就是「硬性條文」與「建議」的分界線。**
- **它的實績，值得單獨記下來當成下次的反例**：**抓到違反 0 次；害正確的實作差點被判 fail 3 次；為了修這條條文本身花了 3 輪。** 三次都是同一個形狀 —— **想表達「不准做某件壞事」，卻寫成「只能長成某個樣子」**。
- **一條條文如果每次被讀都要重修一次措辭，那不是措辭問題**：三次修正各自都成立，但**三次合起來才看得出真正的病灶**：它要求的是程式碼的**形狀**，而形狀會隨著防護加在哪一層、回呼裡順手做了什麼而變。行為條文沒有這個性質。
- **這不是「§A–§G 重新分類」的開端。**（**v3.11 更正**：回頭看它就是 —— 使用者在 v3.11 把同一個判準套到整份 §A–§G 與全部驗收條文上。當時寫這句話是因為 §6.5 在行為規格裡、不在 §A–§G，那個事實沒錯；**錯的是把「範圍不同」讀成「不會擴大」**。）

**（v3.10）決策 F 拍板為 (a)：維持現狀 —— 但它是暫緩，不是問題消失。**（**v3.11：整題已被取消**，見下一條。）
- **裁決**：那七條「一字未改」類驗收的三項判定**一字不改**。
- **理由（使用者的）**：他要先想清楚這個專案要不要做版本控制、怎麼做 —— **那一題與這一題是同一個根**。
- **當初列出的四個選項與代價，原樣保留**：

| 選項 | 得到 | 代價 |
|---|---|---|
| **(a) 維持現狀**（**當時選這個**） | 不動任何條文；時間仍是三項裡最便宜的一項 | 它每一輪都會不成立，於是每一輪都要走補救條款，而補救條款的後半句判不出來 —— **一條每輪都要靠人情放行的條文，實際效果是零** |
| **(b) 把第 1 項從「判定」降成「線索」** | 誠實：時間本來就只是旁證；七條的判定退回成兩項，兩項都在產物裡拿得到 | 比現在弱一點點 |
| **(c) 換一個基準**：每一輪開工時記下這幾個檔的時間 | 時間這一項重新有鑑別力 | 那份基準要有人每輪產生與維護（PM 沒有那個能力，RD 產生等於自己給自己開證明） |
| **(d) 把補救條款的後半句拿掉** | 拿掉判不出來的那半句 | 把一條判不出來的條文換成一條判準模糊的條文 |

- **PM 當時的傾向是 (b)，使用者選了 (a)。** 兩者要的東西不同：(b) 想讓這一項現在就變得可判定，(a) 想等一個會讓整組判定都變好的前提。

**（v3.11）決策 F 被取消：問題不在「拿什麼證據」，在「這件事值不值得驗」。**
- **怎麼結案的**：使用者沒有從四個選項裡挑（**第三次了** —— 決策 E 兩次、決策 F 一次）。他問的是更前面的一題：**「這條被違反時，使用者會遇到什麼？」** 答案是什麼都不會 —— **程式碼被動了而行為沒變，使用者感受不到；行為變了，那是任務 1 驗收 6 與任務 9 驗收 6 在守**。**於是那七條連同整節判定基準一起被移除，決策 F 隨之消失。**
- **這是「被取消」不是「被解決」，與決策 E、決策 G 第一點同一個形狀。** 三次都一樣：**問題本身依賴一條條文，那條條文一被拿掉，問題就沒有了。**
- **一件必須寫下來的事，否則下一個讀附錄的人會誤會**：**這一題原本是「引入版本控制」的主要理由。** 使用者已決定這個專案會接 git，**但不是為了那七條** —— 那七條就算有 git 也仍然答不出使用者會遇到什麼。**git 的理由換成純工程安全網**：本 session 曾把一個含數十支測試的檔案誤寫成 0 bytes，靠反編譯 build 產物救回。
- **「使用者兩次不回答同一個問題就是問題問錯了」這個訊號，這次是第三次應驗，而且形狀更清楚**：決策 E 問「紀錄放哪裡」、決策 F 問「拿什麼證據」，**兩題都預設了「這件事要驗」**。使用者兩次都往上跳一層，去問那個預設本身。

**（v3.11）§A–§G 重新分類、§F 整節收掉。**
- **使用者的裁決**：依准入判準重新分類 —— **說得出使用者後果的搬進行為規格或驗收條文，說不出來的留在架構那一節當建議（QA 不驗），已經是別處通則的整節收掉。** 並要求兩件事同一輪做完（重新分類與驗收條文的全面掃描），理由是分開做會自己製造一次跨輪衝突。
- **為什麼會有這批存量**：這份檔案成形的時候，下面這些規則都還不存在 —— 准入判準、架構是建議不是裁決、QA 不驗架構、引用不留腐爛物、每條驗收要說得出獨自抓得到什麼。**當時沒有做錯**，要處理的是規則生效前留下的存量。
- **收件匣裡那張去向表是主對話的判斷，不是裁決；PM 逐條重判，六處判得不一樣**（§A.3、§C.4、§C.7、§E.1、§C.1／§C.2、§F.6），理由逐項寫在 `spec-decisions.md`／「本輪我自己做的判斷」。**其中最重要的一處是 §C.4**：表上說留在架構當建議，PM 把它搬進行為規格並補上可判定條文 —— 因為**使用者自己在 v3.10 就是拿它當「硬性 vs. 建議」的分界線用的**，把分界線的參考點放進一個 QA 不驗的檔案，那條線就沒有人在守了。
- **§F 為什麼不留摘要**：那七條寫的是每一份規格都適用的通則。**抄一份進 spec 就會出現第二個版本，而「同一件事被寫成兩份、兩份遲早分歧」正是本規格從頭到尾在防的那個失效模式。** 只留一句指路，另把**本規格特有**的那兩件事（比對語意的權威為什麼落在不經過實機的測試上、實機負責的是算繪不是算對）寫進任務 3、7、9、13 的說明 —— 那是條文自己需要的事實，不是通則。
- **一個可以拿來驗這件事有沒有做成的東西**：`qa-agent.md` 執行步驟 1 有一段補丁，寫著「『架構決策』那一節照樣要讀完 —— 因為那一節常常混著行為要求」。**那段補丁存在的唯一理由就是這份文件混了。分類做完之後它可以刪掉** —— 判斷寫在 `changes-v3.11.md`。

**（v3.11）驗收條文的一次性全面掃描：v3.5 那次補掃的對偶。**
- **判準（使用者裁決）**：**每一條驗收條文都要指得出它守的那條規則，而那條規則要說得出使用者會遇到什麼。** 指不出來 → 改寫或移除。**驗證方式不設限** —— 由單元測試判定不構成問題。
- **它與 v3.5 那次補掃的關係**：那次問「**規則有沒有條文守**」，這次問「**條文有沒有後果撐**」。**兩次都是一次性的**，之後由准入判準每輪擋住新的。
- **最值得記的一件事：這次移除的條文，有一大半正是 v3.5 那次補上的。** v3.5 的補掃把「違反它不會讓任何行為改變」列成**第 3 型缺口**（所以要補條文）；v3.11 的判準把同一個事實讀成**「所以它們不該是規則」**。**事實一個字都沒變，變的是問題怎麼問。**
  - **這不代表 v3.5 做錯了。** 它照當時的規則做到最好，而且它的產出裡有一半（正向對照那一型、規則寫了兩件事只覆蓋一件那一型）到今天仍然全數保留 —— **被移除的集中在「純結構規則」那一型。**
  - **要記的是這個**：一次補掃能不能產出好東西，**取決於當時手上那把判準**。判準換了，產物就要重掃一次。
- **移除留下的缺口，誠實寫**：純結構的那些性質（畫面端不得有比對邏輯、搜尋狀態聚成一組、不引入導覽框架、存取層只有四個方法、跨層要交出訊息本身…）**從此沒有任何條文在守**。**這是知情接受的**：它們被違反時使用者感受不到，而它們可能造成的那些**看得見的**後果（標示蓋錯字、該命中的沒命中、看得到的搜不到、列表崩潰）每一個都有行為條文在守。觸發條件寫在「已知取捨與風險」。

**（記錄搬家，v3.6）歷次收件匣「要不要升級成待決事項」的判斷。** 這些原本放在 `spec-decisions.md`，但該檔只該反映「現在還欠誰一個決定」，所以拍板後搬到這裡：

- **（v3.2）當輪那一筆不需要升級。** 它是 RD 的實測觀察，我獨立查證後認定成立；它只推翻了一條理由的舉證，沒有動到任何規則，修正方向唯一，沒有可選項。
- **（v3.4）當輪那一筆不需要升級。** 條文問題只有一個修法（把「誰來記錄」寫對，可觀察的要求不變），核對結果則是事實而不是選項。
- **（v3.5）第一筆是使用者裁決本身，不需要升級**；第二筆（RD 觀察）**升級成了決策 C 與 D**，我自己讀過 `chat/ChatViewModel.kt` 確認診斷成立才升級。兩筆之間沒有衝突，反而有一個交互作用。
- **（v3.6）兩筆都不需要新的升級**：一筆是使用者裁決，一筆是 RD 觀察（修法只有一個方向）。本輪唯一需要判斷的是決策 C 的**落位**，而落位不改變任何判定結果。
- **（v3.7）收件匣只有一筆（QA 全驗結果 + RD 的處理），內含四塊，我先整份讀完再動手**：四塊之間沒有衝突、沒有順序相依。**其中三塊我自己決定，一塊升級成決策 E。兩處我沒有照單全收，自己去核對過。**
- **（v3.8）兩筆都是使用者裁決，都不需要新的升級 —— 但其中一筆我沒有照 RD 的推論結案。** **有一個交互作用值得記**：新的寫法規則正好指向任務 10 的執行紀錄，而那就是決策 E 未決的那一半，**所以我停在那裡沒有動它**。
- **（v3.9）兩筆一起讀完再動手，兩筆指向同一個處置，另外升級了兩項。** **交集是明顯的**：QA 的第一點與使用者的裁決**各自撞到同一條條文**。
- **（v3.10）只有一筆（使用者裁決，三項），三步檢查照做，沒有新的升級。** 三項之間**沒有互相抵觸**，但**有一個順序相依，而且它會改變結論**：第三項的第一半之所以「整個消失」，前提正是第二項。
- **（v3.11）四筆一起讀完再動手，四筆本身都不需要升級 —— 但核對過程中撞到一件別的事，升級成了決策 H。**
  - **升級的那一項不在四筆裡**：核對 `docs/product/features.md` 需不需要更新時發現，它有三十幾處指向 `docs/specs/<slug>/spec.md`，而**那些路徑因為資料夾版控而全部斷掉**。**它波及這個功能以外，而且同一題在 `stop-generation` 那一輪一模一樣** —— 依「兩輪之間的衝突不得由 PM 自己選一邊」，我沒有自己選，也一個字都沒有動 `features.md`。**這是那條規則第一次真的被啟動**（先前它只被寫下來過）。
  - **衝突**：沒有。四筆全部是使用者裁決，方向一致（都在收縮「spec 該保管什麼」）。
  - **順序相依**：**有，而且使用者自己點名了它** —— 第 2 筆（重新分類）會改變「哪些規則還在、在哪裡」，而第 3 筆（掃驗收條文）要問的正是「這條條文守的規則說不說得出後果」。**先分類再掃，兩件事必須同一輪做完**；分兩輪做會讓第二輪面對一份「規則已經搬走、條文還指著舊位置」的文件。
  - **合起來有沒有改變架構決策的根本假設**：**有，而且這正是使用者要的** —— 架構決策從「必須成立的性質」變成「建議」，QA 不再驗它。**這件事不能當成幾個小修改混過去**，所以它在 `spec.md` 卷首、`spec-architecture.md` 開頭、`spec-decisions.md` 與 `changes-v3.11.md` 四個地方都明寫了。
  - **一件我判斷成「不在本輪判準範圍內」而沒有動的事**：任務 8 驗收 14 剩下的行為那一半、任務 12 驗收 7、任務 13 驗收 8 這三條**重疊**條文。本輪的判準問的是「它守的規則有沒有使用者後果」，三條都有；重疊是另一個問題，而使用者上一輪才對其中一條表過態。寫在 `spec-decisions.md` 讓他有機會不同意。

---

以下四段是 **v3 三項裁決、以及裁決帶出的決策 A** 的處理紀錄（含 v3.1 的更正）。

**兩個沒有呼叫端的派生屬性：原本問「刪掉還是改用」→ 裁決「留著兩個 method」→ 回問後確認原意是 (c)：就只是留著，不動 UI。**
- **我原本的技術分析保留在這裡，因為那個推導本身有價值**：那兩個屬性與畫面自己算的摘要數字，是同一個量的兩份寫法。**留著但沒有呼叫端，不是「單一定義處」，是「第二份寫法還沒被用到」** —— 下一個人仍要自己判斷該用哪一個，而兩份寫法哪天分岐時，沒有任何東西會告訴他。
- **我上一輪把「留著」讀成 (b)（留著並讓畫面改用它們），那個讀法是錯的。** 回問使用者時，選項文字已經明講了代價：「目前 UI 自己又算了一次同樣的數字，保留 method 卻不用它，重複依然存在」。**使用者在知道這個代價的前提下選了 (c)。這是知情裁決，不再重新爭論。**
- **落實方式**：規格照 (c) 寫（只保留「畫面顯示的值必須符合 §3.4」這一條可觀察要求，不規定由誰算）；任務 11 因此沒有程式改動。**重複被保留下來這件事不藏起來。**

**查詢欄位「不得 trim」→ 三段式的空白規則（使用者裁決）。**
- 舊規則是「欄位保存原樣輸入、不得 trim」，理由是：若把整形後的字串寫回欄位，使用者連一個空白都打不出來。**那個理由本身仍然成立，只是它的守備範圍被切小了。**
- 新規則不會踩同一個坑，因為它**只動兩端、而且兩端的時機不同**：開頭空白對比對完全沒有作用，移除它不會讓任何查詢變成打不出來；尾端空白在**打字過程中一律保留**，正是為了讓「北京 天氣」中間那一格打得出來 —— 那個空白在被打出來的當下就是尾端空白。中間的空白則完全不動。舊理由要守的東西由 §2.8b 與 §2.8d 承接。
- **連帶改變**：回顯顯示的是**欄位當下的內容**（§2.9）。舊條文那句「連前後空白都在」因此作廢，任務 8 驗收 10 換成新的 kiwi 案例。
- **一併補強（PM 的判斷，不是使用者的指示）**：§2.7 舊條文只要求「`"  北京  "` 與 `"北京"` 結果相同」，而「兩邊都回空」也算相同 —— 這是 v2 驗收記錄過、但依條文字面不能判 fail 的弱斷言。所以補上「且結果不得為空」。**這是驗收強度的調整，不是新規則。**
- **（v3.2）這一段當時寫下的「為什麼不能用內建 trim」有一半是錯的舉證**，已修正，見上面「第二次了」那一條。**規則本身（三段式、只動兩端、必須用 §2.1 的定義）完全沒有改變。**

**切換對話讓時間戳往前跳：原本列在「明確不做」→ 裁決「依照規格實作，這塊如果要改就一起改」。**
- **我讀過的機制版本**：狀態層有一條共用的「把對話寫回去」的路徑，它**無條件**把最後更新時間設成現在 —— 即使這一次的變換根本沒有改到任何訊息。而「停止生成」會呼叫它，「切換對話 / 切換人格 / 切換模型」又都先呼叫停止、接著把當前對話存檔。於是「切走」這個純粹的閱讀動作，被寫成了一個新的時間戳並落盤。整份程式碼裡寫入這個時間的地方只有兩處，所以診斷成立，範圍也是收斂的。
- **所以定義從「有沒有經過那條路徑」換成「內容有沒有改變」**（§8.1），而「只有一個地方回答這件事」是達成它的建議（`spec-architecture.md` §G.1）。
- **刻意不擴大**：本輪不動「什麼時候寫檔」（見「明確不做」）。切換時存檔是既有的正確行為，它保住了尚未落盤的內容；動它會影響一條與搜尋無關、而且正在正常運作的路徑。

**決策 A：時間戳的修正會一併改變抽屜歷史列表的順序 → 裁決 (a) 兩邊一起改。**
- **問題**：搜尋結果與抽屜歷史列表讀的是同一個時間。§8 生效之後，抽屜的順序也會從「最近碰過的在最前」變成「最近真的有新內容的在最前」。這正是裁決三要的語意，但它發生在**搜尋以外的畫面**上，所以當時列為待拍板。
- **當初提出的兩個選項與代價，原樣保留**：
  - **(a) 兩邊一起改** —— 得到：系統裡只有一個時間概念，而且它是誠實的。**代價：使用者若把抽屜當成「我剛才看過的那幾段」在用，那條線索會消失。**
  - **(b) 只讓搜尋依內容時間排序、抽屜維持「碰過就浮上來」** —— 得到：抽屜保留近用線索。代價：必須引入第二個時間概念，兩個時間各自儲存、各自顯示；存檔格式要多一個欄位；而且從此每次講到「時間」都要先問是哪一個。
- **使用者在知悉 (a) 的代價之後，仍選 (a)。** 規格與任務 13 原本就照 (a) 寫，因此**沒有任何條文因為這次裁決而改變**。

## 歷次全驗的結論

**（v3.7 新增）為什麼這一節存在**：spec 原本在三個地方引用同目錄的 `test-report.md` 當證據，但流程要求每次重驗之前把上一輪的報告移出這個目錄（避免下一個 QA 讀到舊結論而破壞冷讀），**所以每當有 QA 在跑，那個引用就是斷的**。**這一節只留結論，不抄報告內容。**

| 驗收日期 | 範圍 | 結論 |
|---|---|---|
| 2026-09-10（受測的是 **v3.10** 那一版的條文） | 全驗：任務 1–14 的每一條、`spec-architecture.md` §A–§G 的每一條、`spec.md` §1–§8 的邊界情況 | **fail —— 但產品行為沒有找到任何缺陷。** 三條**條文本身判不出它宣稱要守的東西**：**任務 3 驗收 16** 指名的測試經突變證明**零鑑別力**（把不得切開 surrogate pair 的兩處保護整個拿掉，它照樣全綠）；**任務 9 驗收 3、7** 在當時的「未被本次改動觸及」三項判定基準下**照字面就不可能成立**。**三項全部在 v3.11 處理掉**：驗收 16 改成指定測資形狀（兩個方向各一案例）；驗收 3、7 拿掉「未被改動」那半句，只留在產物裡驗得到的後半句 —— 而那半句之所以被拿掉，是因為它**答不出使用者會遇到什麼**（見決策歷程「決策 F 被取消」）。 |
| 2026-09-09（同日第二次，受測的是 **v3.8** 那一版的條文） | 同上 | **pass，沒有任何一條 fail。** 三條依 spec 自己的但書記為「本環境無法觀察／無法執行」。**同輪回給 PM 四點條文品質問題**（都不影響 pass），四點全部在 v3.9／v3.10 結案。 |
| 2026-09-09 | 同上 | **fail（兩條不成立）**：**任務 4 驗收 6**（條文指名的測試不存在於測試套件裡 —— 條文 → 測試的缺口，歸屬 RD，已補）與**任務 10 驗收 6**（架構文件已多出兩處與本任務有關的更動，而 spec 的執行紀錄沒有列到 —— 歸屬 PM，已補記，該條文其後被移除）。**其餘全部 pass。** |
| v2 那一輪 | 任務 1–9 | **通過。** **誠實註記（v3.7）**：那一輪的報告已依流程移出本目錄，**這個結論目前沒有可查證的原始來源**，它的地位是「當時記下來的結論」而不是「隨時查得到的事實」。 |

## 已知取捨與風險

**每一次狀態變動都會重算全部結果**，包括串流時每秒好幾次。換到的是「不可能忘記重算」，代價是重複的工。目前對話量下這是划算的；**低階裝置沒有實測過** —— 若打字開始鈍，第一個該看的就是這裡，允許的緩解是空查詢提早返回（已做）與輸入停止後才觸發（可選）。

**開場白重複命中會灌大摘要數字。** 已接受（§3.7）。換到的是「使用者看過的任何一句話都搜得到」。

**分層搬移沒有自動化測試守著。** 它的全部風險都在「搬移時把既有行為改壞」，其中兩個最不顯眼的是空白對話刪檔與壞檔容錯（現行的 §9.1、§9.2）。可接受的前提是這一層保持零邏輯。

**兩個全螢幕子畫面的行為不一致**（返回鍵、旋轉）。這是刻意的。

**搜尋結果與歷史列表對同一則訊息的空白呈現可能略有不同**（見「明確不做」）。

**英文子字串會有非預期命中**（`cat` 命中 `concatenate`）。這是不斷詞的直接後果（§1.3），已接受。

**（v3.1）摘要的兩個數字保留了兩份寫法 —— 這與本文自己訂的判準有落差，而且是知情的。** 判準說「必須永遠相等的兩個量，該由同一份程式碼保證，讓不相等變成不可能發生」；這兩個數字正是那種關係，但它們現在由比對層與畫面各算一次，兩份寫法之間沒有任何東西保證它們相等。使用者是在被明確告知「保留 method 卻不用它，重複依然存在」之後裁決保留現狀，**所以這不是疏漏，是取捨**。
- **具體風險**：日後有人改動其中一邊，另一邊不會跟著變，摘要顯示的數字就會與畫面上實際列出的內容互相矛盾。**而且不會有測試發現** —— 比對層那兩個量今天沒有呼叫端，也沒有測試。
- **該回來重新處理的觸發條件（任一成立即可）**：(a) 摘要顯示的數字與畫面上實際列出的分組數／命中數出現不一致；(b) 出現**第三個**地方要用到同樣的量；(c) §3.4 對 N 或 M 的定義本身要改。
- **屆時要重新拍板一次**，**不要默默選一個**：這一輪的現狀是使用者裁決的結果。

**（v3，v3.1 定案）時間戳修正之後，「哪一段對話最近被碰過」這個線索會消失。** 這是決策 A 選 (a) 的已知代價，使用者知悉後仍選 (a)。
→ 觸發：使用者回報「我找不到剛才看過的那段對話了」。屆時該做的是新增第二個時間概念，而不是讓現在這個時間退回去被閱讀行為推動。

**（v3）「失去焦點時移除尾端空白」在這個畫面上可能觀察不到。** 搜尋畫面上會讓輸入框失焦的動作很少（點擊命中列會直接離開畫面、而離開會清空查詢）。**這條規則仍然要做**，因為它是「打字過程中不移除尾端空白」得以成立的另一半。驗收因此分成兩層：規則本身用不經過畫面的方式驗（任務 12 驗收 6），實機能不能看到失焦則記「本環境無法觀察」。**這裡我不確定的是：這條規則對使用者的實際可見價值有多大。** 它的成本很低，所以照裁決做。

**（v3）最後更新時間相同時的排序未規範。** 兩段對話的時間戳相同的機率極低（毫秒級），但一旦發生，它們的相對順序沒有定義。這次不處理；若日後出現「順序會跳來跳去」的回報，該補的是一條決勝規則（例如以建立時間或 id 決勝）。

**（v3.2）U+001C–U+001F 這個唯一的差分方向，實務上幾乎不會被使用者觸發。** 它們是 C0 控制字元，一般輸入法與剪貼簿內容裡都不會出現。**這不是把規則放寬的理由** —— §2.11 要守的是「四個使用點共用同一個答案」這個性質，而不是「擋住某個特定字元」；共用之後這四個字元只是它最容易被觀察到的後果。但誠實地說：如果哪天有人主張「改用內建 trim，實務上沒有差別」，他在**這四個字元上**的說法是對的，錯的是他把一個結構規則降格成了一個字元清單。
→ 觸發：若有人提出這個改動，回頭讀 §2.11 的後果那一段，而不是重新爭論這四個字元有多罕見。

**（v3.6）§8.6 的預期一相依於另一輪的修正，這是這份 spec 第一條跨輪相依的驗收。** §8.6 新的預期一（被停止的那則回覆，文字結尾是中斷標記）只有在 `stop-generation` 那一輪的修正落到受測建置之後才會成立；而那個中斷標記的**字串**也由那一輪定義，本文只引用。
- **具體風險**：兩件事。(a) **時序** —— 若 QA 拿到的建置還沒有那份修正，這一條會 fail，而成因不在本輪（處置寫在任務 13 驗收 3）。(b) **漂移** —— 若那一輪日後改了標記字串，§8.6 與任務 13 驗收 3 會靜靜地變成錯的，**沒有任何機制會通知這邊**。
- **為什麼仍然這樣寫**：唯一分得出「停止有沒有推進時間」的可觀察物就是那則回覆的文字（時間差是次秒級）。**要嘛引用那個字串，要嘛這一條繼續驗不到東西。**
- **(b) 那個漂移風險有一個對得上的機制（跨輪衝突由使用者裁決），但沒有消失**：那條規則要有人先注意到兩輪碰到同一個東西。本文的引用已寫成「**以那一輪的現行定義為準**」。
→ 觸發：`stop-generation` 那一輪改動中斷標記的字串或它的追加規則時，回頭改 §8.6 與任務 13 驗收 3。

**（v3.8）本輪掃掉的是「會被別人改掉而自己變錯」的引用，不是文件裡全部的機械資訊。** 留下來的是**描述一件已經結束的事、而且在同一個地方就列得出來**的數量（例如某次補掃掃過幾條規則）。它們不會因為別人改東西而變錯，但**如果有人把它們當成現況用**，一樣會被誤導。
- **另一個殘留風險**：那次是人工掃的，**沒有任何機制保證掃乾淨**。
→ 觸發：有人拿其中一個歷史數字當成現況來用；或又出現一次「別的輪次改了東西，而本規格裡某句引用因此變錯」。

**（v3.9）任務 10 的執行紀錄現在沒有任何驗收在要求它了，它會靜靜地過期 —— 這是知情接受的。** **它們仍然被兩件事需要**：驗收 3 的逐字基準在裡面（那一半不會過期，因為它記的是一個**已經發生過的版本**）；以及「哪一處由另一輪執行」這類跨輪事實。
- **具體風險**：紀錄裡描述現況的那些句子會隨程式碼與文件變動而變舊，而**沒有任何驗收會發現**。緩解只有一個，而且是弱的：那一區的開頭明寫著「不是現行狀態」。
→ 觸發：有人拿那一區的句子當成現況使用；或驗收 3 的逐字基準有一天需要更新。

**（v3.9）任務 10 驗收 6 移除之後留下的缺口。** 如果架構文件裡有一處與任務 10 有關、而其餘五條都沒指名到的更動是錯的，現在沒有任何條文會發現。**這是使用者裁決的直接後果，不是疏漏。**
→ 觸發：真的發生一次。屆時該做的是**替那件事補一條指名它的條文**，而不是把「有沒有漏掉什麼」的問法加回來。

**（v3.10）§6.5 從此沒有任何驗收在守，這是知情接受的。** 「有人為空白對話開了一條特例分支、而四種情境的行為仍然全對」這件事**不會被任何條文發現**。**接受它的理由就是降級的理由**：那個狀態下使用者感受不到差別，而且 §6.4 那句「走錯分支時看起來剛好無害是巧合、不得倚賴」仍然是規則、仍由任務 8 驗收 5 守。
→ 觸發（任一成立就該回頭看）：(a) 真的出現一次因為那種特例分支而壞掉的**使用者可見行為**（那時要補的是**那個行為**的條文）；(b) 有人主張要把結構條文加回來 —— 那時先讀 §6.5 底下記的實績（0 / 3 / 3）與決策歷程裡 QA 找到的那個反例（存取層的空白判斷），**那個反例沒有被解決，只是隨著條文一起消失了**。

**（v3.11）一整類結構性質從此沒有任何條文在守 —— 這是本輪最大的一塊知情接受。**

被移除的條文所守的那些性質，今天沒有任何東西會發現它們被違反：畫面端不得出現比對或整形程式碼、搜尋狀態聚成一組、不引入導覽框架、存取層對外只有四個方法且不是全域單例、跨層交出去的命中要含訊息本身、分層與折疊的說明文字要寫出理由、既有檔案有沒有被順手改動。

- **接受它的理由**：這些性質被違反時，**使用者感受不到任何事**。而它們可能造成的那些**看得見**的後果 —— 標示蓋錯字、該命中的沒命中、看得到的搜不到、列表崩潰、歷史長出空殼、整份歷史消失 —— **每一個都有行為條文在守**（§4.7、§4.8、§1.8、§3.6、§9.1、§9.2）。
- **換到的東西**：每一輪全驗少掉一批「讀程式碼判形狀」的條文，而那一批**已經證明是最容易腐爛的**（結構驗收在兩輪之內被改寫兩次的實例記在決策歷程）。
- **誠實寫出它的弱點**：這個推論靠的是「看得見的後果都被列全了」。**如果有一個後果沒被想到，它現在既沒有結構條文擋、也沒有行為條文驗。** 這一點無法自我證明。
→ 觸發（任一成立）：(a) 出現一次**使用者看得見**的問題，事後追出來的成因是上面那些結構性質之一被違反 —— 屆時要補的是**那個後果**的行為條文，**不是把結構條文整批加回來**；(b) 有人主張把某一條結構條文加回 spec —— 屆時要先回答「它被違反時使用者會遇到什麼」，答不出來就仍然只能是建議。

**（v3.11）架構決策變成建議之後，「RD 沒照它做」不會有任何人知道。**
- **這是刻意的**（架構是建議不是裁決），但代價要寫出來：`spec-architecture.md` 裡那些建議附帶的**理由**（例如「加了快取就同時改變了什麼時候看到新資料的語意，而且沒有任何呼叫端會改變，沒有人看得出來」）是這份 spec 累積下來最有價值的東西之一，而**沒有任何機制保證下一個人會讀到它**。
- **緩解只有兩個，都是弱的**：RD 若決定不照建議走，有義務在收件匣裡講出來（那是角色定義的要求，不是 spec 的條文）；以及這一節本身寫得夠具體，讓讀到的人知道自己會踩到什麼。
→ 觸發：某一條建議被不採用，而事後證明那個選擇造成了使用者看得見的問題 —— 那一刻該做的是**替那個後果補一條行為規格**，而不是把建議升回硬性條文。

**（v3.11）`spec-architecture.md` 開頭那張「去向表」是本輪新增的、需要維護的東西。**
- 它指的是章節而不是行號或數量，所以別人在另一輪改動不會讓它自己變錯。**但如果本規格自己日後又搬動某一條規則，那張表就要跟著改** —— 它是目前唯一一份「舊架構代號 → 現在位置」的對照。
- 附錄的「舊 spec 代號對照表」**刻意沒有逐格改寫**：逐格改寫會多出 30 幾個可以出錯的地方，而它們指的是同一件事。**代價是那張舊表裡的 §A–§G 標的是 v3.10 之前的位置**，讀的人必須先看到表格上方那句提醒。
→ 觸發：有人照舊代號對照表去找一條架構決策而找不到 —— 那代表那句提醒放得不夠顯眼，該做的是把提醒往上移，不是逐格改寫。
