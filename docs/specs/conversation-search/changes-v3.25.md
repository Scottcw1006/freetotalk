# v3.25 變更說明（對話內容搜尋）

> **這一份是索引，不是規格。** 規則的原文在 `v3.25/spec.md`、`v3.25/spec-tasks.md`、`v3.25/spec-architecture.md`、`v3.25/spec-decisions.md`、`v3.25/spec-appendix.md`。這裡只告訴你**這一輪改了哪裡、要去讀哪幾節**。
> **入口檔是 `v3.25/spec.md`**（v3.24 之後的最新版號目錄）。`v3.24/` 一個字都沒有動；v3.25 只複製了五份 spec 檔（v3.24 目錄裡本來就沒有 `test-cases`／`test-report`）。

# 這一輪改了什麼

**只改引用。** 「切換後歷史清單漏掉對話」那份 bug 規格（目錄 `docs/specs/history-missing-after-switch/`）的正確行為與決策 D1，已被 `docs/specs/conversation-store-db/` 吸收並驗收通過；使用者要刪掉那個目錄，所以本規格現行版裡指向它的地方，全部改指 `conversation-store-db`。

- **使用者裁決：這一輪不算 `conversation-store-db` 決策 D4 所說的「修訂」。** 本規格裡綁 JSON 檔的條文（「對話檔」「對話目錄」「把檔放進對話所在的目錄」等）**沒有改寫**，那份改寫清單留到本規格真正修訂時。
- **使用者裁決：這一輪不跑 QA 驗收。**
- **沒有新功能、沒有 bug、沒有改動任何規則，也沒有改變任何驗收要驗的性質；沒有要求任何實作改動；`spec-decisions.md` 維持 0 項待決；`spec-architecture.md` 與 `spec.md` 一個字都沒有動。**
- **`v3.25/` 裡已經沒有任何一處出現那個舊目錄名**（五份檔案搜過，零筆）。舊版號目錄（v3.24 以前）與舊的 `changes-v3.15.md`、`changes-v3.18.md`、`changes-v3.20.md` 裡仍有，它們是當時交出去的樣子，不改。

# 本輪處理並移除的收件匣項目

| 收件匣項目（標題與日期） | 實質內容現在在哪 |
|---|---|
| [2026-09-17] 使用者裁決：把指向 history-missing-after-switch 的引用改指 conversation-store-db，之後刪掉那個目錄 | 改寫後的引用：`v3.25/spec-tasks.md` 任務 16 驗收 1（移除理由）、「明確不做」「任務 16 的行為不要求自動化測試」；裁決內容、範圍界線（不算 D4 的修訂、不跑 QA）、觸發條件的判斷理由、知情留著的過期敘述：`v3.25/spec-appendix.md`／決策歷程「只改引用的一輪：被吸收的 bug 規格要刪掉之前」；PM 自己做的判斷：`v3.25/spec-decisions.md`「本輪（v3.25）我自己做的判斷」 |

**三個前置條件逐一確認過**：(1) 實質內容都已進 spec 與附錄（上表）；(2) 本檔就是那份索引；(3) 這一筆不是「維持現狀的 bug」，沒有重現步驟需要安置。

# 這一輪要做的任務（完整清單）

**這一輪沒有任何人要執行任何任務。** RD 沒有實作工作；依使用者裁決，QA 這一輪不驗收 —— 包括平常每輪必做的那幾條。

- 任務 1–15、17–19　〔未變動；**本輪不執行**（使用者裁決不跑 QA）〕
  - 其中任務 9（回歸驗證）、任務 10（更新架構文件）、任務 15（`features.md` 的引用）、任務 19（`features.md` 的「未經查證」）平常每輪必做，**本輪同樣不執行**。
  - **任務 10 的結論**：架構文件本輪無需更新 —— 本輪只改了 spec 裡指向別份 spec 的引用，`plan/architecture.html` 描述的東西沒有任何一項因此改變。`v3.25/spec-tasks.md` 任務 10 的「目前狀態」**沒有改寫**（依 2026-09-17 起的寫法規則，本文不新增版本標註），它寫的「架構文件無需更新」對本輪仍然成立。
- 任務 16　只含助理訊息的既有對話檔，打開再離開就清掉　〔**修改**：只有**驗收 1 的移除理由**改寫成現況（驗收 1 仍是移除狀態、編號不遞補）；驗收 2–5 一字未改。**本輪不執行**〕

# 本輪移除或失效的任務／驗收

- 無。

# 改動的規格章節（要讀原文的地方）

- `v3.25/spec-tasks.md`
  - **任務 16 驗收 1** 的「移除理由」：拿掉「在這裡要求那個測試，等於繞過那次暫緩」（前提已不存在），改寫成現況 —— 存取層那個判斷由 `conversation-store-db` 任務 1 驗收 4 守，狀態層那一半沒有自動化測試。
  - **「明確不做」「任務 16 的行為不要求自動化測試」**：理由、代價、觸發條件改寫成現況。**這條的標題沒有改**（`conversation-store-db` 的任務 1 驗收 4 以這個標題指進來）。
- `v3.25/spec-decisions.md`：目前狀態換成 v3.25（仍是 0 項）、新增本輪判斷六項，v3.24 那兩節降為歷史；歷史敘述裡五處舊目錄名改寫。
- `v3.25/spec-appendix.md`：修訂記錄新增 v3.25 一列；決策歷程最前面新增「只改引用的一輪：被吸收的 bug 規格要刪掉之前」；歷史敘述裡九處舊目錄名改寫（提到 H1、H4 的地方補上它們在 `conversation-store-db` 的節號與節名）。

# 那條觸發條件怎麼寫成現況

舊的觸發條件是「那一輪的決策 D1、或『對話儲存改成資料庫』那一題拍板時，回來補一個自動化測試斷言任務 16 驗收 2 的預期」。**判定：已成立一半。**

- **成立的那一半**：`conversation-store-db` 已上線，「存一段沒有使用者訊息的對話判成刪除、有使用者訊息的判成寫入」由它的任務 1 驗收 4 以不碰資料庫的測試守。
- **沒成立的那一半**：驗收 2 還要求四種離開與冷啟動入口都會先存當下那段 —— 那在聊天狀態那一層，沒有任何自動化測試。
- **所以那一條保留**，內容改寫成兩半各自的現況，剩下那一半的觸發條件改成「聊天狀態那一層可以在不經過裝置的測試裡直接驅動時（`conversation-store-db` 的 T1）」。
- **沒有改到任何規則或驗收**：這一條在「明確不做」裡；任務 16 驗收 2 要驗的東西一字未變。理由在 `v3.25/spec-appendix.md`／決策歷程，判斷列在 `v3.25/spec-decisions.md` 讓使用者可以否決。

# 未改動但與本輪改動互相牽動的章節

- **`spec-tasks.md` 任務 16 驗收 2**：本輪沒有動它，而它仍是狀態層那一半唯一的守衛。
- **`docs/specs/conversation-store-db/spec-tasks.md` 任務 1 驗收 4**：本規格現在靠它守存取層那一半。那份規格改動這一條時，本規格「明確不做」那一條的現況敘述要跟著核對。
- **知情留著、已經過期的敘述**（本輪只改引用，不處理）：列在 `v3.25/spec-appendix.md`／決策歷程「只改引用的一輪：被吸收的 bug 規格要刪掉之前」的「沒有改的、已經過期的敘述」。
- **`v3.25/spec.md` 卷首**仍寫著 v3.24 那一段（標著「本輪」）；依寫法規則沒有新增 v3.25 的段落，舊標註本輪不清。

# 規則 → 條文的反向核對

**本輪判定為「被改動」的章節：`spec.md` §9。** 依據：本輪改寫的任務 16 驗收 1 位於任務 16，而任務 16 守的是 §9.1、§9.4（規則字面一個字都沒有動；判不出來算被改動）。**沒有被列進來的章節（§1–§8），等於本輪明確宣告它們不需要核對** —— 本輪沒有改它們的任何文字，新寫的文字也沒有引用它們的節號。

| 規則 | 由哪一條守 |
|---|---|
| §9.1 存空白對話等於刪檔 | 任務 1 驗收 6；任務 8 驗收 5、13、14；任務 16 驗收 2、4 |
| §9.2 壞檔不得讓其他對話消失 | 任務 1 驗收 6 |
| §9.3 兩條取用路徑答案一致 | 任務 1 驗收 10 |
| §9.4 按垃圾桶就是真的刪掉 | 任務 16 驗收 3 |

- **本輪沒有補任何新的驗收條文**，也沒有拿掉任何覆蓋：任務 16 驗收 1 在 v3.15 就已移除，本輪只改寫它的移除理由。
- `conversation-store-db` 任務 1 驗收 4 是另一份規格的條文，**不算進上表**；它讓 §9.1 多了一層自動化的守衛，但本規格不依賴它來回答「有沒有條文在守」。
