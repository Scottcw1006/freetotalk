# 驗證結果總覽

**pass。** conversation-search v3.23 的 19 個任務、140 條驗收標準全部驗過，沒有任何一條 fail；其中 2 條依條文明文記為「本環境無法執行／未能觀察」（任務 5 驗收 2、任務 9 驗收 12），1 條走了條文明文允許的替代做法並附上真正動作失敗在哪一步（任務 12 驗收 1 的「長按空白鍵」與「貼上單一 U+3000 / U+00A0」）。

**受測版本**：`a0229fe`。建置與安裝當下工作區乾淨（`git status --porcelain` 無輸出）。
**建置與安裝**：`./gradlew clean testDebugUnitTest installDebug`，BUILD SUCCESSFUL；`:app:clean`、`:app:testDebugUnitTest`、`:app:installDebug` 三個 Task 都實際執行（log 無 UP-TO-DATE／FROM-CACHE）。
單元測試結果 XML：9 個檔、**tests 136 / failures 0 / errors 0 / skipped 0**。
安裝證據：`lastUpdateTime=2026-09-16 15:50:40`，晚於產品程式碼最後一次變動 `0afd2a6 2026-09-16 02:17:27`。
**驗收裝置**：Android 模擬器 `emulator-5554`（AVD Pixel_9），Android 17，1080x2424、density 420（無 override），`/data` 尚餘 12G。裝置時鐘與主機時鐘本輪一致（同秒）。

> **本輪開始時，本版號目錄裡沒有既有的 `test-cases.md` 或 `test-report.md`**（`ls -a` 只有五份 spec 檔），所以沒有刪除任何檔案。

> **工作區在驗收途中被別人動過，要知道**：`docs/specs/conversation-search/pm-inbox.md` 在 16:49 多了 10 行（`git diff --stat` 判定，**我沒有讀它的內容**，依角色規則那份檔案連讀都不讀）。那不是我做的。**產品程式碼（`app/src`）與 `plan/` 全程乾淨**，所以受測的建置仍然等於 HEAD 的產品程式碼。本輪由我產生／修改的檔案是：`docs/specs/conversation-search/v3.23/test-cases.md`、`docs/specs/conversation-search/v3.23/test-report.md`、`docs/qa-tools/uitext.py`、`docs/qa-environment.md`、`docs/qa-random-coverage.md`。

---

# 逐項驗收結果

## 任務 1：對話存取的分層抽出

- **驗收 6（既有行為逐項不變，在裝置上）**：**pass**
  - 冷啟動續接（檔案不存在）：清空使用者資料後冷啟動 → 開在一段只有開場白的新對話（🙌 朋友／Gemma3 1B）。
  - 冷啟動續接（檔案存在）：`session.xml` 指著 `qa01` 時冷啟動 → 續接到 qa01；另一次指著「填充對話 17」那段冷啟動也續接到它。
  - 空白對話不進歷史：切人格／開始新的對話產生的空白對話，前後 `files/conversations` 檔案數與內容完全相同（多次量測，19 檔不變）。
  - 歷史排序：抽屜由新到舊（16:27 → 15:57 → 9/11 → 9/10 → …）。
  - 開啟／刪除／送出後更新：點列可開啟；刪除鈕按下後那一列消失且檔案少一個；在 qa01 送出訊息並收到完整回覆後，qa01 的 `updatedAt` 由 15:57:02 前進到 16:39:58，並在抽屜與搜尋結果都排到最前。
  - 壞檔容錯：推入一個內容為 `this is not json {{{` 的 `.json` 後冷啟動，抽屜仍完整顯示其餘 **18** 列（19 個可讀檔扣掉開著的那一段），App 未崩潰。
- **驗收 10（依 id 取一筆 == 取全部再挑同一個 id）**：**pass**。`ConversationRepository.find(id)` 的實作就是 `all().firstOrNull { it.id == id }`，兩條路徑在結構上同源，包含「兩邊都找不到」。冷讀位置：`app/src/main/java/com/example/demo/data/ConversationRepository.kt`。

## 任務 2：比對與片段的純邏輯

- **驗收 1**：**pass**。`chat/ConversationSearch.kt` 整檔**沒有任何 import**（第一行 package 之後直接是註解），全套 JVM 單元測試直接呼叫它並通過。
- **驗收 2、3、4、5、6、8、9、11**：**pass**，各有對應測試並實際執行（見任務 3 逐條）。驗收 11 另以冷讀確認：`SearchUiState.isActive` 的實作是 `query.flattenToLine().isNotEmpty()`，不是 `isBlank()`。
- **驗收 7（整形與折疊能被單元測試直接呼叫並單獨斷言）**：**pass**。`flattenToLine()` 與 `normalizeForSearch()` 都是 `internal`，測試 `every unicode separator is collapsed`、`a run of mixed whitespace becomes exactly one space`、`normalising never changes the length of the text` 直接呼叫並單獨斷言。
- **驗收 10（整形後的字串不得被寫回查詢欄位）**：**pass**。狀態層對查詢欄位只套用 `withoutLeadingSpace()`（每次輸入變更）與 `withoutTrailingSpace()`（失焦），沒有任何地方把整形結果寫回；裝置上逐碼位確認欄位可以留下 `北京 天氣`（U+5317 U+4EAC **U+0020** U+5929 U+6C23）與尾端空白。

## 任務 3：比對語意的單元測試（1–26）

**全部 pass。** 逐條核對「條文指名的測資形狀與預期」是否真的寫在測試裡：

| 條 | 對應測試 | 核對到的關鍵斷言 |
|---|---|---|
| 1 | `a message the user wrote is found` / `a message the assistant wrote is found` | 作者分別為 You / Assistant |
| 2 | `matching ignores case` | `hello` 與 `HELLO` 各一 |
| 3 | `matching ignores full-width and half-width` | `ABC`→`ＡＢＣ`、`ｈｅｌｌｏ`→`Hello` 兩個方向 |
| 4 | `traditional and simplified are not folded together` | 「台」不命中「臺」，且「臺」命中（正向對照） |
| 5 | `surrounding whitespace in the query is ignored` | `assertEquals(1, bare.size)` **不得為空**＋`assertEquals(bare, padded)` 比整個結果 |
| 6 | `an empty query is not a search` | 案例逐碼位確認為 `""`、`"   "`、`"\n\t"`、**U+3000**、**U+00A0** |
| 7 | `a query that matches nothing returns nothing` | — |
| 8 | `normalising never changes the length of the text` | 含 U+0130（İ）斷言長度不變 |
| 9 | `a highlight points at the matched text even when the message had newlines` | 訊息「第一行\n這裡有北京兩個字」，取出恰為「北京」 |
| 10 | `every match inside the window is highlighted` | 2 段標示都取出「北京」 |
| 11 | `a hit deep inside a long message is cropped with a leading ellipsis` | 前後「…」＋長度 62＋標示正確 |
| 12 | `a conversation shows at most three hits but reports the true total` | 顯示 3、總數 5 |
| 13 | `the persona opener can be searched` | 命中為第 0 則、Author.Assistant |
| 14 | `a thread holding nothing but its opener still matches` | 顯示 1、總數 1；不命中時完全不出現 |
| 15 | `a shared opener matches in every thread that has it` | 三段全部出現且順序正確 |
| 16 | `cropping never splits a surrogate pair` / `…at the window end` | 測資形狀與條文一字對上（15 emoji＋1 BMP＋查詢詞＋60 尾巴；30 BMP＋查詢詞＋7 BMP＋20 emoji）；兩案例都斷言無落單代理**且**片段仍含查詢詞 |
| 17 | `every unicode separator is collapsed` / `zero-width characters are not whitespace` / `a run of mixed whitespace becomes exactly one space` | 下限用 BMP 全掃且斷言字面就是 `"a${c}b".flattenToLine() != "a b"` 的篩選、違反清單為空；上限 U+200B/200C/200D/FEFF ＋ ZWJ 家庭不變；收合與去除 `"北京\r\n\t 　天氣"`→「北京 天氣」 |
| 18 | `a message written with a full-width space is found with a plain one` | — |
| 19 | 四個案例分別是 `a query written with a full-width space finds a plain message`、`a run of spaces in the query matches a single space`、`a message written with a non-breaking space is found with a plain one`（訊息端逐碼位確認為 U+00A0）、`a query of nothing but whitespace is not a search` | 四個都走 `searchConversations` 入口 |
| 20 | `a highlight is as long as the flattened query, not what was typed` | 長度 5、取出「北京 天氣」 |
| 21 | `a query with a space inside is one string, not two words to find separately` | 正向＋否定兩半 |
| 22 | `isActive agrees with flattening about what counts as empty` | U+3000／U+00A0／`" \t\n"` 非搜尋、`"a"` 搜尋，**加上 BMP 全掃斷言不存在「認為在搜尋但整形後為空」的字元** |
| 23 | `a hit shorter than the window keeps its lead-in` / `…exactly fills the window has no lead-in` / `…longer than the window is clamped` | L=59 片段「…」＋20 甲＋40 乙＋「…」且引子 20；L=60 片段不含「甲」、標示 start=1 長度 60、**並斷言 59 與 60 的片段不同**；L=61 與 L=60 片段與標示完全相同 |
| 24 | `persona and model names are not searched` | 「老師」「Qwen」「qwen」皆不命中＋「想不通」必須命中 |
| 25 | `ellipses appear only where text was actually cut` | 短訊息不得有「…」與長訊息必須有「…」寫在**同一個測試方法**裡 |
| 26 | `searching finds message text rather than the derived title` | 標題「還沒說話」不命中＋開場白裡的字必須命中 |

## 任務 4：「所有對話」的定義與搜尋子狀態

- **驗收 2**：**pass**（`allThreads contains the open thread even when it is blank`：空白的開啟中那段在清單裡且排序依 updatedAt）。
- **驗收 3**：**pass**（`allThreads keeps the in-memory copy when a thread arrives twice`：同 id 只出現一次、訊息較多那份）。
- **驗收 6**：**pass**（`the in-memory copy wins even when the saved one looks newer`：斷言 `listOf("other","same")` 的**順序**與 `same` 的命中數為 2，排序位置那一半在）。

## 任務 5：狀態層的搜尋入口與衍生結果

- **驗收 2（串流中搜尋，盡力而為）**：**未能觀察（依條文不 fail）**。詳情見下方「本環境做不到的事」。**同一節要求的間接佐證則直接觀察到了**：送出後 t≈2.2 秒，`run-as ... grep -l zzpeach files/conversations/*.json` 在磁碟上**找不到任何檔案**含該字串、也沒有新增檔案，而搜尋畫面此時對查詢 `zzpeach` 顯示「1 段對話・2 則訊息」（使用者訊息與助理回覆都命中）。也就是**尚未落盤的新訊息（含當下的回覆文字）確實搜得到**。
- **驗收 5（旋轉後仍在搜尋畫面，查詢與結果都在）**：**pass**。查詢「北」→ 轉橫向後仍在搜尋畫面、欄位逐碼位仍為 U+5317、摘要仍是「6 段對話・9 則訊息」、輸入框仍在頂端（bounds y=138–304）。
- **驗收 6（搜尋期間送出訊息、停止生成、切換人格/模型等既有行為不受影響）**：**pass**。送出、串流、停止、切換人格、切換模型本輪各做過多次，全部正常（細節見任務 13 驗收 8）。
- **驗收 7（切人格後的空白對話可用開場白搜到）**：**pass**。切到 🧒 孫子 → 搜「你」→ 那段從未存檔的新對話出現且排在最前（標頭時間 16:25），檔案數維持 19 未變。
- **驗收 8（搜尋全程不觸發推論、不產生檔案讀寫）**：**pass**。`hashdir.sh` 前後兩份雜湊 `diff` 無輸出（**逐檔位元組相同**）；狀態列文字前後皆為「Qwen2.5 0.5B・離線」；以 pid 過濾的 logcat 共 20 行、含 `llm|mediapipe|inference|.task` 的行數為 **0**；**正向對照**：同一次操作的查詢產生「6 段對話・8 則訊息」的非空結果。

## 任務 6：抽屜入口與搜尋畫面外殼

- **驗收 1**：**pass**，逐項：
  - (i) 上半部依序為：模型清單（Gemma3 1B／Qwen2.5 0.5B／SmolLM 135M）→「並排比較所有模型」→「歷史紀錄」＋「每段對話各自獨立，不會互相影響」→「開始新的對話」→「搜尋對話內容」。推入 20 段對話讓列表需要捲動後，捲動歷史列表時上半部節點 bounds 完全不動（模型 184–247、搜尋對話內容 1214–1267 前後一致），抽屜裡只有歷史列表會動。
  - (ii) 三個入口都是點下去就動作的列（dump 裡不是 EditText）：「並排比較所有模型」→ 進入比較畫面；「開始新的對話」→ 開一段新對話且人格與模型不變（老師／Qwen2.5 0.5B → 老師／Qwen2.5 0.5B）；「搜尋對話內容」→ 進入搜尋畫面。三者點下去抽屜都關閉。
  - (iii) 每一列都有人格 emoji、人格名稱、模型名稱、時間、標題、預覽、`content-desc="刪除這段對話"` 的刪除鈕。**時間兩種寫法都看到**：今天更新的顯示「15:57」，不是今天的顯示「9/11」「9/10」…。省略號兩個方向：長英文句那一段標題為 `this is a very long first …`、預覽為 `this is a very long last message used as…`（**尾端都有「…」**）；短句那一段標題與預覽都是整句「你好」且**尾端沒有「…」**。
  - (iv) 點一列 → 開啟那段對話、抽屜關閉；按刪除鈕 → 那一列立刻消失、**沒有任何確認步驟**。
  - (v) 清空資料後冷啟動 → 歷史列表位置顯示「還沒有其他對話。」。
  - (vi) 開著 qa01 時列表上有 qa02、沒有 qa01；從列表點 qa02 之後再打開抽屜，列表上有 qa01、沒有 qa02。
- **驗收 2**：**pass**（搜尋入口是列不是輸入框，點擊後抽屜關閉並進入搜尋畫面）。
- **驗收 3**：**pass**（進入時 dump 顯示 EditText `focused="true"`，`mInputShown=true`）。
- **驗收 4**：**pass**（捲動結果前後 EditText bounds 皆為 `[148,143][1058,309]`）。
- **驗收 5**：**pass**。畫面上的返回鍵 → 回聊天；系統返回鍵：鍵盤開著時第一次 BACK 只收鍵盤（`mInputShown` true→false、欄位內容不變），第二次 BACK 回到聊天畫面，`mCurrentFocus` 全程仍是 `com.example.demo/.MainActivity`（**沒有退出 App**）。
- **驗收 7（比較畫面跑得出每個模型的回答）**：**pass**。抽屜 →「並排比較所有模型」→ 進入比較畫面；模型勾選保持預設（三個都在），輸入「Say hello」按「執行」→ 三張卡片依序跑完，每張都顯示「完成」且都有回答文字（Gemma3 1B「你好！」、Qwen2.5 0.5B「你好！有什么可以帮助您的吗？」、SmolLM 135M 一段較長的英文回答）。按左上角箭頭 → 回到聊天畫面。沒有卡片顯示「失敗」，畫面沒有出錯或崩潰。
  - *判讀方式註記*：SmolLM 那張卡片的回答文字**不在 uiautomator dump 裡**（dump 只看得到卡名與「完成」），改用截圖才看得到；已把這個環境特性記進 `docs/qa-environment.md`。
- **驗收 8**：**pass**（旋轉後仍在搜尋畫面、輸入框仍在頂端；橫向＋鍵盤的可見量依 §7.3 不判）。
- **驗收 9（即時、沒有送出鈕）**：**pass**。以裝置上的注音輸入法打「北」→ **不按任何鈕、不收鍵盤**，輸入停止 1 秒內出現「6 段對話・9 則訊息」；接著打「京」（同樣不做其他動作）→ 結果換成「4 段對話・5 則訊息」。判讀當下 `mInputShown=true`。**否定那一半**：搜尋畫面上只有左上角「返回」與右上角「清除搜尋」兩個控制項，兩者都不是搜尋觸發鈕 —— 判準「不按它結果也已經是對的」成立（上面兩次結果都是在沒有按任何鈕的情況下出現的）。

## 任務 7：結果列表

- **驗收 1**：**pass**（結果列表佔滿輸入框以下空間、可整片捲動；捲動後最上面的命中列被捲到 y=310 仍在列表內）。
- **驗收 2**：**pass**。命中字元以主題色加粗顯示（截圖可見「北京 天氣」整段變色加粗、其餘文字不變），位置正確。**顏色取自主題不寫死色碼**：`SearchScreen.kt` 的 `withHighlights` 用 `MaterialTheme.colorScheme.primary`；該檔案裡唯一的字面顏色是 TextField 容器／指示器的 `Color.Transparent`，與標示無關。
- **驗收 3**：**pass**。查「你」時 qa04 與 qa11（同為姊姊）、qa03 與 qa10（同為情人）的**第 0 則同時命中**，5 組 7 列全部正確列出、捲動不錯亂、不崩潰。
- **驗收 4**：**pass**（5 則命中的那組顯示 3 則＋「還有 2 則符合」）。
- **驗收 5**：**pass**（長片段單行顯示、尾端以「…」截斷，中文、英文、標點、emoji 皆未破版）。
- **驗收 6（中文算繪，查詢經過裝置上的中文輸入法產生）**：**pass**。輸入法：**Gboard 繁體中文（台灣）注音版面**；送鍵方式：`adb shell input text` 送注音鍵序＋`input tap` 點候選字（組字、選字都走輸入法，**沒有**繞過輸入法直接塞字串）。測資 qa11 的訊息為「…一場超級大雨🌧接著我就開始想念**北京**的天氣…」，emoji 與命中距離 8 個字（在 20 字內），片段視窗必涵蓋它。結果：標示恰好蓋在「北京」兩個字上、🌧 完整算繪、片段單行未破版、中文標點正常。
- **驗收 7**：**pass**（分組標頭只有人格 emoji／人格名／模型名／時間，沒有對話標題；空白對話的標頭沒有出現「還沒說話」）。
- **驗收 8（§7.1）**：**pass**。直向、1080x2424/420、鍵盤彈出（ime frame 上緣 1452）、不捲動、不收鍵盤，完整看到 **4 個對話標頭 ＋ 5 個命中列**（遠超過 1＋4）。
- **驗收 9（§7.2 小螢幕）**：**pass**。`wm density 606`（等效 285x640dp，比 360x640dp 更嚴格）後重跑，ime frame 上緣 1246，完整看到 **2 個標頭 ＋ 3 個命中列**（超過 1＋2）。收尾已 `wm density reset` 並讀回 420 確認。
- **驗收 11（標頭四樣都在且會跟著改變）**：**pass**。三組標頭分別是「🙌 朋友／Gemma3 1B／9/5」「📘 老師／Qwen2.5 0.5B／9/4」「💗 情人／SmolLM 135M／9/3」—— 四樣都看得到，換一段人格或模型不同的對話時四樣都跟著改變。

## 任務 8：空狀態、無結果、點擊與返回

- **驗收 1**：**pass**（剛進入只有引導文案「搜尋所有對話的訊息內容」，沒有「找不到…」、沒有空的結果列表）。
- **驗收 2**：**pass**（輸入無命中字串後畫面上只有「找不到符合「kiwi  」的訊息」一句，無殘留舊結果）。
- **驗收 3**：**pass**（點另一段對話的命中 → 回到聊天且內容是 qa06；再次進入搜尋時欄位逐碼位為空）。
- **驗收 4**：**pass**（點目前開啟對話的命中 → 回到聊天、內容不變、以 pid 過濾的 logcat 只有 7 行 IME/視窗訊息、沒有模型載入紀錄、沒有新建對話）。
- **驗收 5**：**pass**（切人格產生空白對話 → 搜其開場白 → 點擊 → 回到聊天、對話不變、無模型載入紀錄、檔案數 19 不變、內容完全相同）。
- **驗收 6**：**pass**（搜尋畫面上 `刪除這段對話` 節點數為 **0**）。
- **驗收 7**：**pass**（三段分別命中 5／2／2 → 摘要「3 段對話・9 則訊息」，畫面上列出 3＋2＋2 筆）。
- **驗收 8**：**pass**（三種離開都清空查詢：畫面返回鍵、系統返回鍵、點擊命中列各驗一次，再次進入欄位皆為空；旋轉不是離開，查詢「北」與結果「6 段對話・9 則訊息」都保留）。
- **驗收 9**：**pass**（Home → `am kill` → `pidof` 無輸出 → `am start` 回報 `its current task has been brought to the front`：停在搜尋畫面、查詢為空、顯示引導文案。依條文此為可接受狀態）。
- **驗收 10**：**pass**（逐碼位）。輸入 `"  kiwi  "` → 欄位 `k i w i U+0020 U+0020`，文案逐碼位為 `找不到符合「kiwi  」的訊息`（引號內確實兩個 U+0020）；使輸入框失焦後欄位為 `kiwi`、文案同步變成 `找不到符合「kiwi」的訊息`。
- **驗收 11**：**pass**。搜尋畫面在未搜尋／有結果／無命中三種狀態下，結果區只出現引導文案、結果列表、「找不到…」三種樣子之一，**沒有任何進度指示、骨架列或「載入中」字樣**。**正向對照**：同一次執行裡歷史載入完成後的查詢，結果確實包含歷史裡的對話（例如「北」→ 6 段對話全部來自歷史檔）。
- **驗收 12**：**pass**。輸入查詢後 `shared_prefs/session.xml` 的內容仍只有 `last_thread_id` / `last_model` / `last_persona` 三個鍵，**找不到查詢字串**（grep 該字元數為 0），鍵的組成沒有增加。**正向對照**：那組游標確實還在（冷啟動仍續接到 qa01）。
- **驗收 13**：**pass**。在對話目錄放一個**只含助理訊息**的檔 `xonly1`（id 與開著的那段不同），搜 `zzxonly` 並點擊那一列 → **切換到那段舊對話**，聊天畫面顯示「這是助理自己說的 zzxonly 內容」。
- **驗收 14**：**pass**。前置條件都滿足（被切走的那段 qa01 含使用者訊息；基準線在前置擺設做完之後才取，取基準線到操作結束之間沒有送出任何訊息）。三種情境各做一次，每次以「解析後內容」比對整個對話目錄：
  - §6.2（點另一段對話）→ 19 檔對 19 檔，`IDENTICAL CONTENT`
  - §6.3（點目前這段）→ 19 檔對 19 檔，`IDENTICAL CONTENT`
  - §6.4（空白且未存檔那段）→ 19 檔對 19 檔，`IDENTICAL CONTENT`
  - 比對涵蓋訊息集合、每則訊息的文字與作者、以及 `updatedAt`；**沒有多出新的對話檔，每一段既有對話讀回來的內容也都相同**。（依 v3.23 的新基準，不判位元組、不判有沒有發生寫檔。）

## 任務 9：回歸驗證與中文算繪的手動清單

- **驗收 2**：**pass**。`chat/TextRepair.kt` 裡 grep 不到 `flattenToLine` / `normalizeForSearch` / `withoutLeadingSpace` / `withoutTrailingSpace`；反方向 `ConversationSearch.kt` 也沒有引用 `TextRepair` / `repair`。
- **驗收 3**：**pass**。「送進模型的上下文」那道過濾指得出來，在 `ChatViewModel.kt:242–244`：`thread.messages.map { Turn(...) }.dropWhile { !it.fromUser }`（再加上 `LlmEngine.buildPrompt` 的 token 預算裁切）。它**不是**搜尋用的那一份 —— 搜尋走 `searchConversations(query, allThreads)`，沒有經過任何 `dropWhile`／過濾。
- **驗收 4**：**pass**（依 v3.21 指向任務 1 驗收 6，該條已判 pass，結論與證據見上面任務 1）。
- **驗收 5**：**pass**（依 v3.21 指向任務 6 驗收 1，該條已逐項判 pass，結論與證據見上面任務 6 驗收 1）。
- **驗收 7**：**pass**。同一則含 U+3000 的訊息：抽屜歷史列的**預覽**逐碼位讀出來是 `北京 U+3000 天氣如何`（**U+3000 原樣保留**），而搜尋片段逐碼位是 `北京 U+0020 天氣如何`（**收成一個半形空白**）。兩份攤平規則確實不同。
- **驗收 8**：**pass**（依 v3.21 指向任務 7 驗收 2／3／5／6 與任務 11 驗收 2，四條都已判 pass）。
- **驗收 9**：**pass**（與任務 7 驗收 6 同一次觀察）。qa11 那一列的片段逐碼位為 `U+2026 …（60 個 code unit）… U+2026`，**前後兩端都看得到「…」**。
- **驗收 10**：**pass**。以注音輸入法打「台」（U+53F0）→「2 段對話・2 則訊息」，只命中含「台北」的兩段；打「臺」（U+81FA）→「1 段對話・1 則訊息」，只命中含「臺北」的那段。欄位逐碼位確認兩次是兩個不同的字，**輸入法沒有把「臺」收成「台」**。
- **驗收 11**：**pass**。測資由檔案推入：一則訊息在兩個中文詞之間放 **U+3000**、另一則放 **U+00A0**；操作時以注音輸入法打「北京」，**按一般的半形空白鍵**（`keyevent 62`，欄位逐碼位確認為 U+0020），再打「天氣」。預期成立：**兩則都命中**（「2 段對話・2 則訊息」），且標示恰好蓋住整個詞組「北京 天氣」（含中間那一個空白位置，截圖可見整段變色加粗，後面的「如何」「真好」未被標示）。
- **驗收 12（選作）**：**本環境無法執行**。詳情見下方「本環境做不到的事」。
- **驗收 13**：**pass**。判讀 8–11 的那一刻鍵盤都沒有被收起來（判讀「北」→「北京」與 §7.1／§7.2 量測時明確以 `mInputShown=true` 與 `type=ime frame` 確認；其餘各條是以輸入法打完字之後直接判讀，中間沒有做過收鍵盤的動作）。

## 任務 10：更新架構文件（冷讀 `plan/architecture.html`）

- **驗收 1**：**pass**（「搜尋」那一節寫著「開頭的空白一打就消失」「尾端的空白…要等到輸入框失去焦點才移除」「拿去比對的一律是整形過的文字」）。
- **驗收 2**：**pass**（規則對照表「（只有空白）→ 任何 → 不算搜尋」那一列接著寫「**這是比對層的契約：搜尋框本身打不出這種輸入（見下），但比對層仍然必須答對**」）。
- **驗收 3**：**pass**，三件事都成立：
  - (i) 「不變條件」那一條同時說出三件事 ——「對話的『最後更新時間』只跟著內容走」、「新增訊息、訊息文字改變、訊息被移除才算一次更新；切過去看一眼、切走都不算」、以及理由「否則『最近』會變成『最近被看過』，而歷史清單與搜尋結果的排序都靠這個時間」。
  - (ii) 「不算」那一側**不含「按下停止」**四個字（只列了「切過去看一眼、切走」）。
  - (iii) 全檔沒有另一處宣稱按下停止不算一次更新（文件裡提到停止的地方講的是「取消整個工作」與「停止留下的是『…（已停止）』」）。
- **驗收 4**：**pass**，三列逐列判：
  - 搜尋畫面 → `ui/SearchScreen.kt`，路徑存在；職責「搜尋輸入與命中列表。自己不持有狀態，也不決定什麼算命中」與現況相符（該檔只收 state 與回呼，沒有任何比對／整形程式碼）。
  - 比對純邏輯 → `chat/ConversationSearch.kt`，路徑存在；職責「什麼算命中、片段怎麼裁、標示要落在哪幾格」相符。
  - 對話存取層 → `data/ConversationRepository.kt`，路徑存在；職責「對話從哪裡來、到哪裡去。聊天層唯一的入口」相符 —— 全專案只有該檔引用 `ConversationStore`，`ChatViewModel` 一律走 `repository.*`。
- **驗收 5**：**pass**（全檔沒有「最後開啟時間」這類第二個時間概念的描述）。
- **驗收 7**：**pass**（「命中本身有 60 個字（含）以上時裝不下引子，視窗就從命中的第一個字開始」→「命中長度剛好是 60 個字時前面留幾個引子字元」的答案唯一且為 **0**）。

## 任務 11：摘要的兩個數字維持現狀

- **驗收 2**：**pass**（在裝置上：三段對話分別命中 5／2／2 → 摘要列顯示「3 段對話・9 則訊息」，畫面上列出 3＋2＋2 筆）。
- **驗收 3**：**pass**（空查詢時畫面上只有引導文案、無命中時只有「找不到…」，兩種情況摘要列都不出現）。

## 任務 12：查詢欄位的空白規則

- **驗收 1**：**pass**。空欄位按空白鍵（`keyevent 62`）→ 欄位逐碼位仍為空、仍顯示引導文案、**不**顯示「找不到」。
  - **「長按空白鍵」→ 走替代做法（依條文不 fail）**。**真正的動作這一輪實際試過，失敗在這一步**：在空欄位上對空白鍵做 1500 ms 的長按（`input swipe <空白鍵中心> <同點> 1500`）→ 畫面跳出 Gboard 的 **「Change keyboard」對話框**（列出 English (US) 與 繁體中文(台灣) 兩個版面），**一個字元都沒有進到欄位**；`keyevent 4` 關掉對話框後欄位仍為原樣。替代做法：連續送 5 次 `keyevent 62`（讓空欄位連續收到多個空白字元輸入）→ 欄位逐碼位**仍為空**、仍顯示引導文案、仍不顯示「找不到」。要驗的性質（一個空白留不住、多個也留不住）成立。
  - **「貼上單一 U+3000 / U+00A0」→ 記「本環境無法觀察」（依條文不 fail）**。**這一輪實際試過哪些方式、各自卡在哪裡**：(1) `adb shell "input text '　'"`（U+3000）→ shell 端直接拋 `java.lang.NullPointerException: Attempt to get length of null array`（`InputShellCommand.sendText`），欄位逐碼位不變；(2) `adb shell "input text ' '"`（U+00A0）→ 無任何輸出的**無聲失敗**，欄位逐碼位不變；(3) 逐頁看鍵盤：**英文版面的主鍵盤／`?123`／`=\<` 與注音版面的主鍵盤／`?123`／`=\<` 共六張圖，沒有任何一鍵是 U+3000 或 U+00A0**。**卡住的那一步是「這兩個字元在這個環境產不出來」**，不是剪貼簿 —— 剪貼簿這一輪是通的（見驗收 2）。承接這件事的是本任務驗收 6（已 pass）。
- **驗收 2**：**pass，而且走的是真正的「貼上」，不是替代做法**。做法：在聊天輸入框（會原樣保留內容的欄位）先以英文版面 `input text "%s%s%s"` 打三個半形空白，再切注音版面打「北京」並選字，逐碼位確認欄位為 `U+0020 U+0020 U+0020 U+5317 U+4EAC`；長按欄位 → **Select all** → **Copy**；切到搜尋畫面長按欄位 → **Paste**。結果：搜尋欄位逐碼位為 `U+5317 U+4EAC`（**「北京」**），開頭三個空白被移除。
  - **依條文第 4 點回報：這個環境已經做得到真正的貼上。** 條文裡那段替代做法本輪沒有用到。要不要就此移除由 PM 判斷。
- **驗收 3**：**pass**（欄位為「北京␠␠」時把「北京」刪掉 → 欄位逐碼位變成空、回到未搜尋狀態顯示引導文案，**沒有**顯示「找不到符合「  」的訊息」）。
- **驗收 4**：**pass**（輸入「北京」＋空白 → 欄位 `U+5317 U+4EAC U+0020`；再輸入「天氣」→ 欄位 `U+5317 U+4EAC U+0020 U+5929 U+6C23`，且該查詢命中含「北京 天氣」的兩則訊息）。
- **驗收 5**：**pass**（`keyevent 61` 讓欄位失焦後，dump 顯示 `focused="true"` 落到另一個節點，欄位逐碼位由 `北京␠␠` 變成 `北京`；重新點回欄位聚焦後仍是 `北京`，**沒有把尾端空白加回來**）。
- **驗收 6**：**pass**，兩個案例都在：`assertEquals("北京", " 北京".withoutLeadingSpace())`（U+00A0 開頭必須被移除）與 `assertEquals("北京", "北京".withoutLeadingSpace())`（U+001C 開頭必須留著），並同時斷言內建 `trim()` 在這兩個字元上的行為以指出差異方向。
- **驗收 7**：**pass**（同任務 8 驗收 10 的完整案例，逐碼位）。
- **驗收 8**：**pass**。開頭移除：貼上「␠␠␠北京」後的結果是「4 段對話・5 則訊息」，與直接查「北京」相同。失焦移除：欄位為「北京␠␠」時記下的結果列表與摘要，與失焦後的**逐行 diff 無任何差異**（`RESULTS IDENTICAL`）。
- **驗收 9**：**pass**（比對層的空查詢契約測試 `an empty query is not a search` 仍存在且通過，案例含 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0）。
- **驗收 12**：**pass**。欄位為「北京」時 `keyevent 122`（移到最前）＋`keyevent 62` → 欄位逐碼位**仍為 `U+5317 U+4EAC`**；**正向對照**：同一次操作裡 `keyevent 123`（移到尾端）＋`keyevent 62` → 欄位變成 `U+5317 U+4EAC U+0020`。不經過畫面的那一層也成立（見驗收 6 的 `withoutLeadingSpace` 斷言）。

## 任務 13：對話「最後更新時間」的定義

- **驗收 1（§8.3）**：**pass**。開著 A(qa01)，從抽屜開 B(qa06) 再開回 A；前後兩次以同一查詢「北」比對，**搜尋結果整份逐行 diff 無差異**（分組順序、每個標頭的時間文字都一樣），**抽屜歷史列表整份逐行 diff 也無差異**，磁碟內容 `IDENTICAL CONTENT`。
- **驗收 2（§8.4）**：**pass**。切換離開一段沒有內容改變的對話後，該對話的存檔內容（訊息、文字、作者、`updatedAt`）與切換前完全相同 —— 本輪多次以整個對話目錄的解析後內容比對，全部 `IDENTICAL CONTENT`。
- **驗收 3（§8.6）**：**pass，兩個預期都判**。在 Gemma3 1B 的串流途中按停止（停止當下畫面上已經有文字），等 65 秒不做任何事，再切換到另一段對話。
  - 預期一（文字）：該對話存檔中那則被停止的回覆，文字結尾是 `…（已停止）`（逐字讀出：`…是一种可以形容的，是那种… 感觉。就像你一直想的…（已停止）`）。
  - 預期二（時間）：存檔中的 `updatedAt = 16:44:32.303`，切換時刻 = `16:45:37.754`，**早 65.5 秒 ≥ 30 秒**；而且它與按下停止的時刻（16:44:32.3）相差 0.0 秒 —— 落在停止當下而不是切換時刻。
- **驗收 4（§8.5，不得修過頭）**：**pass**。在 qa01 送出訊息並收到完整回覆後：`updatedAt` 由 `15:57:02` 前進到 `16:39:58`（回覆完成時刻附近）；抽屜歷史列表 qa01 升到最前（16:39，在原本較新的 16:27 那段之上）；搜尋「哈拉」（Friend 開場白）→ 12 段對話中 qa01(16:39) 排第一、原本較新的那段(16:27) 排第二。整個目錄只有 qa01 這一檔改變，其餘全部不變（刪除、切換人格／模型、開新對話等流程本輪也都照常運作）。
- **驗收 5（§8.7，有對手時仍排最前）**：**pass**。前置：人格 P=朋友 有一段有內容的對話 B(qa01)，B 剛剛更新過（16:39:58 收到回覆）。操作：等 **2 分 17 秒**（≥1 分鐘）後切換到人格 朋友 產生只有開場白的新對話 N，進入搜尋查「哈拉」。預期成立：N 與 B 都出現（共 13 段）；**N 的分組排在 B 前面**；N 標頭時間 **16:42**、B 標頭時間 **16:39**，**N 晚於 B 且分鐘數不同**。
- **驗收 6（只有一個判斷處）**：**pass**。`ChatViewModel.updateThread(transform)` 是唯一以 `hasSameContentAs` 決定要不要蓋 `System.currentTimeMillis()` 的地方；所有會改動訊息集合的路徑（送出、串流寫入、settle 的 Drop/Keep、停止的 Drop/Keep、`updateMessage`）都在 `updateThread { … }` 裡面；`newThread` 建立時直接記 `updatedAt = now`（§8.7）。冷讀位置：`ChatViewModel.kt:248/314/344/374/391`。
- **驗收 7（存檔時機沒有被改壞）**：**pass**。
  - **可判定一**：四種離開各做一次，每次一段新的 S，都在「回覆已出現文字且還在增加」時切走（用 **Gemma3 1B ＋「Write a very long detailed story about the sea, at least 600 words」**，切走時機為送出後 1.5 秒）：
    - 從抽屜開另一段 → S 存檔的回覆 **402** 字，結尾 `…（已停止）`
    - 開始新的對話 → **416** 字，結尾 `…（已停止）`
    - 切換人格 → **424** 字，結尾 `…（已停止）`
    - 切換模型 → **147** 字，結尾 `…（已停止）`
    四次都是**切走之後當場讀存檔**判的（不靠最後那次冷啟動），每次使用者那則訊息也都在。四次的結尾都是中斷標記，所以四次都算數。
  - **冷啟動那一步**：四種離開都做完之後 `am force-stop` ＋ 冷啟動一次，從抽屜依「人格＋時間」定位把四段 S 各打開一次 → 四段都與上面相符，且四段讀回來的回覆結尾各不相同（`Just saw a…（已停止）` / `e color of old…（已停止）` / `the water, and…（已停止）` / `暖暖的，浪花Rolling，…（已停止）`），確認不是同一段被重複打開。
  - **可判定二**：送出「Say hello in one short sentence」等回覆結束（回覆為「嗨！ 😊 很高興跟你聊天！」），**不做任何其他操作**直接 `am force-stop` ＋ 冷啟動 → 續接到那段對話，使用者那則訊息與完整回覆都在。
- **驗收 8（回歸六項）**：**pass**，逐項：
  - 送出 → 使用者訊息與回覆都出現。
  - 串流 → 回覆逐步增加直到結束（本輪多次在中途讀到 147–424 字的部分內容，完成後為 800 字上限版本）。
  - 停止 → 按下後回覆不再增加，文字結尾補上 `…（已停止）`；35 秒後再讀存檔，內容與停止當下相同。
  - 刪除 → 那一列從抽屜消失，檔案少一個。
  - 切換人格 / 切換模型 → 各開了一段新的對話，頂端換成選的人格（🧒 孫子／🧡 姊姊／💗 情人）或模型（Gemma3 1B ↔ Qwen2.5 0.5B）。
  - 冷啟動續接 → 回到上次停的那段對話（多次驗證）。
  沒有任何一項做不完、出錯或崩潰。

## 任務 14：讓補上來的條文各自有東西在驗

- **驗收 1**：**pass**。九條各指得出怎麼被驗的 —— 任務 1 驗收 10 →「一段可冷讀的程式碼位置」（`ConversationRepository.find`）；任務 3 驗收 25 → 測試方法 `ellipses appear only where text was actually cut`；任務 5 驗收 8、任務 6 驗收 9、任務 7 驗收 11、任務 8 驗收 11／12／13／14 → 本報告各自寫出了在裝置上的步驟與判讀點（並在 `test-cases.md` 的觀 5-5、3-6、4-1、3-1、6-3、5-4、5-1~5-3 各有對應的觀察點）。
- **驗收 2**：**pass**。九條裡唯一可自動化的是任務 3 驗收 25，它確實在執行（本輪 136 支測試全綠），而且說得出什麼樣的實作會讓它變紅：**一個無條件在頭尾補「…」的實作**會讓同一方法裡 `assertFalse(whole.snippet.contains("…"))` 變紅；**一個從不補「…」的實作**會讓 `assertTrue(cropped.snippet.startsWith("…"))` 變紅。（本輪未被要求做突變全掃，所以這是推理不是實測，已如實標明。）
- **驗收 3**：**pass**。六條配了正向對照的，兩半本輪都**實際執行到**：任務 3 驗收 25（兩半在同一個測試方法裡）、任務 5 驗收 8（無讀寫/無推論 ＋ 查詢確實有非空結果）、任務 6 驗收 9（結果會自己出現與改變 ＋ 畫面上沒有搜尋觸發鈕）、任務 7 驗收 11（四樣都在 ＋ 換對話時跟著變）、任務 8 驗收 11（沒有載入中 ＋ 結果含歷史對話）、任務 8 驗收 12（找不到查詢字串 ＋ 游標仍在）。

## 任務 15：`docs/product/features.md` 指向本規格的引用仍然追得到

- **驗收 1**：**pass**。列出 `features.md` 裡每一處路徑含 `docs/specs/conversation-search/` 的引用（同一格多個節號逐個算），共 **41 處**；對每一處到 `v3.23/` 底下該檔案判三件事（節號存在、節名 grep 得到、節號與節名落在同一節）→ **不合格數 0**。
- **驗收 2**：**pass**。`搜尋-02`（§1.7）、`搜尋-13`（§1.2）、`搜尋-20`（§2.8）三列的「對應 spec」欄都仍帶著指向本規格的引用，沒有被改成「尚無 spec」也沒有留空。
- **驗收 3**：**pass**。全檔沒有任何一處引用的路徑含版號目錄（`docs/specs/conversation-search/v*/` 的比對結果為空）。

## 任務 16：只含助理訊息的既有對話檔，打開再離開就清掉

- **驗收 2**：**pass**，逐項：
  - X 出現在歷史清單上、標題是「還沒說話」；點它 → 畫面換成 X 的內容。
  - **四種離開方式各走一次（每次重新放一個 X）**：開另一段 → `xonly1.json` 不在；開始新的對話 → `xonly2.json` 不在；換人格 → `xonly3.json` 不在；換模型 → `xonly4.json` 不在。
  - **冷啟動入口**：把 `shared_prefs/session.xml` 的 `last_thread_id` 設成 `xonly5` 並冷啟動（確認畫面續接到 X），不點任何東西直接從抽屜開另一段 → `xonly5.json` 不在。
  - **訊息清單為空的檔**：放一個 `xempty.json`（`messages: []`）→ 它以「還沒說話」出現在清單上、點得開、再開另一段之後檔不在。
  - 冷啟動後歷史清單與搜尋都找不到 X（搜 `zzxonly` → 「找不到符合「zzxonly」的訊息」；抽屜沒有「還沒說話」那一列）。
  - **正向對照**：同一次操作裡放的含使用者訊息的對話檔 `yuser.json`，在上述**每一次**離開之後都**仍在**，訊息與放進去時相同。
- **驗收 3（按垃圾桶就是真的刪掉）**：**pass**。兩段有內容的對話 A(`yuser`)、B(`qa03`)，開著 A。按 B 那一列的垃圾桶 → 那一列立刻消失、對話檔由 20 個變 19 個、`qa03.json` 不在；整個目錄的解析後內容比對只報 `MISSING now: ['qa03.json']`，**沒有任何其他檔案的內容改變**（A 的檔仍在、內容與刪除前相同）。冷啟動後抽屜找不到 B，搜尋「你」由「5 段對話・7 則訊息」變成「4 段對話・4 則訊息」，B 的分組（情人／SmolLM 135M／9/10）消失而同人格的另一段（9/3）仍在。
- **驗收 4（App 新開、沒說過話的對話仍然不留下檔）**：**pass**。換人格產生只有開場白的對話，分別用四種方式離開（開另一段／開始新的對話／換人格／換模型），**四次的前後對話檔數量與解析後內容都相同**（`IDENTICAL CONTENT` × 4）。**正向對照**：在一段新開的對話送出一則訊息、等回覆結束再離開 → 那段對話的檔存在（例如 `6f5bb284-…json` 等四段 S，以及 qa01 的新增訊息）。
- **驗收 5（`features.md` 保存-02 與 §9.1 一致）**：**pass**。保存-02 寫著「一句話都沒說過的對話**不會被存起來**，也不會出現在歷史清單裡。**例外**：從 App 以外放進手機、裡面沒有任何一句你說的話的對話檔（只有助理的話，或完全沒有訊息），會出現在歷史清單裡（標題「還沒說話」），**但打開再離開就會被清掉，不另外提示** —— 這種檔被當成髒資料。」，與 §9.1 一致；那一格指向本規格的引用（§9.1、§6.4）照任務 15 的三條判也通過。

## 任務 17：抽屜標題／預覽截短不得把一個字切成兩半

- **驗收 1（重現案例成為永久測試，不經過裝置）**：**pass**。測試 `a title cut in the middle of an emoji keeps no half of it` 與 `a preview cut in the middle of an emoji keeps no half of it`，測資形狀與條文一致（標題 25 個 BMP 字＋🌧＋尾巴；預覽 39 個 BMP 字＋🌧＋尾巴），四項預期都斷言：無落單代理（`hasLoneSurrogate()` 為 false）、尾端是「…」、「…」前面是原文開頭一段（`first.startsWith(kept)`）、那個 emoji 之前的字全部都在（`kept.startsWith("字".repeat(25/39))`）。
- **驗收 2（剛好放得下的 emoji 不截、不補「…」）**：**pass**（`an emoji that exactly fills the limit is kept whole with no ellipsis`：24 字＋🌧 的標題與 38 字＋🌧 的預覽都等於整句）。
- **驗收 3（不得修過頭）**：**pass**（`guarding emoji leaves the rest of the title and preview rules as they were`：長的一般文字有「…」、剛好等於上限的沒有「…」、換行變空白、**U+3000 原樣保留未被收成半形**、沒有使用者訊息時標題為「還沒說話」）。
- **驗收 4（在裝置上：抽屜那一列看不到壞掉的字）**：**pass**。把驗收 1 形狀的對話推進對話目錄（不是開著的那段），打開抽屜逐碼位讀節點文字：標題 = 25×`U+5B57` ＋ `U+2026`，預覽 = 39×`U+5B57` ＋ `U+2026` —— 沒有落單代理、沒有方框或問號、emoji 之前的字都在。**`uiautomator dump` 在那一列可見時正常完成（`selftest OK: 46 nodes`），logcat 沒有 `Bad surrogate pair`。**
- **驗收 5（在裝置上：省略號兩個方向沒有被改壞）**：**pass**（同任務 6 驗收 1 (iii) 的觀察：長英文句那一段標題與預覽尾端都有「…」；「你好」那一段都是整句、尾端都沒有「…」）。
- **驗收 6（組合 emoji 不被切開，不經過裝置）**：**pass**。`a title cut inside a combined emoji keeps all of it or none of it` 與 `a preview cut inside a combined emoji keeps all of it or none of it` 對 👨‍👩‍👧 / 🇹🇼 / 👍🏽 三類各一案例，預期直接寫死成兩個可接受字串（`"$lead…"` 或 `"$lead$character…"`），不是用被測的斷字算出來的；`stepping out of a combined emoji keeps the character before it`（21 字＋👍🏽＋👨‍👩‍👧）守「前面那個字也要在」；`a combined emoji that exactly fills the limit is kept whole with no ellipsis`（18 字／32 字＋👨‍👩‍👧）守「剛好放得下」；`a single character longer than the limit is not cut down to a bare ellipsis`（e＋40／60 個 U+0301）守「極端輸入」。
- **驗收 7（在裝置上：抽屜那一列的組合 emoji 是完整的）**：**pass**。推入一段第一句為 24 個 BMP 字＋👨‍👩‍👧、最後一則為 38 個 BMP 字＋🇹🇼 的對話，逐碼位讀抽屜那一列：標題 = 24×`U+5B57` ＋ `U+2026`（**不是落單的 👨**），預覽 = 38×`U+5B57` ＋ `U+2026`（**不是落單的 🇹**）—— 兩者都是驗收 6 預期的其中一種（「只有那些 BMP 字」那一種），**與驗收 6 的預期一致**。

## 任務 18：搜尋片段不得把一個字切成兩半

- **驗收 1（起點落在一個字中間）**：**pass**。`a window starting inside a combined emoji starts at a whole character` 對三類字各一案例，測資形狀與條文一致（5 甲＋字＋14/18/18 乙＋kiwi＋50 丙）；預期寫死成兩種可接受前綴（`…$character${lead}kiwi` 或 `…${lead}kiwi`），並以 `assertKiwiIsMarked()` 斷言片段含 kiwi 且標示取出恰為 `kiwi`。
- **驗收 2（終點落在一個字中間）**：**pass**。`a window ending inside a combined emoji ends at a whole character`，形狀 30 甲＋kiwi＋34 乙＋字＋30 丙，預期 `endsWith("乙$character…")` 或 `endsWith("乙乙…")`，同樣斷言 kiwi 與標示。
- **驗收 3（不得修過頭）**：**pass**，四件事都在：截點本來就在兩個字之間時不移動（3 甲＋👍🏽＋👍🏽…／…🇹🇼＋🇯🇵…）、§4.2 三條界線（L=59/60/61）片段與 §4.2 寫的預期輸出完全相同、§4.3 短訊息前後不補「…」、下限（連續 15／20 個單一 emoji 的兩個形狀）片段頭尾沒有落單代理且完整含查詢詞。
- **驗收 4（一個字長到視窗裝不下）**：**pass**。`a character longer than the window still leaves the hit on screen`，形狀 100 甲＋「e」＋80 個 U+0301＋kiwi＋30 丙；斷言片段**含有「k」**且「要嘛完整含有那個長字、要嘛完全沒有它」。
- **驗收 5（在裝置上：命中列上看到的是完整的字）**：**pass**。推入一段對話，兩則訊息分別是驗收 1 的 👨‍👩‍👧 形狀與驗收 2 的 🇹🇼 形狀，進入搜尋查 `kiwi`，**逐碼位**讀命中列：
  - 起點形狀那一列 = `U+2026` ＋ 14×`U+4E59`（乙）＋ `kiwi` ＋ 42×`U+4E19`（丙）＋ `U+2026` —— 「…」之後**不是 ZWJ 也不是 👩**，是驗收 1 預期的「直接從第一個乙開始」那一種。
  - 終點形狀那一列 = `U+2026` ＋ 20×`U+7532`（甲）＋ `kiwi` ＋ 36×`U+4E59`（乙）＋ `U+2026` —— 「…」之前**不是落單的 🇹**，是驗收 2 預期的「它前面的最後一個乙」那一種。
  兩者都與驗收 1、2 的預期相符。

## 任務 19：`features.md` 的「未經查證」不綁死在某一種驗收裝置上

- **驗收 1**：**pass**。冷讀「「※未經查證」是什麼意思」那一節，對「某一項如果有人在模擬器上開 App 確認過，它算不算已經查證？」該節給得出答案而且答案是「算」——「**「查證」看的是有沒有人實際開 App 確認過，不看那是什麼裝置。** 實體手機、模擬器都算 —— 只要那一項真的被操作過、被看過，它就不是「未經查證」。」
- **驗收 2**：**pass**。該節仍然存在，而且仍說出兩件事：(i)「**看到這個標記，就表示那一項還需要有人實際開 App 確認過。**」；(ii)「這份文件是讀程式碼與既有文件寫出來的」。

---

# 邊界情況檢查

本規格沒有獨立的「已知衝突與邊界情況」章節，邊界情況散在各條文的可判定條款與「明確不做」裡。逐項主動檢查的結果：

| 邊界情況（出處） | 有沒有被處理 | 怎麼確認的 |
|---|---|---|
| 空查詢／全空白查詢（§2.4，含只有 U+3000、只有 U+00A0） | 有 | 單元測試五個案例逐碼位確認；畫面上因 §2.8 不可達，改以刪字到空觀察回到引導文案 |
| 「算不算空查詢」與整形分歧（§2.5） | 有 | BMP 全掃斷言違反數為 0；`isActive` 實作直接用 `flattenToLine()` |
| 四個使用點對空白的答案必須一致（§2.11，U+0020／U+00A0／U+001C） | 有 | 兩端整形 BMP 全掃；`isActive` 全掃；欄位規則的 U+00A0 移除與 U+001C 保留兩個案例 |
| 零寬字元與 ZWJ 組合 emoji（§2.2） | 有 | `zero-width characters are not whitespace`，含 👨‍👩‍👧 整形後字串完全不變 |
| 欄位開頭空白（空欄位、已有字時插在最前、貼上前導空白） | 有 | 驗收 1／12／2 三種情境都在裝置上逐碼位驗過 |
| 欄位尾端空白（打字中保留、失焦移除、重新聚焦不加回） | 有 | 驗收 4／5 逐碼位 |
| 命中長度剛好等於視窗（L=59/60/61 的界線） | 有 | 三個案例，59 與 60 的預期片段不同 |
| 片段截點落在代理對中間（起點／終點兩個方向） | 有 | 任務 3 驗收 16 兩個案例，測資形狀確實碰到邊界 |
| 片段截點落在組合 emoji 中間（ZWJ／國旗／膚色） | 有 | 任務 18 驗收 1、2 各三類；裝置上任務 18 驗收 5 逐碼位 |
| 截點本來就在兩個字之間（不得修過頭） | 有 | 任務 18 驗收 3；兩個 👍🏽 之間、兩面國旗之間 |
| 一個字長到視窗／上限裝不下 | 有 | 任務 18 驗收 4（e＋80 accents）、任務 17 驗收 6（e＋40／60 accents） |
| 抽屜標題／預覽截點落在 emoji 中間 | 有 | 任務 17 驗收 1、4、6、7（不經過裝置與裝置上各一層） |
| 剛好放得下的 emoji／組合 emoji 不該補「…」 | 有 | 任務 17 驗收 2、6 |
| 同一段對話同時在記憶體與歷史（去重、留哪一份、排序先後） | 有 | 任務 4 驗收 3、6（畫面上不可達，只能靠直接測試，該測試仍在） |
| 跨對話訊息編號撞號（多段的第 0 則同時命中） | 有 | 任務 7 驗收 3 在裝置上捲完全部結果，不錯亂不崩潰 |
| 開場白造成大量重複命中 | 有 | 查「哈拉」→ 12 段同人格對話全部出現，沒有去重或降權 |
| 只含助理訊息／訊息清單為空的既有對話檔 | 有 | 任務 16 驗收 2（四種離開＋冷啟動入口＋空訊息檔＋正向對照） |
| 讀不起來的對話檔（§9.2） | 有 | 推入壞 JSON 後其餘 18 列完整顯示 |
| 依 id 取一筆 vs 取全部再挑（含都找不到，§9.3） | 有 | 兩條路徑同源（`find = all().firstOrNull`） |
| 歷史尚未載入完成前搜尋（§1.9） | 有 | 結果區只有三種樣子、沒有第四種；正向對照確認載入完成後含歷史對話 |
| 串流中的回覆、尚未落盤的新訊息（§1.8） | 有 | 磁碟上 grep 不到該字串時搜尋已能找到該對話的兩則訊息 |
| 串流中切走（§8.2 的複合動作） | 有 | 任務 13 驗收 7 四種離開，回覆留下部分文字＋中斷標記，時間落在停止當下 |
| 零文字時按停止（§8.1 第三項） | 有（由 `stop-generation` 那一輪守） | 本規格不另立條文；本輪觀察到一次「停止時回覆尚無文字 → 那則回覆整則不留下」，與 §8.1 一致 |
| 行程被系統回收後還原（§5.7） | 有 | `am kill` 後還原落在搜尋畫面、查詢為空、引導文案 |
| 旋轉不是離開（§5.6） | 有 | 橫向後查詢與結果都在 |
| 新對話在有對手時排最前（§8.7） | 有 | 任務 13 驗收 5，B 剛更新過、隔 2 分 17 秒 |
| 跨午夜的日期判讀（環境面） | 不適用 | 本輪全程在 2026-09-16 同一天內（開始 15:50、結束 17:0x），沒有跨日 |

---

# 如果 fail：問題歸屬

**本輪沒有 fail。** 以下是**打回 PM 的條文層面發現**（不影響任何一條的 pass/fail）：

1. **條文之間的測資互相衝突：任務 8 驗收 10／任務 12 驗收 7 的「kiwi 保證無命中」，與任務 18 驗收 1／2／5 的「查詢 kiwi、而且把含 kiwi 的對話推進對話目錄」不能同時成立。**
   - 任務 18 驗收 5 明文要求「把一段對話放進對話所在的目錄，其中兩則訊息分別是驗收 1 的 👨‍👩‍👧 形狀與驗收 2 的 🇹🇼 形狀（兩者都含 `kiwi`）。進入搜尋，查「kiwi」」；任務 8 驗收 10 明文要求「輸入 `"  kiwi  "`（前後各兩個空白，**且 kiwi 保證無命中**）」並期望文案是 `找不到符合「kiwi  」的訊息`。
   - 兩份測資同時在裝置上時，後者的前提直接被前者破壞。本輪靠**排程**繞開（先做任務 18 驗收 5，再把那段測資移除，才做 kiwi 無命中那兩條），但條文沒有寫任何順序或隔離要求，**下一個人照條文各自準備測資就會撞到，而症狀是「任務 8 驗收 10 莫名其妙 fail」**。
   - 建議：在任務 8 驗收 10／任務 12 驗收 7 把字串換成一個不會與別條測資撞號的 token，或在任務 18 明寫「這段測資判完就要移除」。

2. **環境變化回報（任務 12 驗收 2 第 4 點明文要求回報）：這個驗收環境已經做得到真正的「貼上」。**
   - 做法與逐碼位證據見上面任務 12 驗收 2。決策 N 選 (b) 保留的那段替代做法，本輪完全沒有用到。要不要移除由 PM 判斷。

3. **環境變化回報（任務 12 驗收 1 第 4 點）：「長按空白鍵」與「貼上單一 U+3000 / U+00A0」這兩個案例，這個環境仍然做不到**，而且**卡住的地方與條文正文描述的成因不同**：不是剪貼簿（剪貼簿是通的），而是**那兩個字元在這個環境根本產不出來**（`input text` 送 U+3000 拋例外、送 U+00A0 無聲失敗；六張鍵盤圖上都沒有這兩鍵）。條文本身已經寫成「不預設成因、每輪當場試」，所以這只是依條文回報事實，不需要改條文。

4. **`spec-architecture.md` 本輪沒有讀**（依角色定義它全部是建議、不是驗收對象），所以沒有「行為要求殘留在架構文件裡」這類發現可回報。

---

# 驗證方式說明

## 每一輪都要做的六件事

1. **跑專案的標準測試指令** —— **做了**。`./gradlew clean testDebugUnitTest installDebug`，`:app:clean` 與 `:app:testDebugUnitTest` 都實際執行（無 UP-TO-DATE／FROM-CACHE），結果 XML 為 tests 136 / failures 0 / errors 0 / skipped 0。
2. **每一條指名了測試的驗收條文** —— **做了**。任務 3 的 26 條、任務 2、4、12 驗收 6／9、任務 17 驗收 1–3／6、任務 18 驗收 1–4、任務 14 的核對，全部逐條打開測試原始碼讀斷言，並核對「條文指名的測資形狀與預期」是否真的寫在裡面（見任務 3 的對照表）。含逐碼位檢查測試字面量（例如空查詢那五個案例確認是 U+3000 與 U+00A0、欄位規則那兩個案例確認是 U+00A0 與 U+001C）。
3. **每一條邊界情況** —— **做了**，逐條列在上面「邊界情況檢查」。
4. **每一條實機條文** —— **做了**，全部在模擬器 `emulator-5554` 上實際操作。沒有靜靜降級成靜態閱讀的條目；唯二沒有在裝置上取得完整觀察的是任務 5 驗收 2 與任務 9 驗收 12，兩條都依條文記為「未能觀察／本環境無法執行」並寫明卡在哪一步。
5. **隨機抽測恰好三項** —— **做了**（`啟動-07`、`歷史-07`、`聊天-17`，見下面專節）。
6. **真實資料的快照 / 還原 / 逐位元組驗證** —— **走「清空」那一種**。呼叫者在 prompt 裡明講「使用者已同意清空驗收裝置上的 App 資料」，所以驗收開始前清空了使用者資料，沒有做快照、還原與逐位元組還原驗證。
   - **清掉了什麼**：`files/conversations/*.json`（13 個既有對話檔）與 `shared_prefs/session.xml`。
   - **保留了什麼**：`files/gemma3-1b.task`（1.05 GB）、`files/model.task`（547 MB）、`files/smollm.task`（167 MB）三個模型資產檔與 `files/profileInstalled`。清除方式是 `am force-stop` ＋ `run-as sh -c 'rm -f files/conversations/*.json; rm -f shared_prefs/session.xml'`，**沒有用 `pm clear`**（那會連資產檔一起刪）；刪完 `ls -1 files` 確認三個 `.task` 都還在。
   - 驗收結束時裝置上留著本輪造出來的測資與對話；系統設定（density、accelerometer_rotation、user_rotation、輸入法版面）都已還原並讀回確認（density 420 無 override、accel=1、user_rotation=0、版面回到進場時的 English (US)），`/data/local/tmp/qa`、`/data/local/tmp/qa2` 已刪除。

## 實際執行偏離 `test-cases.md` 的地方

1. **觀 2-6（比較畫面）改到最後才做，不是跟在組 2 後面。** 理由：它要載入三個模型，會把引擎狀態與當下對話推到別的地方，放在組 3–5 的中間會污染「不觸發推論」「磁碟內容不變」那幾條的判讀。
2. **觀 4-1 的 5/2/2 測資把查詢字從「熊貓」改成 ASCII 的 `zzpanda`。** 條文只要求「三段對話分別命中 5/2/2」，沒有規定查詢語言；改成 ASCII 省掉一次注音組字。**任務 7 驗收 6／任務 9 驗收 9/10/11 這幾條明文要求查詢經過中文輸入法的，一條都沒有改，全部照注音輸入法組字選字做。**
3. **觀 3-5（`"  kiwi  "`）被拆到觀 4-5（任務 18 驗收 5）之後才做，並且中間把含 kiwi 的測資檔移除。** 理由是上面「問題歸屬」第 1 點的條文衝突。
4. **觀 5-4（任務 8 驗收 13）與組 5-4 的任務 16 驗收 2 合併成同一串操作**：點擊 `zzxonly` 命中列切換到 X，正好就是任務 16 驗收 2 的第一種離開（開另一段）的前置，兩條在同一次操作上各自判定。
5. **觀 7-1（§8.3）的共同查詢字從「北京」改成「北」。** 理由：注音候選列的排序在同一組鍵序的不同次呼叫之間會改變（第一次「北京」在第一格、第二次變成「北疆」），改用單字「北」讓候選穩定；要比的是「切走再切回前後整份結果與抽屜順序是否逐行相同」，換一個共同字不影響這件事。
6. **任務 13 驗收 7 的四段 S 在冷啟動後改用「人格＋時間」定位開啟，不是照抽屜第一列連點四次。** 第一次照第一列連點時，開過的那段會回到列表、順序跟著變，結果重複開到同兩段；改成定位後四段各開一次、四段讀回來的內容各不相同。
7. 其餘各組都照 `test-cases.md` 的分組與觀察點執行，沒有偏離。

---

# 本輪驗證範圍

**全驗。** spec 的 19 個任務、140 條驗收標準全部驗過，沒有依呼叫者給的「變更範圍」縮小（呼叫者給的範圍本來就是「conversation-search 這個功能的全部」）。`spec-decisions.md`（0 項待決）、`spec-appendix.md`、`spec-architecture.md` 依角色定義不是驗收對象，本輪沒有拿它們當判準；`spec-architecture.md` 本輪完全沒有讀。`pm-inbox.md` 依角色定義連讀都沒有讀（連被別人改動的那 10 行也只用 `git diff --stat` 看行數）。也沒有讀任何舊版號目錄、任何先前的 `test-cases.md` 或 `test-report.md`。

---

# 環境文件的處置

對 `docs/qa-environment.md` 做的事（**只寫處置**）：

- **驗過並成立**（依賴前先跑過確認方式）：§0 zsh 陷阱（`adb shell -n`、`exec-out` 結束碼恆為 0、glob 報錯）、§1 JDK 與 Gradle（`java -version` 回 openjdk 25.0.2、clean/test 判準）、§2 裝置查詢（序號、版本、解析度、density、`df -h /data`）、§3 安裝時間對照、§4 `run-as` 讀寫與多欄排版陷阱、§5 dump/`ui.py`/`hashdir.sh`、§5a 手勢區、§6.1–6.5 輸入法與剪貼簿、§7 焦點（TAB 判準）、§8 density 與旋轉、§9 啟動/回收/返回鍵、§10 pid 過濾 logcat、§11 時差量測、§13 工具與守衛。
- **確認失敗、已當場改成現在成立的樣子**：§5「同一列用下緣分組，下緣通常完全相同」—— 實測同一列不同字級的節點下緣會差 2 px，照「完全相等」分組會整列找不到。已改寫成「下緣也不保證相同，分組要給容差（±8 px）」並附新的確認方式與症狀描述。
- **新增**：
  - §5「dump 裡沒有 ≠ 畫面上沒有」—— 富文字算繪的整段內容有時整段不出現在 dump 裡，斷言「某處是空的」之前要再截一張圖。
  - §6.2「鍵帽英數標示是標點的那幾個注音鍵送不進去」—— `input text` 會把它變成全形標點並提交組字，要用 `input tap` 點那個鍵。
  - §6「鍵盤開著與收起來時同一顆按鈕座標不一樣」—— 照舊座標點會點在鍵盤上，最右下角常是 Enter／動作鍵，於是變成往欄位插一個換行。
  - §13 第 4 點「要在只存在幾秒的狀態裡動作時，把整串操作寫進同一個 `adb shell`」（含實測數字：拆成 4 次 Bash 呼叫要 6–8 秒，包成一條約 2.2 秒）。
  - §13 第 5 點「跑批次之前先確認現在在哪個畫面」—— 點到沒有東西的地方不會有任何錯誤，批次照樣跑完（本輪實際踩到兩次）。
  - §13 工具清單新增 `uitext.py`。
  - §6.5 剪貼簿條目的最後確認日期更新為 2026-09-16，並補上「選取工具列整條路走通、貼進去逐碼位相同（含前導空白）」。
- **新增工具**：`docs/qa-tools/uitext.py`（一次列出畫面上每個文字節點的 `y0,y1`、`repr(text)` 與逐碼位；附 `selftest`）。它通過「換成完全不同的產品還成立嗎」這把尺 —— 它不知道任何產品的字串、座標或步驟。三支工具（`ui.py`、`hashdir.sh`、`uitext.py`）本輪都跑過 selftest 並通過。
- **刪掉的越界內容**：**沒有**。本輪逐節讀過，沒有讀到通不過「換成完全不同的產品還成立嗎」那把尺的句子。

---

# 本輪隨機抽測的三項功能

來源 `docs/product/features.md`；挑選規則是「優先挑最久沒被挑過的，其餘隨機」，並跳過對應欄指向 conversation-search spec 的項目（那些本輪已經驗過）。三項都是從未被挑過的。

1. **啟動-07**：「關掉 App 再打開，會回到**上次離開時看的那一段對話**，不是最新的那一段。」
   - 做了什麼：從抽屜開啟一段明顯較舊的對話（朋友／Gemma3 1B／**8/13**，標題「填充對話 17 內容」），此時最新的一段是 16:57。`am force-stop` 後 `am start`。
   - 看到什麼：冷啟動後畫面停在「填充對話 17 內容」那一段，不是 16:57 那一段。**與描述相符。**
2. **歷史-07**：「點任何一列就會打開那段對話，抽屜自動關起來。」
   - 做了什麼：打開抽屜、捲到一列（8/13 那一列）、點它。
   - 看到什麼：畫面換成那段對話的內容，且 dump 裡 `Close navigation menu`（抽屜遮罩）節點數為 **0**，抽屜確實關掉了。**與描述相符。**
3. **聊天-17**：「同樣的問題問第二次，答案不會跟第一次一模一樣。」
   - 做了什麼：在同一段對話裡先後兩次送出完全相同的問題「Name one colour」（Gemma3 1B）。
   - 看到什麼：第一次回「红。」，第二次回「蓝。」。**與描述相符。**

**三項都沒有與功能總覽的描述不符的地方**，所以沒有需要呼叫者裁決的項目；也沒有拿這三項去改變上面任何一條的 pass/fail。

---

# 隨機挑選記錄的處置

`docs/qa-random-coverage.md` **已存在，本輪是追加**，加了三行：

```
2026-09-16  啟動-07
2026-09-16  歷史-07
2026-09-16  聊天-17
```

只記「哪一條、什麼時候」，沒有記任何結果（結果寫在上面「本輪隨機抽測的三項功能」）。
