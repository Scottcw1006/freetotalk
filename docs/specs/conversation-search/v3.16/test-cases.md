# conversation-search v3.16 驗收 test cases

> 依「需要的環境狀態」分組。每個觀察點指向它守的條文，並引用條文的預期。**判定依據永遠是條文原文**（`spec.md`、`spec-tasks.md`），這份只是執行順序。
> 使用者已同意清空驗收裝置上的 App 資料：驗收開始前清掉對話檔目錄與 session 偏好，模型檔（`files/*.task`）保留。

---

## 情境 S0：不經過裝置（原始碼、測試、文件）

準備：工作區 HEAD、乾淨。`./gradlew clean testDebugUnitTest`，讀 XML 結果。

- S0-1 → 每輪六件事之一：標準測試指令全綠（tests/failures/errors 數字）。
- S0-2 → 任務 3 驗收 1–26：逐條找出對應測試方法並讀斷言，對照條文預期（例：驗收 6「案例必須包含 `""`、`"   "`、`"\n\t"`、U+3000、U+00A0」；驗收 16「第一個 code unit 不得是落單的低位代理…且片段裡仍然完整含有查詢詞」「兩個案例必須各自獨立」；驗收 17「`整形("a" + c + "b") == "a b"`」；驗收 23 L=59/60/61）。字面上看不見的字元用逐碼位掃描確認。
- S0-3 → 任務 3 驗收 16「只讓起點那一側的保護失效時，只有第一個案例變紅；只讓尾端那一側失效時，只有第二個案例變紅」：在 scratchpad 複本上各拿掉一側保護，跑該測試類別（qa-environment §12 的做法），專案原始碼不動。
- S0-4 → 任務 2 驗收 1–11：比對層檔案與它依賴的檔案沒有 Android/Compose/持久化 import；其餘各條對照測試與程式碼。驗收 8（§2.11）「U+0020 與 U+00A0 在四處都算空白，U+001C 在四處都不算」：用探針對四個使用點各跑三個字元。
- S0-5 → 任務 4 驗收 2、3、6：對應測試（驗收 6「結果恰為兩個分組，順序是 `other`、`same`…命中訊息數為 2」）。
- S0-6 → 任務 1 驗收 10（§9.3）：讀「依 id 取一筆」與「取全部」兩條路徑，判「對一個確實存在的 id 與一個不存在的 id…兩條路徑的答案相同」。
- S0-7 → 任務 9 驗收 2「文字修復那一份程式碼裡不得出現搜尋的整形／折疊，也不得被它們引用」、驗收 3「指得出『送進模型的上下文』那道過濾在哪裡，而且它不是搜尋也在用的那一份」。
- S0-8 → 任務 12 驗收 6（U+00A0 開頭必須被移除／U+001C 開頭必須不被移除）、驗收 9（空查詢契約測試仍在且通過）、驗收 12 不經過畫面那一層（「(1 個空白)北京」→「北京」，用探針）。
- S0-9 → 任務 13 驗收 6「『什麼算一次更新』在程式碼中只有一個判斷處，所有寫回對話的路徑都經過它」。
- S0-10 → 任務 14 驗收 1–3：任務 1 驗收 10；任務 3 驗收 25；任務 5 驗收 8；任務 6 驗收 9；任務 7 驗收 11；任務 8 驗收 11–14 各指得出驗法，正向與否定兩半都被執行。
- S0-11 → 任務 10 驗收 1、2、3（三件事 (i)(ii)(iii)）、4（三列路徑存在且職責相符）、5（只有一個時間概念）、7（「命中長度剛好是 60 個字時，前面留幾個引子字元」答案唯一且是 0）：讀 `plan/architecture.html`。
- S0-12 → 任務 15 驗收 1–3、任務 16 驗收 5：列出 `features.md` 每一處 `docs/specs/conversation-search/` 引用，在 v3.16 目錄對應檔案裡判節號存在、節名 grep 得到、兩者落在同一節；搜尋-02/13/20 仍帶引用；不含版號目錄；保存-02 描述與 §9.1 一致。

## 情境 S1：建置、覆蓋安裝、清空 App 資料

準備：`installDebug`（只覆蓋安裝），記 `lastUpdateTime`；force-stop；刪 `files/conversations/*` 與 `shared_prefs/session.xml`；確認 `files/*.task` 仍在。

## 情境 A：空資料、冷啟動（重置點：S1 之後）

- A-1 → 任務 6 驗收 1 (v)「沒有其他對話時，顯示『還沒有其他對話。』」。
- A-2 → 任務 6 驗收 2／1 (ii)：點「搜尋對話內容」→ 抽屜關閉、進搜尋畫面；入口是列不是輸入框。
- A-3 → 任務 6 驗收 3「進入時輸入框已聚焦、鍵盤自動彈出」（dump focused + mInputShown）。
- A-4 → 任務 8 驗收 1「剛進入時顯示引導文案，沒有『找不到…』、沒有空的結果列表」；任務 11 驗收 3 摘要列不出現。
- A-5 → 任務 12 驗收 1「空欄位按空白鍵 → 欄位仍為空、顯示引導文案、不顯示『找不到』」（單一 keyevent 62；長按的替代：連送多個 keyevent 62；貼上 U+3000/U+00A0 → 記本環境無法觀察）。逐碼位讀欄位。
- A-6 → 任務 8 驗收 2「只出現『找不到…』一句，無殘留舊結果」；任務 11 驗收 3。
- A-7 → 任務 8 驗收 10 / 任務 12 驗收 7：`"  kiwi  "` →「欄位內容為 `"kiwi  "`、文案為 `找不到符合「kiwi  」的訊息`；使輸入框失焦後，欄位與文案都變成 `kiwi`」。任務 12 驗收 5 重新聚焦不加回。
- A-8 → 任務 12 驗收 3「欄位內容為『北京(2 個空白)』時刪掉『北京』→ 欄位為空、回到未搜尋狀態」（北京以注音輸入）。
- A-9 → 任務 6 驗收 5 畫面返回鍵、系統返回鍵都回聊天「不是退出 App」；任務 8 驗收 8 離開清空查詢。
- A-10 → 任務 5 驗收 7／任務 13 驗收 5／§1.8：換人格 → 搜開場白 → 該段出現「且排在最前」。
- A-11 → 任務 8 驗收 5「回到聊天、對話不變、不重載模型、不建立新對話、不把空白對話存檔」。
- A-12 → 任務 16 驗收 4：換人格產生只有開場白的對話，四種離開（開另一段、開始新的對話、換人格、換模型），「每一次離開前後，對話檔的數量與內容都相同」；正向：送出一則訊息、等回覆結束再離開 →「那段對話的檔存在」。

## 情境 B：推入測資（重置點：清空後推入整組測資、冷啟動）

測資（檔名前綴可辨識）：
- m5：5 則含 `mango` 的使用者訊息（其中第 0 則含 mango），人格／模型 α；m2a、m2b：各 2 則含 `mango`（第 0 則含 mango），人格／模型與 m5 不同。
- 北：一段含「北京」與「北方」使 `北` 與 `北京` 命中則數不同；一則含「北京 天氣」。
- 長中文訊息：命中詞位於中段，命中 20 字內含 emoji。
- 空白：一則「詞(U+3000)詞」、一則「詞(U+00A0)詞」；最後一則訊息含 U+3000 的一段（預覽用）。
- 台／臺各一則。
- 昨天更新的一段（月/日）；大量有內容的對話使歷史列表需要捲動。

- B-1 → 任務 11 驗收 2／任務 8 驗收 7：「摘要列顯示『3 段對話・9 則訊息』，畫面上列出 3 + 2 + 2 筆」；任務 7 驗收 4「顯示 3 則 + 『還有 N 則符合』，N = 總數 − 3」。
- B-2 → 任務 7 驗收 1（佔滿、可整片捲動）、任務 6 驗收 4（輸入框固定頂端）、任務 7 驗收 3（「兩段對話的第 0 則同時命中時，捲動列表不錯亂、不崩潰」）、任務 8 驗收 6（命中列沒有刪除鈕）、任務 7 驗收 5（單行省略）、任務 7 驗收 2（標示視覺、主題色）、任務 7 驗收 11（標頭四樣且隨人格／模型改變）、任務 7 驗收 7（空白對話標頭無「還沒說話」）。
- B-3 → 任務 7 驗收 8 §7.1「直向、鍵盤彈出、不捲動…至少完整看到 1 個對話標頭 + 4 個命中列」；任務 7 驗收 9 §7.2 density 調到最大「至少 1 個對話標頭 + 2 個命中列」（改完還原）。
- B-4 → 任務 12 驗收 2（替代：`%s%s%smango` 一次送入 → 欄位「mango」）；任務 12 驗收 8／§2.10 輸入「mango(2 個空白)」記結果與摘要 → 失焦 →「結果與摘要一字不差」。
- B-5 → 任務 12 驗收 12：欄位「北京」→ MOVE_HOME + 空白 → 仍「北京」；正向：MOVE_END + 空白 →「北京(1 個空白)」。
- B-6 → 任務 12 驗收 4：「北京」+ 空白 →「北京 」；再「天氣」→「北京 天氣」且命中含「北京 天氣」的訊息。
- B-7 → 任務 6 驗收 9：輸入「北」不按鈕不收鍵盤、1 秒內結果出現；再「京」結果換成「北京」那一組；畫面上沒有按下才搜的控制項。
- B-8 → 任務 9 驗收 8–11、13（鍵盤保持彈出、中文輸入法）：開場白詞多段、標示與摘要；長訊息中段片語「前後有『…』、標示恰好蓋住輸入的那幾個字」＋任務 7 驗收 6；「臺」「台」結果不同；U+3000/U+00A0 兩則都命中且標示含中間空白位置。驗收 12 做不到記本環境無法執行。
- B-9 → 任務 9 驗收 7：抽屜預覽逐碼位仍含 U+3000；搜尋片段收成一個半形空白。
- B-10 → 任務 5 驗收 5／任務 6 驗收 8／任務 8 驗收 8：搜尋中旋轉 → 仍在搜尋畫面、查詢與結果都在、輸入框在頂端（改方向前記原值、收尾還原）。
- B-11 → 任務 5 驗收 8：進入搜尋前後對話檔數量與內容逐一比對一致、推論無紀錄、狀態列不變；正向：查詢非空結果。任務 8 驗收 11：結果區三種樣子，無載入中；正向：歷史裡的對話在結果中。
- B-12 → 任務 8 驗收 12：輸入查詢後讀 session 偏好，找不到查詢字串、鍵只有三個；正向：冷啟動續接原對話。任務 8 驗收 9：搜尋畫面有查詢 → Home → am kill → am start →「停在搜尋畫面、查詢為空、顯示引導文案」。
- B-13 → 任務 8 驗收 3（點另一段：回聊天、內容是該對話、再進搜尋輸入框空）、驗收 4（點目前這段：內容不變、不重載模型）、驗收 14（三種點擊後回聊天顯示對應對話，且不新增或改寫任何對話檔；基準用 App 自己寫過一次的檔）。
- B-14 → 任務 13 驗收 1（§8.3）：A 新 B 舊含共同字，開 B 再開回 A →「搜尋分組順序、分組標頭的時間文字、抽屜歷史順序都與操作前相同」。任務 13 驗收 2（§8.4）：A 昨天、切走 →「存檔內容與切換前完全相同」。
- B-15 → 任務 6 驗收 1 (i)–(vi) 與任務 9 驗收 5 逐項。
- B-16 → 任務 1 驗收 6 / 任務 9 驗收 4：冷啟動續接（檔存在／不存在）、空白對話不進歷史、歷史排序、開啟／刪除／送出後更新、放損毀檔後歷史仍完整。
- B-17 → 任務 16 驗收 3（垃圾桶）、任務 16 驗收 2（X 四種離開各一次重新放檔、冷啟動入口、空訊息檔、正向 Y 仍在）；任務 8 驗收 13（點 X 的命中 → 切換到 X）。

## 情境 C：生成中（重置點：模型載入完成、有一段可送訊息的對話）

- C-1 → 任務 5 驗收 2（盡力而為，寫明模型、提示、串流時長、是否攔到）。
- C-2 → 任務 13 驗收 3（§8.6）：串流中有文字時停止，等 ≥30 秒，切換 → 存檔回覆「結尾是中斷標記」且最後更新時間比切換時刻早 ≥30 秒。
- C-3 → 任務 13 驗收 4（§8.5）：送出並收到完整回覆 → 時間前進、搜尋結果與抽屜歷史排最前。
- C-4 → 任務 13 驗收 7：切走一段有未存內容的對話再切回，內容仍在。
- C-5 → 任務 5 驗收 6、任務 13 驗收 8、任務 9 驗收 6：送出、串流、停止、刪除、切換人格／模型、冷啟動續接照常（進過搜尋之後）。

## 情境 D：比較畫面（重置點：聊天畫面）

- D-1 → 任務 6 驗收 7「比較畫面既有行為完全未變 —— 含它仍沒有接住系統返回鍵、仍會被旋轉踢回聊天」。

## 情境 R：隨機抽測三項（本 spec 以外，從 features.md 挑，最久沒被挑過優先）
