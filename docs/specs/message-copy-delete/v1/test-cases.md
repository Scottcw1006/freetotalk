# test-cases：message-copy-delete v1

- **觀察點數：30**（另加規格範圍以外的隨機抽測 3 項）
- **涵蓋的驗收條數：44**（任務 1：10、任務 2：8、任務 3：8、任務 4：4、任務 5：5、任務 6：6、任務 7：3）

受測版本：`90c438f`（工作區乾淨）。判定依據一律是 spec.md 的條文原文；下面的引用是原文摘錄，判的時候回頭對照原文。
本檔在第一次操作裝置之前寫成。App 資料走「使用者已同意清空」那一條路（模型檔保留）。

---

## 情境 S0：不經過裝置（無前置）

- **O1** 跑 `./gradlew clean testDebugUnitTest`，讀結果 XML
  - → 任務 1 驗收 9：「`./gradlew testDebugUnitTest` 全數通過，含本輪之前就存在的全部測試。」
  - → 任務 5 驗收 1：「任務 1 驗收 9（全部既有測試通過）。」
- **O2** 逐條讀 `MessageDeletionTest.kt` 的斷言，對照條文指名的案例有沒有全在
  - → 任務 1 驗收 1：「刪除「first」→「second」；復原 →「first」…第一則已刪除、第二則超過截短上限 → 第二則截短後的樣子…全部已刪除 → 標題**恰好是**「（訊息已刪除）」；復原其中一則 → 標題是那一則；…從來沒說過話 →「還沒說話」」
  - → 任務 1 驗收 2：「§4.4 的三個可判定案例（含「全部已刪除 → 空字串」）」
  - → 任務 1 驗收 3：「「你說的話」全部已刪除的對話**不是**空白、存檔時的處置是「寫入」；正向對照：只有開場白的對話、以及只有一則已刪除開場白的對話，**是**空白、處置是「移除」」
  - → 任務 1 驗收 4：「刪除後「apple」無命中、「kiwi」恰好一則且兩個數字是 1 與 1、「已刪除」無命中；復原後「apple」命中、「kiwi」兩則…四則含「kiwi」、刪除其中一則 → 顯示三則、…餘數為 0」
  - → 任務 1 驗收 5：「§4.1 的三個可判定案例（次序、復原、全刪），外加…沒有任何已刪除訊息時…」
  - → 任務 1 驗收 6：「只差在某一則訊息的刪除狀態 →「有改變」；…只差在「是否生成中」→「沒改變」」
  - → 任務 1 驗收 7：「已完成的訊息（你說的、對方的、id 為 0 的開場白、文字以中斷標記結尾的、文字以「出錯了：」開頭的）→ 複製與刪除；生成中的訊息（文字為空、文字不為空各一）→ 沒有任何動作；已刪除的訊息 → 沒有長按動作；同一段對話裡有一則生成中的訊息時，其餘已完成的訊息仍然是複製與刪除」
  - → 任務 1 驗收 8：「轉成儲存形式再經由「依 id 取」與「取全部」共用的讀取規則轉回來 → 每一則的刪除狀態與文字都與原本相同。測資：已刪除的你說的話、已刪除且以「…（已停止）」結尾的回覆、未刪除的訊息各至少一則；文字含換行。」「舊資料…測資直接寫成本版之前的儲存形式（…commit `e8cf9aa`…），不得用本版的程式產生。」（另以 `git show e8cf9aa:` 對照舊形式）
  - → 任務 1 驗收 10：「對方的訊息原始字串是 §2.1 可判定的那一串 → 逐碼位相同…；你說的話 `a*b*  **x**`（含連續空白）→ 相同；以「…（已停止）」結尾的回覆 → 結果以同一串標記結尾、與原始字串相同。」
- **O3** 讀 `plan/architecture.html`
  - → 任務 6 驗收 1–6（1「訊息有刪除狀態；已刪除的訊息留在對話裡，原文不動」；2 哪些用途當作不存在、哪個照樣算數與為什麼；3 上下文次序與存檔時機；4「說得出刪除狀態記在哪裡、長什麼樣、沒有這項紀錄的舊資料代表什麼」；5 那張表與長按選單；6 其他不符處）
- **O4** grep `docs/product/features.md`
  - → 任務 7 驗收 1–3

## 情境 S1：升級（必須排第一個上裝置；前置：清空使用者資料 → 裝 `e8cf9aa` 的建置）

在專案以外的暫存目錄取出 `e8cf9aa` 建置。舊版上建立兩段有來有往的對話（其中一段含「…（已停止）」結尾的回覆），記下每一則文字；`df` 確認空間；覆蓋安裝工作區的建置（不清資料；這同時就是本輪的「覆蓋安裝」）。

- **O5** → 任務 4 驗收 4：「打開 → 不出現「之前的對話紀錄讀不出來…」；兩段都在抽屜或畫面上；每一則都不是佔位、文字與記下的相同；對其中一則刪除再復原，結果正確；冷啟動一次之後以上仍然成立。」

## 情境 S2：一段對話〔開場白、你「first kiwi」、回覆、你「second」、回覆〕（開一段新的）

- **O6** 對你說的話、正常結束的回覆、開場白各長按一次 → 任務 2 驗收 1（前三種）：「都出現恰好含「複製」「刪除」兩項的選單」。（第四種在 O20；「出錯了」「沒有產生回覆」造不出來時記「本環境無法執行」。）
- **O7** 刪「first kiwi」、再刪最後一則對方的回覆 → 任務 3 驗收 1：「沒有任何確認；原本的位置上是靠右的佔位，讀畫面的工具讀得到「已刪除的對話」與「復原」，**整個畫面上讀不到「first kiwi」**；其餘四則原樣。再刪除一則對方的回覆 → 它的佔位靠左。」（第一次刪除後先 dump 判一次，再刪第二則）
- **O8** 長按佔位（「復原」以外）→ 任務 3 驗收 2：「沒有選單，佔位還在。」
- **O9** 搜尋（「first kiwi」已刪除）→ 任務 3 驗收 6 前半：「查「first」→ 那段對話不因為這一則而命中；查「已刪除」→ 顯示「找不到符合…」；正向對照：查「second」→ 命中。」
- **O10** 開新對話、開抽屜 → 任務 3 驗收 5：「那一列的標題是「second」、預覽是「second」。回到那段對話復原「first kiwi」，再切走 → 標題是「first kiwi」。」
- **O11** 復原之後搜尋 → 任務 3 驗收 6 後半：「復原之後查「first」→ 命中。」

注意：S1 留下的對話若含「first」「second」會污染 O9/O11；S1 的測資避開這些字，也避開保留字「已刪除」「復原」。

## 情境 S3：複製（開一段新的，送 `Copy  me: a*b*c  **x** - y`）

剪貼簿判讀一律：先複製一段已知的別的文字（哨兵）→ 做受測的複製 → 輸入框長按 → 貼上 → `ui.py field` 逐碼位讀 → 清空、不送出。

- **O12** 基準（App 停止時讀資料庫）→ 長按＋點外面、長按＋BACK → 停止再讀 → 任務 2 驗收 3：「兩次之後：仍在聊天畫面、那一則仍是原樣；儲存的內容與基準相同（含最後更新時間）。」
- **O13** → 任務 2 驗收 4：「讀回的內容與送出的逐碼位相同（星號、連續空白都在）。」
- **O14** 複製 → 看狀態列 → 結束行程再讀資料庫 → 任務 2 驗收 8：「與基準相同（含最後更新時間）；複製之後狀態列沒有變成「輸入中…」。」
- **O15** → 任務 2 驗收 5 (i)：「讀回的內容與畫面上那則開場白逐碼位相同。」
- **O16** 請模型用條列與粗體回答 → 複製得 C；停止後讀儲存得 S → 任務 2 驗收 5 (ii)：「**C 與 S 逐碼位相同**」；算數條件：「S 裡至少有一個畫面上看不到的記號」。
- **O17** 3,000+ 字元、`BEGIN-`…`-END` → 任務 2 驗收 7：「在它的開頭捲出螢幕的情況下長按看得到的部分 → 複製 → 讀回的內容以 `BEGIN-` 開頭、以 `-END` 結尾、長度與送出的相同。」（送出前先逐碼位確認欄位長度，`input text` 可能掉字）
- **O18** → 任務 5 驗收 3：「快速上下捲動、在泡泡上輕點，都不會跳出選單；輸入框的長按（系統的貼上選單）照常可用。」

## 情境 S4：生成中與停止（開一段新的；動作寫進同一個 `adb shell` 批次、用截圖判）

- **O19** → 任務 2 驗收 2：「回覆結束之前長按那則回覆 → 沒有選單、畫面沒有變化。」算數條件「長按之後那則回覆仍在變長才算」；「**後半每次都判**：那則回覆結束之後長按它 → 出現選單。」
- **O20** 造「…（已停止）」回覆 → 長按（任務 2 驗收 1 第四種）→ 複製 → 任務 2 驗收 6：「讀回的內容以「…（已停止）」結尾，標記之後沒有任何字元；App 停止後讀儲存裡那一則的文字，與讀回的內容逐碼位相同。」
- **O21** → 任務 3 驗收 3：「先記下它在畫面上的文字；刪除 → 點佔位上的「復原」→ 它回到原位、靠左、文字與記下的逐碼位相同（含標記）。」
- **O22** → 任務 3 驗收 7：「回覆結束之前刪除更早的一則訊息 → 那一則變成佔位，回覆繼續變長直到自己結束。」算數條件同條文。

## 情境 S5：各自獨立（開一段新的，兩句來回）

- **O23** → 任務 3 驗收 4：「刪除三則（含開場白）→ 三個佔位；復原中間那一個 → 另外兩個仍是佔位；同一則再刪除、再復原一次，結果正確。把每一則都刪掉 → 輸入框仍可送出一句話並得到回覆。」

## 情境 S6：時間（三段既有對話 A、B、C，先從資料庫確認彼此相隔 ≥ 1 分鐘）

- **O24** → 任務 3 驗收 8：「抽屜上 **A 排在 B、C 前面**；…A 的最後更新時間落在 t1 前後數秒內，B、C 的不變。等至少 1 分鐘，…復原…A 的時間落在 t2 前後數秒內（**晚於**刪除那一次）。」時刻取裝置時鐘。

## 情境 S7：保存

- **O25** → 任務 4 驗收 1：「刪除一則訊息，**不做任何其他動作**，立刻 force-stop 再打開 → 續接到那段對話，那一則是佔位。點「復原」，同樣立刻強制停止再打開 → 那一則是復原後的原文。」（刪除與 force-stop 寫在同一個批次）
- **O26** → 任務 4 驗收 2：「切到另一段、再切回來 → 兩個佔位都在原位；冷啟動 → 仍在；此時搜那則你說的話裡的字 → 無命中。…**那兩則的原文仍在儲存裡**…再打開 App 點「復原」→ 文字與記下的逐碼位相同。」
- **O27** → 任務 4 驗收 3：「抽屜上那一段**仍在**；冷啟動之後仍在，點開 → 佔位都在，逐一復原得到原文。**反向對照**：…只有開場白的新對話裡刪除開場白 → 開另一段新的對話 → 抽屜上**沒有**多出那一段；App 停止後讀儲存，對話段數沒有因為它而增加。」（乾淨起點：各用新開的對話）

## 情境 S8：回歸（沒有任何已刪除訊息的新對話）

- **O28** → 任務 5 驗收 2：「送出一句、回覆逐字出現、畫面自己捲到最新一則；按停止 → …；切換對話、換人格、開新對話、從抽屜刪掉一段 —— 都照常。」（停止那一半的證據可取自 O20）
- **O29** → 任務 5 驗收 4：「抽屜每一列的標題、預覽，以及搜尋的命中與兩個數字，與 conversation-search §5.2、§3.4 的規定相同（抽一段對話各看一次即可）。」
- **O30** → 任務 5 驗收 5：「回覆生成途中切換到另一段對話 → 被切走的那段留下部分文字加中斷標記，且在抽屜上。攔不到記「本環境無法觀察」。」

## 規格範圍以外的隨機抽測（恰好三項；以亂數從「從未被挑過」的項目抽出）

- 歷史-10（清空資料後第一次開抽屜時順便看）
- 搜尋-20
- 保存-08（會毀掉資料庫，排在全部條文判完之後最後做）

## 重置點

- S1 之前清空使用者資料（模型檔保留）。S1 → S2 不重置（S6 要用到既有對話）。
- 每個情境各開一段新的對話；S7 的三條各自用新的對話。
- 保存-08 排最後。
