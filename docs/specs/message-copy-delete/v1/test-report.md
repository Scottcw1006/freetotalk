# 驗證結果總覽

**pass。** 44 條驗收中 43 條 pass；1 條（任務 6 驗收 6）可判的部分 pass、其中一句要求的證據不在產物裡，判不出來，打回 PM（條文問題，不是實作問題）。沒有任何一條判 fail。另有 5 個條文層面的發現打回 PM（見「問題歸屬」）。

- 受測版本：`90c438f`，開始時 `git status --porcelain` 為空（乾淨）。產品程式碼最後變更 `2026-09-18 16:26:10 +0800`；裝置上的安裝時間 `2026-09-18 16:33:10`（晚於前者）；裝置上 APK 大小 1,858,272,288 bytes，與工作區建出的相同。
- 開始時 `test-cases.md`、`test-report.md` 的位置上都沒有既有檔案，沒有刪除任何東西。
- 裝置：AVD Pixel_9（emulator-5554），Android 17，1080x2424。
- 沒有讀 `spec-decisions.md`、`spec-appendix.md`、任何 `pm-inbox.md`、任何先前的 test-report / test-cases、commit 訊息。引用到的別份規格只讀了現行版號目錄裡被指名的那幾節（conversation-store-db v6「測資與讀取的約定」「攔住還在生成中的回覆」、conversation-search v3.25 §3.4 與 §5.2、stop-generation v5「共同前置」）。

# 逐項驗收結果

## 任務 1：「當作不存在」的純邏輯

全部以 `app/src/test/java/com/example/demo/MessageDeletionTest.kt`（22 個測試）逐條讀斷言判定，並實際跑過（見驗收 9）。

- **驗收 1（標題）**：pass。`the title is the first thing the user said that is not deleted`（刪 first →「second」、復原 →「first」）；`a title taken from a later message is still cut short`（第一則已刪、第二則 40 字 → 26 字＋「…」）；`a thread whose every user message is deleted says so…`（斷言恰好等於「（訊息已刪除）」、復原其中一則 → 那一則、只有開場白 →「還沒說話」）。條文指名的案例全在。
- **驗收 2（預覽）**：pass。`the preview is the last message that is not deleted…`：刪 answer →「question」；全刪 → `""`；復原 answer →「answer」。三個都在。
- **驗收 3（空白判斷）**：pass。`deleting everything the user said does not make the thread blank`（`assertFalse(isBlank)`、`SaveAction.Write`）；`a thread nobody wrote in is blank whether or not its opener is deleted`（只有開場白、只有已刪除開場白 → `isBlank`、`SaveAction.Delete`）。
- **驗收 4（搜尋）**：pass。`a deleted message cannot be found…`：apple 空、kiwi `size==1` 且 `matchedMessageCount==1`、命中的是 id 2、「已刪除」空（另外也斷言了「復原」空）；復原後 apple 命中 id 1、kiwi 為 2。`a deleted message is not counted among the hits left out`：四則 kiwi 刪一則 → `hits.size==3`、餘數 0（每段上限 3 則，少了過濾時餘數會是 1，所以這個案例確實碰得到邊界）。
- **驗收 5（上下文）**：pass。四個測試分別斷言：無刪除時 〔U1,A1,U2,A2〕；刪 U1 → 〔U2,A2〕；復原 → 〔U1,A1,U2,A2〕；全刪 → 空。`ChatViewModel.send` 已改成呼叫 `thread.contextForNextTurn()`（讀 diff 確認），測試測到的就是產品走的那一段。
- **驗收 6（內容有沒有改變）**：pass。`deleting or restoring a message is a change of content`（兩個方向都 `assertFalse(hasSameContentAs)`）；`a reply that merely stopped streaming is still not a change of content`。
- **驗收 7（能做哪些動作）**：pass。已完成的五種（你說的、對方的、id 0 開場白、「…（已停止）」結尾、「出錯了：」開頭）→ 複製＋刪除；生成中兩種（空字串、非空）→ 空集合；已刪除 → 空集合；同段有一則生成中時其餘兩則仍是複製＋刪除。畫面的長按確實是問這個屬性（`MessageBubble` 讀 `message.longPressActions`，空集合時不掛長按偵測；`setMessageDeleted` 也用它擋）。
- **驗收 8（存進去讀回來＋舊資料）**：pass。來回測試用 `toRecord()` → `ConversationReader.find` 與 `.all`，測資含已刪除的你說的話（含換行）、已刪除且以「…（已停止）」結尾的回覆（含換行）、未刪除的訊息，斷言整段 `assertEquals`。舊資料測試的 `messages` 是手寫 JSON 字串 `{"id","author","text","createdAt"}`，我用 `git show e8cf9aa:…/ConversationRecords.kt` 對過：那一版的 `StoredMessage` 正是這四個欄位、同一個預設 `Json`，測資不是用本版程式產生的。
- **驗收 9**：pass。`./gradlew clean testDebugUnitTest`：log 有 `:app:clean` 與 `:app:testDebugUnitTest`、無 UP-TO-DATE；結果 XML 合計 170 tests / 0 failures / 0 errors / 0 skipped，`MessageDeletionTest` 22/0/0，timestamp 是當下。
- **驗收 10（複製出來的文字）**：pass。`copying takes the words as stored, not as drawn`：`"**重點**：\n- 蘋果\n\n1) 香蕉"`、`a*b*  **x**`、「講到一半…（已停止）」三者皆 `assertEquals` 原字串，另斷言以標記結尾。畫面的複製用的就是 `message.clipboardText`（讀 diff 確認）。

## 任務 2：長按選單與複製（裝置上）

- **驗收 1（選單）**：pass。你說的話、正常結束的回覆、開場白、「…（已停止）」結尾的回覆各長按一次，選單視窗的文字節點恰為 `['複製','刪除']`。額外：「（這次沒有產生回覆，再說一次試試）」這一種在本輪剛好造得出來（送 3,010 字元那一次的回覆），長按同樣是複製＋刪除；「…（已達長度上限）」結尾的也是。「出錯了：…」造不出來，**本環境無法執行**，由任務 1 驗收 7 守。
- **驗收 2（生成中）**：pass，**這一次算數**。提示 `write a very long essay about the history of Rome in 500 words`（Gemma3 1B）。送出後 1.2 秒截圖（回覆 4 行、狀態「輸入中…」）→ 在回覆泡泡內 (200,900) 長按 900ms → 截圖：沒有選單、沒有選取、回覆已長到 9 行且仍是「輸入中…」→ 之後兩張繼續變長。後半：回覆以「…（已達長度上限）」結束後長按同一則 → 出現複製／刪除。
  - 條文字面的「畫面沒有變化」在生成中不可能成立（回覆本身一直在變長）；我照「沒有選單、沒有選取、其餘不變」判。打回 PM 修字面。
- **驗收 3（關掉選單）**：pass。基準（App 停止時唯讀資料庫）→ 長按＋點選單外、長按＋BACK → 兩次選單都消失、`mCurrentFocus` 仍是 App、訊息原樣 → 停止後再讀：內容 diff 為空（含 updatedAt）。
- **驗收 4（你說的話）**：pass。送出的欄位內容逐碼位確認為 `Copy  me: a*b*c  **x** - y`；先複製開場白（哨兵）再複製它，貼回輸入框讀到 26 字元、與送出字串相等（連續兩個空白、星號都在）。
- **驗收 5（對方說的話）**：pass。(i) 貼回的開場白與畫面上開場白節點文字 `==`（15 字元）。(ii) **這一次算數**：提示用條文給的例句；畫面顯示「苹果 真是个好东西！」（粗體、無星號），C＝`'苹果，橙子，葡萄\n**苹果** 真是个好东西！'`，App 停止後讀儲存 S 與 C `==`；S 含畫面上看不到的 `**`。
- **驗收 6（中斷標記）**：pass。貼回的 122 字元以「…（已停止）」結尾、其後無字元；與儲存裡那一則的 text `==`。
- **驗收 7（超過一個螢幕）**：pass。送出 3,010 字元（送出前逐碼位確認 `BEGIN-`…`-END`、無空白）；送出後泡泡開頭已捲出螢幕（截圖確認），長按可見部分 (640,1000) → 複製 → 貼回的檔與送出的檔 `cmp` 完全相同（3,010 bytes）。
- **驗收 8（複製不改資料）**：pass。複製後 0.7 秒狀態列是「Gemma3 1B・離線」，不是「輸入中…」；結束行程後讀儲存，與基準 diff 為空（含 updatedAt）。

## 任務 3：刪除、佔位、復原與時間（裝置上）

- **驗收 1**：pass。刪「first kiwi」：點下去就變，沒有對話框；原位置出現靠右的佔位（bounds x 633–1001），dump 有「已刪除的對話（」「復原」「）」，整份 dump grep「first kiwi」為 0；其餘四則文字不變。再刪最後一則回覆 → 佔位靠左（x 79–447）。截圖：佔位是透明底＋外框＋斜體灰字，「復原」粗體主題色，與一般泡泡分得出來。
- **驗收 2**：pass。在靠右佔位的「已刪除的對話」字上、靠左佔位的左半各長按一次 → 沒有選單，佔位還在。
- **驗收 3**：pass。記下「Okay, here’s a long story about a raven …it’…（已停止）」→ 刪除 → 點該佔位的「復原」→ 同一個 bounds `[79,1796,793,2048]`（靠左、原位）、文字與記下的 `==`。
- **驗收 4**：pass。刪開場白、「mango one」、第一則回覆 → 三個佔位；復原中間那個 →「mango one」回來、另兩個仍是佔位；同一則再刪、再復原，結果相同。全部刪掉（5 個「復原」、0 則訊息）→ 送出「hello after all」→ 得到回覆，無閃退。
- **驗收 5**：pass。驗收 1 的狀態＋最後一則回覆已刪 → 開新對話 → 抽屜那一列標題「second」、預覽「second」。復原「first kiwi」再切走 → 標題「first kiwi」。（中途我自己點錯一次、先復原到最後一則回覆，當時那一列的預覽變回該回覆、標題仍是「second」——與 §4.4 一致；之後改點正確的那個再判。）
- **驗收 6**：pass。「first kiwi」已刪時：查 first →「找不到符合「first」的訊息」；查「已刪除」（經注音輸入法，逐碼位確認欄位為 U+5DF2 U+522A U+9664）→「找不到符合「已刪除」的訊息」；查 second →「1 段對話・1 則訊息」命中。復原後查 first →「1 段對話・1 則訊息」命中「first kiwi」。
- **驗收 7**：pass，**這一次算數**。同上提示，送出後 1.0 秒長按開場白 → 截圖：選單（複製／刪除）出現、下方回覆仍在生成 → 點刪除 → 截圖：開場白變佔位、狀態仍「輸入中…」、回覆從 12 行長到 22 行以上 → 最後自己以「…（已達長度上限）」結束。
- **驗收 8**：pass。A/B/C 的 updatedAt 分別為 …279458／…448777／…753287（彼此 >1 分鐘）。從搜尋結果打開 A（不說話）→ 刪一則 → t1=1789721739287（裝置時鐘）→ 開新對話 → 抽屜第一列是 A。儲存：A=1789721738235（早於 t1 約 1.05 秒，t1 是動作後才取的），B、C 不變。91 秒後回到 A 復原 → t2=1789721841546；儲存：A=1789721840507（晚於刪除那次），B、C 不變。

## 任務 4：保存與升級（裝置上）

- **驗收 1**：pass。長按→點刪除→`am force-stop` 寫在同一個 `adb shell`（點擊與停止之間沒有別的動作）→ 冷啟動：續接到那段、那一則是佔位。點「復原」＋立刻 force-stop（同一批次）→ 冷啟動：原文回來。
- **驗收 2**：pass。刪「second」與一則回覆 → 切到另一段再切回 → 兩個佔位在原位 → 冷啟動仍在 → 查 second →「找不到符合…」→ 停止後讀儲存：id 2、id 3 兩則帶 `"deleted":true`，text 是完整原文（怎麼讀照架構文件儲存那一節，照著做得出來）→ 再開 App 兩個都復原 → 整段的文字與 bounds 與刪除前的紀錄 diff 為空。「沒有期限」只驗到本輪的時間跨度。
- **驗收 3**：pass。把「pear one」「pear two」都刪掉 → 開新對話 → 抽屜上那段仍在（標題「（訊息已刪除）」、預覽是最後一則回覆）→ 冷啟動仍在 → 點開：兩個靠右佔位 → 逐一復原 → 與刪除前 diff 為空。反向：只有開場白的新對話裡刪開場白（畫面變佔位）→ 再開一段新的 → 抽屜上沒有「還沒說話」的列；儲存前後都是 8 段、id 集合相同。
- **驗收 4（升級）**：pass。在 scratchpad 裡 clone 出 `e8cf9aa` 建置（專案工作區沒有切版本），清空使用者資料後覆蓋安裝舊版，建兩段對話（一段含「Okay, here’s a…（已停止）」），記下文字與舊版儲存內容（每則四個欄位）。`df` 13G 可用 → 覆蓋安裝工作區建置（不清資料，Success）。打開：續接到第二段，五則文字與紀錄 diff 為空、沒有佔位；dump 無「讀不出來」；抽屜上有第一段，點開三則相同。對「…（已停止）」那則刪除再復原 → 與紀錄相同。冷啟動一次（啟動後 2 秒截圖＋5 秒 dump）：無提示、兩段都在、文字相同。
  - 限制：升級後**第一次**打開時我的第一次讀畫面在啟動後約 6 秒，沒有更早的截圖。不過那句提示出現的前提是資料被清掉，而兩段對話都完整保留；另外本輪抽測保存-08 時看到那句提示在畫面上是帶 Dismiss 鈕的，2.5 秒時仍在。

## 任務 5：回歸驗證

- **驗收 1**：pass（同任務 1 驗收 9，170/0/0）。
- **驗收 2**：pass。送出一句、回覆逐步變長（連續截圖）、長訊息送出後畫面停在最新一則；按停止 → 回覆以「…（已停止）」結尾、輸入列回到送出鈕（隨後即可再送）；從抽屜切換對話、開新對話、換人格（→ 老師，新對話、開場白換了）、從抽屜刪一段（那一列消失，儲存裡也沒了）都照常。
- **驗收 3**：pass。四次快速上下捲、兩次輕點泡泡、一次從泡泡上起手的慢拖，之後都沒有選單；輸入框長按照常跳出 Paste／Autofill（整輪的貼上都靠它）。
- **驗收 4**：pass。抽屜列：人格 emoji／名稱／模型／時:分／標題／預覽／刪除鈕俱全；長標題「write a very long essay ab…」有截短記號、短標題無；預覽是最後一則。搜尋「pear」→「1 段對話・2 則訊息」，列出兩則。
- **驗收 5**：pass，攔到了。送出後 2.5 秒開抽屜（截圖上送出鈕位置仍是轉圈）→ 點「開始新的對話」→ 抽屜上有被切走那段；打開它，最後一則是「…The roots of Rome lie in the rugged…（已停止）」。

## 任務 6：更新架構文件

- **驗收 1–5**：pass。以 `git diff e8cf9aa..HEAD -- plan/architecture.html` 讀：1「刪除一則訊息是在它身上做記號，不是把它拿走…位置、id、原文都不動」；2 列出上下文／搜尋／標題／預覽略過、空白判斷照算與原因；3 寫明「先拿掉已刪除的，再略過開頭連續的助理訊息」與理由、存檔時機四種；4 寫明 `"deleted":true` 記在 messages JSON 每則自己的物件上、未刪除不寫欄位、復原後欄位消失、舊資料＝全部未刪除，附查詢例子——**我照著它在任務 4 驗收 2、任務 3 驗收 8 實際判出了刪除狀態與原文，並確認復原後欄位確實消失**；5 表內 ChatScreen／ChatViewModel／Conversation／ConversationSearch／ConversationRecords 五列職責已更新、路徑都存在，三個畫面那一節提到長按選單。
- **驗收 6**：**判不出來（打回 PM）**。「沒改的列在交付說明裡」要求的「交付說明」不在我能讀的產物裡；我沒有主動發現文件與程式碼的其他不符，但這一句無從確認。

## 任務 7：features.md 一致

- **驗收 1**：pass。聊天-18～21 都在，23 個節號＋節名逐一 grep：features.md 與 spec.md 兩邊都各命中 1 次。
- **驗收 2**：pass。歷史-04（§4.4）、歷史-05（§4.3）、歷史-12（§5.1/§5.2）、搜尋-15（§4.2）、保存-02（§4.5）、保存-10 都有。
- **驗收 3**：pass。聊天-19 寫明原始的字與 `**重點**` 例；歷史-05 寫明「（訊息已刪除）」；保存-11 在。
- **本輪在裝置上確認過、可以拿掉 `※未經查證` 的列**：聊天-18、聊天-19、聊天-20、歷史-04（已刪除那一半）、歷史-05（那一半）、歷史-12（那一半）、搜尋-15（那一半）、保存-02（那一半）、保存-10、保存-11。**聊天-21 沒有在裝置上確認**（規格明文不在裝置上判，只有 JVM 測試）。

# 邊界情況檢查

spec 沒有名為「已知衝突與邊界情況」的一節；我把「這次會踩到的既有事實」與行為規格裡的「可判定／反方向不變」當邊界清單逐條查：

| 邊界 | 怎麼確認 | 結果 |
|---|---|---|
| 中斷標記是文字的一部分（事實 1） | 複製／復原／儲存三處比對 | 有處理 |
| 助理訊息原始字串≠畫面（事實 2） | 任務 2 驗收 5(ii) C==S 且含 `**` | 有處理 |
| 只改刪除狀態時時間要前進（事實 3） | `hasSameContentAs` 多比 `deleted`；裝置上兩次都前進 | 有處理 |
| 刪除當下存檔（事實 4） | 刪除＋立刻 force-stop | 有處理 |
| 刪光自己的話不得變空白（事實 5）；從沒說過話仍是空白 | `isBlank` 未過濾 deleted；裝置上正反兩向 | 有處理 |
| 不改表格結構／舊資料缺欄位（事實 6） | `@Database(version = 1)` 未變、`deleted` 有預設值；裝置升級 | 有處理 |
| 先過濾再略過開頭助理訊息（事實 8） | 測試＋讀 `contextForNextTurn` | 有處理 |
| 查「已刪除」「復原」不得命中佔位 | 測試兩個詞都斷言；裝置上查「已刪除」 | 有處理 |
| 全部「你說的話」已刪 → 標題「（訊息已刪除）」、與「還沒說話」不混用 | 測試＋裝置 | 有處理 |
| 全部訊息已刪仍可說下一句 | 裝置 | 有處理 |
| 生成中刪別則不打斷生成 | 裝置（攔到） | 有處理 |
| 從清單刪掉整段 → 已刪除訊息的原文一起消失（§6.2 第二點） | 刪掉含 5 則已刪除訊息的那段後讀儲存：id 不在、grep 原文 0 | 有處理（無驗收條文指名，見下） |
| 已刪除訊息的 id 不被新訊息撞到 | 全刪後再送一句，無閃退、清單正常 | 有處理 |
| §6.6 存檔失敗 | 規格知情留白，未驗；讀碼確認刪除／復原走 `persist()` 同一個入口 | 靜態閱讀層次 |

# 如果 fail：問題歸屬

沒有判 fail 的條文。以下全部是**規格問題，打回 PM**，沒有一項要打回 RD：

1. **任務 6 驗收 6 要的證據不在產物裡**（第四型）：「沒改的列在交付說明裡」——交付說明不是 QA 讀得到的產物。請標明這句由誰確認、QA 驗不到，或改成產物裡查得到的東西。
2. **任務 2 驗收 2「畫面沒有變化」字面不可能成立**：生成中回覆本身在變長。建議改成「沒有選單、沒有文字選取，其餘不變」。
3. **有規則沒有被任何驗收條文指名**：(a) §1.6「剪貼簿…不變」——任務 2 驗收 3 沒列剪貼簿，本輪未驗；(b) §3.2「佔位與一般泡泡在視覺上分得出來、顏色取自主題」——任務 3 驗收 1 沒有這一項（本輪截圖看過：外框、斜體，分得出來）；(c) §6.2「從歷史清單刪掉整段對話時，已刪除訊息與原文跟著一起真的刪掉」——無驗收（本輪順手看過，成立）。
4. **架構決策那一節裡有說得出使用者後果的條目**，代表規格沒搬乾淨：§A.2（同 id → 清單閃退）、§A.5（每刪一則畫面跳到最底）。兩者都沒有對應的行為條文或驗收。本輪操作中沒有遇到閃退；刪除／復原後清單位置沒有跳到最底（刪除長對話末段那則時，前後 dump 的相鄰訊息位置只差佔位與原泡泡的高度差），但這不是照條文驗的。
5. **任務 4 驗收 4「不出現…提示」沒有說要在打開後多久之內看**：那句提示實際上是要手動 Dismiss 的，但條文沒寫，QA 只能自己決定時間點。建議寫明「打開後第一次讀畫面即可」或給出提示的停留性質。

# 驗證方式說明

每一輪都要做的六件事：

1. **標準測試指令**：做了。`./gradlew clean testDebugUnitTest`，以結果 XML 判（170/0/0/0）。
2. **每一條指名了測試的驗收條文**：做了。任務 1 驗收 1–8、10 逐條讀斷言並對到測試名稱；驗收 8 的舊形式另以 `git show e8cf9aa:` 對照。
3. **每一條邊界情況**：做了，見上表。§6.6 為規格知情留白，只到靜態閱讀。
4. **每一條實機條文**：做了，全部在模擬器上實際操作（25 條裝置條文）；唯一「本環境無法執行」的是任務 2 驗收 1 裡的「出錯了：…」那一種（條文自己允許）。三條「攔得到才判」的（任務 2 驗收 2、任務 3 驗收 7、任務 5 驗收 5）本輪都攔到了。
5. **隨機抽測恰好三項**：做了，見下。
6. **真實資料**：走「**使用者已同意清空**」那一條路，沒有做快照／還原／逐位元組驗證。驗收開始前清掉：`databases/conversations.db`（含 -wal/-shm）、`shared_prefs/conversation_store.xml`、`shared_prefs/session.xml`。保留：`files/` 下三個模型檔（gemma3-1b.task、model.task、smollm.task）與 `profileInstalled`。結束時同樣清了一次（抽測保存-08 弄壞過資料庫），模型檔仍在。全程只有覆蓋安裝（`adb install -r -t`），沒有解除安裝。

**偏離 `test-cases.md` 的地方**：

- O14（複製不改資料）與 O13 的複製動作重做了三次：前兩次貼不進輸入框，原因是系統的剪貼簿預覽卡蓋住輸入框（環境問題，已寫進環境文件），不是產品問題。最後一次貼上成功後重新讀一次儲存再判。
- O10 中途我點錯「復原」（點到另一個佔位），改正後重做；不影響判定，過程寫在任務 3 驗收 5。
- O20／O21 的「…（已停止）」回覆試了兩次才造出來（第一次 1.3 秒就按停止，當時還沒有字，回覆整則不留——那是既有行為）。
- S4 的順序改成 O19 → O22 → O20 → O21（同一個生成提示連著用），觀察點與條文對應不變。
- 隨機抽測的歷史-10 除了在保存-08 之後看到一次，另外在乾淨清空之後獨立再看了一次。
- 生成中的判讀（O19、O22、O30）用同一個 `adb shell` 批次加連續截圖，沒有用 dump（生成中 dump 不可靠）。
- 其餘沒有偏離。

突變全掃：呼叫者沒有要求，沒有做。

# 本輪驗證範圍

全驗（7 個任務、44 條驗收）。

# 環境文件的處置

`docs/qa-environment.md`：依賴之前跑過確認方式的條目——第 1 節（JDK、clean 測試的判準）、第 2 節（裝置四條指令、`df`）、第 3 節（安裝時間 vs 程式碼變更、串流安裝）、第 4 節（清空時只刪使用者資料、`sqlitepull.sh selftest`、`sqlitecontent.py selftest`）、第 5 節（`ui.py selftest`、`uitext.py selftest`）、第 6 節（版面判斷、注音鍵序選字、`cmd clipboard` 不存在的剪貼簿 UI 路）、第 11 節（取裝置時鐘）、第 13 節（批次寫法）。都成立，沒有需要修正的條目，也沒有發現越界的產品內容。新增第 14 節（剪貼簿預覽卡會擋住底部欄位、貼上的順序、BACK 收鍵盤後點欄位不一定再彈鍵盤、彈出選單開著時 dump 只有選單視窗、CTRL+A 清欄位、分段送長字串、在專案外 clone 舊 commit 建置的做法、scratchpad 根目錄有別輪殘留），每條附確認方式。`docs/qa-tools/` 沒有新增或修改。

# 本輪隨機抽測的三項功能

以亂數從「從未被挑過、且不指向本規格」的項目裡抽出：

- **歷史-10**：清空資料後冷啟動、開抽屜 → 歷史列表位置顯示「還沒有其他對話。」，沒有任何刪除鈕。與描述相符。
- **保存-08**：App 停止時把 `conversations.db` 覆寫成 39 bytes 的亂碼並刪掉 -wal/-shm → 冷啟動：不閃退，畫面是一段新的對話（人格與模型是上次用的），底部出現「之前的對話紀錄讀不出來，已經清空，從這裡重新開始。」；抽屜「還沒有其他對話。」；送出一句得到回覆，切走後那段出現在抽屜上；再冷啟動一次不再出現那句提示；資料庫檔已被重建。與描述相符。
- **搜尋-20**：空的搜尋框按空白 → 欄位仍空；打 `pear` 再按空白 → 欄位 `pear␠`（尾端 0x20 留著）；游標移到最前面按空白 → 欄位不變；TAB 讓焦點離開 → 欄位變成 `pear`。與描述相符。

三項都與功能總覽相符；沒有拿它們改變上面任何一條的 pass／fail。

# 隨機挑選記錄的處置

`docs/qa-random-coverage.md`：追加三行（檔案已存在）——`2026-09-18  歷史-10`、`2026-09-18  保存-08`、`2026-09-18  搜尋-20`。
