# 這一輪改了什麼（v5）

起因只有一筆收件匣：**[2026-09-16 06:10] S3 裁決選 (A)** —— v3 那次在 Pixel 9 實機上的全驗結論原樣沿用，**不在模擬器上重跑任何裝置端驗收**，往後只有程式再被動到時才重驗。裁決同時要求：**把使用者知情接受的代價寫在下一個人會撞到它的地方**，不要只放附錄。

**沒有任何一條規則被新增或改動，沒有任何一條驗收的判定被放寬，也沒有任何實作改動。** 這一輪動的是「那些結論是在哪個環境取得的」這件事要寫在哪裡。

**入口檔仍然是 `v5/spec.md`**（v4 之後未拆檔，三份：`spec.md`、`spec-decisions.md`、`spec-appendix.md`）。

---

# 這一輪要做的任務(完整清單)

**這一輪對 RD 與 QA 都是 0 個任務。** 逐條走過 `spec.md`／「任務拆解」，答案如下：

- 任務 1　把「留下什麼」收成一個地方　　〔本輪不做：v3 已完成並通過，本輪無實作改動〕
- 任務 2　停止不走錯誤路、狀態收斂　　　〔本輪不做：同上。**驗收 3 底下新增了一段環境註記**，判定未變，見下面「改動的規格章節」〕
- 任務 3　停止那一次必須落盤　　　　　　〔本輪不做：同上〕
- 任務 4　回歸驗證　　　　　　　　　　　〔**本輪不做**，但它的觸發條件被改寫了 —— 見下一段，這是本輪唯一一處需要 RD／QA 注意的條文措辭變動〕
- 任務 5　更新架構文件　　　　　　　　　〔**本輪無需更新**：`plan/architecture.html` 一個字都沒有動。理由寫在該任務的「現行結論（v5 核對）」〕

**任務 4 為什麼不做，以及它的說明為什麼被改寫**：它原本寫「這一條**每輪必做**」。S3 的裁決在定下「不重跑」的同時，也定下了重驗的觸發條件 ——「**只有在程式再被動到時才重驗**」。v4 與 v5 都是純文件輪，照舊寫法會要求跑一次完整回歸，與裁決直接牴觸。所以說明改成「**凡是動到程式的那一輪，這一條必做**」（含測試程式），**八條驗收標準一個字都沒有改**。

---

# 本輪移除或失效的任務／驗收

**沒有。** 沒有任何驗收被移除或失效；沒有任何條文被放寬。

---

# 改動的規格章節（RD 與 QA 要讀原文的地方）

**QA 只需要讀第一項。** 其餘是狀態與記錄的同步。

1. **`spec.md`／「正確行為」的 R3、「任務拆解」的任務 2 驗收 3、「已知衝突與邊界情況」第 2 條** —— 三處各新增一段註記，講同一件事：R3 唯一一次在裝置上通過是 **2026-09-09 的實機**，模擬器上是第一次被驗；**fail 時先回報成「環境差異」，不要寫成「本輪改壞」**（除非那一輪真的動過程式）。**三處都明寫不放寬判定。** 為什麼寫三處而不只寫附錄：冷讀的人不會翻附錄，而 QA 走任務清單時先看到的是驗收、不是規則。
2. **`spec.md`／「這份文件怎麼讀」與「目前狀態」** —— 代號索引的 S3 那一列改成「已拍板」；目前狀態改寫成 v5，明講「規則全部有效、判定全部不變，但裝置端的通過是在實機上取得的」，並點名唯一一條會因裝置速度而翻面的是 R3。
3. **`spec.md`／任務 4 說明** —— 「每輪必做」改成「凡是動到程式的那一輪必做」（理由同上）。
4. **`spec.md`／任務 5** —— 現行結論由 v4 核對改為 v5 核對，並記下順帶確認的結果（架構文件描述停止生成的四個地方都已含「使用者按下停止」）。
5. **`spec-decisions.md`** —— S3 移出，**回到 0 項待決**；已拍板表格新增 S3 那一列，並寫明代價落在哪三處條文旁。
6. **`spec-appendix.md`／決策歷程** —— 新增「已拍板：S3」（選項表、我當時傾向 (C) 的理由記為未被採納、使用者知情接受的代價原文、代價的三個落位、裁決的邊界）；「升級給使用者的」改為 0 項待決並把 S3 降級為歷史；「已知取捨與風險」中那條時序競爭補上現行狀態；查證事實中「驗收環境已換」那一項補上 S3 的答案；修訂記錄新增 v5 一列。
7. **`spec-appendix.md`／已知取捨與風險（順帶修掉一個會靜靜失敗的引用）** —— 「停止補寫的那次檔可能寫錯對象」那一列原本只寫 `conversation-search` **§8.4**，已補成 `docs/specs/conversation-search/spec.md` §8.4「可判定（儲存體裡的時間不動）」。**理由**：只有節號時，對方重新編號會讓這一句落在一個真實存在、但講別件事的節，而不會讓任何驗收變紅。**已核對過那一節現在的節名就是這個。**
8. **`spec.md`／「明確不做」中 `plan/architecture.html` 以外的文件那一列** —— 補上 v5 的狀態（`docs/product/features.md` 本輪一個字都沒有動，以及那一項仍未處理的轉交事項）。

---

# 未改動但與本輪改動互相牽動的章節

- **`spec.md`／R5、R6、R10 與任務 3** —— 它們的「通過」同樣只在實機上取得過。**本輪刻意沒有在它們旁邊加註記**：S3 的代價原文點名的是那個**時序競爭**，而這幾條驗的是落盤與寫入對象，與生成速度無關（判定是檔案內容與位元組比對，不是時間窗）。**在每一條旁邊都貼一句「這是實機上驗的」會稀釋掉真正需要被看到的那一條。** 整體狀態寫在「目前狀態」那一節，讀得到。
- **`spec.md`／「明確不做」中「改寫本輪那幾條過程條文」那一列（未動；同一節的 features.md 那一列本輪有改，見上面第 8 點）** —— 它的觸發條件之一是「任務 1 或任務 5 要**重做**時」。S3 定下的重驗觸發條件（程式再被動到）與它相容：程式被動到 → 任務要重做 → 那幾條要先改成 S2 (B) 的寫法。**兩條觸發條件沒有互相牴觸**，本輪不需要改它。

---

# 反向核對：本輪被改動的章節，每一條規則由哪一條驗收在守

**本輪判定為「被改動」的章節**（沒有被列進來的章節，等於這一輪明確宣告不需要核對）：
`spec.md`／「這份文件怎麼讀」、「目前狀態」、「正確行為」、「任務拆解」、「明確不做」、「已知衝突與邊界情況」；`spec-decisions.md` 全檔；`spec-appendix.md`／決策歷程、升級給使用者的、已知取捨與風險、查證事實、修訂記錄。

其中**只有「正確行為」底下有規則**（R1–R11）。R3 的旁註改動了該節，依「以節為單位」的判準，整節核對如下：

| 規則 | 被違反時，哪一條驗收會發現 |
|---|---|
| R1 停止不是錯誤 | 任務 2 驗收 1 |
| R2 畫面上的文字 + 固定標記 | 任務 2 驗收 2(a)(b)(c)（逐字）；任務 1 驗收 1（JVM 完整字串相等） |
| R3 停止後不再變動 | 任務 2 驗收 3 |
| R4 零文字時整則不留 | 任務 2 驗收 4 |
| R5 停止當下落盤 | 任務 3 驗收 1、2 |
| R6 落盤的是被停止的那一段 | 任務 3 驗收 3 |
| R7 真正的失敗維持現狀 | 任務 1 驗收 3（JVM，守門）；任務 2 驗收 6 |
| R8 兩種自動中斷維持現狀 | 任務 4 驗收 2 |
| R9 停止後可以立刻再說話 | 任務 2 驗收 5 |
| R10 隱含停止與手動一致 | 任務 3 驗收 4 |
| R11 最後更新時間 | **刻意沒有獨立條文**，由哪一層守已寫在 R11 規則旁（時間那一半在本規格分不出來；內容那一半由任務 2 驗收 2 與驗收 4 守；跨輪那一半由 `docs/specs/conversation-search/spec.md` §8.6「可判定（停止之後的切換不得再推進時間）」守） |

**沒有任何一條規則缺少覆蓋，本輪不需要新增條文。**

---

# 給呼叫者轉交的事項（本輪沒有動別人的檔案）

1. **`docs/product/features.md`／「「※未經查證」是什麼意思」那一節仍寫著「沒有實機跑過」** —— 驗收環境已改成模擬器，這個說法會誤導（讀者會以為標記講的是「還沒在實機上跑」，而實機從此不再是驗收環境）。這一項在 `changes-v4.md` 就已列入轉交，**目前仍未處理**。那份文件是跨 slug 的單一份文件，本輪有另一位 PM 正在動它，**我一個字都沒有改**。
2. **同一份文件現在多了一件可以順手做的事**：它列的比較-15、16 兩項的實測記錄是在**實機**上量的（v3），日後若有人要重新查證，那是模擬器上還沒看過的東西 —— 這只是提醒，不是要求。
