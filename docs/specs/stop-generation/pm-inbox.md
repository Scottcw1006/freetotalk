# PM 收件匣（stop-generation）

**目前沒有待處理事項。**

---

- **這個檔是意見信箱，不是檔案櫃。** 有事情要交給 PM 時，在這裡加一筆（標題含日期、來源是「使用者裁決」還是「RD 觀察」／「QA 報告」、相關條目、內容）。**PM 處理完就把那一筆刪掉。**
- **被刪掉的是通知，不是理由。** 每一筆的實質內容在刪之前都已落在 spec 與 `v<N>/spec-appendix.md`／決策歷程；原文在版本控制歷史裡（`git log -p -- docs/specs/stop-generation/pm-inbox.md`）。
- **這一份先前依舊規則保留了全部原文並在每筆補「處理結果」。** 九筆（S1 裁決、任務 5 的兩筆 RD 回報、QA 打回的三點、全驗 pass 的四件事、來自 `conversation-search` 的引用問題、驗收環境改模擬器、S2 裁決、S3 裁決）都已標為已處理，處理結果分別落在 v3、v4、v5，原文與當時的處理結果都在 git 裡。
