# PM 收件匣（stop-generation）

每筆處理完在下面補「處理結果」，**不要刪除原文**。

---

## [2026-09-08 17:05] 使用者裁決：S1 = 分支 B（加標記）

- **來源**：使用者裁決
- **內容**：

**S1 拍板為分支 B —— 手動停止會在部分文字後面追加一個看得見的中斷標記**，與現有的兩種自動中斷（長度上限、重複迴圈）一致。

**你在 `spec-decisions.md` 裡傾向 A，理由是「自動中斷需要告知、手動停止使用者自己知道」。使用者選了 B，所以 R2 與 R11 走分支 B 那一支。**

### 使用者在裁決前提出的一個質疑，以及查證結果

使用者問：「看起來跟現有規則一樣，有調整到 §8.2 第三點嗎？」

**這個質疑的前半是對的，請把它寫進理由，不要只寫結論：**

- **§8.1 不必改** —— 追加標記就是一次「訊息的文字改變」，§8.1 早已涵蓋，與 `…（已達長度上限）` 走同一條路。
- **§G.2 也不必改** —— B 不違反「時間是內容的性質」，內容真的變了，時間本來就該動。
- **會壞掉的是 §8.2 第三點，因為它不是原則，是舉例。** §8.2 開頭寫「下列**動作**不得讓時間前進」，列的是「哪些動作不改內容」。停止被列進去的前提是「停止只會關標記」—— B 讓那個前提變成假的。

**所以正確的說法是：線沒有移動，是「停止生成」這件事從線的一邊換到另一邊。** 使用者先前對「時間只跟著內容走」的裁決一個字都沒有被推翻。請照這個說法寫，不要寫成「使用者改變了先前的決定」。

### 待你決定的一件小事（實作細節，不需要回頭問使用者）

標記的**文字**還沒定。使用者說「（已停止）」大概就夠，但沒有指定，並表示有偏好會另外講。現有兩個是「…（已達長度上限）」與「…（模型卡在重複迴圈，已中斷）」，兩者都說明了「為什麼停」。請在 R2 定一個具體字串（含前面的刪節號要不要留），讓它可判定。

- **相關條目**：`spec-decisions.md`／S1（拍板後移出，該檔應回到「本次沒有需要決策的事項」）；`spec.md`／R2、R11、任務 2 驗收 2、任務 5 驗收 3；「對 conversation-search 的影響」那一節的表格（分支 B 那一列成為現行結論）
- **狀態**：已處理
- **處理結果**：
  - **標記字串定為〔刪節號〕+`（已停止）`（共 6 個字元）**，刪節號保留，理由與現有兩個標記同形；寫進 `spec.md`／R2，含「直接接在最後一個字元後、不得插入空白」與逐字比對的判定方式。
  - `spec.md`／**R2 重寫**為單一結論（分支 A 敘述移除）；**R11 重寫**為「停止無條件算一次更新」，附兩種停止當下狀態的對照表。
  - `spec.md`／**任務 2 驗收 2** 改成三點可判定（結尾字串、去掉標記後與截圖逐字相同、標記前無多餘空白），移除「待裁決不判 fail」。
  - `spec.md`／**任務 5 驗收 3** 改成具體可驗，見第二筆的處理結果。
  - 「對 conversation-search 的影響」那一節的兩支表格**整個移除**，改成四節單一結論。
  - **R4 未改動規則內容**，只補一句：零文字時不是追加標記，而是整則不留，且不得產生「內容只有 `…（已停止）`」的回覆。
  - `spec-decisions.md`**已清空**，回到「本次沒有需要決策的事項」，並留下已拍板事項的一列表格指回附錄。
  - **使用者的質疑（§8.1／§G.2 不必改、會壞的是 §8.2 第三點、線沒有移動）已原樣寫進理由**，位置在 `spec.md`／「對 conversation-search 的影響」第一、二節與 `spec-appendix.md`／決策歷程。**我原本傾向 (a) 的理由照實記錄為「未被採納」，沒有改寫成「當初判斷有誤」。**
  - **本輪沒有因此浮出任何新的待決事項。**

---

## [2026-09-08 17:05] RD 觀察：任務 5（架構文件）分支 B 的實際目標句

- **來源**：RD 觀察
- **內容**：

既然 S1 裁決成 B，`plan/architecture.html` 第 964 行那條要改。**實際的句子我查過了，抄在這裡讓你寫驗收條文時不必再找：**

> **對話的「最後更新時間」只跟著內容走。**新增訊息、訊息文字改變、訊息被移除才算一次更新；切過去看一眼、切走、按下停止都不算。否則「最近」會變成「最近被看過」，而歷史清單與搜尋結果的排序都靠這個時間。

要動的是「**按下停止**」那三個字（它與「切過去看一眼、切走」並列在「不算」那一側）。**句子的其餘部分在 B 底下仍然完全正確**，尤其是開頭那句原則與結尾的理由，不需要動。

**另外提醒一個容易連帶漏掉的地方**：R4（停止時還沒有任何文字 → 那則空回覆被移除）在**分支 A 與 B 底下都成立**，而「訊息被移除」在同一句話裡就列著要算一次更新。所以就算沒有 S1，這條也已經不是無條件的。你在 spec.md 第 159 行已經寫到這點，這裡只是提醒它同樣要落到架構文件那句話上。

- **相關條目**：`spec.md`／任務 5 驗收 3；R4；R11
- **狀態**：已處理
- **處理結果**（涵蓋原文與其下三點補充裁決）：
  - **原句已自行核對過**：`plan/architecture.html` 第 964 行的內容與這裡抄的**逐字相同**，抄錄無誤。任務 5 驗收 3 改寫成：移除「按下停止」四個字，其餘一字不動；並附三條可判定條件（不再含該四字／句首原則與句尾理由逐字未變／「不算」側只剩兩項）＋「行號會漂移，以句子內容為準」。另明寫**不得**在該條裡補一句「按下停止算一次更新」（理由同 §8.1），若 RD 判斷要補，補的內容不得與 R4 衝突。
  - **第一點（標記文字）**：見上一筆處理結果。
  - **第二點（§8.2 第三點的落位）**：採納。`spec.md`／「對 conversation-search 的影響」第一節寫成「**整條移除**，連同它下面那段『已知矛盾、待裁決』的引言區塊」，附三個理由（不能把肯定句放進否定清單／移除後由 §8.1 涵蓋不留缺口／它是舉例不是原則）。**要不要在 §8.1 底下另外明寫一句：我的判斷是「建議加，但不是要求」** —— 理由寫在同節第二節（§8.2 曾明寫過停止，整條移除後「刻意移除」與「不小心漏掉」在讀者眼中分不出來；§8.1 第二項的括號本來就是列舉，是它最自然的位置），並註明硬性要求只有一條：不論加不加都不得與 R4 衝突。**實際寫入仍由 `conversation-search` 那一輪的 PM 執行，我沒有動他們任何檔案。**
  - **第三點（例外消失）**：**我自己驗過，推論成立，已照辦。** 驗法：把「按下停止」當下的狀態列完，只有「已經有文字」與「還沒有文字」兩種（沒有第三種）；前者被 R2 要求追加標記→文字改變，後者被 R4 要求移除→訊息被移除，兩者在 §8.1 底下都算更新。**所以「停止算一次更新」在 B 底下無條件成立，不需要例外。** 已刪除 `spec.md` 舊第 159 行那條「§8.2 需要補一個例外」，R11 改為附狀態對照表並明寫「這一節與下一節都不得留下任何形式的例外條款」。
  - **順帶修正**：已知衝突第 5 條補上「S1 拍板後 §8.6 的節名『停止生成不算』已是假的」，並告訴 QA 以本輪 R11 為準、不要拿對方尚未更新的 §8.6 來 fail 本輪。

- **補充裁決（同一次對話，2026-09-08 17:30）**：

**一、標記文字定為「（已停止）」。** 使用者明確指定。前面的刪節號要不要留、以及與現有兩個標記（`…（已達長度上限）`、`…（模型卡在重複迴圈，已中斷）`）的格式一致性，由你在 R2 定成可判定的具體字串。

**二、`conversation-search` §8.2 第三點的處置：停止生成算一次更新。**

使用者的原話是「這邊改成『本身也算一次更新』」。**但那一行不能原地改寫成肯定句** —— §8.2 那一節的標題是「不算一次更新」，開頭寫「下列動作**不得**讓任何一段對話的最後更新時間前進」。在那張清單裡放一條「算一次更新」會讓標題與內容互相矛盾。

**RD 已向使用者說明這一點，使用者的意圖是「停止生成從此算一次更新」，怎麼落位交給你。** 最直接的做法是把那一行從 §8.2 移除（移除後它自動被 §8.1 的「訊息文字改變」涵蓋），要不要在 §8.1 底下另外明寫一句「按下停止也是」由你判斷。

**這一節仍然是由 `conversation-search` 那一輪的 PM 實際寫入的**，你只負責在「對 conversation-search 的影響」那一節把結論寫成他們可以直接取用的形式。

**三、分支 B 讓你原本擔心的那個例外消失了，請一併修正。**

你在 `spec.md` 第 159 行寫「§8.2 需要補一個例外，這一點兩支都適用」，理由是 R4（停止時還沒有任何文字 → 那則回覆被移除 → §8.1 要求推進時間）。

**在分支 B 底下不需要例外，因為兩條路的答案相同：**

| 停止當下 | 發生什麼 | §8.1 怎麼說 |
|---|---|---|
| 已經有文字 | 追加「（已停止）」 | 訊息文字改變 → 算更新 |
| 還沒有文字 | 那則空回覆被移除（R4） | 訊息被移除 → 算更新 |

所以「停止生成算一次更新」在 B 底下是**無條件**的。需要寫成「除了…之外」的是分支 A，而 A 沒有被選上。請把第 159 行與「對 conversation-search 的影響」表格裡的分支 B 那一列一併改成無條件的說法 —— **留著一個不存在的例外，下一輪會有人照著它去寫一條驗不出東西的條文。**

---

## [2026-09-08 19:05] RD：任務 5 驗收 6 的回報（先前寫「沒有發現」是錯的）

- **來源**：RD 觀察
- **內容**：

我在第一次做任務 5 時，驗收 6（「更新過程中若發現文件其他地方與程式碼不符，列出來回報」）回報的是「沒有發現」。**那個結論是錯的**，QA 全驗抓出來了。

**實際有兩處不符，都在同一張圖上**，而我只改了 `figcaption`：

1. **`plan/architecture.html` 的 `aria-label`**（「訊息生命週期」那張 SVG）：原文「若超過 800 字元或偵測到重複迴圈則取消生成並跳過 Complete。」**只寫兩種提早中斷。**
2. **同一張圖的圖內邊標**（`class="etxt-cut"`）：原文「超過 800 字元，或尾巴開始重複 → 取消生成，跳過 Complete」**同樣只寫兩種。**

**兩處都已修正**，各補上「或使用者按下停止」。下一行「改用串流累積的文字，並在結尾標記中斷原因」本來就是通用寫法，三種都涵蓋，未動。修完全檔已無只寫兩種分支的地方。

**我為什麼會漏掉**：我把「圖說」理解成 `figcaption` 這個標籤，改完就去核對別的條目了，沒有想到同一張圖的敘述其實散在三個地方（`aria-label`、圖內文字、`figcaption`）。這正好是 QA 打回來的條文問題第 1 點。

- **狀態**：已處理（v3）
- **處理結果**：
  - **回報內容我接受，兩處都已由你修好、也已通過全驗**（見 `test-report.md` 任務 5 驗收 1），**本輪不需要任何人再動那份文件**。
  - **「圖說」定義不明是條文的問題，不是你的問題** —— 這一條與下一筆（QA 打回的三點）是同一件事，我在下一筆一併處理：本輪不改寫已完成的條文，但把「指向一張圖的敘述時要明講涵蓋哪幾種載體、或改成整份可檢查的性質」記成寫法要求，寫在 `spec-appendix.md`／決策歷程第六點。
  - **你自陳的漏掉原因很有用，我原樣留著**：它說明了這一型條文的失效方式不是「有人偷懶」，而是「條文允許兩種讀法，而執行者選了成本較低的那一種」。

---

## [2026-09-08 19:05] QA 打回 PM 的三點條文問題（原樣轉交）

- **來源**：QA 報告（`test-report.md`）
- **內容**：

**1. 任務 5 驗收 1 的「圖說」定義不明。** 同一張圖的 `aria-label` 算不算「圖說」，兩種讀法會給出相反結論。這一輪的實際後果就是上面那一筆：RD 只改 `figcaption` 就以為做完了。

**2. 任務 5 驗收 2 與任務 1 驗收 5 是「過程條文」，QA 驗不到。** 它們要求的是「必須逐字核對過」「由 RD 在本機做一次即可，回報結果」—— **QA 只能驗結果，驗不到某個人有沒有做過某件事**。這一型條文的通病是：做了沒做都可能通過，取決於有沒有人留下痕跡，而條文沒有要求留痕跡。

**3. 任務 5 驗收 3(ii)「與修改前逐字相同」在沒有 git 的專案裡本質不可驗。** QA 這次判得下去，是因為**剛好** `pm-inbox.md` 裡有 RD 事前抄錄的原句，而且那筆的時間戳早於文件修改時間。那是巧合，不是機制。

**我（RD）的補充**：第 2、3 點是同一個形狀 —— **條文要求的證據不存在於產物裡，只存在於某個人的記憶或某次操作中**。這與附錄記過的三型失效（驗錯對象、規則沒條文覆蓋、條文不可達）都不同，比較接近 v3.4 在 `conversation-search` 記過的「條文指定的執行主體做不到那件事」，但這次的問題不是分工而是**可觀察物根本不存在**。要不要記成第六型、以及要不要改寫這幾條（例如改成「必須留下可查證的痕跡」），是你的判斷。

- **相關條目**：`spec.md`／任務 5 驗收 1、2、3(ii)、6；任務 1 驗收 5；`spec-appendix.md`／決策歷程
- **狀態**：已處理（v3）
- **處理結果**：
  - **三點我都判定成立**，包含 RD 的補充（第 2、3 點是同一個形狀）。第 3 點我特別確認過：這次判得下去確實是因為收件匣裡剛好有事前抄錄的原句，**那是巧合不是機制**。
  - **本輪不改寫那幾條條文。** 三個理由寫在 `spec-appendix.md`／決策歷程第六點：對應任務都已執行並通過，改寫改變不了已經發生的事、只會讓條文與驗證記錄對不起來；本輪範圍是記錄與措辭；真正有價值的是「下一次怎麼寫」。**觸發條件寫進 `spec.md`／「明確不做」**（下一輪要寫同型條文時就要改成「必須留下可查證的痕跡」）。
  - **三條寫法要求已寫成規格可直接照抄的形式**（同上決策歷程第六點）：指向一張圖的敘述要明講涵蓋哪幾種載體／不要寫「某人必須做過某事」，要指定留在產物裡的痕跡／要驗 before-after 就必須刻意留一份標明時點的快照。**最後一條順帶釐清了一件事**：「不要抄別份文件的內容」與「留快照」不衝突 —— 抄一份會持續演進的文件會腐爛，明確標成某個時點的快照不會，因為它宣稱的是過去的狀態。
  - **關於「要不要記成第六型」**：我記下這一型的**性質**（條文要求的證據不存在於產物裡），但**不寫編號** —— 那份失效型清單由 `conversation-search` 那一輪維護，長度會變，寫死序號等於再製造一個會自己變錯的引用。要不要併進那份清單由該清單的維護者決定，**建議已寫進 `changes-v3.md`，由呼叫者轉交**（本輪沒有動他們任何檔案）。
  - **升級了一項**：要不要把「禁止過程條文」變成每一輪都適用的硬性規則 → `spec-decisions.md`／**S2**。理由：它改變每一輪的固定成本，且同時約束 PM、RD、QA 三方，不是配合性判斷。**它不擋任何人開工。**

---

## [2026-09-09 20:10] 全驗 pass ＋ 三件要你記錄的事 ＋ 一條新的寫法規則

- **來源**：QA 報告（同目錄 `test-report.md`）＋ 使用者裁決
- **內容**：

**這一輪的重驗結論是 pass。** B1、B2 都不再重現，R1–R11 全數成立，任務 1–5 每一條驗收都通過，「明確不做」沒有被越界。

### 一、比較畫面的現況已經量出來了（任務 4 驗收 8 要求交給你補進附錄）

停止之後，**Qwen2.5 0.5B 與還沒輪到的 SmolLM 135M 都被標成「失敗」，內容都是 `StandaloneCoroutine was cancelled`。**

**與你當初純讀程式碼的推論完全一致** —— 你標過「這是讀出來的，不是量出來的」，現在量出來了，連「還沒輪到的模型也會被標成失敗」這個細節都對。請補進 `spec-appendix.md`。

**RD 補充**：`docs/product/features.md` 的比較-15、16 兩項目前標著「※未經查證」，內容正是這件事。那份文件歸 PM 維護（跨輪、全專案一份），**這兩項現在可以把標記拿掉了** —— 但那不屬於這一輪 spec 的範圍，你判斷要不要順手處理，或留給下一個動到那份文件的人。

### 二、QA 附帶回報的兩處文件與程式不符

1. **`plan/architecture.html` 與 `Degeneration.kt` 都寫重複尾巴「收成兩輪」，實作與測試實際保留三輪。**

   **RD 自陳**：我今天手算那個測資時就看到這個不一致了（`"abababab"` 收成 `"ababab"` 是三輪），當時判斷「我要驗的是標記不是輪數」就跳過去了。那是錯的 —— 那正是「文件與程式碼不符」，而我當時做的就是這類核對。**本輪不該動它**（R8 明令兩種自動中斷一個字都不准改），這是給你的記錄。

2. **任務 4 驗收 7 寫死「既有的四支 JVM 測試」，而 `conversation-search` 那一輪之後已經是五支。** 見下面第三點。

### 三、使用者定下的新寫法規則

**使用者原話**：

> 如果有參照到其他文件、程式碼的部分，都不要留下會腐爛的東西

**已經寫進 `pm-agent` 的定義**，之後每一輪都適用。不要寫進條文的：**行號、數量、檔案清單、抄別份文件的內容**。判準是「**這件事被別人在另一輪改掉時，我這條會不會自己變錯，而且沒有人會發現？**」

上面第二點那條「四支 JVM 測試」正是活生生的例子：**別人在另一輪加了一支測試，而這條條文自己變錯了，沒有任何人會發現。** 請改掉它，並回頭掃一次本規格有沒有同型的寫法。

### 四、順帶一提：跨輪衝突的新規則

使用者同時定下：**兩輪之間出現衝突時，不要自己決定用哪一邊，拉給使用者判斷；拍板後由 PM 同步兩邊的 spec。** 這一輪與 `conversation-search` 的相依已經處理完畢，寫在這裡是為了讓下次動到跨輪相依時有依據。

- **相關條目**：`spec.md`／任務 4 驗收 7、驗收 8；`spec-appendix.md`（比較畫面現況、兩處文件不符）
- **狀態**：已處理（v3）
- **處理結果**（四塊逐一）：
  - **一、比較畫面的實測記錄**已補進 `spec-appendix.md`／「我查證過、值得記下來的事實」，含三張卡片各自的狀態與實際字串（原樣抄錄）。同時把 `spec.md`／已知衝突第 6 條那句「這是讀出來的，不是量出來的」改成現行狀態，並在任務 4 驗收 8 底下註明**已執行完畢、不需要再做一次**。另外記下兩件留給下一輪的事：比較畫面顯示的是與 B1 同一個例外訊息（同型缺陷）；「還沒輪到的模型該顯示什麼」現在有了具體形狀（顯示失敗），要修它得先回答那個產品問題。
  - **二之 1（收成兩輪）**：**我自己讀過那塊純邏輯確認診斷成立**（`abababab` → `ababab`，三輪）。**本輪沒有動架構文件、沒有動那塊程式、沒有動它的註解**，記在 `spec.md`／「明確不做」並附觸發條件。**順帶做了一件事**：R8 自己也抄了「收成兩輪」這個數字，已改成它真正依賴的性質（收短的程度與修正前相同）——**行為要求一個字都沒有變**，理由寫在 R8 底下與 `spec-appendix.md`。
  - **二之 2 ＋ 三（會腐爛的引用）**：任務 4 驗收 7 的「四支」已改成「除本輪新增之外的既有 JVM 測試全部通過」。**回頭掃過整份規格與附錄，同型的還有這些**：診斷一節的四組程式行號、「這是不是 bug」與附錄兩處的「第 964 行」、修正範圍的「四塊純邏輯與四支測試」、任務 5 驗收 3 的行號定位、任務 5 驗收 5 抄來的「四支測試各對應一塊」、附錄參考資料的行號。全部換成用職責或句子內容定位，並在附錄寫明「這裡刻意不寫行號」。
  - **四（跨輪衝突的新規則）**：已寫進 `spec-appendix.md`／查證事實第 11 點，連同「這一輪與 `conversation-search` 的相依已經結清」這個現行狀態（對方已移除 §8.2 那一項、換掉 §8.6，兩份文件之間不再有相反宣稱）。`spec.md`／已知衝突第 5 條也已把現行狀態放到最前面，舊敘述降級為明確標示的歷史。
  - **一併處理的 `docs/product/features.md`**：比較-15、16 的 `※未經查證` 標記**拿掉**（那份文件自己定義這個標記的意思是「還需要有人實際開 App 確認過」，現在確認過了），並把「對應 spec」欄改成指向實測記錄；比較-15 順帶補上「原本已經寫出來的字會被整個蓋掉」這個量到的細節。**比較-17、18（返回鍵、轉螢幕）這一輪沒有人量過，標記原樣保留** —— 順手清掉沒被查證的標記，會讓這個標記整個失去意義。
  - **本輪浮出一項新的待決事項**（來自上一筆 QA 的第 2、3 點）：`spec-decisions.md`／**S2**，不擋工。

---

## [2026-09-10 10:30] 來自 `conversation-search` 的 PM：你們附錄裡有一處指進我們 spec 的引用已經指不到了

- **來源**：另一輪的 PM（`conversation-search` v3.12）
- **相關條目**：`spec-appendix.md`／「跨輪相依」那一列（它列的三個標的）
- **狀態**：已處理（v4）

**內容**：

**這不是兩輪的做法對不起來，所以我沒有把它升級成待決事項** —— 它只是一個斷掉的引用，修不修、怎麼修都在你們自己這一輪的判斷裡。**依「不要自己去改別輪的 spec」，我一個字都沒有動你們的檔案。**

你們 `spec-appendix.md` 裡那句「跨輪相依」列了三個標的，其中**兩個在 `conversation-search` v3.11 各自搬了家**：

1. **`spec-architecture.md` §G.3**（「本輪只改時間值怎麼來，不改什麼時候寫檔」）—— 那一輪把 §A–§G 依准入判準重新分類，**§G.3 已經不在架構決策裡**，它整條搬進了 `spec-tasks.md`／「明確不做」，現行的那一條標題是「時間戳這件事只改『寫下去的時間值怎麼來』，不改『什麼時候寫檔』」，而且它底下明寫了「另一輪確實跨過了這條線，而兩邊的說法對得起來」。
2. **`spec-decisions.md` 決策 C** —— 決策 C 早在 v3.6 就拍板並移出決策檔（那個檔案只放還沒拍板的事），現在記在 `spec-appendix.md`／決策歷程「決策 C 已拍板：停止生成從此算一次更新」。

第三個（`spec.md` §8）仍然指得到，它現在的節名是「對話的『最後更新時間』什麼時候前進」。

**為什麼現在講**：使用者這一輪拍板了跨文件引用的寫法 —— **slug 目錄 + 檔名 + 節號 + 節名，略過版號目錄**（`docs/specs/<slug>/<檔名> §節號「節名」`）。理由的重點正好是這個案例：**節號指錯是靜靜失敗**（落在一個真實存在、但講別件事的節），**節名指錯是 grep 不到、當場就知道**。上面那兩處就是靜靜失敗的樣子 —— 它們不會讓任何驗收變紅。

**我已經做完、你們不必再做的**：`docs/product/features.md` 是跨 slug 的單一份文件，那 40 處引用（含指向你們的 12 處）我一次改完了，兩邊都對，**`stop-generation` 不必為它另開一輪**。你們要處理的只有上面那一處自己文件裡的引用。

- **處理結果**：
  - **兩處我都自己去對方的現行定版核對過，你說的都成立**：§G.3 已不在架構決策裡，整條搬進 `spec-tasks.md`／「明確不做」（開頭句「時間戳這件事只改『寫下去的時間值怎麼來』，不改『什麼時候寫檔』」，規則字面未變）；決策 C 已移出決策檔，記在 `spec-appendix.md`／決策歷程「決策 C 已拍板：停止生成從此算一次更新」。第三個（`spec.md` §8）仍然指得到，現行節名「對話的「最後更新時間」什麼時候前進」。
  - **`spec-appendix.md`／參考資料的「跨輪相依」整列重寫**成三條「檔名 + 節號 + 節名」，並寫明哪兩條原本已經指不到、以及**為什麼沒有人發現**（舊寫法只有節號；節號指錯是靜靜失敗、節名指錯是當場 grep 不到）。
  - **順著這條線掃過本規格其餘同型的指法**，一併處理：`spec.md`／「修正範圍」指對方 §G.3 那一句改用檔名加句子定位；任務 4 驗收 6 的 §8.3、§8.4 補上節名；已知衝突第 5 條的 §8.6 補上現行節名；「對 conversation-search 的影響」整節**加一個警示框**說明裡面的節號是**取用當時**的、不是現況（§8.2 第三點已照該節要求整條移除，**找不到是預期的結果，不是引用壞掉**）。
  - **另外修掉三個同類但更基本的毛病**：`spec.md` 多處寫的裸 `findings.md` 與 `../stop-generation/findings.md` 從版號目錄解不開，改成 `../findings.md`；附錄把驗證報告寫成 `docs/specs/stop-generation/test-report.md`，實際在 `v3/` 底下，已更正並寫明「報告留在它所驗的那一版」。
  - **`docs/product/features.md` 我一個字都沒有動**（當時你那一輪正在處理它）。我核對過它指向本規格的那十幾處引用在 v4 之後仍然全部指得到 —— R 編號與任務名稱本輪都沒有動。**但那份文件有一處與本輪的環境裁決相衝**：「「※未經查證」是什麼意思」那一節寫著「沒有實機跑過」，驗收環境換成模擬器之後這個說法會誤導。**已寫進 `changes-v4.md` 的轉交清單，由呼叫者轉交，我沒有動手。**
  - **沒有升級成待決事項**：同意你的判定，這不是兩輪的做法對不起來，只是斷掉的引用。

## [2026-09-16 03:00] 驗收環境從 Pixel 9 實機改成模擬器（轉自 conversation-search 的使用者裁決）
- **來源**：使用者裁決
- **內容**：使用者 2026-09-16 裁決 demo 專案的 QA 驗收**完全改用 Android 模擬器**（AVD `Pixel_9`：Google Play 版映像、內建 Gboard 並已加注音版面；不再用 Pixel 9 實機，也不保留實機補驗）。這筆裁決原本只寫進 `docs/specs/conversation-search/pm-inbox.md`（2026-09-16 01:40），conversation-search 的 PM 在 v3.20 處理時指出**本規格也寫著「實機」**，使用者裁決轉給本規格處理。請 PM 下一次處理本規格時掃過全部 spec 檔：
  1. **條文字面寫「實機」、意思是「在裝置上實際操作、看畫面」的**：改成不指定實機的說法（例如「在裝置上」），**要驗的東西不得因此放寬**。
  2. **寫進條文的環境限制**（例如「這台裝置送不進非 ASCII／設不了剪貼簿」）：改成不預設環境能力，限制是否成立由 QA 在 `docs/qa-environment.md` 查證。
  3. **已經發生在實機上的歷史事實**保留原文，不改寫。
  4. 做法可參考 conversation-search 的處理：`docs/specs/conversation-search/changes-v3.20.md`（它把條文裡的環境限制改成「每輪先試真的，做不出來才替代，做得出來要回報」）。
- **不歸 spec 的部分**：`docs/qa-environment.md` 與 `docs/qa-tools/` 歸 QA，換環境後由 QA 自己更新。
- **相關條目**：本規格各處「實機」；`docs/specs/conversation-search/pm-inbox.md` [2026-09-16 01:40]
- **狀態**：已處理（v4）
- **處理結果**（照裁決的四點逐一）：
  - **第一點（字面「實機」）**：全 spec 掃過，**意思是「在裝置上實際操作、看畫面」的只有兩處**，都已改掉：`spec.md`／任務 2 驗收 6「實機不強制重現」→「**在裝置上**不強制重現」（並補一句：不強制的理由與裝置型態無關 —— 在裝置上自然觸發一次真正的推論失敗沒有可靠做法，所以這條的判定一直在 JVM 測試那一層）；`spec-appendix.md`／已知取捨「任務 2 的實機驗收」→「任務 2 **在裝置上的驗收**」。**要驗的東西一個字都沒有放寬。**
  - **第二點（條文裡的環境預設）**：本規格沒有「這台裝置送不進某種字元」那一型的限制，但**找到另一型**：R1–R11 的共同前置寫「在回覆開始串流後 **3–5 秒內**按下停止」—— 那**預設了生成速度**，而生成快慢正是換裝置會變的東西。已改寫成狀態條件：「**在那則回覆已經顯示出至少一個字、而且還沒有自然結束的時候**」。零文字那一種情境由 R4 自己的前置定義，兩者互補、沒有第三種。**3–5 秒降級成參考值，不再是判定的一部分。**
  - **第三點（實機上的歷史事實）**：全部原文保留，並在最容易被下一個人「順手修掉」的兩處標明它是歷史 —— 比較畫面那次實測（`spec-appendix.md`）、v3 全驗報告的環境（`spec.md`／目前狀態）。`plan/architecture.html` 頁尾那句「量測環境：Pixel 9 …」同理，**沒有動它**：那是「這些效能數字在哪裡量的」的標註，不是驗收環境。
  - **第四點（參考 conversation-search 的做法）**：讀過 `changes-v3.20.md` 的處理方式。本規格沒有「做不出來才替代」那一型的條文，所以沒有照抄那個寫法。
  - **`docs/qa-environment.md` 與 `docs/qa-tools/` 一個字都沒有動**（歸 QA）。附錄新增的查證事實只寫「權威在那份文件、本規格不抄它的內容」。
  - **浮出一項新的待決事項，已升級**：`v4/spec-decisions.md`／**S3** —— v3 的全驗是在實機上做的，那些裝置端驗收要不要在模擬器上重跑。**這筆裁決本身沒有要求重跑任何東西**，所以我沒有自己決定。升級的具體理由：本規格有一條行為對生成速度敏感（停止與串流最後一次寫入的時序競爭，見已知衝突第 2 條與 R3），**換一台推論速度不同的裝置，那個競爭的勝負可能就換一邊**。**S3 不擋任何人開工。**

## [2026-09-16 04:30] S2 裁決：選 (B)，過程條文可以寫，但必須標明它驗不到什麼
- **來源**：使用者裁決
- **內容**：`v3/spec-decisions.md` 的 **S2 選 (B)**：不禁止「過程條文」（要求某人做過某件事的驗收），但**條文本身要寫清楚：由誰執行、QA 只驗得到什麼、驗不到什麼**。使用者接受它的代價：那幾條實際上仍然驗不到，規格裡會長期存在一批「名義上是驗收、實際上是待辦」的條文 —— 換到的是不會再發生「QA 以為自己驗過了」。請把 S2 連同選項表與這個代價移進 `spec-appendix.md`／決策歷程，`spec-decisions.md` 回到 0 項待決。
- **本輪已完成的那三條不改寫**（S2 自己寫明的），這個裁決只約束以後怎麼寫。
- **重點不在這一輪**：使用者明講這主要是**以後 pm-agent 的寫法**，不是 `stop-generation` 一輪的事。RD 已把規則寫進 `~/.claude/agents/pm-agent.md`，對所有 slug 生效；這筆收件匣只是把裁決記在它發生的地方。
- **另一件相關的事實（可寫進附錄）**：S2 舉的第三條「與修改前逐字相同」當時無法判定，是因為專案沒有版本控制；**現在有了**，這一型可以改寫成指名 commit（例如「與 `git show <hash>:<路徑>` 的內容相同」），不必再靠誰事先抄一份。第一型（核對過、在本機跑過）仍然無法從產物判定，那才是 (B) 要標明的對象。
- **相關條目**：`v3/spec-decisions.md` S2；`spec-appendix.md`／決策歷程第六點
- **狀態**：已處理（v4）
- **處理結果**：
  - **S2 連同選項表與代價已移進 `v4/spec-appendix.md`／決策歷程「已拍板：S2」**，含拍板日期、(B) 的三項要求（由誰執行／QA 驗得到什麼／驗不到什麼）、以及使用者知情接受的那個代價（規格裡會長期存在一批名義上是驗收、實際上是待辦的條文）。**我原本傾向 (A) 的理由照實記錄為「未被採納」，沒有改寫成「當初判斷有誤」。**
  - **`v4/spec-decisions.md` 已把 S2 移出**，並在已拍板表格新增一列。**本檔沒有回到 0 項** —— 本輪從第二筆收件匣浮出一項新的 **S3**，我在檔案開頭明寫了「S3 是本輪新浮出來的，不是 S2 改頭換面」，免得下一個人以為裁決沒有被執行。
  - **本輪已完成的那三條不改寫**，照裁決。但留著它們就等於留著風險，所以**在 `spec.md`／「明確不做」那一列補了一個觸發條件**：任務 1 或任務 5 因為任何理由要**重做**時，重做之前先把那幾條改成 (B) 的寫法 —— 否則重驗的人會再一次以為自己驗過了。
  - **「現在有版本控制」這件事我自己查證過**（專案根有 git，`git log` 讀得到歷史，這筆收件匣本身就是一次 commit），已寫進附錄的查證事實，並據此把決策歷程第六點的第三條寫法要求**升級**：要驗「與修改前逐字相同」，**首選指名 commit**（`git show <hash>:<路徑>`），留快照降級為備案（用在沒進版控的東西上）。同時誠實寫下它沒有解掉什麼：版控存的是檔案內容，**「某個人有沒有做過某個動作」仍然驗不到**，那才是 (B) 要標明的對象。
  - **這一筆與第二筆之間沒有衝突，也沒有相依**：一個管條文怎麼寫，一個管在哪裡驗。合起來也沒有動到本規格的任何架構假設。
